--- a/src/object_files.rs
+++ b/src/object_files.rs
@@ -843,6 +843,18 @@
             return Ok(());
         }
 
+        if matches!(reloc_kind, RelocKind::ConjuredStatic { .. })
+            && self.object.symbol_id(reloc_name.as_bytes()).is_some()
+        {
+            self.add_relocation(
+                reloc_name,
+                ObjectLocation::Extern(object::SymbolKind::Data),
+                reloc_offset,
+                RelocationEncoding::Absolute,
+            )?;
+            return Ok(());
+        }
+
         // `Function` and `ReviewedData` reference a real definition elsewhere; a
         // `Conjured*` target is materialized as a private per-TU copy, unless the
         // caller only wants a reference.
@@ -1892,6 +1904,64 @@
     }
 
     #[test]
+    fn reviewed_static_relocation_reuses_defined_symbol_without_materializing_data() {
+        let mut output = ObjectFile::empty_with_topology(false, SectionTopology::Synthesized);
+        let data_section_id = output.data_section();
+        output
+            .object
+            .append_section_data(data_section_id, &[1, 2, 3, 4], 4);
+        output.object.add_symbol(object::write::Symbol {
+            name: b"reviewed".to_vec(),
+            value: 0,
+            size: 4,
+            kind: object::SymbolKind::Data,
+            scope: object::SymbolScope::Linkage,
+            weak: false,
+            section: object::write::SymbolSection::Section(data_section_id),
+            flags: object::SymbolFlags::None,
+        });
+        let text_section_id = output.text_section();
+        let field = output.append_section_data(text_section_id, &[0; 4], 0x90);
+        output
+            .add_relocation_at(
+                RelocKind::ConjuredStatic {
+                    symbol: RawString::from(&b"reviewed"[..]),
+                    target_rva: 0x100,
+                },
+                field,
+                &SymbolMatcher::off(),
+                &[],
+                &BTreeMap::new(),
+                &mut HashSet::new(),
+                TargetMaterialization::Materialize,
+            )
+            .unwrap();
+
+        let bytes = output.object.write().unwrap();
+        let parsed = object::File::parse(bytes.as_slice()).unwrap();
+        assert_eq!(parsed.section_by_name(".data").unwrap().size(), 4);
+        let matching = parsed
+            .symbols()
+            .filter(|symbol| symbol.name() == Ok("reviewed"))
+            .collect::<Vec<_>>();
+        assert_eq!(matching.len(), 1);
+        assert!(matching[0].is_definition());
+        let relocation = parsed
+            .section_by_name(".text")
+            .unwrap()
+            .relocations()
+            .next()
+            .unwrap();
+        let object::RelocationTarget::Symbol(index) = relocation.1.target() else {
+            panic!("reviewed relocation must target a symbol");
+        };
+        let target = parsed.symbol_by_index(index).unwrap();
+        assert_eq!(target.name(), Ok("reviewed"));
+        assert!(target.is_definition());
+        assert_eq!(target.address(), 0);
+    }
+
+    #[test]
     fn absolute_function_reference_uses_coff_function_type() {
         let mut object = ObjectFile::empty_with_topology(false, SectionTopology::Synthesized);
         let text_section_id = object.text_section();
--- a/src/data_manifest.rs
+++ b/src/data_manifest.rs
@@ -324,5 +324,13 @@
         (addend < definition.size).then_some((definition, addend))
     }
+
+    pub fn has_external_definition(&self, symbol: RawString<'_>) -> bool {
+        self.definitions.iter().any(|definition| {
+            definition.scope == DataScope::External
+                && definition.symbol_name.as_bytes() == symbol.as_bytes()
+        })
+    }
+
 }
 
 fn validate_text(path: &Path, line: usize, field: &str, value: &[u8]) -> anyhow::Result<()> {
@@ -427,6 +434,20 @@
         assert_eq!(constant.scope, DataScope::Local);
     }
 
+    #[test]
+    fn identifies_only_reviewed_external_definitions() {
+        let parsed = parse(&format!(
+            "{HEADER_TEXT}one.c\t0x100\t4\tdata\t4\t-\t0\texternal\n\
+             two.c\t0x300\t4\tdata\t4\t-\t0\tlocal\n"
+        ))
+        .unwrap();
+        assert!(parsed.has_external_definition(RawString::from(&b"table"[..])));
+        assert!(!parsed.has_external_definition(RawString::from(&b"scratch"[..])));
+        assert!(!parsed.has_external_definition(RawString::from(
+            &b"absent"[..]
+        )));
+    }
+
     #[test]
     fn accepts_missing_final_line_ending_and_uses_raw_pdb_symbol_bytes() {
         let mut bytes = HEADER.to_vec();
         bytes.extend_from_slice(b"\nu.c\t0x180\t4\tdata\t4\t-\t0\texternal");
--- a/src/relocs.rs
+++ b/src/relocs.rs
@@ -221,6 +221,10 @@
         target_rva,
     )? {
-        let kind = if is_static {
+        let kind = if data_manifest.has_external_definition(owner) {
+            RelocKind::ReviewedData {
+                symbol: owner,
+            }
+        } else if is_static {
             RelocKind::ConjuredStatic {
                 symbol: owner,
                 target_rva,
@@ -715,11 +719,16 @@
             {
                 coff_data_reloc.copy_from_slice(&addend.to_le_bytes());
+                let kind = if data_manifest.has_external_definition(owner) {
+                    RelocKind::ReviewedData { symbol: owner }
+                } else {
+                    RelocKind::ConjuredConstant {
+                        symbol: owner,
+                        target_rva,
+                    }
+                };
                 relocs_rva.insert(
                     reloc_rva,
-                    RelocKind::ConjuredConstant {
-                        symbol: owner,
-                        target_rva,
-                    },
+                    kind,
                 );
                 return Ok(());
             }
@@ -797,11 +806,16 @@
             {
                 coff_data_reloc.copy_from_slice(&addend.to_le_bytes());
+                let kind = if data_manifest.has_external_definition(owner) {
+                    RelocKind::ReviewedData { symbol: owner }
+                } else {
+                    RelocKind::ConjuredStatic {
+                        symbol: owner,
+                        target_rva,
+                    }
+                };
                 relocs_rva.insert(
                     reloc_rva,
-                    RelocKind::ConjuredStatic {
-                        symbol: owner,
-                        target_rva,
-                    },
+                    kind,
                 );
                 return Ok(());
             }
