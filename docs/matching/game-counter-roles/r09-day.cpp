// Parent bef26afa0; complete structural product, no AST mutations or TU-state census.
// All 3 arms compiled in 4.240239144302905 seconds; source restored, no truncation.
// Retain trial 0: {'structure': 'original', 'candidate': 'baseline'}.
// Selected size 2576, text b412aa0c9090779b, normalized relocs 79ee460cc3b42937.
// Artifacts: build/source-variant-batch/r09-day/results.json; build/r09-day-manifest.json.
// Counter separation changes native bytes. Full native/retail proof and domains: docs/reconstruction/C34-C39-R09.md.
/* Exact source product:
{
  "schema": 1,
  "source": "src/SOURCE/GAME.cpp",
  "rva": "0x574c6",
  "axes": [
    {
      "name": "structure",
      "find": "void game::PerDay(void) {\n    i32 maxSpellPoints9;\n    i32 player;\n    H2_ENUM_STORAGE_STEPPED(ResourceType, i32) resource8;\n    i32 H2_UNUSED(income8);\n    i32 dailyIncome0;\n    MineType resourceType1;\n    hero* currentHero7;\n    i32 restoredSpellPoints14;\n    hero* townHero6;\n    town* currentTown1;\n\n    for (player = 0; player < gpGame->m_playerCount; player++) {\n        for (resource8 = RES_WOOD; resource8 < RES_COUNT; resource8++) {\n            gpGame->m_players[player].m_aiData.m_income[IDX(resource8)] =\n                -m_players[player].m_resources[IDX(resource8)];\n        }\n    }\n\n    memset(m_townBuiltToday, 0, sizeof(m_townBuiltToday));\n\n    for (player = 0; player < GAME_MINE_COUNT; player++) {\n        if (m_mines[player].owner != -1) {\n            resourceType1 = m_mines[player].resourceType;\n            dailyIncome0 = 0;\n            if (resourceType1 == MINE_TYPE_ORE)\n                dailyIncome0 = DAILY_MINE_BULK_RESOURCE_INCOME;\n            else if (resourceType1 == MINE_TYPE_WOOD)\n                dailyIncome0 = DAILY_MINE_BULK_RESOURCE_INCOME;\n            else if (resourceType1 != MINE_TYPE_GOLD)\n                dailyIncome0 = 1;\n\n            if (resourceType1 != MINE_TYPE_GOLD && resourceType1 <= MINE_TYPE_GOLD)\n                m_players[m_mines[player].owner].m_resources[IDX(resourceType1)] += dailyIncome0;\n        }\n    }\n\n    for (player = 0; player < GAME_TOWN_COUNT; player++)\n        m_castleRecs[player].m_turnsOwned++;\n\n    for (player = 0; player < m_playerCount; player++) {\n        m_players[player].m_resources[IDX(RES_SULFUR)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_POUCH_SULFUR);\n        m_players[player].m_resources[IDX(RES_MERCURY)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_VIAL_MERCURY);\n        m_players[player].m_resources[IDX(RES_GEMS)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_POUCH_GEMS);\n        m_players[player].m_resources[IDX(RES_WOOD)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_CORD_WOOD);\n        m_players[player].m_resources[IDX(RES_ORE)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_CART_ORE);\n        m_players[player].m_resources[IDX(RES_CRYSTAL)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_POUCH_CRYSTAL);\n        m_players[player].m_resources[IDX(RES_GOLD)] += ComputeDailyGold(player);\n    }\n\n    if (xIsPlayingExpansionCampaign && xCampaign.HasAward(AWARD_WOOD_BONUS))\n        m_players[0].m_resources[IDX(RES_WOOD)] += DAILY_CAMPAIGN_WOOD_BONUS;\n\n    for (player = 0; player < m_playerCount; player++) {\n        if (!gbHumanPlayer[player]) {\n            if (gpGame->m_difficulty >= DIFFICULTY_HARD) {\n                m_players[player].m_resources[IDX(RES_WOOD)]++;\n                m_players[player].m_resources[IDX(RES_ORE)]++;\n            }\n            if (gpGame->m_difficulty >= DIFFICULTY_EXPERT\n                && m_day >= DAILY_RESOURCE_BONUS_FIRST_DAY\n                && m_day <= DAILY_RESOURCE_BONUS_LAST_DAY)\n                m_players[player].m_resources[m_day - 1] += 1;\n            if (gpGame->m_difficulty >= DIFFICULTY_IMPOSSIBLE\n                && m_day >= DAILY_RESOURCE_BONUS_FIRST_DAY\n                && m_day <= DAILY_RESOURCE_BONUS_LAST_DAY)\n                m_players[player].m_resources[m_day - 1] += 1;\n            if (gpGame->m_players[player].m_aiDifficulty == PLAYER_PERSONALITY_BUILDER\n                && m_day >= DAILY_RESOURCE_BONUS_FIRST_DAY\n                && m_day <= DAILY_RESOURCE_BONUS_LAST_DAY)\n                m_players[player].m_resources[m_day - 1] += 1;\n        }\n    }\n\n    m_day++;\n    giCurTurn = m_day + (m_week - 1) * EVENT_DAYS_PER_WEEK\n                + (m_month - 1) * EVENT_DAYS_PER_MONTH;\n    if (!gbGameOver) {\n        if (m_day > EVENT_DAYS_PER_WEEK) {\n            m_day = 1;\n            PerWeek();\n        }\n        if (m_week > GAME_WEEKS_PER_MONTH) {\n            m_week = 1;\n            PerMonth();\n        }\n    }\n\n    for (player = 0; player < GAME_HERO_COUNT; player++)\n        m_heroRecs[player].m_eventFlags &= ~WEEKLY_HERO_RESERVED_FLAG;\n\n    for (player = 0; player < gpGame->m_playerCount; player++) {\n        for (resource8 = RES_WOOD; resource8 < RES_GOLD; resource8++) {\n            if (m_playerHandicap[player] != PLAYER_HANDICAP_NONE) {\n                m_players[player].m_resources[IDX(resource8)] -= static_cast<i32>(\n                    (gpGame->m_players[player].m_aiData.m_income[IDX(resource8)]\n                     + m_players[player].m_resources[IDX(resource8)])\n                    * (m_playerHandicap[player] == PLAYER_HANDICAP_MODERATE ? GAME_HANDICAP_MODERATE_DAILY_PENALTY : GAME_HANDICAP_SEVERE_DAILY_PENALTY)\n                );\n            }\n        }\n    }\n\n    for (player = 0; player < gpGame->m_playerCount; player++) {\n        for (resource8 = RES_WOOD; resource8 < RES_COUNT; resource8++) {\n            gpGame->m_players[player].m_aiData.m_income[IDX(resource8)] +=\n                m_players[player].m_resources[IDX(resource8)];\n        }\n    }\n\n    for (player = 0; player < GAME_HERO_COUNT; player++) {\n        currentHero7 = &m_heroRecs[player];\n        restoredSpellPoints14 = currentHero7->m_spellPoints;\n        maxSpellPoints9 =\n            currentHero7->Stats(HERO_PRIMARY_KNOWLEDGE) * HERO_SPELL_POINTS_PER_KNOWLEDGE;\n        restoredSpellPoints14 +=\n            IDX(currentHero7->m_secondarySkills[IDX(HERO_SKILL_MYSTICISM)]) + 1;\n        if (currentHero7->HasArtifact(ARTIFACT_POWER_RING))\n            restoredSpellPoints14 += POWER_RING_DAILY_MANA_BONUS;\n        if (restoredSpellPoints14 > maxSpellPoints9)\n            restoredSpellPoints14 = maxSpellPoints9;\n        if (restoredSpellPoints14 > currentHero7->m_spellPoints)\n            currentHero7->m_spellPoints = restoredSpellPoints14;\n        if (HAS(currentHero7->m_eventFlags, HERO_EVENT_MAGIC_WELL))\n            H2_ENUM_CLEAR_FLAG(currentHero7->m_eventFlags, HERO_EVENT_MAGIC_WELL);\n    }\n\n    for (player = 0; player < GAME_TOWN_COUNT; player++) {\n        currentTown1 = GetTown(player);\n        if (!(currentTown1->m_buildings & BIT(BUILDING_SLOT_MAGE_GUILD)))\n            continue;\n        if (currentTown1->m_occupyingHeroId != -1) {\n            townHero6 = GetHero(currentTown1->m_occupyingHeroId);\n            maxSpellPoints9 =\n                townHero6->Stats(HERO_PRIMARY_KNOWLEDGE) * HERO_SPELL_POINTS_PER_KNOWLEDGE;\n            if (maxSpellPoints9 > townHero6->m_spellPoints)\n                townHero6->m_spellPoints = maxSpellPoints9;\n        }\n    }\n}",
      "options": [
        {
          "name": "original",
          "replace": "void game::PerDay(void) {\n    i32 maxSpellPoints9;\n    i32 player;\n    H2_ENUM_STORAGE_STEPPED(ResourceType, i32) resource8;\n    i32 H2_UNUSED(income8);\n    i32 dailyIncome0;\n    MineType resourceType1;\n    hero* currentHero7;\n    i32 restoredSpellPoints14;\n    hero* townHero6;\n    town* currentTown1;\n\n    for (player = 0; player < gpGame->m_playerCount; player++) {\n        for (resource8 = RES_WOOD; resource8 < RES_COUNT; resource8++) {\n            gpGame->m_players[player].m_aiData.m_income[IDX(resource8)] =\n                -m_players[player].m_resources[IDX(resource8)];\n        }\n    }\n\n    memset(m_townBuiltToday, 0, sizeof(m_townBuiltToday));\n\n    for (player = 0; player < GAME_MINE_COUNT; player++) {\n        if (m_mines[player].owner != -1) {\n            resourceType1 = m_mines[player].resourceType;\n            dailyIncome0 = 0;\n            if (resourceType1 == MINE_TYPE_ORE)\n                dailyIncome0 = DAILY_MINE_BULK_RESOURCE_INCOME;\n            else if (resourceType1 == MINE_TYPE_WOOD)\n                dailyIncome0 = DAILY_MINE_BULK_RESOURCE_INCOME;\n            else if (resourceType1 != MINE_TYPE_GOLD)\n                dailyIncome0 = 1;\n\n            if (resourceType1 != MINE_TYPE_GOLD && resourceType1 <= MINE_TYPE_GOLD)\n                m_players[m_mines[player].owner].m_resources[IDX(resourceType1)] += dailyIncome0;\n        }\n    }\n\n    for (player = 0; player < GAME_TOWN_COUNT; player++)\n        m_castleRecs[player].m_turnsOwned++;\n\n    for (player = 0; player < m_playerCount; player++) {\n        m_players[player].m_resources[IDX(RES_SULFUR)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_POUCH_SULFUR);\n        m_players[player].m_resources[IDX(RES_MERCURY)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_VIAL_MERCURY);\n        m_players[player].m_resources[IDX(RES_GEMS)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_POUCH_GEMS);\n        m_players[player].m_resources[IDX(RES_WOOD)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_CORD_WOOD);\n        m_players[player].m_resources[IDX(RES_ORE)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_CART_ORE);\n        m_players[player].m_resources[IDX(RES_CRYSTAL)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_POUCH_CRYSTAL);\n        m_players[player].m_resources[IDX(RES_GOLD)] += ComputeDailyGold(player);\n    }\n\n    if (xIsPlayingExpansionCampaign && xCampaign.HasAward(AWARD_WOOD_BONUS))\n        m_players[0].m_resources[IDX(RES_WOOD)] += DAILY_CAMPAIGN_WOOD_BONUS;\n\n    for (player = 0; player < m_playerCount; player++) {\n        if (!gbHumanPlayer[player]) {\n            if (gpGame->m_difficulty >= DIFFICULTY_HARD) {\n                m_players[player].m_resources[IDX(RES_WOOD)]++;\n                m_players[player].m_resources[IDX(RES_ORE)]++;\n            }\n            if (gpGame->m_difficulty >= DIFFICULTY_EXPERT\n                && m_day >= DAILY_RESOURCE_BONUS_FIRST_DAY\n                && m_day <= DAILY_RESOURCE_BONUS_LAST_DAY)\n                m_players[player].m_resources[m_day - 1] += 1;\n            if (gpGame->m_difficulty >= DIFFICULTY_IMPOSSIBLE\n                && m_day >= DAILY_RESOURCE_BONUS_FIRST_DAY\n                && m_day <= DAILY_RESOURCE_BONUS_LAST_DAY)\n                m_players[player].m_resources[m_day - 1] += 1;\n            if (gpGame->m_players[player].m_aiDifficulty == PLAYER_PERSONALITY_BUILDER\n                && m_day >= DAILY_RESOURCE_BONUS_FIRST_DAY\n                && m_day <= DAILY_RESOURCE_BONUS_LAST_DAY)\n                m_players[player].m_resources[m_day - 1] += 1;\n        }\n    }\n\n    m_day++;\n    giCurTurn = m_day + (m_week - 1) * EVENT_DAYS_PER_WEEK\n                + (m_month - 1) * EVENT_DAYS_PER_MONTH;\n    if (!gbGameOver) {\n        if (m_day > EVENT_DAYS_PER_WEEK) {\n            m_day = 1;\n            PerWeek();\n        }\n        if (m_week > GAME_WEEKS_PER_MONTH) {\n            m_week = 1;\n            PerMonth();\n        }\n    }\n\n    for (player = 0; player < GAME_HERO_COUNT; player++)\n        m_heroRecs[player].m_eventFlags &= ~WEEKLY_HERO_RESERVED_FLAG;\n\n    for (player = 0; player < gpGame->m_playerCount; player++) {\n        for (resource8 = RES_WOOD; resource8 < RES_GOLD; resource8++) {\n            if (m_playerHandicap[player] != PLAYER_HANDICAP_NONE) {\n                m_players[player].m_resources[IDX(resource8)] -= static_cast<i32>(\n                    (gpGame->m_players[player].m_aiData.m_income[IDX(resource8)]\n                     + m_players[player].m_resources[IDX(resource8)])\n                    * (m_playerHandicap[player] == PLAYER_HANDICAP_MODERATE ? GAME_HANDICAP_MODERATE_DAILY_PENALTY : GAME_HANDICAP_SEVERE_DAILY_PENALTY)\n                );\n            }\n        }\n    }\n\n    for (player = 0; player < gpGame->m_playerCount; player++) {\n        for (resource8 = RES_WOOD; resource8 < RES_COUNT; resource8++) {\n            gpGame->m_players[player].m_aiData.m_income[IDX(resource8)] +=\n                m_players[player].m_resources[IDX(resource8)];\n        }\n    }\n\n    for (player = 0; player < GAME_HERO_COUNT; player++) {\n        currentHero7 = &m_heroRecs[player];\n        restoredSpellPoints14 = currentHero7->m_spellPoints;\n        maxSpellPoints9 =\n            currentHero7->Stats(HERO_PRIMARY_KNOWLEDGE) * HERO_SPELL_POINTS_PER_KNOWLEDGE;\n        restoredSpellPoints14 +=\n            IDX(currentHero7->m_secondarySkills[IDX(HERO_SKILL_MYSTICISM)]) + 1;\n        if (currentHero7->HasArtifact(ARTIFACT_POWER_RING))\n            restoredSpellPoints14 += POWER_RING_DAILY_MANA_BONUS;\n        if (restoredSpellPoints14 > maxSpellPoints9)\n            restoredSpellPoints14 = maxSpellPoints9;\n        if (restoredSpellPoints14 > currentHero7->m_spellPoints)\n            currentHero7->m_spellPoints = restoredSpellPoints14;\n        if (HAS(currentHero7->m_eventFlags, HERO_EVENT_MAGIC_WELL))\n            H2_ENUM_CLEAR_FLAG(currentHero7->m_eventFlags, HERO_EVENT_MAGIC_WELL);\n    }\n\n    for (player = 0; player < GAME_TOWN_COUNT; player++) {\n        currentTown1 = GetTown(player);\n        if (!(currentTown1->m_buildings & BIT(BUILDING_SLOT_MAGE_GUILD)))\n            continue;\n        if (currentTown1->m_occupyingHeroId != -1) {\n            townHero6 = GetHero(currentTown1->m_occupyingHeroId);\n            maxSpellPoints9 =\n                townHero6->Stats(HERO_PRIMARY_KNOWLEDGE) * HERO_SPELL_POINTS_PER_KNOWLEDGE;\n            if (maxSpellPoints9 > townHero6->m_spellPoints)\n                townHero6->m_spellPoints = maxSpellPoints9;\n        }\n    }\n}"
        },
        {
          "name": "roles_top",
          "replace": "void game::PerDay(void) {\n    i32 maxSpellPoints9;\n    i32 player;\n    i32 mineIndex;\n    i32 townIndex;\n    i32 heroIndex;\n    H2_ENUM_STORAGE_STEPPED(ResourceType, i32) resource8;\n    i32 H2_UNUSED(income8);\n    i32 dailyIncome0;\n    MineType resourceType1;\n    hero* currentHero7;\n    i32 restoredSpellPoints14;\n    hero* townHero6;\n    town* currentTown1;\n\n    for (player = 0; player < gpGame->m_playerCount; player++) {\n        for (resource8 = RES_WOOD; resource8 < RES_COUNT; resource8++) {\n            gpGame->m_players[player].m_aiData.m_income[IDX(resource8)] =\n                -m_players[player].m_resources[IDX(resource8)];\n        }\n    }\n\n    memset(m_townBuiltToday, 0, sizeof(m_townBuiltToday));\n\n    for (mineIndex = 0; mineIndex < GAME_MINE_COUNT; mineIndex++) {\n        if (m_mines[mineIndex].owner != -1) {\n            resourceType1 = m_mines[mineIndex].resourceType;\n            dailyIncome0 = 0;\n            if (resourceType1 == MINE_TYPE_ORE)\n                dailyIncome0 = DAILY_MINE_BULK_RESOURCE_INCOME;\n            else if (resourceType1 == MINE_TYPE_WOOD)\n                dailyIncome0 = DAILY_MINE_BULK_RESOURCE_INCOME;\n            else if (resourceType1 != MINE_TYPE_GOLD)\n                dailyIncome0 = 1;\n\n            if (resourceType1 != MINE_TYPE_GOLD && resourceType1 <= MINE_TYPE_GOLD)\n                m_players[m_mines[mineIndex].owner].m_resources[IDX(resourceType1)] += dailyIncome0;\n        }\n    }\n\n    for (townIndex = 0; townIndex < GAME_TOWN_COUNT; townIndex++)\n        m_castleRecs[townIndex].m_turnsOwned++;\n\n    for (player = 0; player < m_playerCount; player++) {\n        m_players[player].m_resources[IDX(RES_SULFUR)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_POUCH_SULFUR);\n        m_players[player].m_resources[IDX(RES_MERCURY)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_VIAL_MERCURY);\n        m_players[player].m_resources[IDX(RES_GEMS)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_POUCH_GEMS);\n        m_players[player].m_resources[IDX(RES_WOOD)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_CORD_WOOD);\n        m_players[player].m_resources[IDX(RES_ORE)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_CART_ORE);\n        m_players[player].m_resources[IDX(RES_CRYSTAL)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_POUCH_CRYSTAL);\n        m_players[player].m_resources[IDX(RES_GOLD)] += ComputeDailyGold(player);\n    }\n\n    if (xIsPlayingExpansionCampaign && xCampaign.HasAward(AWARD_WOOD_BONUS))\n        m_players[0].m_resources[IDX(RES_WOOD)] += DAILY_CAMPAIGN_WOOD_BONUS;\n\n    for (player = 0; player < m_playerCount; player++) {\n        if (!gbHumanPlayer[player]) {\n            if (gpGame->m_difficulty >= DIFFICULTY_HARD) {\n                m_players[player].m_resources[IDX(RES_WOOD)]++;\n                m_players[player].m_resources[IDX(RES_ORE)]++;\n            }\n            if (gpGame->m_difficulty >= DIFFICULTY_EXPERT\n                && m_day >= DAILY_RESOURCE_BONUS_FIRST_DAY\n                && m_day <= DAILY_RESOURCE_BONUS_LAST_DAY)\n                m_players[player].m_resources[m_day - 1] += 1;\n            if (gpGame->m_difficulty >= DIFFICULTY_IMPOSSIBLE\n                && m_day >= DAILY_RESOURCE_BONUS_FIRST_DAY\n                && m_day <= DAILY_RESOURCE_BONUS_LAST_DAY)\n                m_players[player].m_resources[m_day - 1] += 1;\n            if (gpGame->m_players[player].m_aiDifficulty == PLAYER_PERSONALITY_BUILDER\n                && m_day >= DAILY_RESOURCE_BONUS_FIRST_DAY\n                && m_day <= DAILY_RESOURCE_BONUS_LAST_DAY)\n                m_players[player].m_resources[m_day - 1] += 1;\n        }\n    }\n\n    m_day++;\n    giCurTurn = m_day + (m_week - 1) * EVENT_DAYS_PER_WEEK\n                + (m_month - 1) * EVENT_DAYS_PER_MONTH;\n    if (!gbGameOver) {\n        if (m_day > EVENT_DAYS_PER_WEEK) {\n            m_day = 1;\n            PerWeek();\n        }\n        if (m_week > GAME_WEEKS_PER_MONTH) {\n            m_week = 1;\n            PerMonth();\n        }\n    }\n\n    for (heroIndex = 0; heroIndex < GAME_HERO_COUNT; heroIndex++)\n        m_heroRecs[heroIndex].m_eventFlags &= ~WEEKLY_HERO_RESERVED_FLAG;\n\n    for (player = 0; player < gpGame->m_playerCount; player++) {\n        for (resource8 = RES_WOOD; resource8 < RES_GOLD; resource8++) {\n            if (m_playerHandicap[player] != PLAYER_HANDICAP_NONE) {\n                m_players[player].m_resources[IDX(resource8)] -= static_cast<i32>(\n                    (gpGame->m_players[player].m_aiData.m_income[IDX(resource8)]\n                     + m_players[player].m_resources[IDX(resource8)])\n                    * (m_playerHandicap[player] == PLAYER_HANDICAP_MODERATE ? GAME_HANDICAP_MODERATE_DAILY_PENALTY : GAME_HANDICAP_SEVERE_DAILY_PENALTY)\n                );\n            }\n        }\n    }\n\n    for (player = 0; player < gpGame->m_playerCount; player++) {\n        for (resource8 = RES_WOOD; resource8 < RES_COUNT; resource8++) {\n            gpGame->m_players[player].m_aiData.m_income[IDX(resource8)] +=\n                m_players[player].m_resources[IDX(resource8)];\n        }\n    }\n\n    for (heroIndex = 0; heroIndex < GAME_HERO_COUNT; heroIndex++) {\n        currentHero7 = &m_heroRecs[heroIndex];\n        restoredSpellPoints14 = currentHero7->m_spellPoints;\n        maxSpellPoints9 =\n            currentHero7->Stats(HERO_PRIMARY_KNOWLEDGE) * HERO_SPELL_POINTS_PER_KNOWLEDGE;\n        restoredSpellPoints14 +=\n            IDX(currentHero7->m_secondarySkills[IDX(HERO_SKILL_MYSTICISM)]) + 1;\n        if (currentHero7->HasArtifact(ARTIFACT_POWER_RING))\n            restoredSpellPoints14 += POWER_RING_DAILY_MANA_BONUS;\n        if (restoredSpellPoints14 > maxSpellPoints9)\n            restoredSpellPoints14 = maxSpellPoints9;\n        if (restoredSpellPoints14 > currentHero7->m_spellPoints)\n            currentHero7->m_spellPoints = restoredSpellPoints14;\n        if (HAS(currentHero7->m_eventFlags, HERO_EVENT_MAGIC_WELL))\n            H2_ENUM_CLEAR_FLAG(currentHero7->m_eventFlags, HERO_EVENT_MAGIC_WELL);\n    }\n\n    for (townIndex = 0; townIndex < GAME_TOWN_COUNT; townIndex++) {\n        currentTown1 = GetTown(townIndex);\n        if (!(currentTown1->m_buildings & BIT(BUILDING_SLOT_MAGE_GUILD)))\n            continue;\n        if (currentTown1->m_occupyingHeroId != -1) {\n            townHero6 = GetHero(currentTown1->m_occupyingHeroId);\n            maxSpellPoints9 =\n                townHero6->Stats(HERO_PRIMARY_KNOWLEDGE) * HERO_SPELL_POINTS_PER_KNOWLEDGE;\n            if (maxSpellPoints9 > townHero6->m_spellPoints)\n                townHero6->m_spellPoints = maxSpellPoints9;\n        }\n    }\n}"
        },
        {
          "name": "roles_scoped",
          "replace": "void game::PerDay(void) {\n    i32 maxSpellPoints9;\n    i32 player;\n    H2_ENUM_STORAGE_STEPPED(ResourceType, i32) resource8;\n    i32 H2_UNUSED(income8);\n    i32 dailyIncome0;\n    MineType resourceType1;\n    hero* currentHero7;\n    i32 restoredSpellPoints14;\n    hero* townHero6;\n    town* currentTown1;\n\n    for (player = 0; player < gpGame->m_playerCount; player++) {\n        for (resource8 = RES_WOOD; resource8 < RES_COUNT; resource8++) {\n            gpGame->m_players[player].m_aiData.m_income[IDX(resource8)] =\n                -m_players[player].m_resources[IDX(resource8)];\n        }\n    }\n\n    memset(m_townBuiltToday, 0, sizeof(m_townBuiltToday));\n\n    {\n        i32 mineIndex;\n        for (mineIndex = 0; mineIndex < GAME_MINE_COUNT; mineIndex++) {\n            if (m_mines[mineIndex].owner != -1) {\n                resourceType1 = m_mines[mineIndex].resourceType;\n                dailyIncome0 = 0;\n                if (resourceType1 == MINE_TYPE_ORE)\n                    dailyIncome0 = DAILY_MINE_BULK_RESOURCE_INCOME;\n                else if (resourceType1 == MINE_TYPE_WOOD)\n                    dailyIncome0 = DAILY_MINE_BULK_RESOURCE_INCOME;\n                else if (resourceType1 != MINE_TYPE_GOLD)\n                    dailyIncome0 = 1;\n    \n                if (resourceType1 != MINE_TYPE_GOLD && resourceType1 <= MINE_TYPE_GOLD)\n                    m_players[m_mines[mineIndex].owner].m_resources[IDX(resourceType1)] += dailyIncome0;\n            }\n        }\n    }\n\n    {\n        i32 townIndex;\n        for (townIndex = 0; townIndex < GAME_TOWN_COUNT; townIndex++)\n            m_castleRecs[townIndex].m_turnsOwned++;\n    }\n\n    for (player = 0; player < m_playerCount; player++) {\n        m_players[player].m_resources[IDX(RES_SULFUR)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_POUCH_SULFUR);\n        m_players[player].m_resources[IDX(RES_MERCURY)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_VIAL_MERCURY);\n        m_players[player].m_resources[IDX(RES_GEMS)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_POUCH_GEMS);\n        m_players[player].m_resources[IDX(RES_WOOD)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_CORD_WOOD);\n        m_players[player].m_resources[IDX(RES_ORE)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_CART_ORE);\n        m_players[player].m_resources[IDX(RES_CRYSTAL)] +=\n            m_players[player].NumOfGivenArtifact(ARTIFACT_ENDLESS_POUCH_CRYSTAL);\n        m_players[player].m_resources[IDX(RES_GOLD)] += ComputeDailyGold(player);\n    }\n\n    if (xIsPlayingExpansionCampaign && xCampaign.HasAward(AWARD_WOOD_BONUS))\n        m_players[0].m_resources[IDX(RES_WOOD)] += DAILY_CAMPAIGN_WOOD_BONUS;\n\n    for (player = 0; player < m_playerCount; player++) {\n        if (!gbHumanPlayer[player]) {\n            if (gpGame->m_difficulty >= DIFFICULTY_HARD) {\n                m_players[player].m_resources[IDX(RES_WOOD)]++;\n                m_players[player].m_resources[IDX(RES_ORE)]++;\n            }\n            if (gpGame->m_difficulty >= DIFFICULTY_EXPERT\n                && m_day >= DAILY_RESOURCE_BONUS_FIRST_DAY\n                && m_day <= DAILY_RESOURCE_BONUS_LAST_DAY)\n                m_players[player].m_resources[m_day - 1] += 1;\n            if (gpGame->m_difficulty >= DIFFICULTY_IMPOSSIBLE\n                && m_day >= DAILY_RESOURCE_BONUS_FIRST_DAY\n                && m_day <= DAILY_RESOURCE_BONUS_LAST_DAY)\n                m_players[player].m_resources[m_day - 1] += 1;\n            if (gpGame->m_players[player].m_aiDifficulty == PLAYER_PERSONALITY_BUILDER\n                && m_day >= DAILY_RESOURCE_BONUS_FIRST_DAY\n                && m_day <= DAILY_RESOURCE_BONUS_LAST_DAY)\n                m_players[player].m_resources[m_day - 1] += 1;\n        }\n    }\n\n    m_day++;\n    giCurTurn = m_day + (m_week - 1) * EVENT_DAYS_PER_WEEK\n                + (m_month - 1) * EVENT_DAYS_PER_MONTH;\n    if (!gbGameOver) {\n        if (m_day > EVENT_DAYS_PER_WEEK) {\n            m_day = 1;\n            PerWeek();\n        }\n        if (m_week > GAME_WEEKS_PER_MONTH) {\n            m_week = 1;\n            PerMonth();\n        }\n    }\n\n    {\n        i32 heroIndex;\n        for (heroIndex = 0; heroIndex < GAME_HERO_COUNT; heroIndex++)\n            m_heroRecs[heroIndex].m_eventFlags &= ~WEEKLY_HERO_RESERVED_FLAG;\n    }\n\n    for (player = 0; player < gpGame->m_playerCount; player++) {\n        for (resource8 = RES_WOOD; resource8 < RES_GOLD; resource8++) {\n            if (m_playerHandicap[player] != PLAYER_HANDICAP_NONE) {\n                m_players[player].m_resources[IDX(resource8)] -= static_cast<i32>(\n                    (gpGame->m_players[player].m_aiData.m_income[IDX(resource8)]\n                     + m_players[player].m_resources[IDX(resource8)])\n                    * (m_playerHandicap[player] == PLAYER_HANDICAP_MODERATE ? GAME_HANDICAP_MODERATE_DAILY_PENALTY : GAME_HANDICAP_SEVERE_DAILY_PENALTY)\n                );\n            }\n        }\n    }\n\n    for (player = 0; player < gpGame->m_playerCount; player++) {\n        for (resource8 = RES_WOOD; resource8 < RES_COUNT; resource8++) {\n            gpGame->m_players[player].m_aiData.m_income[IDX(resource8)] +=\n                m_players[player].m_resources[IDX(resource8)];\n        }\n    }\n\n    {\n        i32 heroIndex;\n        for (heroIndex = 0; heroIndex < GAME_HERO_COUNT; heroIndex++) {\n            currentHero7 = &m_heroRecs[heroIndex];\n            restoredSpellPoints14 = currentHero7->m_spellPoints;\n            maxSpellPoints9 =\n                currentHero7->Stats(HERO_PRIMARY_KNOWLEDGE) * HERO_SPELL_POINTS_PER_KNOWLEDGE;\n            restoredSpellPoints14 +=\n                IDX(currentHero7->m_secondarySkills[IDX(HERO_SKILL_MYSTICISM)]) + 1;\n            if (currentHero7->HasArtifact(ARTIFACT_POWER_RING))\n                restoredSpellPoints14 += POWER_RING_DAILY_MANA_BONUS;\n            if (restoredSpellPoints14 > maxSpellPoints9)\n                restoredSpellPoints14 = maxSpellPoints9;\n            if (restoredSpellPoints14 > currentHero7->m_spellPoints)\n                currentHero7->m_spellPoints = restoredSpellPoints14;\n            if (HAS(currentHero7->m_eventFlags, HERO_EVENT_MAGIC_WELL))\n                H2_ENUM_CLEAR_FLAG(currentHero7->m_eventFlags, HERO_EVENT_MAGIC_WELL);\n        }\n    }\n\n    {\n        i32 townIndex;\n        for (townIndex = 0; townIndex < GAME_TOWN_COUNT; townIndex++) {\n            currentTown1 = GetTown(townIndex);\n            if (!(currentTown1->m_buildings & BIT(BUILDING_SLOT_MAGE_GUILD)))\n                continue;\n            if (currentTown1->m_occupyingHeroId != -1) {\n                townHero6 = GetHero(currentTown1->m_occupyingHeroId);\n                maxSpellPoints9 =\n                    townHero6->Stats(HERO_PRIMARY_KNOWLEDGE) * HERO_SPELL_POINTS_PER_KNOWLEDGE;\n                if (maxSpellPoints9 > townHero6->m_spellPoints)\n                    townHero6->m_spellPoints = maxSpellPoints9;\n            }\n        }\n    }\n}"
        }
      ]
    }
  ]
}
*/
