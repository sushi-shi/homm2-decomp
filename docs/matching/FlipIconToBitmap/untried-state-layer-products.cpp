/*
 * UNTRIED FlipIconToBitmap state-layer products.
 *
 * Compiler-state probes are disposable inputs, never reconstructed source.
 * These products remain to be run for legitimate source survivors:
 *
 *   - every source survivor against clean plus the identical requested state
 *     set, never only against one favorable state;
 *   - a focused replay including the current census's trial 5 (71/5 blocks)
 *     and trial 7 (exact retail size);
 *   - a second independent forest seed after a source shape improves topology;
 *   - declaration-order products crossed with state, not separate one-axis
 *     screens;
 *   - helper-boundary products crossed with setup ownership and state;
 *   - an unchanged-source census after every retained effective-source change.
 *
 * Existing layer source:
 *   build/Iconf2b-70block-clean-50-summary-20260727.json
 *
 * Exact closure still requires exact bytes, retail size, and complete ordered
 * relocation identity/addends.  Exact block count or exact size alone is only
 * a selection signal.
 */

void FlipIconToBitmap_untried_state_layer_products() {
}
