// Base commit 3e738b4bd plus shared wire-header recovery documented in U09-U14.md.
// Complete three-arm source product: 4.135787491686642 seconds; all compiled, restored, untruncated.
// Retain trial 2, typed: size 1971, text 8001d38acae64c9a, normalized relocs e44b6f46977541d4.
// These source trials used the first RemoteSaveChunk data[200] layout; final retained data[245]
// covers the complete receive payload and separately passes full native/layout/retail checks.
// No AST mutations or TU-state probes. See docs/reconstruction/U09-U14.md for all controls and limits.
// Artifacts: build/source-variant-batch/u14-transmit-full/results.json and build/u14-transmit-full-manifest.json.
/* Exact source product:
{
  "schema": 1,
  "source": "src/SOURCE/GAME.cpp",
  "rva": "0x5da83",
  "axes": [
    {
      "name": "packet_owner",
      "find": "i32 game::TransmitSaveGame(i32 remotePlayer, i32 playerExited, i32 useCurrentSave) {\n    char filename[TRANSMIT_FILENAME_CAPACITY];\n    u32 transmitCrc;\n    i32 packetsInBatch;\n    i32 H2_UNUSED(unused1d0);\n    u32 fileCrc;\n    i32 oldTrack;\n    i32 batchCount;\n    i32 chunkSize;\n    i32 result;\n    i32 packetCount;\n    char* reply;\n    b32 success;\n    i32 fileSize;\n    i32 packet;\n    u8* transmitData;\n    i32 file;\n    i32 H2_UNUSED(unused208);\n    b32 done;\n    i32 H2_UNUSED(unused21c);\n    bool samplesReady;\n    u8* fileData;\n    char* acknowledged;\n    i32* header;\n    i32 H2_UNUSED(unused8);\n    i32 batch;\n    i32 H2_UNUSED(unused9);\n\n    gpAdvManager->TrimLoopingSounds(REMOTE_LOOPING_SOUND_COUNT);\n    header = NULL;\n    reply = NULL;\n    transmitData = NULL;\n    fileData = NULL;\n    success = false;\n    result = 0;\n    acknowledged = NULL;\n    oldTrack = -1;\n\n    samplesReady = gSoundBackendsReady;\n    gSoundBackendsReady = 1;\n    oldTrack = gpSoundManager->m_musicTrack;\n    gpSoundManager->SwitchAmbientMusic(-1);\n    gSoundBackendsReady = samplesReady;\n\n    LogStr(\"Transmit Game Start\");\n    if (gpAdvManager->m_active == 1)\n        BVResMsg(\"\\xcf\\xe5\\xf0\\xe5\\xf1\\xfb\\xeb\\xea\\xe0 \\xe4\\xe0\\xed\\xed\\xfb\\xf5\", RES_NONE, 0);\n    AiPrint(\"Transmit Start - Compressing\");\n\n    acknowledged = static_cast<char*>(H2_ALLOC(REMOTE_PACKET_TRACKING_CAPACITY));\n    memset(acknowledged, 0, REMOTE_PACKET_TRACKING_CAPACITY);\n    SaveGame(gConfig.rmtSCName, 0, 0);\n    if (!gbUseDiffCompression)\n        useCurrentSave = 1;\n    CreateDiffFile(\n        gConfig.rmtSLName,\n        gConfig.rmtSCName,\n        gConfig.rmtSDName,\n        remotePlayer,\n        useCurrentSave\n    );\n    sprintf(filename, \"%s%s\", \".\\\\DATA\\\\\", gConfig.rmtSDName);\n    fileSize = FileSize(filename);\n    LogInt(\n        \"PostDiffFileSize\",\n        fileSize,\n        LOG_UNUSED_VALUE,\n        LOG_UNUSED_VALUE,\n        LOG_UNUSED_VALUE,\n        LOG_UNUSED_VALUE,\n        LOG_UNUSED_VALUE,\n        LOG_UNUSED_VALUE\n    );\n\n    header = static_cast<i32*>(H2_ALLOC(REMOTE_HEADER_CAPACITY));\n    if (gbUseRegularCompression)\n        transmitData = static_cast<u8*>(H2_ALLOC(fileSize + REMOTE_BUFFER_EXTRA));\n    fileData = static_cast<u8*>(H2_ALLOC(fileSize + REMOTE_BUFFER_EXTRA));\n\n    file = open(filename, _O_BINARY);\n    if (file == -1)\n        FileError(filename);\n    if (file == -1) {\n        goto transmitCleanup;\n    }\n    {\n        read(file, fileData, fileSize);\n        close(file);\n        fileCrc = calc_crc_long(fileData, fileSize);\n        if (gbUseRegularCompression)\n            fileSize = EncodeData(\n                reinterpret_cast<char*>(transmitData),\n                reinterpret_cast<char*>(fileData),\n                fileSize\n            );\n        else\n            transmitData = fileData;\n\n        AiPrint(\"Transmit Start - Sending\");\n        if (gbUseRegularCompression)\n            transmitCrc = calc_crc_long(transmitData, fileSize);\n        else\n            transmitCrc = fileCrc;\n        LogInt(\n            \"Send\",\n            fileSize,\n            transmitCrc,\n            LOG_UNUSED_VALUE,\n            LOG_UNUSED_VALUE,\n            LOG_UNUSED_VALUE,\n            LOG_UNUSED_VALUE,\n            LOG_UNUSED_VALUE\n        );\n\n        header[REMOTE_SAVE_HEADER_FILE_SIZE] = fileSize;\n        header[REMOTE_SAVE_HEADER_FILE_CRC] = fileCrc;\n        header[REMOTE_SAVE_HEADER_TRANSMIT_CRC] = transmitCrc;\n        header[REMOTE_SAVE_HEADER_PLAYER_EXITED] = playerExited;\n        result = TransmitAndWait(\n            reinterpret_cast<char*>(header),\n            remotePlayer,\n            REMOTE_SAVE_HEADER_SIZE,\n            REMOTE_SAVE_INIT_COMMAND,\n            REMOTE_SAVE_INIT_RESPONSE,\n            &reply\n        );\n        if (!result)\n            ShutDown(NULL);\n\n        packetCount = (fileSize - 1) / REMOTE_PACKET_PAYLOAD_SIZE + 1;\n        batchCount = (packetCount - 1) / REMOTE_PACKET_BATCH_SIZE + 1;\n        for (batch = 0; batch < batchCount; batch++) {\n            if (batch + 1 == batchCount)\n                packetsInBatch = packetCount - batch * REMOTE_PACKET_BATCH_SIZE;\n            else\n                packetsInBatch = REMOTE_PACKET_BATCH_SIZE;\n\n            done = false;\n            while (!done) {\n                for (packet = batch * REMOTE_PACKET_BATCH_SIZE;\n                     packet < batch * REMOTE_PACKET_BATCH_SIZE + packetsInBatch;\n                     packet++) {\n                    PollSound();\n                    CheckDoMain(0, 1);\n                    if (!acknowledged[packet]) {\n                        if (packet + 1 == packetCount)\n                            chunkSize = fileSize - packet * REMOTE_PACKET_PAYLOAD_SIZE;\n                        else\n                            chunkSize = REMOTE_PACKET_PAYLOAD_SIZE;\n                        *reinterpret_cast<i16*>(header) = packet;\n                        memcpy(\n                            reinterpret_cast<char*>(header) + REMOTE_PACKET_INDEX_SIZE,\n                            transmitData + packet * REMOTE_PACKET_PAYLOAD_SIZE,\n                            chunkSize\n                        );\n                        result = TransmitRemoteData(\n                            reinterpret_cast<char*>(header),\n                            remotePlayer,\n                            chunkSize + REMOTE_PACKET_INDEX_SIZE,\n                            REMOTE_SAVE_DATA_COMMAND,\n                            0,\n                            1,\n                            REMOTE_MESSAGE_DEFAULT\n                        );\n                        if (!result)\n                            ShutDown(NULL);\n                    }\n                }\n                LogStr(\"PreWait\");\n                *reinterpret_cast<i16*>(header) =\n                    batch * REMOTE_PACKET_BATCH_SIZE;\n                result = TransmitAndWait(\n                    reinterpret_cast<char*>(header),\n                    remotePlayer,\n                    REMOTE_PACKET_INDEX_SIZE,\n                    REMOTE_SAVE_ACK_REQUEST_COMMAND,\n                    REMOTE_SAVE_ACK_RESPONSE_COMMAND,\n                    &reply\n                );\n                LogStr(\"PostWait\");\n                if (!result)\n                    ShutDown(NULL);\n                for (packet = 0; packet < packetsInBatch; packet++) {\n                    if (reinterpret_cast<RemoteMessage*>(reply)->payload[packet] > 0)\n                        *(acknowledged + packet + batch * REMOTE_PACKET_BATCH_SIZE) = 1;\n                }\n                done = true;\n                for (packet = batch * REMOTE_PACKET_BATCH_SIZE;\n                     packet < batch * REMOTE_PACKET_BATCH_SIZE + packetsInBatch;\n                     packet++) {\n                    if (!acknowledged[packet])\n                        done = false;\n                }\n            }\n        }\n        result = TransmitRemoteData(\n            NULL,\n            remotePlayer,\n            0,\n            REMOTE_SAVE_FINISH_COMMAND,\n            1,\n            1,\n            REMOTE_MESSAGE_DEFAULT\n        );\n        if (!result)\n            ShutDown(NULL);\n        success = true;\n    }\n\ntransmitCleanup:\n    if (header)\n        H2_FREE(header);\n    if (transmitData)\n        H2_FREE(transmitData);\n    if (fileData && fileData != transmitData)\n        H2_FREE(fileData);\n    if (acknowledged)\n        H2_FREE(acknowledged);\n\n    AiPrint(\"Transmit End\");\n    if (gpAdvManager->m_active == 1) {\n        giBottomViewOverride = BOTTOM_VIEW_NONE;\n        gpAdvManager->UpdBottomView(true, true, true);\n    }\n    if (oldTrack != -1) {\n        samplesReady = gSoundBackendsReady;\n        gSoundBackendsReady = 1;\n        gpSoundManager->SwitchAmbientMusic(oldTrack);\n        gSoundBackendsReady = samplesReady;\n    }\n    return success;\n}",
      "options": [
        {
          "name": "raw"
        },
        {
          "name": "typed_keep_indices",
          "replace": "i32 game::TransmitSaveGame(i32 remotePlayer, i32 playerExited, i32 useCurrentSave) {\n    char filename[TRANSMIT_FILENAME_CAPACITY];\n    u32 transmitCrc;\n    i32 packetsInBatch;\n    i32 H2_UNUSED(unused1d0);\n    u32 fileCrc;\n    i32 oldTrack;\n    i32 batchCount;\n    i32 chunkSize;\n    i32 result;\n    i32 packetCount;\n    char* reply;\n    b32 success;\n    i32 fileSize;\n    i32 packet;\n    u8* transmitData;\n    i32 file;\n    i32 H2_UNUSED(unused208);\n    b32 done;\n    i32 H2_UNUSED(unused21c);\n    bool samplesReady;\n    u8* fileData;\n    char* acknowledged;\n    RemoteSaveBuffer* header;\n    i32 H2_UNUSED(unused8);\n    i32 batch;\n    i32 H2_UNUSED(unused9);\n\n    gpAdvManager->TrimLoopingSounds(REMOTE_LOOPING_SOUND_COUNT);\n    header = NULL;\n    reply = NULL;\n    transmitData = NULL;\n    fileData = NULL;\n    success = false;\n    result = 0;\n    acknowledged = NULL;\n    oldTrack = -1;\n\n    samplesReady = gSoundBackendsReady;\n    gSoundBackendsReady = 1;\n    oldTrack = gpSoundManager->m_musicTrack;\n    gpSoundManager->SwitchAmbientMusic(-1);\n    gSoundBackendsReady = samplesReady;\n\n    LogStr(\"Transmit Game Start\");\n    if (gpAdvManager->m_active == 1)\n        BVResMsg(\"\\xcf\\xe5\\xf0\\xe5\\xf1\\xfb\\xeb\\xea\\xe0 \\xe4\\xe0\\xed\\xed\\xfb\\xf5\", RES_NONE, 0);\n    AiPrint(\"Transmit Start - Compressing\");\n\n    acknowledged = static_cast<char*>(H2_ALLOC(REMOTE_PACKET_TRACKING_CAPACITY));\n    memset(acknowledged, 0, REMOTE_PACKET_TRACKING_CAPACITY);\n    SaveGame(gConfig.rmtSCName, 0, 0);\n    if (!gbUseDiffCompression)\n        useCurrentSave = 1;\n    CreateDiffFile(\n        gConfig.rmtSLName,\n        gConfig.rmtSCName,\n        gConfig.rmtSDName,\n        remotePlayer,\n        useCurrentSave\n    );\n    sprintf(filename, \"%s%s\", \".\\\\DATA\\\\\", gConfig.rmtSDName);\n    fileSize = FileSize(filename);\n    LogInt(\n        \"PostDiffFileSize\",\n        fileSize,\n        LOG_UNUSED_VALUE,\n        LOG_UNUSED_VALUE,\n        LOG_UNUSED_VALUE,\n        LOG_UNUSED_VALUE,\n        LOG_UNUSED_VALUE,\n        LOG_UNUSED_VALUE\n    );\n\n    header = static_cast<RemoteSaveBuffer*>(H2_ALLOC(REMOTE_HEADER_CAPACITY));\n    if (gbUseRegularCompression)\n        transmitData = static_cast<u8*>(H2_ALLOC(fileSize + REMOTE_BUFFER_EXTRA));\n    fileData = static_cast<u8*>(H2_ALLOC(fileSize + REMOTE_BUFFER_EXTRA));\n\n    file = open(filename, _O_BINARY);\n    if (file == -1)\n        FileError(filename);\n    if (file == -1) {\n        goto transmitCleanup;\n    }\n    {\n        read(file, fileData, fileSize);\n        close(file);\n        fileCrc = calc_crc_long(fileData, fileSize);\n        if (gbUseRegularCompression)\n            fileSize = EncodeData(\n                reinterpret_cast<char*>(transmitData),\n                reinterpret_cast<char*>(fileData),\n                fileSize\n            );\n        else\n            transmitData = fileData;\n\n        AiPrint(\"Transmit Start - Sending\");\n        if (gbUseRegularCompression)\n            transmitCrc = calc_crc_long(transmitData, fileSize);\n        else\n            transmitCrc = fileCrc;\n        LogInt(\n            \"Send\",\n            fileSize,\n            transmitCrc,\n            LOG_UNUSED_VALUE,\n            LOG_UNUSED_VALUE,\n            LOG_UNUSED_VALUE,\n            LOG_UNUSED_VALUE,\n            LOG_UNUSED_VALUE\n        );\n\n        header->init.dataSize = fileSize;\n        header->init.crc = fileCrc;\n        header->init.wireCrc = transmitCrc;\n        header->init.playerExited = playerExited;\n        result = TransmitAndWait(\n            header->bytes,\n            remotePlayer,\n            REMOTE_SAVE_HEADER_SIZE,\n            REMOTE_SAVE_INIT_COMMAND,\n            REMOTE_SAVE_INIT_RESPONSE,\n            &reply\n        );\n        if (!result)\n            ShutDown(NULL);\n\n        packetCount = (fileSize - 1) / REMOTE_PACKET_PAYLOAD_SIZE + 1;\n        batchCount = (packetCount - 1) / REMOTE_PACKET_BATCH_SIZE + 1;\n        for (batch = 0; batch < batchCount; batch++) {\n            if (batch + 1 == batchCount)\n                packetsInBatch = packetCount - batch * REMOTE_PACKET_BATCH_SIZE;\n            else\n                packetsInBatch = REMOTE_PACKET_BATCH_SIZE;\n\n            done = false;\n            while (!done) {\n                for (packet = batch * REMOTE_PACKET_BATCH_SIZE;\n                     packet < batch * REMOTE_PACKET_BATCH_SIZE + packetsInBatch;\n                     packet++) {\n                    PollSound();\n                    CheckDoMain(0, 1);\n                    if (!acknowledged[packet]) {\n                        if (packet + 1 == packetCount)\n                            chunkSize = fileSize - packet * REMOTE_PACKET_PAYLOAD_SIZE;\n                        else\n                            chunkSize = REMOTE_PACKET_PAYLOAD_SIZE;\n                        header->chunk.packetIndex = packet;\n                        memcpy(\n                            header->chunk.data,\n                            transmitData + packet * REMOTE_PACKET_PAYLOAD_SIZE,\n                            chunkSize\n                        );\n                        result = TransmitRemoteData(\n                            header->bytes,\n                            remotePlayer,\n                            chunkSize + REMOTE_PACKET_INDEX_SIZE,\n                            REMOTE_SAVE_DATA_COMMAND,\n                            0,\n                            1,\n                            REMOTE_MESSAGE_DEFAULT\n                        );\n                        if (!result)\n                            ShutDown(NULL);\n                    }\n                }\n                LogStr(\"PreWait\");\n                header->chunk.packetIndex =\n                    batch * REMOTE_PACKET_BATCH_SIZE;\n                result = TransmitAndWait(\n                    header->bytes,\n                    remotePlayer,\n                    REMOTE_PACKET_INDEX_SIZE,\n                    REMOTE_SAVE_ACK_REQUEST_COMMAND,\n                    REMOTE_SAVE_ACK_RESPONSE_COMMAND,\n                    &reply\n                );\n                LogStr(\"PostWait\");\n                if (!result)\n                    ShutDown(NULL);\n                for (packet = 0; packet < packetsInBatch; packet++) {\n                    if (reinterpret_cast<RemoteMessage*>(reply)->payload[packet] > 0)\n                        *(acknowledged + packet + batch * REMOTE_PACKET_BATCH_SIZE) = 1;\n                }\n                done = true;\n                for (packet = batch * REMOTE_PACKET_BATCH_SIZE;\n                     packet < batch * REMOTE_PACKET_BATCH_SIZE + packetsInBatch;\n                     packet++) {\n                    if (!acknowledged[packet])\n                        done = false;\n                }\n            }\n        }\n        result = TransmitRemoteData(\n            NULL,\n            remotePlayer,\n            0,\n            REMOTE_SAVE_FINISH_COMMAND,\n            1,\n            1,\n            REMOTE_MESSAGE_DEFAULT\n        );\n        if (!result)\n            ShutDown(NULL);\n        success = true;\n    }\n\ntransmitCleanup:\n    if (header)\n        H2_FREE(header);\n    if (transmitData)\n        H2_FREE(transmitData);\n    if (fileData && fileData != transmitData)\n        H2_FREE(fileData);\n    if (acknowledged)\n        H2_FREE(acknowledged);\n\n    AiPrint(\"Transmit End\");\n    if (gpAdvManager->m_active == 1) {\n        giBottomViewOverride = BOTTOM_VIEW_NONE;\n        gpAdvManager->UpdBottomView(true, true, true);\n    }\n    if (oldTrack != -1) {\n        samplesReady = gSoundBackendsReady;\n        gSoundBackendsReady = 1;\n        gpSoundManager->SwitchAmbientMusic(oldTrack);\n        gSoundBackendsReady = samplesReady;\n    }\n    return success;\n}",
          "extra_edits": [
            {
              "find": "    REMOTE_HEADER_CAPACITY           = 256,\n",
              "replace": "    REMOTE_HEADER_CAPACITY           = REMOTE_SAVE_BUFFER_SIZE,\n"
            },
            {
              "find": "    REMOTE_PACKET_PAYLOAD_SIZE       = 200,\n",
              "replace": "    REMOTE_PACKET_PAYLOAD_SIZE       = REMOTE_SAVE_CHUNK_SIZE,\n"
            },
            {
              "find": "    REMOTE_SAVE_HEADER_SIZE          = sizeof(i32) * 4,\n",
              "replace": "    REMOTE_SAVE_HEADER_SIZE          = sizeof(RemoteSaveInitialization),\n"
            }
          ]
        },
        {
          "name": "typed",
          "replace": "i32 game::TransmitSaveGame(i32 remotePlayer, i32 playerExited, i32 useCurrentSave) {\n    char filename[TRANSMIT_FILENAME_CAPACITY];\n    u32 transmitCrc;\n    i32 packetsInBatch;\n    i32 H2_UNUSED(unused1d0);\n    u32 fileCrc;\n    i32 oldTrack;\n    i32 batchCount;\n    i32 chunkSize;\n    i32 result;\n    i32 packetCount;\n    char* reply;\n    b32 success;\n    i32 fileSize;\n    i32 packet;\n    u8* transmitData;\n    i32 file;\n    i32 H2_UNUSED(unused208);\n    b32 done;\n    i32 H2_UNUSED(unused21c);\n    bool samplesReady;\n    u8* fileData;\n    char* acknowledged;\n    RemoteSaveBuffer* header;\n    i32 H2_UNUSED(unused8);\n    i32 batch;\n    i32 H2_UNUSED(unused9);\n\n    gpAdvManager->TrimLoopingSounds(REMOTE_LOOPING_SOUND_COUNT);\n    header = NULL;\n    reply = NULL;\n    transmitData = NULL;\n    fileData = NULL;\n    success = false;\n    result = 0;\n    acknowledged = NULL;\n    oldTrack = -1;\n\n    samplesReady = gSoundBackendsReady;\n    gSoundBackendsReady = 1;\n    oldTrack = gpSoundManager->m_musicTrack;\n    gpSoundManager->SwitchAmbientMusic(-1);\n    gSoundBackendsReady = samplesReady;\n\n    LogStr(\"Transmit Game Start\");\n    if (gpAdvManager->m_active == 1)\n        BVResMsg(\"\\xcf\\xe5\\xf0\\xe5\\xf1\\xfb\\xeb\\xea\\xe0 \\xe4\\xe0\\xed\\xed\\xfb\\xf5\", RES_NONE, 0);\n    AiPrint(\"Transmit Start - Compressing\");\n\n    acknowledged = static_cast<char*>(H2_ALLOC(REMOTE_PACKET_TRACKING_CAPACITY));\n    memset(acknowledged, 0, REMOTE_PACKET_TRACKING_CAPACITY);\n    SaveGame(gConfig.rmtSCName, 0, 0);\n    if (!gbUseDiffCompression)\n        useCurrentSave = 1;\n    CreateDiffFile(\n        gConfig.rmtSLName,\n        gConfig.rmtSCName,\n        gConfig.rmtSDName,\n        remotePlayer,\n        useCurrentSave\n    );\n    sprintf(filename, \"%s%s\", \".\\\\DATA\\\\\", gConfig.rmtSDName);\n    fileSize = FileSize(filename);\n    LogInt(\n        \"PostDiffFileSize\",\n        fileSize,\n        LOG_UNUSED_VALUE,\n        LOG_UNUSED_VALUE,\n        LOG_UNUSED_VALUE,\n        LOG_UNUSED_VALUE,\n        LOG_UNUSED_VALUE,\n        LOG_UNUSED_VALUE\n    );\n\n    header = static_cast<RemoteSaveBuffer*>(H2_ALLOC(REMOTE_HEADER_CAPACITY));\n    if (gbUseRegularCompression)\n        transmitData = static_cast<u8*>(H2_ALLOC(fileSize + REMOTE_BUFFER_EXTRA));\n    fileData = static_cast<u8*>(H2_ALLOC(fileSize + REMOTE_BUFFER_EXTRA));\n\n    file = open(filename, _O_BINARY);\n    if (file == -1)\n        FileError(filename);\n    if (file == -1) {\n        goto transmitCleanup;\n    }\n    {\n        read(file, fileData, fileSize);\n        close(file);\n        fileCrc = calc_crc_long(fileData, fileSize);\n        if (gbUseRegularCompression)\n            fileSize = EncodeData(\n                reinterpret_cast<char*>(transmitData),\n                reinterpret_cast<char*>(fileData),\n                fileSize\n            );\n        else\n            transmitData = fileData;\n\n        AiPrint(\"Transmit Start - Sending\");\n        if (gbUseRegularCompression)\n            transmitCrc = calc_crc_long(transmitData, fileSize);\n        else\n            transmitCrc = fileCrc;\n        LogInt(\n            \"Send\",\n            fileSize,\n            transmitCrc,\n            LOG_UNUSED_VALUE,\n            LOG_UNUSED_VALUE,\n            LOG_UNUSED_VALUE,\n            LOG_UNUSED_VALUE,\n            LOG_UNUSED_VALUE\n        );\n\n        header->init.dataSize = fileSize;\n        header->init.crc = fileCrc;\n        header->init.wireCrc = transmitCrc;\n        header->init.playerExited = playerExited;\n        result = TransmitAndWait(\n            header->bytes,\n            remotePlayer,\n            REMOTE_SAVE_HEADER_SIZE,\n            REMOTE_SAVE_INIT_COMMAND,\n            REMOTE_SAVE_INIT_RESPONSE,\n            &reply\n        );\n        if (!result)\n            ShutDown(NULL);\n\n        packetCount = (fileSize - 1) / REMOTE_PACKET_PAYLOAD_SIZE + 1;\n        batchCount = (packetCount - 1) / REMOTE_PACKET_BATCH_SIZE + 1;\n        for (batch = 0; batch < batchCount; batch++) {\n            if (batch + 1 == batchCount)\n                packetsInBatch = packetCount - batch * REMOTE_PACKET_BATCH_SIZE;\n            else\n                packetsInBatch = REMOTE_PACKET_BATCH_SIZE;\n\n            done = false;\n            while (!done) {\n                for (packet = batch * REMOTE_PACKET_BATCH_SIZE;\n                     packet < batch * REMOTE_PACKET_BATCH_SIZE + packetsInBatch;\n                     packet++) {\n                    PollSound();\n                    CheckDoMain(0, 1);\n                    if (!acknowledged[packet]) {\n                        if (packet + 1 == packetCount)\n                            chunkSize = fileSize - packet * REMOTE_PACKET_PAYLOAD_SIZE;\n                        else\n                            chunkSize = REMOTE_PACKET_PAYLOAD_SIZE;\n                        header->chunk.packetIndex = packet;\n                        memcpy(\n                            header->chunk.data,\n                            transmitData + packet * REMOTE_PACKET_PAYLOAD_SIZE,\n                            chunkSize\n                        );\n                        result = TransmitRemoteData(\n                            header->bytes,\n                            remotePlayer,\n                            chunkSize + REMOTE_PACKET_INDEX_SIZE,\n                            REMOTE_SAVE_DATA_COMMAND,\n                            0,\n                            1,\n                            REMOTE_MESSAGE_DEFAULT\n                        );\n                        if (!result)\n                            ShutDown(NULL);\n                    }\n                }\n                LogStr(\"PreWait\");\n                header->chunk.packetIndex =\n                    batch * REMOTE_PACKET_BATCH_SIZE;\n                result = TransmitAndWait(\n                    header->bytes,\n                    remotePlayer,\n                    REMOTE_PACKET_INDEX_SIZE,\n                    REMOTE_SAVE_ACK_REQUEST_COMMAND,\n                    REMOTE_SAVE_ACK_RESPONSE_COMMAND,\n                    &reply\n                );\n                LogStr(\"PostWait\");\n                if (!result)\n                    ShutDown(NULL);\n                for (packet = 0; packet < packetsInBatch; packet++) {\n                    if (reinterpret_cast<RemoteMessage*>(reply)->payload[packet] > 0)\n                        *(acknowledged + packet + batch * REMOTE_PACKET_BATCH_SIZE) = 1;\n                }\n                done = true;\n                for (packet = batch * REMOTE_PACKET_BATCH_SIZE;\n                     packet < batch * REMOTE_PACKET_BATCH_SIZE + packetsInBatch;\n                     packet++) {\n                    if (!acknowledged[packet])\n                        done = false;\n                }\n            }\n        }\n        result = TransmitRemoteData(\n            NULL,\n            remotePlayer,\n            0,\n            REMOTE_SAVE_FINISH_COMMAND,\n            1,\n            1,\n            REMOTE_MESSAGE_DEFAULT\n        );\n        if (!result)\n            ShutDown(NULL);\n        success = true;\n    }\n\ntransmitCleanup:\n    if (header)\n        H2_FREE(header);\n    if (transmitData)\n        H2_FREE(transmitData);\n    if (fileData && fileData != transmitData)\n        H2_FREE(fileData);\n    if (acknowledged)\n        H2_FREE(acknowledged);\n\n    AiPrint(\"Transmit End\");\n    if (gpAdvManager->m_active == 1) {\n        giBottomViewOverride = BOTTOM_VIEW_NONE;\n        gpAdvManager->UpdBottomView(true, true, true);\n    }\n    if (oldTrack != -1) {\n        samplesReady = gSoundBackendsReady;\n        gSoundBackendsReady = 1;\n        gpSoundManager->SwitchAmbientMusic(oldTrack);\n        gSoundBackendsReady = samplesReady;\n    }\n    return success;\n}",
          "extra_edits": [
            {
              "find": "    REMOTE_HEADER_CAPACITY           = 256,\n",
              "replace": "    REMOTE_HEADER_CAPACITY           = REMOTE_SAVE_BUFFER_SIZE,\n"
            },
            {
              "find": "    REMOTE_PACKET_PAYLOAD_SIZE       = 200,\n",
              "replace": "    REMOTE_PACKET_PAYLOAD_SIZE       = REMOTE_SAVE_CHUNK_SIZE,\n"
            },
            {
              "find": "    REMOTE_SAVE_HEADER_SIZE          = sizeof(i32) * 4,\n",
              "replace": "    REMOTE_SAVE_HEADER_SIZE          = sizeof(RemoteSaveInitialization),\n"
            },
            {
              "find": "    REMOTE_SAVE_HEADER_FILE_SIZE     = 0,\n    REMOTE_SAVE_HEADER_FILE_CRC      = 1,\n    REMOTE_SAVE_HEADER_TRANSMIT_CRC  = 2,\n    REMOTE_SAVE_HEADER_PLAYER_EXITED = 3,\n",
              "replace": ""
            }
          ]
        }
      ]
    }
  ]
}
*/
