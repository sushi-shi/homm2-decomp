// VC6 SP5 REQUEST source family c73-request-knob-numeric; parent13e6af40.
// Source record only. All alternatives below are relative to the stated BEFORE,
// not cumulative diffs. No cross-family/TU-state census is claimed.
// build/source-variant-batch/c73-request-knob-numeric/results.json; RVA 0x9085b
// Complete 32 arms; elapsed 18.282151s; input restored.
// Disposition: retain all scalar removals in this family (nine-character scratch for S66); combined all98 native proof.
// EH private symbol hashes may differ; native comparison canonicalizes actual section owners.
// trial / choices / bytes / text hash / normalized reloc hash / blocks / exact,size,shift,flow / graph exact
// 0 / {'source_family': 'keep', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 1 / {'source_family': 'number4', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 2 / {'source_family': 'number3', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 3 / {'source_family': 'number3_number4', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 4 / {'source_family': 'number2', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 5 / {'source_family': 'number2_number4', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 6 / {'source_family': 'number2_number3', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 7 / {'source_family': 'number2_number3_number4', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 8 / {'source_family': 'number1', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 9 / {'source_family': 'number1_number4', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 10 / {'source_family': 'number1_number3', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 11 / {'source_family': 'number1_number3_number4', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 12 / {'source_family': 'number1_number2', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 13 / {'source_family': 'number1_number2_number4', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 14 / {'source_family': 'number1_number2_number3', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 15 / {'source_family': 'number1_number2_number3_number4', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 16 / {'source_family': 'number0', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 17 / {'source_family': 'number0_number4', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 18 / {'source_family': 'number0_number3', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 19 / {'source_family': 'number0_number3_number4', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 20 / {'source_family': 'number0_number2', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 21 / {'source_family': 'number0_number2_number4', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 22 / {'source_family': 'number0_number2_number3', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 23 / {'source_family': 'number0_number2_number3_number4', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 24 / {'source_family': 'number0_number1', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 25 / {'source_family': 'number0_number1_number4', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 26 / {'source_family': 'number0_number1_number3', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 27 / {'source_family': 'number0_number1_number3_number4', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 28 / {'source_family': 'number0_number1_number2', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 29 / {'source_family': 'number0_number1_number2_number4', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 30 / {'source_family': 'number0_number1_number2_number3', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True
// 31 / {'source_family': 'number0_number1_number2_number3_number4', 'candidate': 'baseline'} / 603 / 82381f8743daa35e / 3791a5d03a7db36f / 20:20 / 20,0,0,0 / True

// AXIS source_family
// BEFORE (complete exact span):
// |         if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// |             if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// |                 knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// |             }
// |             if (static_cast<float>(knobMessage.payload.mouse.y)
// |                 > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                 knobMessage.payload.mouse.y =
// |                     static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// |             }
// |             gpMouseManager->Main(knobMessage);
// |             m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// |             if (m_fileCount > iMaxListSize) {
// |                 topIndex = static_cast<i32>((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// |                 if (topIndex != oldTopIndex) {
// ALTERNATIVE number4 (unified source delta from BEFORE):
// | --- before
// | +++ number4
// | @@ -2,7 +2,7 @@
// |              if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// |                  knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// |              }
// | -            if (static_cast<float>(knobMessage.payload.mouse.y)
// | +            if ((knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// |                      static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// ALTERNATIVE number3 (unified source delta from BEFORE):
// | --- before
// | +++ number3
// | @@ -1,5 +1,5 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// | -            if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | +            if ((knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// |                  knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// |              }
// |              if (static_cast<float>(knobMessage.payload.mouse.y)
// ALTERNATIVE number3_number4 (unified source delta from BEFORE):
// | --- before
// | +++ number3_number4
// | @@ -1,8 +1,8 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// | -            if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | +            if ((knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// |                  knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// |              }
// | -            if (static_cast<float>(knobMessage.payload.mouse.y)
// | +            if ((knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// |                      static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// ALTERNATIVE number2 (unified source delta from BEFORE):
// | --- before
// | +++ number2
// | @@ -10,5 +10,5 @@
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// |              if (m_fileCount > iMaxListSize) {
// | -                topIndex = static_cast<i32>((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// | +                topIndex = ((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// |                  if (topIndex != oldTopIndex) {
// ALTERNATIVE number2_number4 (unified source delta from BEFORE):
// | --- before
// | +++ number2_number4
// | @@ -2,7 +2,7 @@
// |              if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// |                  knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// |              }
// | -            if (static_cast<float>(knobMessage.payload.mouse.y)
// | +            if ((knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// |                      static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// | @@ -10,5 +10,5 @@
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// |              if (m_fileCount > iMaxListSize) {
// | -                topIndex = static_cast<i32>((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// | +                topIndex = ((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// |                  if (topIndex != oldTopIndex) {
// ALTERNATIVE number2_number3 (unified source delta from BEFORE):
// | --- before
// | +++ number2_number3
// | @@ -1,5 +1,5 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// | -            if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | +            if ((knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// |                  knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// |              }
// |              if (static_cast<float>(knobMessage.payload.mouse.y)
// | @@ -10,5 +10,5 @@
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// |              if (m_fileCount > iMaxListSize) {
// | -                topIndex = static_cast<i32>((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// | +                topIndex = ((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// |                  if (topIndex != oldTopIndex) {
// ALTERNATIVE number2_number3_number4 (unified source delta from BEFORE):
// | --- before
// | +++ number2_number3_number4
// | @@ -1,8 +1,8 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// | -            if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | +            if ((knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// |                  knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// |              }
// | -            if (static_cast<float>(knobMessage.payload.mouse.y)
// | +            if ((knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// |                      static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// | @@ -10,5 +10,5 @@
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// |              if (m_fileCount > iMaxListSize) {
// | -                topIndex = static_cast<i32>((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// | +                topIndex = ((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// |                  if (topIndex != oldTopIndex) {
// ALTERNATIVE number1 (unified source delta from BEFORE):
// | --- before
// | +++ number1
// | @@ -5,7 +5,7 @@
// |              if (static_cast<float>(knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// | -                    static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// | +                    (knobOffset + fGutterMinY + fGutterTravelLength);
// |              }
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// ALTERNATIVE number1_number4 (unified source delta from BEFORE):
// | --- before
// | +++ number1_number4
// | @@ -2,10 +2,10 @@
// |              if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// |                  knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// |              }
// | -            if (static_cast<float>(knobMessage.payload.mouse.y)
// | +            if ((knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// | -                    static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// | +                    (knobOffset + fGutterMinY + fGutterTravelLength);
// |              }
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// ALTERNATIVE number1_number3 (unified source delta from BEFORE):
// | --- before
// | +++ number1_number3
// | @@ -1,11 +1,11 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// | -            if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | +            if ((knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// |                  knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// |              }
// |              if (static_cast<float>(knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// | -                    static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// | +                    (knobOffset + fGutterMinY + fGutterTravelLength);
// |              }
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// ALTERNATIVE number1_number3_number4 (unified source delta from BEFORE):
// | --- before
// | +++ number1_number3_number4
// | @@ -1,11 +1,11 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// | -            if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | +            if ((knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// |                  knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// |              }
// | -            if (static_cast<float>(knobMessage.payload.mouse.y)
// | +            if ((knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// | -                    static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// | +                    (knobOffset + fGutterMinY + fGutterTravelLength);
// |              }
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// ALTERNATIVE number1_number2 (unified source delta from BEFORE):
// | --- before
// | +++ number1_number2
// | @@ -5,10 +5,10 @@
// |              if (static_cast<float>(knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// | -                    static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// | +                    (knobOffset + fGutterMinY + fGutterTravelLength);
// |              }
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// |              if (m_fileCount > iMaxListSize) {
// | -                topIndex = static_cast<i32>((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// | +                topIndex = ((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// |                  if (topIndex != oldTopIndex) {
// ALTERNATIVE number1_number2_number4 (unified source delta from BEFORE):
// | --- before
// | +++ number1_number2_number4
// | @@ -2,13 +2,13 @@
// |              if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// |                  knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// |              }
// | -            if (static_cast<float>(knobMessage.payload.mouse.y)
// | +            if ((knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// | -                    static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// | +                    (knobOffset + fGutterMinY + fGutterTravelLength);
// |              }
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// |              if (m_fileCount > iMaxListSize) {
// | -                topIndex = static_cast<i32>((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// | +                topIndex = ((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// |                  if (topIndex != oldTopIndex) {
// ALTERNATIVE number1_number2_number3 (unified source delta from BEFORE):
// | --- before
// | +++ number1_number2_number3
// | @@ -1,14 +1,14 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// | -            if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | +            if ((knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// |                  knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// |              }
// |              if (static_cast<float>(knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// | -                    static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// | +                    (knobOffset + fGutterMinY + fGutterTravelLength);
// |              }
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// |              if (m_fileCount > iMaxListSize) {
// | -                topIndex = static_cast<i32>((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// | +                topIndex = ((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// |                  if (topIndex != oldTopIndex) {
// ALTERNATIVE number1_number2_number3_number4 (unified source delta from BEFORE):
// | --- before
// | +++ number1_number2_number3_number4
// | @@ -1,14 +1,14 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// | -            if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | +            if ((knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// |                  knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// |              }
// | -            if (static_cast<float>(knobMessage.payload.mouse.y)
// | +            if ((knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// | -                    static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// | +                    (knobOffset + fGutterMinY + fGutterTravelLength);
// |              }
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// |              if (m_fileCount > iMaxListSize) {
// | -                topIndex = static_cast<i32>((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// | +                topIndex = ((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// |                  if (topIndex != oldTopIndex) {
// ALTERNATIVE number0 (unified source delta from BEFORE):
// | --- before
// | +++ number0
// | @@ -1,6 +1,6 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// |              if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | -                knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// | +                knobMessage.payload.mouse.y = (knobOffset + fGutterMinY);
// |              }
// |              if (static_cast<float>(knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// ALTERNATIVE number0_number4 (unified source delta from BEFORE):
// | --- before
// | +++ number0_number4
// | @@ -1,8 +1,8 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// |              if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | -                knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// | +                knobMessage.payload.mouse.y = (knobOffset + fGutterMinY);
// |              }
// | -            if (static_cast<float>(knobMessage.payload.mouse.y)
// | +            if ((knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// |                      static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// ALTERNATIVE number0_number3 (unified source delta from BEFORE):
// | --- before
// | +++ number0_number3
// | @@ -1,6 +1,6 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// | -            if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | -                knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// | +            if ((knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | +                knobMessage.payload.mouse.y = (knobOffset + fGutterMinY);
// |              }
// |              if (static_cast<float>(knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// ALTERNATIVE number0_number3_number4 (unified source delta from BEFORE):
// | --- before
// | +++ number0_number3_number4
// | @@ -1,8 +1,8 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// | -            if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | -                knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// | +            if ((knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | +                knobMessage.payload.mouse.y = (knobOffset + fGutterMinY);
// |              }
// | -            if (static_cast<float>(knobMessage.payload.mouse.y)
// | +            if ((knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// |                      static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// ALTERNATIVE number0_number2 (unified source delta from BEFORE):
// | --- before
// | +++ number0_number2
// | @@ -1,6 +1,6 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// |              if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | -                knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// | +                knobMessage.payload.mouse.y = (knobOffset + fGutterMinY);
// |              }
// |              if (static_cast<float>(knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// | @@ -10,5 +10,5 @@
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// |              if (m_fileCount > iMaxListSize) {
// | -                topIndex = static_cast<i32>((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// | +                topIndex = ((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// |                  if (topIndex != oldTopIndex) {
// ALTERNATIVE number0_number2_number4 (unified source delta from BEFORE):
// | --- before
// | +++ number0_number2_number4
// | @@ -1,8 +1,8 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// |              if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | -                knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// | +                knobMessage.payload.mouse.y = (knobOffset + fGutterMinY);
// |              }
// | -            if (static_cast<float>(knobMessage.payload.mouse.y)
// | +            if ((knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// |                      static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// | @@ -10,5 +10,5 @@
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// |              if (m_fileCount > iMaxListSize) {
// | -                topIndex = static_cast<i32>((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// | +                topIndex = ((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// |                  if (topIndex != oldTopIndex) {
// ALTERNATIVE number0_number2_number3 (unified source delta from BEFORE):
// | --- before
// | +++ number0_number2_number3
// | @@ -1,6 +1,6 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// | -            if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | -                knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// | +            if ((knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | +                knobMessage.payload.mouse.y = (knobOffset + fGutterMinY);
// |              }
// |              if (static_cast<float>(knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// | @@ -10,5 +10,5 @@
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// |              if (m_fileCount > iMaxListSize) {
// | -                topIndex = static_cast<i32>((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// | +                topIndex = ((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// |                  if (topIndex != oldTopIndex) {
// ALTERNATIVE number0_number2_number3_number4 (unified source delta from BEFORE):
// | --- before
// | +++ number0_number2_number3_number4
// | @@ -1,8 +1,8 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// | -            if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | -                knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// | +            if ((knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | +                knobMessage.payload.mouse.y = (knobOffset + fGutterMinY);
// |              }
// | -            if (static_cast<float>(knobMessage.payload.mouse.y)
// | +            if ((knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// |                      static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// | @@ -10,5 +10,5 @@
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// |              if (m_fileCount > iMaxListSize) {
// | -                topIndex = static_cast<i32>((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// | +                topIndex = ((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// |                  if (topIndex != oldTopIndex) {
// ALTERNATIVE number0_number1 (unified source delta from BEFORE):
// | --- before
// | +++ number0_number1
// | @@ -1,11 +1,11 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// |              if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | -                knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// | +                knobMessage.payload.mouse.y = (knobOffset + fGutterMinY);
// |              }
// |              if (static_cast<float>(knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// | -                    static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// | +                    (knobOffset + fGutterMinY + fGutterTravelLength);
// |              }
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// ALTERNATIVE number0_number1_number4 (unified source delta from BEFORE):
// | --- before
// | +++ number0_number1_number4
// | @@ -1,11 +1,11 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// |              if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | -                knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// | +                knobMessage.payload.mouse.y = (knobOffset + fGutterMinY);
// |              }
// | -            if (static_cast<float>(knobMessage.payload.mouse.y)
// | +            if ((knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// | -                    static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// | +                    (knobOffset + fGutterMinY + fGutterTravelLength);
// |              }
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// ALTERNATIVE number0_number1_number3 (unified source delta from BEFORE):
// | --- before
// | +++ number0_number1_number3
// | @@ -1,11 +1,11 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// | -            if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | -                knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// | +            if ((knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | +                knobMessage.payload.mouse.y = (knobOffset + fGutterMinY);
// |              }
// |              if (static_cast<float>(knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// | -                    static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// | +                    (knobOffset + fGutterMinY + fGutterTravelLength);
// |              }
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// ALTERNATIVE number0_number1_number3_number4 (unified source delta from BEFORE):
// | --- before
// | +++ number0_number1_number3_number4
// | @@ -1,11 +1,11 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// | -            if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | -                knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// | +            if ((knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | +                knobMessage.payload.mouse.y = (knobOffset + fGutterMinY);
// |              }
// | -            if (static_cast<float>(knobMessage.payload.mouse.y)
// | +            if ((knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// | -                    static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// | +                    (knobOffset + fGutterMinY + fGutterTravelLength);
// |              }
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// ALTERNATIVE number0_number1_number2 (unified source delta from BEFORE):
// | --- before
// | +++ number0_number1_number2
// | @@ -1,14 +1,14 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// |              if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | -                knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// | +                knobMessage.payload.mouse.y = (knobOffset + fGutterMinY);
// |              }
// |              if (static_cast<float>(knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// | -                    static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// | +                    (knobOffset + fGutterMinY + fGutterTravelLength);
// |              }
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// |              if (m_fileCount > iMaxListSize) {
// | -                topIndex = static_cast<i32>((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// | +                topIndex = ((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// |                  if (topIndex != oldTopIndex) {
// ALTERNATIVE number0_number1_number2_number4 (unified source delta from BEFORE):
// | --- before
// | +++ number0_number1_number2_number4
// | @@ -1,14 +1,14 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// |              if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | -                knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// | +                knobMessage.payload.mouse.y = (knobOffset + fGutterMinY);
// |              }
// | -            if (static_cast<float>(knobMessage.payload.mouse.y)
// | +            if ((knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// | -                    static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// | +                    (knobOffset + fGutterMinY + fGutterTravelLength);
// |              }
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// |              if (m_fileCount > iMaxListSize) {
// | -                topIndex = static_cast<i32>((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// | +                topIndex = ((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// |                  if (topIndex != oldTopIndex) {
// ALTERNATIVE number0_number1_number2_number3 (unified source delta from BEFORE):
// | --- before
// | +++ number0_number1_number2_number3
// | @@ -1,14 +1,14 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// | -            if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | -                knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// | +            if ((knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | +                knobMessage.payload.mouse.y = (knobOffset + fGutterMinY);
// |              }
// |              if (static_cast<float>(knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// | -                    static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// | +                    (knobOffset + fGutterMinY + fGutterTravelLength);
// |              }
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// |              if (m_fileCount > iMaxListSize) {
// | -                topIndex = static_cast<i32>((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// | +                topIndex = ((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// |                  if (topIndex != oldTopIndex) {
// ALTERNATIVE number0_number1_number2_number3_number4 (unified source delta from BEFORE):
// | --- before
// | +++ number0_number1_number2_number3_number4
// | @@ -1,14 +1,14 @@
// |          if (knobMessage.type == MESSAGE_MOUSE_MOVE) {
// | -            if (static_cast<float>(knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | -                knobMessage.payload.mouse.y = static_cast<i32>(knobOffset + fGutterMinY);
// | +            if ((knobMessage.payload.mouse.y) < knobOffset + fGutterMinY) {
// | +                knobMessage.payload.mouse.y = (knobOffset + fGutterMinY);
// |              }
// | -            if (static_cast<float>(knobMessage.payload.mouse.y)
// | +            if ((knobMessage.payload.mouse.y)
// |                  > knobOffset + fGutterMinY + fGutterTravelLength) {
// |                  knobMessage.payload.mouse.y =
// | -                    static_cast<i32>(knobOffset + fGutterMinY + fGutterTravelLength);
// | +                    (knobOffset + fGutterMinY + fGutterTravelLength);
// |              }
// |              gpMouseManager->Main(knobMessage);
// |              m_scrollKnob->m_y = knobMessage.payload.mouse.y - knobOffset;
// |              if (m_fileCount > iMaxListSize) {
// | -                topIndex = static_cast<i32>((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// | +                topIndex = ((m_scrollKnob->m_y - fGutterMinY) / gutterStep7);
// |                  if (topIndex != oldTopIndex) {
