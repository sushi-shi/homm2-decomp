// resourceManager::PointToFile (PoL RVA 0xc8c00; Buka RVA 0xb8af0)
//
// Artifact:
//   build/source-variant-batch/resmgr-point-names/results.json
// Complete 16 local-name arms x (clean + 50 forest/top states), 816/816.
// Seed 1213156658; source restored; wall-time and generator untruncated.
//
// `aggregateIndex -> i` is exact in every paired state. Clean scores:
// `found` 99.867470%, `entry` 99.891560%, `position` 99.795180%, and the
// complete Buka name family 99.795180%. Keep the three PoL slot-sensitive
// names and adopt only Buka's exact-compatible `i` loop index.
// `/Od` frame audit: 1/1 aligned.
//
// Final build: exact 284 bytes, exact 16/16 CFG, complete 6/6 ordered
// relocations.
