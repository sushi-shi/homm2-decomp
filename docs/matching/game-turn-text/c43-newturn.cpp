// Parent e490bc850; complete structural product, no AST mutations or TU-state census.
// All 20 arms compiled in 20.82432448491454 seconds; source restored, no truncation.
// Retain trial 12: {'structure': 'helper_char_retain_cast', 'candidate': 'baseline'}.
// Selected size 1277, text 7afb499d99319750, normalized relocs f170ebf171cc5ce1.
// Artifacts: build/source-variant-batch/c43-newturn/results.json; build/c43-newturn-manifest.json.
// Dispositions, complete native/retail proofs and bounds: docs/reconstruction/C43-S41-S42-B65.md.
/* Exact source product:
{
  "schema": 1,
  "source": "src/SOURCE/GAME.cpp",
  "rva": "0x5e8fe",
  "axes": [
    {
      "name": "structure",
      "find": "void game::DoNewTurn(void) {\n    char musicFile18[NEW_TURN_MUSIC_FILENAME_CAPACITY];\n    u8 lowerName19[NEW_TURN_LOWER_NAME_CAPACITY];\n    i32 musicTrack2;\n\n    CheckForTimeEvent();\n    if (!gbThisNetHumanPlayer[giCurPlayer]) {\n        CheckEndGame(END_GAME_FORCE_NONE, false);\n        return;\n    }\n    giBottomViewOverrideEndTime = KBTickCount() + NEW_TURN_BOTTOM_VIEW_DURATION;\n    giBottomViewOverride = BOTTOM_VIEW_NEW_TURN;\n    gpAdvManager->UpdBottomView(true, true, true);\n    gpAdvManager->SetInitialMapOrigin();\n    gpAdvManager->CompleteDraw(0);\n    gpAdvManager->UpdateScreen(0, 0);\n    CheckEndGame(END_GAME_FORCE_NONE, false);\n\n    if (gpCurPlayer->m_daysLeft >= 0) {\n        if (gpCurPlayer->m_daysLeft == 1) {\n            sprintf(gText, cNewTurn[1], cPlayerNames[giCurPlayer]);\n        } else {\n            sprintf(gText, cNewTurn[0], cPlayerNames[giCurPlayer], gpCurPlayer->m_daysLeft);\n        }\n        NormalDialog(\n            gText,\n            1,\n            -1,\n            -1,\n            NEW_TURN_DIALOG_TYPE,\n            gpGame->GetPlayerColor(static_cast<i8>(giCurPlayer)),\n            -1,\n            0,\n            -1,\n            0\n        );\n    }\n\n    if (gpCurPlayer->m_heroCount > 0) {\n        gpAdvManager->SetHeroContext(gpCurPlayer->NextHero(0), 0);\n    } else if (gpCurPlayer->m_townCount > 0) {\n        gpAdvManager->SetTownContext(gpCurPlayer->m_townIds[0]);\n    }\n    gpAdvManager->CheckDimNextHeroBut();\n\n    if (m_day == 1 && (m_month != 1 || m_week != 1 || m_day != 1)) {\n        if (gbThisNetHumanPlayer[giCurPlayer])\n            gSoundBackendsReady = 1;\n        if (giWeekType != CALENDAR_PERIOD_NONE) {\n            musicTrack2 = -1;\n            if (m_week == 1) {\n                musicTrack2 = NEW_MONTH_MUSIC_TRACK;\n                strcpy(musicFile18, \"newmonth.82m\");\n                if (giMonthType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_NORMAL_TEXT],\n                        gMonthNames[giMonthTypeExtra]\n                    );\n                } else if (giMonthType == CALENDAR_PERIOD_CREATURE) {\n                    u8 lowerFirst;\n                    strcpy(\n                        reinterpret_cast<char*>(lowerName19),\n                        gArmyNamesPlural[giMonthTypeExtra]\n                    );\n                    if (lowerName19[0] >= 'A' && lowerName19[0] <= 'Z')\n                        lowerFirst = lowerName19[0] + ('a' - 'A');\n                    else if (lowerName19[0] >= CP1251_CAPITAL_A\n                             && lowerName19[0] <= CP1251_CAPITAL_YA)\n                        lowerFirst = lowerName19[0] + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (lowerName19[0] == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = lowerName19[0];\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_CREATURE_TEXT],\n                        gArmyNamesPlural[giMonthTypeExtra],\n                        reinterpret_cast<char*>(lowerName19)\n                    );\n                } else {\n                    sprintf(gText, cNewTurn[NEW_MONTH_PLAGUE_TEXT]);\n                }\n            } else {\n                musicTrack2 = NEW_WEEK_MUSIC_TRACK;\n                strcpy(musicFile18, \"newweek.82m\");\n                if (giWeekType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(gText, cNewTurn[NEW_WEEK_NORMAL_TEXT], gWeekNames[giWeekTypeExtra]);\n                } else {\n                    u8 lowerFirst;\n                    strcpy(\n                        reinterpret_cast<char*>(lowerName19),\n                        gArmyNamesPlural[giWeekTypeExtra]\n                    );\n                    if (lowerName19[0] >= 'A' && lowerName19[0] <= 'Z')\n                        lowerFirst = lowerName19[0] + ('a' - 'A');\n                    else if (lowerName19[0] >= CP1251_CAPITAL_A\n                             && lowerName19[0] <= CP1251_CAPITAL_YA)\n                        lowerFirst = lowerName19[0] + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (lowerName19[0] == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = lowerName19[0];\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_WEEK_CREATURE_TEXT],\n                        gArmyNamesPlural[giWeekTypeExtra],\n                        reinterpret_cast<char*>(lowerName19)\n                    );\n                }\n            }\n            gpSoundManager->PlayAmbientMusic(musicTrack2);\n            gpMouseManager->SetPointer(0);\n            NormalDialog(gText, 1, -1, -1, -1, 0, -1, 0, -1, 0);\n            gpSoundManager->SwitchAmbientMusic(\n                giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n            );\n        }\n    }\n    gpSoundManager->SwitchAmbientMusic(\n        giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n    );\n    gpAdvManager->SetEnvironmentOrigin(\n        gpAdvManager->m_mapOriginX + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        gpAdvManager->m_mapOriginY + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        1\n    );\n}",
      "options": [
        {
          "name": "byte_retain_cast",
          "replace": "void game::DoNewTurn(void) {\n    char musicFile18[NEW_TURN_MUSIC_FILENAME_CAPACITY];\n    u8 lowerName19[NEW_TURN_LOWER_NAME_CAPACITY];\n    i32 musicTrack2;\n\n    CheckForTimeEvent();\n    if (!gbThisNetHumanPlayer[giCurPlayer]) {\n        CheckEndGame(END_GAME_FORCE_NONE, false);\n        return;\n    }\n    giBottomViewOverrideEndTime = KBTickCount() + NEW_TURN_BOTTOM_VIEW_DURATION;\n    giBottomViewOverride = BOTTOM_VIEW_NEW_TURN;\n    gpAdvManager->UpdBottomView(true, true, true);\n    gpAdvManager->SetInitialMapOrigin();\n    gpAdvManager->CompleteDraw(0);\n    gpAdvManager->UpdateScreen(0, 0);\n    CheckEndGame(END_GAME_FORCE_NONE, false);\n\n    if (gpCurPlayer->m_daysLeft >= 0) {\n        if (gpCurPlayer->m_daysLeft == 1) {\n            sprintf(gText, cNewTurn[1], cPlayerNames[giCurPlayer]);\n        } else {\n            sprintf(gText, cNewTurn[0], cPlayerNames[giCurPlayer], gpCurPlayer->m_daysLeft);\n        }\n        NormalDialog(\n            gText,\n            1,\n            -1,\n            -1,\n            NEW_TURN_DIALOG_TYPE,\n            gpGame->GetPlayerColor(static_cast<i8>(giCurPlayer)),\n            -1,\n            0,\n            -1,\n            0\n        );\n    }\n\n    if (gpCurPlayer->m_heroCount > 0) {\n        gpAdvManager->SetHeroContext(gpCurPlayer->NextHero(0), 0);\n    } else if (gpCurPlayer->m_townCount > 0) {\n        gpAdvManager->SetTownContext(gpCurPlayer->m_townIds[0]);\n    }\n    gpAdvManager->CheckDimNextHeroBut();\n\n    if (m_day == 1 && (m_month != 1 || m_week != 1 || m_day != 1)) {\n        if (gbThisNetHumanPlayer[giCurPlayer])\n            gSoundBackendsReady = 1;\n        if (giWeekType != CALENDAR_PERIOD_NONE) {\n            musicTrack2 = -1;\n            if (m_week == 1) {\n                musicTrack2 = NEW_MONTH_MUSIC_TRACK;\n                strcpy(musicFile18, \"newmonth.82m\");\n                if (giMonthType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_NORMAL_TEXT],\n                        gMonthNames[giMonthTypeExtra]\n                    );\n                } else if (giMonthType == CALENDAR_PERIOD_CREATURE) {\n                    u8 lowerFirst;\n                    strcpy(\n                        reinterpret_cast<char*>(lowerName19),\n                        gArmyNamesPlural[giMonthTypeExtra]\n                    );\n                    if (lowerName19[0] >= 'A' && lowerName19[0] <= 'Z')\n                        lowerFirst = lowerName19[0] + ('a' - 'A');\n                    else if (lowerName19[0] >= CP1251_CAPITAL_A\n                             && lowerName19[0] <= CP1251_CAPITAL_YA)\n                        lowerFirst = lowerName19[0] + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (lowerName19[0] == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = lowerName19[0];\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_CREATURE_TEXT],\n                        gArmyNamesPlural[giMonthTypeExtra],\n                        reinterpret_cast<char*>(lowerName19)\n                    );\n                } else {\n                    sprintf(gText, cNewTurn[NEW_MONTH_PLAGUE_TEXT]);\n                }\n            } else {\n                musicTrack2 = NEW_WEEK_MUSIC_TRACK;\n                strcpy(musicFile18, \"newweek.82m\");\n                if (giWeekType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(gText, cNewTurn[NEW_WEEK_NORMAL_TEXT], gWeekNames[giWeekTypeExtra]);\n                } else {\n                    u8 lowerFirst;\n                    strcpy(\n                        reinterpret_cast<char*>(lowerName19),\n                        gArmyNamesPlural[giWeekTypeExtra]\n                    );\n                    if (lowerName19[0] >= 'A' && lowerName19[0] <= 'Z')\n                        lowerFirst = lowerName19[0] + ('a' - 'A');\n                    else if (lowerName19[0] >= CP1251_CAPITAL_A\n                             && lowerName19[0] <= CP1251_CAPITAL_YA)\n                        lowerFirst = lowerName19[0] + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (lowerName19[0] == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = lowerName19[0];\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_WEEK_CREATURE_TEXT],\n                        gArmyNamesPlural[giWeekTypeExtra],\n                        reinterpret_cast<char*>(lowerName19)\n                    );\n                }\n            }\n            gpSoundManager->PlayAmbientMusic(musicTrack2);\n            gpMouseManager->SetPointer(0);\n            NormalDialog(gText, 1, -1, -1, -1, 0, -1, 0, -1, 0);\n            gpSoundManager->SwitchAmbientMusic(\n                giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n            );\n        }\n    }\n    gpSoundManager->SwitchAmbientMusic(\n        giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n    );\n    gpAdvManager->SetEnvironmentOrigin(\n        gpAdvManager->m_mapOriginX + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        gpAdvManager->m_mapOriginY + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        1\n    );\n}"
        },
        {
          "name": "byte_retain_natural",
          "replace": "void game::DoNewTurn(void) {\n    char musicFile18[NEW_TURN_MUSIC_FILENAME_CAPACITY];\n    u8 lowerName19[NEW_TURN_LOWER_NAME_CAPACITY];\n    i32 musicTrack2;\n\n    CheckForTimeEvent();\n    if (!gbThisNetHumanPlayer[giCurPlayer]) {\n        CheckEndGame(END_GAME_FORCE_NONE, false);\n        return;\n    }\n    giBottomViewOverrideEndTime = KBTickCount() + NEW_TURN_BOTTOM_VIEW_DURATION;\n    giBottomViewOverride = BOTTOM_VIEW_NEW_TURN;\n    gpAdvManager->UpdBottomView(true, true, true);\n    gpAdvManager->SetInitialMapOrigin();\n    gpAdvManager->CompleteDraw(0);\n    gpAdvManager->UpdateScreen(0, 0);\n    CheckEndGame(END_GAME_FORCE_NONE, false);\n\n    if (gpCurPlayer->m_daysLeft >= 0) {\n        if (gpCurPlayer->m_daysLeft == 1) {\n            sprintf(gText, cNewTurn[1], cPlayerNames[giCurPlayer]);\n        } else {\n            sprintf(gText, cNewTurn[0], cPlayerNames[giCurPlayer], gpCurPlayer->m_daysLeft);\n        }\n        NormalDialog(\n            gText,\n            1,\n            -1,\n            -1,\n            NEW_TURN_DIALOG_TYPE,\n            gpGame->GetPlayerColor(giCurPlayer),\n            -1,\n            0,\n            -1,\n            0\n        );\n    }\n\n    if (gpCurPlayer->m_heroCount > 0) {\n        gpAdvManager->SetHeroContext(gpCurPlayer->NextHero(0), 0);\n    } else if (gpCurPlayer->m_townCount > 0) {\n        gpAdvManager->SetTownContext(gpCurPlayer->m_townIds[0]);\n    }\n    gpAdvManager->CheckDimNextHeroBut();\n\n    if (m_day == 1 && (m_month != 1 || m_week != 1 || m_day != 1)) {\n        if (gbThisNetHumanPlayer[giCurPlayer])\n            gSoundBackendsReady = 1;\n        if (giWeekType != CALENDAR_PERIOD_NONE) {\n            musicTrack2 = -1;\n            if (m_week == 1) {\n                musicTrack2 = NEW_MONTH_MUSIC_TRACK;\n                strcpy(musicFile18, \"newmonth.82m\");\n                if (giMonthType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_NORMAL_TEXT],\n                        gMonthNames[giMonthTypeExtra]\n                    );\n                } else if (giMonthType == CALENDAR_PERIOD_CREATURE) {\n                    u8 lowerFirst;\n                    strcpy(\n                        reinterpret_cast<char*>(lowerName19),\n                        gArmyNamesPlural[giMonthTypeExtra]\n                    );\n                    if (lowerName19[0] >= 'A' && lowerName19[0] <= 'Z')\n                        lowerFirst = lowerName19[0] + ('a' - 'A');\n                    else if (lowerName19[0] >= CP1251_CAPITAL_A\n                             && lowerName19[0] <= CP1251_CAPITAL_YA)\n                        lowerFirst = lowerName19[0] + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (lowerName19[0] == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = lowerName19[0];\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_CREATURE_TEXT],\n                        gArmyNamesPlural[giMonthTypeExtra],\n                        reinterpret_cast<char*>(lowerName19)\n                    );\n                } else {\n                    sprintf(gText, cNewTurn[NEW_MONTH_PLAGUE_TEXT]);\n                }\n            } else {\n                musicTrack2 = NEW_WEEK_MUSIC_TRACK;\n                strcpy(musicFile18, \"newweek.82m\");\n                if (giWeekType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(gText, cNewTurn[NEW_WEEK_NORMAL_TEXT], gWeekNames[giWeekTypeExtra]);\n                } else {\n                    u8 lowerFirst;\n                    strcpy(\n                        reinterpret_cast<char*>(lowerName19),\n                        gArmyNamesPlural[giWeekTypeExtra]\n                    );\n                    if (lowerName19[0] >= 'A' && lowerName19[0] <= 'Z')\n                        lowerFirst = lowerName19[0] + ('a' - 'A');\n                    else if (lowerName19[0] >= CP1251_CAPITAL_A\n                             && lowerName19[0] <= CP1251_CAPITAL_YA)\n                        lowerFirst = lowerName19[0] + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (lowerName19[0] == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = lowerName19[0];\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_WEEK_CREATURE_TEXT],\n                        gArmyNamesPlural[giWeekTypeExtra],\n                        reinterpret_cast<char*>(lowerName19)\n                    );\n                }\n            }\n            gpSoundManager->PlayAmbientMusic(musicTrack2);\n            gpMouseManager->SetPointer(0);\n            NormalDialog(gText, 1, -1, -1, -1, 0, -1, 0, -1, 0);\n            gpSoundManager->SwitchAmbientMusic(\n                giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n            );\n        }\n    }\n    gpSoundManager->SwitchAmbientMusic(\n        giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n    );\n    gpAdvManager->SetEnvironmentOrigin(\n        gpAdvManager->m_mapOriginX + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        gpAdvManager->m_mapOriginY + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        1\n    );\n}"
        },
        {
          "name": "byte_remove_cast",
          "replace": "void game::DoNewTurn(void) {\n    u8 lowerName19[NEW_TURN_LOWER_NAME_CAPACITY];\n    i32 musicTrack2;\n\n    CheckForTimeEvent();\n    if (!gbThisNetHumanPlayer[giCurPlayer]) {\n        CheckEndGame(END_GAME_FORCE_NONE, false);\n        return;\n    }\n    giBottomViewOverrideEndTime = KBTickCount() + NEW_TURN_BOTTOM_VIEW_DURATION;\n    giBottomViewOverride = BOTTOM_VIEW_NEW_TURN;\n    gpAdvManager->UpdBottomView(true, true, true);\n    gpAdvManager->SetInitialMapOrigin();\n    gpAdvManager->CompleteDraw(0);\n    gpAdvManager->UpdateScreen(0, 0);\n    CheckEndGame(END_GAME_FORCE_NONE, false);\n\n    if (gpCurPlayer->m_daysLeft >= 0) {\n        if (gpCurPlayer->m_daysLeft == 1) {\n            sprintf(gText, cNewTurn[1], cPlayerNames[giCurPlayer]);\n        } else {\n            sprintf(gText, cNewTurn[0], cPlayerNames[giCurPlayer], gpCurPlayer->m_daysLeft);\n        }\n        NormalDialog(\n            gText,\n            1,\n            -1,\n            -1,\n            NEW_TURN_DIALOG_TYPE,\n            gpGame->GetPlayerColor(static_cast<i8>(giCurPlayer)),\n            -1,\n            0,\n            -1,\n            0\n        );\n    }\n\n    if (gpCurPlayer->m_heroCount > 0) {\n        gpAdvManager->SetHeroContext(gpCurPlayer->NextHero(0), 0);\n    } else if (gpCurPlayer->m_townCount > 0) {\n        gpAdvManager->SetTownContext(gpCurPlayer->m_townIds[0]);\n    }\n    gpAdvManager->CheckDimNextHeroBut();\n\n    if (m_day == 1 && (m_month != 1 || m_week != 1 || m_day != 1)) {\n        if (gbThisNetHumanPlayer[giCurPlayer])\n            gSoundBackendsReady = 1;\n        if (giWeekType != CALENDAR_PERIOD_NONE) {\n            musicTrack2 = -1;\n            if (m_week == 1) {\n                musicTrack2 = NEW_MONTH_MUSIC_TRACK;\n                if (giMonthType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_NORMAL_TEXT],\n                        gMonthNames[giMonthTypeExtra]\n                    );\n                } else if (giMonthType == CALENDAR_PERIOD_CREATURE) {\n                    u8 lowerFirst;\n                    strcpy(\n                        reinterpret_cast<char*>(lowerName19),\n                        gArmyNamesPlural[giMonthTypeExtra]\n                    );\n                    if (lowerName19[0] >= 'A' && lowerName19[0] <= 'Z')\n                        lowerFirst = lowerName19[0] + ('a' - 'A');\n                    else if (lowerName19[0] >= CP1251_CAPITAL_A\n                             && lowerName19[0] <= CP1251_CAPITAL_YA)\n                        lowerFirst = lowerName19[0] + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (lowerName19[0] == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = lowerName19[0];\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_CREATURE_TEXT],\n                        gArmyNamesPlural[giMonthTypeExtra],\n                        reinterpret_cast<char*>(lowerName19)\n                    );\n                } else {\n                    sprintf(gText, cNewTurn[NEW_MONTH_PLAGUE_TEXT]);\n                }\n            } else {\n                musicTrack2 = NEW_WEEK_MUSIC_TRACK;\n                if (giWeekType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(gText, cNewTurn[NEW_WEEK_NORMAL_TEXT], gWeekNames[giWeekTypeExtra]);\n                } else {\n                    u8 lowerFirst;\n                    strcpy(\n                        reinterpret_cast<char*>(lowerName19),\n                        gArmyNamesPlural[giWeekTypeExtra]\n                    );\n                    if (lowerName19[0] >= 'A' && lowerName19[0] <= 'Z')\n                        lowerFirst = lowerName19[0] + ('a' - 'A');\n                    else if (lowerName19[0] >= CP1251_CAPITAL_A\n                             && lowerName19[0] <= CP1251_CAPITAL_YA)\n                        lowerFirst = lowerName19[0] + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (lowerName19[0] == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = lowerName19[0];\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_WEEK_CREATURE_TEXT],\n                        gArmyNamesPlural[giWeekTypeExtra],\n                        reinterpret_cast<char*>(lowerName19)\n                    );\n                }\n            }\n            gpSoundManager->PlayAmbientMusic(musicTrack2);\n            gpMouseManager->SetPointer(0);\n            NormalDialog(gText, 1, -1, -1, -1, 0, -1, 0, -1, 0);\n            gpSoundManager->SwitchAmbientMusic(\n                giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n            );\n        }\n    }\n    gpSoundManager->SwitchAmbientMusic(\n        giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n    );\n    gpAdvManager->SetEnvironmentOrigin(\n        gpAdvManager->m_mapOriginX + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        gpAdvManager->m_mapOriginY + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        1\n    );\n}"
        },
        {
          "name": "byte_remove_natural",
          "replace": "void game::DoNewTurn(void) {\n    u8 lowerName19[NEW_TURN_LOWER_NAME_CAPACITY];\n    i32 musicTrack2;\n\n    CheckForTimeEvent();\n    if (!gbThisNetHumanPlayer[giCurPlayer]) {\n        CheckEndGame(END_GAME_FORCE_NONE, false);\n        return;\n    }\n    giBottomViewOverrideEndTime = KBTickCount() + NEW_TURN_BOTTOM_VIEW_DURATION;\n    giBottomViewOverride = BOTTOM_VIEW_NEW_TURN;\n    gpAdvManager->UpdBottomView(true, true, true);\n    gpAdvManager->SetInitialMapOrigin();\n    gpAdvManager->CompleteDraw(0);\n    gpAdvManager->UpdateScreen(0, 0);\n    CheckEndGame(END_GAME_FORCE_NONE, false);\n\n    if (gpCurPlayer->m_daysLeft >= 0) {\n        if (gpCurPlayer->m_daysLeft == 1) {\n            sprintf(gText, cNewTurn[1], cPlayerNames[giCurPlayer]);\n        } else {\n            sprintf(gText, cNewTurn[0], cPlayerNames[giCurPlayer], gpCurPlayer->m_daysLeft);\n        }\n        NormalDialog(\n            gText,\n            1,\n            -1,\n            -1,\n            NEW_TURN_DIALOG_TYPE,\n            gpGame->GetPlayerColor(giCurPlayer),\n            -1,\n            0,\n            -1,\n            0\n        );\n    }\n\n    if (gpCurPlayer->m_heroCount > 0) {\n        gpAdvManager->SetHeroContext(gpCurPlayer->NextHero(0), 0);\n    } else if (gpCurPlayer->m_townCount > 0) {\n        gpAdvManager->SetTownContext(gpCurPlayer->m_townIds[0]);\n    }\n    gpAdvManager->CheckDimNextHeroBut();\n\n    if (m_day == 1 && (m_month != 1 || m_week != 1 || m_day != 1)) {\n        if (gbThisNetHumanPlayer[giCurPlayer])\n            gSoundBackendsReady = 1;\n        if (giWeekType != CALENDAR_PERIOD_NONE) {\n            musicTrack2 = -1;\n            if (m_week == 1) {\n                musicTrack2 = NEW_MONTH_MUSIC_TRACK;\n                if (giMonthType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_NORMAL_TEXT],\n                        gMonthNames[giMonthTypeExtra]\n                    );\n                } else if (giMonthType == CALENDAR_PERIOD_CREATURE) {\n                    u8 lowerFirst;\n                    strcpy(\n                        reinterpret_cast<char*>(lowerName19),\n                        gArmyNamesPlural[giMonthTypeExtra]\n                    );\n                    if (lowerName19[0] >= 'A' && lowerName19[0] <= 'Z')\n                        lowerFirst = lowerName19[0] + ('a' - 'A');\n                    else if (lowerName19[0] >= CP1251_CAPITAL_A\n                             && lowerName19[0] <= CP1251_CAPITAL_YA)\n                        lowerFirst = lowerName19[0] + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (lowerName19[0] == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = lowerName19[0];\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_CREATURE_TEXT],\n                        gArmyNamesPlural[giMonthTypeExtra],\n                        reinterpret_cast<char*>(lowerName19)\n                    );\n                } else {\n                    sprintf(gText, cNewTurn[NEW_MONTH_PLAGUE_TEXT]);\n                }\n            } else {\n                musicTrack2 = NEW_WEEK_MUSIC_TRACK;\n                if (giWeekType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(gText, cNewTurn[NEW_WEEK_NORMAL_TEXT], gWeekNames[giWeekTypeExtra]);\n                } else {\n                    u8 lowerFirst;\n                    strcpy(\n                        reinterpret_cast<char*>(lowerName19),\n                        gArmyNamesPlural[giWeekTypeExtra]\n                    );\n                    if (lowerName19[0] >= 'A' && lowerName19[0] <= 'Z')\n                        lowerFirst = lowerName19[0] + ('a' - 'A');\n                    else if (lowerName19[0] >= CP1251_CAPITAL_A\n                             && lowerName19[0] <= CP1251_CAPITAL_YA)\n                        lowerFirst = lowerName19[0] + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (lowerName19[0] == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = lowerName19[0];\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_WEEK_CREATURE_TEXT],\n                        gArmyNamesPlural[giWeekTypeExtra],\n                        reinterpret_cast<char*>(lowerName19)\n                    );\n                }\n            }\n            gpSoundManager->PlayAmbientMusic(musicTrack2);\n            gpMouseManager->SetPointer(0);\n            NormalDialog(gText, 1, -1, -1, -1, 0, -1, 0, -1, 0);\n            gpSoundManager->SwitchAmbientMusic(\n                giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n            );\n        }\n    }\n    gpSoundManager->SwitchAmbientMusic(\n        giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n    );\n    gpAdvManager->SetEnvironmentOrigin(\n        gpAdvManager->m_mapOriginX + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        gpAdvManager->m_mapOriginY + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        1\n    );\n}"
        },
        {
          "name": "unsigned_reads_retain_cast",
          "replace": "void game::DoNewTurn(void) {\n    char musicFile18[NEW_TURN_MUSIC_FILENAME_CAPACITY];\n    char lowerName19[NEW_TURN_LOWER_NAME_CAPACITY];\n    i32 musicTrack2;\n\n    CheckForTimeEvent();\n    if (!gbThisNetHumanPlayer[giCurPlayer]) {\n        CheckEndGame(END_GAME_FORCE_NONE, false);\n        return;\n    }\n    giBottomViewOverrideEndTime = KBTickCount() + NEW_TURN_BOTTOM_VIEW_DURATION;\n    giBottomViewOverride = BOTTOM_VIEW_NEW_TURN;\n    gpAdvManager->UpdBottomView(true, true, true);\n    gpAdvManager->SetInitialMapOrigin();\n    gpAdvManager->CompleteDraw(0);\n    gpAdvManager->UpdateScreen(0, 0);\n    CheckEndGame(END_GAME_FORCE_NONE, false);\n\n    if (gpCurPlayer->m_daysLeft >= 0) {\n        if (gpCurPlayer->m_daysLeft == 1) {\n            sprintf(gText, cNewTurn[1], cPlayerNames[giCurPlayer]);\n        } else {\n            sprintf(gText, cNewTurn[0], cPlayerNames[giCurPlayer], gpCurPlayer->m_daysLeft);\n        }\n        NormalDialog(\n            gText,\n            1,\n            -1,\n            -1,\n            NEW_TURN_DIALOG_TYPE,\n            gpGame->GetPlayerColor(static_cast<i8>(giCurPlayer)),\n            -1,\n            0,\n            -1,\n            0\n        );\n    }\n\n    if (gpCurPlayer->m_heroCount > 0) {\n        gpAdvManager->SetHeroContext(gpCurPlayer->NextHero(0), 0);\n    } else if (gpCurPlayer->m_townCount > 0) {\n        gpAdvManager->SetTownContext(gpCurPlayer->m_townIds[0]);\n    }\n    gpAdvManager->CheckDimNextHeroBut();\n\n    if (m_day == 1 && (m_month != 1 || m_week != 1 || m_day != 1)) {\n        if (gbThisNetHumanPlayer[giCurPlayer])\n            gSoundBackendsReady = 1;\n        if (giWeekType != CALENDAR_PERIOD_NONE) {\n            musicTrack2 = -1;\n            if (m_week == 1) {\n                musicTrack2 = NEW_MONTH_MUSIC_TRACK;\n                strcpy(musicFile18, \"newmonth.82m\");\n                if (giMonthType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_NORMAL_TEXT],\n                        gMonthNames[giMonthTypeExtra]\n                    );\n                } else if (giMonthType == CALENDAR_PERIOD_CREATURE) {\n                    u8 lowerFirst;\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giMonthTypeExtra]\n                    );\n                    if (static_cast<u8>(lowerName19[0]) >= 'A' && static_cast<u8>(lowerName19[0]) <= 'Z')\n                        lowerFirst = static_cast<u8>(lowerName19[0]) + ('a' - 'A');\n                    else if (static_cast<u8>(lowerName19[0]) >= CP1251_CAPITAL_A\n                             && static_cast<u8>(lowerName19[0]) <= CP1251_CAPITAL_YA)\n                        lowerFirst = static_cast<u8>(lowerName19[0]) + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (static_cast<u8>(lowerName19[0]) == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = static_cast<u8>(lowerName19[0]);\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_CREATURE_TEXT],\n                        gArmyNamesPlural[giMonthTypeExtra],\n                        lowerName19\n                    );\n                } else {\n                    sprintf(gText, cNewTurn[NEW_MONTH_PLAGUE_TEXT]);\n                }\n            } else {\n                musicTrack2 = NEW_WEEK_MUSIC_TRACK;\n                strcpy(musicFile18, \"newweek.82m\");\n                if (giWeekType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(gText, cNewTurn[NEW_WEEK_NORMAL_TEXT], gWeekNames[giWeekTypeExtra]);\n                } else {\n                    u8 lowerFirst;\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giWeekTypeExtra]\n                    );\n                    if (static_cast<u8>(lowerName19[0]) >= 'A' && static_cast<u8>(lowerName19[0]) <= 'Z')\n                        lowerFirst = static_cast<u8>(lowerName19[0]) + ('a' - 'A');\n                    else if (static_cast<u8>(lowerName19[0]) >= CP1251_CAPITAL_A\n                             && static_cast<u8>(lowerName19[0]) <= CP1251_CAPITAL_YA)\n                        lowerFirst = static_cast<u8>(lowerName19[0]) + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (static_cast<u8>(lowerName19[0]) == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = static_cast<u8>(lowerName19[0]);\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_WEEK_CREATURE_TEXT],\n                        gArmyNamesPlural[giWeekTypeExtra],\n                        lowerName19\n                    );\n                }\n            }\n            gpSoundManager->PlayAmbientMusic(musicTrack2);\n            gpMouseManager->SetPointer(0);\n            NormalDialog(gText, 1, -1, -1, -1, 0, -1, 0, -1, 0);\n            gpSoundManager->SwitchAmbientMusic(\n                giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n            );\n        }\n    }\n    gpSoundManager->SwitchAmbientMusic(\n        giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n    );\n    gpAdvManager->SetEnvironmentOrigin(\n        gpAdvManager->m_mapOriginX + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        gpAdvManager->m_mapOriginY + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        1\n    );\n}"
        },
        {
          "name": "unsigned_reads_retain_natural",
          "replace": "void game::DoNewTurn(void) {\n    char musicFile18[NEW_TURN_MUSIC_FILENAME_CAPACITY];\n    char lowerName19[NEW_TURN_LOWER_NAME_CAPACITY];\n    i32 musicTrack2;\n\n    CheckForTimeEvent();\n    if (!gbThisNetHumanPlayer[giCurPlayer]) {\n        CheckEndGame(END_GAME_FORCE_NONE, false);\n        return;\n    }\n    giBottomViewOverrideEndTime = KBTickCount() + NEW_TURN_BOTTOM_VIEW_DURATION;\n    giBottomViewOverride = BOTTOM_VIEW_NEW_TURN;\n    gpAdvManager->UpdBottomView(true, true, true);\n    gpAdvManager->SetInitialMapOrigin();\n    gpAdvManager->CompleteDraw(0);\n    gpAdvManager->UpdateScreen(0, 0);\n    CheckEndGame(END_GAME_FORCE_NONE, false);\n\n    if (gpCurPlayer->m_daysLeft >= 0) {\n        if (gpCurPlayer->m_daysLeft == 1) {\n            sprintf(gText, cNewTurn[1], cPlayerNames[giCurPlayer]);\n        } else {\n            sprintf(gText, cNewTurn[0], cPlayerNames[giCurPlayer], gpCurPlayer->m_daysLeft);\n        }\n        NormalDialog(\n            gText,\n            1,\n            -1,\n            -1,\n            NEW_TURN_DIALOG_TYPE,\n            gpGame->GetPlayerColor(giCurPlayer),\n            -1,\n            0,\n            -1,\n            0\n        );\n    }\n\n    if (gpCurPlayer->m_heroCount > 0) {\n        gpAdvManager->SetHeroContext(gpCurPlayer->NextHero(0), 0);\n    } else if (gpCurPlayer->m_townCount > 0) {\n        gpAdvManager->SetTownContext(gpCurPlayer->m_townIds[0]);\n    }\n    gpAdvManager->CheckDimNextHeroBut();\n\n    if (m_day == 1 && (m_month != 1 || m_week != 1 || m_day != 1)) {\n        if (gbThisNetHumanPlayer[giCurPlayer])\n            gSoundBackendsReady = 1;\n        if (giWeekType != CALENDAR_PERIOD_NONE) {\n            musicTrack2 = -1;\n            if (m_week == 1) {\n                musicTrack2 = NEW_MONTH_MUSIC_TRACK;\n                strcpy(musicFile18, \"newmonth.82m\");\n                if (giMonthType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_NORMAL_TEXT],\n                        gMonthNames[giMonthTypeExtra]\n                    );\n                } else if (giMonthType == CALENDAR_PERIOD_CREATURE) {\n                    u8 lowerFirst;\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giMonthTypeExtra]\n                    );\n                    if (static_cast<u8>(lowerName19[0]) >= 'A' && static_cast<u8>(lowerName19[0]) <= 'Z')\n                        lowerFirst = static_cast<u8>(lowerName19[0]) + ('a' - 'A');\n                    else if (static_cast<u8>(lowerName19[0]) >= CP1251_CAPITAL_A\n                             && static_cast<u8>(lowerName19[0]) <= CP1251_CAPITAL_YA)\n                        lowerFirst = static_cast<u8>(lowerName19[0]) + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (static_cast<u8>(lowerName19[0]) == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = static_cast<u8>(lowerName19[0]);\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_CREATURE_TEXT],\n                        gArmyNamesPlural[giMonthTypeExtra],\n                        lowerName19\n                    );\n                } else {\n                    sprintf(gText, cNewTurn[NEW_MONTH_PLAGUE_TEXT]);\n                }\n            } else {\n                musicTrack2 = NEW_WEEK_MUSIC_TRACK;\n                strcpy(musicFile18, \"newweek.82m\");\n                if (giWeekType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(gText, cNewTurn[NEW_WEEK_NORMAL_TEXT], gWeekNames[giWeekTypeExtra]);\n                } else {\n                    u8 lowerFirst;\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giWeekTypeExtra]\n                    );\n                    if (static_cast<u8>(lowerName19[0]) >= 'A' && static_cast<u8>(lowerName19[0]) <= 'Z')\n                        lowerFirst = static_cast<u8>(lowerName19[0]) + ('a' - 'A');\n                    else if (static_cast<u8>(lowerName19[0]) >= CP1251_CAPITAL_A\n                             && static_cast<u8>(lowerName19[0]) <= CP1251_CAPITAL_YA)\n                        lowerFirst = static_cast<u8>(lowerName19[0]) + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (static_cast<u8>(lowerName19[0]) == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = static_cast<u8>(lowerName19[0]);\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_WEEK_CREATURE_TEXT],\n                        gArmyNamesPlural[giWeekTypeExtra],\n                        lowerName19\n                    );\n                }\n            }\n            gpSoundManager->PlayAmbientMusic(musicTrack2);\n            gpMouseManager->SetPointer(0);\n            NormalDialog(gText, 1, -1, -1, -1, 0, -1, 0, -1, 0);\n            gpSoundManager->SwitchAmbientMusic(\n                giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n            );\n        }\n    }\n    gpSoundManager->SwitchAmbientMusic(\n        giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n    );\n    gpAdvManager->SetEnvironmentOrigin(\n        gpAdvManager->m_mapOriginX + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        gpAdvManager->m_mapOriginY + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        1\n    );\n}"
        },
        {
          "name": "unsigned_reads_remove_cast",
          "replace": "void game::DoNewTurn(void) {\n    char lowerName19[NEW_TURN_LOWER_NAME_CAPACITY];\n    i32 musicTrack2;\n\n    CheckForTimeEvent();\n    if (!gbThisNetHumanPlayer[giCurPlayer]) {\n        CheckEndGame(END_GAME_FORCE_NONE, false);\n        return;\n    }\n    giBottomViewOverrideEndTime = KBTickCount() + NEW_TURN_BOTTOM_VIEW_DURATION;\n    giBottomViewOverride = BOTTOM_VIEW_NEW_TURN;\n    gpAdvManager->UpdBottomView(true, true, true);\n    gpAdvManager->SetInitialMapOrigin();\n    gpAdvManager->CompleteDraw(0);\n    gpAdvManager->UpdateScreen(0, 0);\n    CheckEndGame(END_GAME_FORCE_NONE, false);\n\n    if (gpCurPlayer->m_daysLeft >= 0) {\n        if (gpCurPlayer->m_daysLeft == 1) {\n            sprintf(gText, cNewTurn[1], cPlayerNames[giCurPlayer]);\n        } else {\n            sprintf(gText, cNewTurn[0], cPlayerNames[giCurPlayer], gpCurPlayer->m_daysLeft);\n        }\n        NormalDialog(\n            gText,\n            1,\n            -1,\n            -1,\n            NEW_TURN_DIALOG_TYPE,\n            gpGame->GetPlayerColor(static_cast<i8>(giCurPlayer)),\n            -1,\n            0,\n            -1,\n            0\n        );\n    }\n\n    if (gpCurPlayer->m_heroCount > 0) {\n        gpAdvManager->SetHeroContext(gpCurPlayer->NextHero(0), 0);\n    } else if (gpCurPlayer->m_townCount > 0) {\n        gpAdvManager->SetTownContext(gpCurPlayer->m_townIds[0]);\n    }\n    gpAdvManager->CheckDimNextHeroBut();\n\n    if (m_day == 1 && (m_month != 1 || m_week != 1 || m_day != 1)) {\n        if (gbThisNetHumanPlayer[giCurPlayer])\n            gSoundBackendsReady = 1;\n        if (giWeekType != CALENDAR_PERIOD_NONE) {\n            musicTrack2 = -1;\n            if (m_week == 1) {\n                musicTrack2 = NEW_MONTH_MUSIC_TRACK;\n                if (giMonthType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_NORMAL_TEXT],\n                        gMonthNames[giMonthTypeExtra]\n                    );\n                } else if (giMonthType == CALENDAR_PERIOD_CREATURE) {\n                    u8 lowerFirst;\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giMonthTypeExtra]\n                    );\n                    if (static_cast<u8>(lowerName19[0]) >= 'A' && static_cast<u8>(lowerName19[0]) <= 'Z')\n                        lowerFirst = static_cast<u8>(lowerName19[0]) + ('a' - 'A');\n                    else if (static_cast<u8>(lowerName19[0]) >= CP1251_CAPITAL_A\n                             && static_cast<u8>(lowerName19[0]) <= CP1251_CAPITAL_YA)\n                        lowerFirst = static_cast<u8>(lowerName19[0]) + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (static_cast<u8>(lowerName19[0]) == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = static_cast<u8>(lowerName19[0]);\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_CREATURE_TEXT],\n                        gArmyNamesPlural[giMonthTypeExtra],\n                        lowerName19\n                    );\n                } else {\n                    sprintf(gText, cNewTurn[NEW_MONTH_PLAGUE_TEXT]);\n                }\n            } else {\n                musicTrack2 = NEW_WEEK_MUSIC_TRACK;\n                if (giWeekType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(gText, cNewTurn[NEW_WEEK_NORMAL_TEXT], gWeekNames[giWeekTypeExtra]);\n                } else {\n                    u8 lowerFirst;\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giWeekTypeExtra]\n                    );\n                    if (static_cast<u8>(lowerName19[0]) >= 'A' && static_cast<u8>(lowerName19[0]) <= 'Z')\n                        lowerFirst = static_cast<u8>(lowerName19[0]) + ('a' - 'A');\n                    else if (static_cast<u8>(lowerName19[0]) >= CP1251_CAPITAL_A\n                             && static_cast<u8>(lowerName19[0]) <= CP1251_CAPITAL_YA)\n                        lowerFirst = static_cast<u8>(lowerName19[0]) + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (static_cast<u8>(lowerName19[0]) == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = static_cast<u8>(lowerName19[0]);\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_WEEK_CREATURE_TEXT],\n                        gArmyNamesPlural[giWeekTypeExtra],\n                        lowerName19\n                    );\n                }\n            }\n            gpSoundManager->PlayAmbientMusic(musicTrack2);\n            gpMouseManager->SetPointer(0);\n            NormalDialog(gText, 1, -1, -1, -1, 0, -1, 0, -1, 0);\n            gpSoundManager->SwitchAmbientMusic(\n                giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n            );\n        }\n    }\n    gpSoundManager->SwitchAmbientMusic(\n        giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n    );\n    gpAdvManager->SetEnvironmentOrigin(\n        gpAdvManager->m_mapOriginX + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        gpAdvManager->m_mapOriginY + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        1\n    );\n}"
        },
        {
          "name": "unsigned_reads_remove_natural",
          "replace": "void game::DoNewTurn(void) {\n    char lowerName19[NEW_TURN_LOWER_NAME_CAPACITY];\n    i32 musicTrack2;\n\n    CheckForTimeEvent();\n    if (!gbThisNetHumanPlayer[giCurPlayer]) {\n        CheckEndGame(END_GAME_FORCE_NONE, false);\n        return;\n    }\n    giBottomViewOverrideEndTime = KBTickCount() + NEW_TURN_BOTTOM_VIEW_DURATION;\n    giBottomViewOverride = BOTTOM_VIEW_NEW_TURN;\n    gpAdvManager->UpdBottomView(true, true, true);\n    gpAdvManager->SetInitialMapOrigin();\n    gpAdvManager->CompleteDraw(0);\n    gpAdvManager->UpdateScreen(0, 0);\n    CheckEndGame(END_GAME_FORCE_NONE, false);\n\n    if (gpCurPlayer->m_daysLeft >= 0) {\n        if (gpCurPlayer->m_daysLeft == 1) {\n            sprintf(gText, cNewTurn[1], cPlayerNames[giCurPlayer]);\n        } else {\n            sprintf(gText, cNewTurn[0], cPlayerNames[giCurPlayer], gpCurPlayer->m_daysLeft);\n        }\n        NormalDialog(\n            gText,\n            1,\n            -1,\n            -1,\n            NEW_TURN_DIALOG_TYPE,\n            gpGame->GetPlayerColor(giCurPlayer),\n            -1,\n            0,\n            -1,\n            0\n        );\n    }\n\n    if (gpCurPlayer->m_heroCount > 0) {\n        gpAdvManager->SetHeroContext(gpCurPlayer->NextHero(0), 0);\n    } else if (gpCurPlayer->m_townCount > 0) {\n        gpAdvManager->SetTownContext(gpCurPlayer->m_townIds[0]);\n    }\n    gpAdvManager->CheckDimNextHeroBut();\n\n    if (m_day == 1 && (m_month != 1 || m_week != 1 || m_day != 1)) {\n        if (gbThisNetHumanPlayer[giCurPlayer])\n            gSoundBackendsReady = 1;\n        if (giWeekType != CALENDAR_PERIOD_NONE) {\n            musicTrack2 = -1;\n            if (m_week == 1) {\n                musicTrack2 = NEW_MONTH_MUSIC_TRACK;\n                if (giMonthType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_NORMAL_TEXT],\n                        gMonthNames[giMonthTypeExtra]\n                    );\n                } else if (giMonthType == CALENDAR_PERIOD_CREATURE) {\n                    u8 lowerFirst;\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giMonthTypeExtra]\n                    );\n                    if (static_cast<u8>(lowerName19[0]) >= 'A' && static_cast<u8>(lowerName19[0]) <= 'Z')\n                        lowerFirst = static_cast<u8>(lowerName19[0]) + ('a' - 'A');\n                    else if (static_cast<u8>(lowerName19[0]) >= CP1251_CAPITAL_A\n                             && static_cast<u8>(lowerName19[0]) <= CP1251_CAPITAL_YA)\n                        lowerFirst = static_cast<u8>(lowerName19[0]) + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (static_cast<u8>(lowerName19[0]) == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = static_cast<u8>(lowerName19[0]);\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_CREATURE_TEXT],\n                        gArmyNamesPlural[giMonthTypeExtra],\n                        lowerName19\n                    );\n                } else {\n                    sprintf(gText, cNewTurn[NEW_MONTH_PLAGUE_TEXT]);\n                }\n            } else {\n                musicTrack2 = NEW_WEEK_MUSIC_TRACK;\n                if (giWeekType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(gText, cNewTurn[NEW_WEEK_NORMAL_TEXT], gWeekNames[giWeekTypeExtra]);\n                } else {\n                    u8 lowerFirst;\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giWeekTypeExtra]\n                    );\n                    if (static_cast<u8>(lowerName19[0]) >= 'A' && static_cast<u8>(lowerName19[0]) <= 'Z')\n                        lowerFirst = static_cast<u8>(lowerName19[0]) + ('a' - 'A');\n                    else if (static_cast<u8>(lowerName19[0]) >= CP1251_CAPITAL_A\n                             && static_cast<u8>(lowerName19[0]) <= CP1251_CAPITAL_YA)\n                        lowerFirst = static_cast<u8>(lowerName19[0]) + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (static_cast<u8>(lowerName19[0]) == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = static_cast<u8>(lowerName19[0]);\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_WEEK_CREATURE_TEXT],\n                        gArmyNamesPlural[giWeekTypeExtra],\n                        lowerName19\n                    );\n                }\n            }\n            gpSoundManager->PlayAmbientMusic(musicTrack2);\n            gpMouseManager->SetPointer(0);\n            NormalDialog(gText, 1, -1, -1, -1, 0, -1, 0, -1, 0);\n            gpSoundManager->SwitchAmbientMusic(\n                giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n            );\n        }\n    }\n    gpSoundManager->SwitchAmbientMusic(\n        giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n    );\n    gpAdvManager->SetEnvironmentOrigin(\n        gpAdvManager->m_mapOriginX + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        gpAdvManager->m_mapOriginY + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        1\n    );\n}"
        },
        {
          "name": "unsigned_local_retain_cast",
          "replace": "void game::DoNewTurn(void) {\n    char musicFile18[NEW_TURN_MUSIC_FILENAME_CAPACITY];\n    char lowerName19[NEW_TURN_LOWER_NAME_CAPACITY];\n    i32 musicTrack2;\n\n    CheckForTimeEvent();\n    if (!gbThisNetHumanPlayer[giCurPlayer]) {\n        CheckEndGame(END_GAME_FORCE_NONE, false);\n        return;\n    }\n    giBottomViewOverrideEndTime = KBTickCount() + NEW_TURN_BOTTOM_VIEW_DURATION;\n    giBottomViewOverride = BOTTOM_VIEW_NEW_TURN;\n    gpAdvManager->UpdBottomView(true, true, true);\n    gpAdvManager->SetInitialMapOrigin();\n    gpAdvManager->CompleteDraw(0);\n    gpAdvManager->UpdateScreen(0, 0);\n    CheckEndGame(END_GAME_FORCE_NONE, false);\n\n    if (gpCurPlayer->m_daysLeft >= 0) {\n        if (gpCurPlayer->m_daysLeft == 1) {\n            sprintf(gText, cNewTurn[1], cPlayerNames[giCurPlayer]);\n        } else {\n            sprintf(gText, cNewTurn[0], cPlayerNames[giCurPlayer], gpCurPlayer->m_daysLeft);\n        }\n        NormalDialog(\n            gText,\n            1,\n            -1,\n            -1,\n            NEW_TURN_DIALOG_TYPE,\n            gpGame->GetPlayerColor(static_cast<i8>(giCurPlayer)),\n            -1,\n            0,\n            -1,\n            0\n        );\n    }\n\n    if (gpCurPlayer->m_heroCount > 0) {\n        gpAdvManager->SetHeroContext(gpCurPlayer->NextHero(0), 0);\n    } else if (gpCurPlayer->m_townCount > 0) {\n        gpAdvManager->SetTownContext(gpCurPlayer->m_townIds[0]);\n    }\n    gpAdvManager->CheckDimNextHeroBut();\n\n    if (m_day == 1 && (m_month != 1 || m_week != 1 || m_day != 1)) {\n        if (gbThisNetHumanPlayer[giCurPlayer])\n            gSoundBackendsReady = 1;\n        if (giWeekType != CALENDAR_PERIOD_NONE) {\n            musicTrack2 = -1;\n            if (m_week == 1) {\n                musicTrack2 = NEW_MONTH_MUSIC_TRACK;\n                strcpy(musicFile18, \"newmonth.82m\");\n                if (giMonthType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_NORMAL_TEXT],\n                        gMonthNames[giMonthTypeExtra]\n                    );\n                } else if (giMonthType == CALENDAR_PERIOD_CREATURE) {\n                    u8 lowerFirst;\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giMonthTypeExtra]\n                    );\n                    u8 firstLetter = lowerName19[0];\n                    if (firstLetter >= 'A' && firstLetter <= 'Z')\n                        lowerFirst = firstLetter + ('a' - 'A');\n                    else if (firstLetter >= CP1251_CAPITAL_A\n                             && firstLetter <= CP1251_CAPITAL_YA)\n                        lowerFirst = firstLetter + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (firstLetter == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = firstLetter;\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_CREATURE_TEXT],\n                        gArmyNamesPlural[giMonthTypeExtra],\n                        lowerName19\n                    );\n                } else {\n                    sprintf(gText, cNewTurn[NEW_MONTH_PLAGUE_TEXT]);\n                }\n            } else {\n                musicTrack2 = NEW_WEEK_MUSIC_TRACK;\n                strcpy(musicFile18, \"newweek.82m\");\n                if (giWeekType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(gText, cNewTurn[NEW_WEEK_NORMAL_TEXT], gWeekNames[giWeekTypeExtra]);\n                } else {\n                    u8 lowerFirst;\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giWeekTypeExtra]\n                    );\n                    u8 firstLetter = lowerName19[0];\n                    if (firstLetter >= 'A' && firstLetter <= 'Z')\n                        lowerFirst = firstLetter + ('a' - 'A');\n                    else if (firstLetter >= CP1251_CAPITAL_A\n                             && firstLetter <= CP1251_CAPITAL_YA)\n                        lowerFirst = firstLetter + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (firstLetter == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = firstLetter;\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_WEEK_CREATURE_TEXT],\n                        gArmyNamesPlural[giWeekTypeExtra],\n                        lowerName19\n                    );\n                }\n            }\n            gpSoundManager->PlayAmbientMusic(musicTrack2);\n            gpMouseManager->SetPointer(0);\n            NormalDialog(gText, 1, -1, -1, -1, 0, -1, 0, -1, 0);\n            gpSoundManager->SwitchAmbientMusic(\n                giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n            );\n        }\n    }\n    gpSoundManager->SwitchAmbientMusic(\n        giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n    );\n    gpAdvManager->SetEnvironmentOrigin(\n        gpAdvManager->m_mapOriginX + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        gpAdvManager->m_mapOriginY + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        1\n    );\n}"
        },
        {
          "name": "unsigned_local_retain_natural",
          "replace": "void game::DoNewTurn(void) {\n    char musicFile18[NEW_TURN_MUSIC_FILENAME_CAPACITY];\n    char lowerName19[NEW_TURN_LOWER_NAME_CAPACITY];\n    i32 musicTrack2;\n\n    CheckForTimeEvent();\n    if (!gbThisNetHumanPlayer[giCurPlayer]) {\n        CheckEndGame(END_GAME_FORCE_NONE, false);\n        return;\n    }\n    giBottomViewOverrideEndTime = KBTickCount() + NEW_TURN_BOTTOM_VIEW_DURATION;\n    giBottomViewOverride = BOTTOM_VIEW_NEW_TURN;\n    gpAdvManager->UpdBottomView(true, true, true);\n    gpAdvManager->SetInitialMapOrigin();\n    gpAdvManager->CompleteDraw(0);\n    gpAdvManager->UpdateScreen(0, 0);\n    CheckEndGame(END_GAME_FORCE_NONE, false);\n\n    if (gpCurPlayer->m_daysLeft >= 0) {\n        if (gpCurPlayer->m_daysLeft == 1) {\n            sprintf(gText, cNewTurn[1], cPlayerNames[giCurPlayer]);\n        } else {\n            sprintf(gText, cNewTurn[0], cPlayerNames[giCurPlayer], gpCurPlayer->m_daysLeft);\n        }\n        NormalDialog(\n            gText,\n            1,\n            -1,\n            -1,\n            NEW_TURN_DIALOG_TYPE,\n            gpGame->GetPlayerColor(giCurPlayer),\n            -1,\n            0,\n            -1,\n            0\n        );\n    }\n\n    if (gpCurPlayer->m_heroCount > 0) {\n        gpAdvManager->SetHeroContext(gpCurPlayer->NextHero(0), 0);\n    } else if (gpCurPlayer->m_townCount > 0) {\n        gpAdvManager->SetTownContext(gpCurPlayer->m_townIds[0]);\n    }\n    gpAdvManager->CheckDimNextHeroBut();\n\n    if (m_day == 1 && (m_month != 1 || m_week != 1 || m_day != 1)) {\n        if (gbThisNetHumanPlayer[giCurPlayer])\n            gSoundBackendsReady = 1;\n        if (giWeekType != CALENDAR_PERIOD_NONE) {\n            musicTrack2 = -1;\n            if (m_week == 1) {\n                musicTrack2 = NEW_MONTH_MUSIC_TRACK;\n                strcpy(musicFile18, \"newmonth.82m\");\n                if (giMonthType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_NORMAL_TEXT],\n                        gMonthNames[giMonthTypeExtra]\n                    );\n                } else if (giMonthType == CALENDAR_PERIOD_CREATURE) {\n                    u8 lowerFirst;\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giMonthTypeExtra]\n                    );\n                    u8 firstLetter = lowerName19[0];\n                    if (firstLetter >= 'A' && firstLetter <= 'Z')\n                        lowerFirst = firstLetter + ('a' - 'A');\n                    else if (firstLetter >= CP1251_CAPITAL_A\n                             && firstLetter <= CP1251_CAPITAL_YA)\n                        lowerFirst = firstLetter + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (firstLetter == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = firstLetter;\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_CREATURE_TEXT],\n                        gArmyNamesPlural[giMonthTypeExtra],\n                        lowerName19\n                    );\n                } else {\n                    sprintf(gText, cNewTurn[NEW_MONTH_PLAGUE_TEXT]);\n                }\n            } else {\n                musicTrack2 = NEW_WEEK_MUSIC_TRACK;\n                strcpy(musicFile18, \"newweek.82m\");\n                if (giWeekType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(gText, cNewTurn[NEW_WEEK_NORMAL_TEXT], gWeekNames[giWeekTypeExtra]);\n                } else {\n                    u8 lowerFirst;\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giWeekTypeExtra]\n                    );\n                    u8 firstLetter = lowerName19[0];\n                    if (firstLetter >= 'A' && firstLetter <= 'Z')\n                        lowerFirst = firstLetter + ('a' - 'A');\n                    else if (firstLetter >= CP1251_CAPITAL_A\n                             && firstLetter <= CP1251_CAPITAL_YA)\n                        lowerFirst = firstLetter + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (firstLetter == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = firstLetter;\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_WEEK_CREATURE_TEXT],\n                        gArmyNamesPlural[giWeekTypeExtra],\n                        lowerName19\n                    );\n                }\n            }\n            gpSoundManager->PlayAmbientMusic(musicTrack2);\n            gpMouseManager->SetPointer(0);\n            NormalDialog(gText, 1, -1, -1, -1, 0, -1, 0, -1, 0);\n            gpSoundManager->SwitchAmbientMusic(\n                giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n            );\n        }\n    }\n    gpSoundManager->SwitchAmbientMusic(\n        giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n    );\n    gpAdvManager->SetEnvironmentOrigin(\n        gpAdvManager->m_mapOriginX + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        gpAdvManager->m_mapOriginY + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        1\n    );\n}"
        },
        {
          "name": "unsigned_local_remove_cast",
          "replace": "void game::DoNewTurn(void) {\n    char lowerName19[NEW_TURN_LOWER_NAME_CAPACITY];\n    i32 musicTrack2;\n\n    CheckForTimeEvent();\n    if (!gbThisNetHumanPlayer[giCurPlayer]) {\n        CheckEndGame(END_GAME_FORCE_NONE, false);\n        return;\n    }\n    giBottomViewOverrideEndTime = KBTickCount() + NEW_TURN_BOTTOM_VIEW_DURATION;\n    giBottomViewOverride = BOTTOM_VIEW_NEW_TURN;\n    gpAdvManager->UpdBottomView(true, true, true);\n    gpAdvManager->SetInitialMapOrigin();\n    gpAdvManager->CompleteDraw(0);\n    gpAdvManager->UpdateScreen(0, 0);\n    CheckEndGame(END_GAME_FORCE_NONE, false);\n\n    if (gpCurPlayer->m_daysLeft >= 0) {\n        if (gpCurPlayer->m_daysLeft == 1) {\n            sprintf(gText, cNewTurn[1], cPlayerNames[giCurPlayer]);\n        } else {\n            sprintf(gText, cNewTurn[0], cPlayerNames[giCurPlayer], gpCurPlayer->m_daysLeft);\n        }\n        NormalDialog(\n            gText,\n            1,\n            -1,\n            -1,\n            NEW_TURN_DIALOG_TYPE,\n            gpGame->GetPlayerColor(static_cast<i8>(giCurPlayer)),\n            -1,\n            0,\n            -1,\n            0\n        );\n    }\n\n    if (gpCurPlayer->m_heroCount > 0) {\n        gpAdvManager->SetHeroContext(gpCurPlayer->NextHero(0), 0);\n    } else if (gpCurPlayer->m_townCount > 0) {\n        gpAdvManager->SetTownContext(gpCurPlayer->m_townIds[0]);\n    }\n    gpAdvManager->CheckDimNextHeroBut();\n\n    if (m_day == 1 && (m_month != 1 || m_week != 1 || m_day != 1)) {\n        if (gbThisNetHumanPlayer[giCurPlayer])\n            gSoundBackendsReady = 1;\n        if (giWeekType != CALENDAR_PERIOD_NONE) {\n            musicTrack2 = -1;\n            if (m_week == 1) {\n                musicTrack2 = NEW_MONTH_MUSIC_TRACK;\n                if (giMonthType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_NORMAL_TEXT],\n                        gMonthNames[giMonthTypeExtra]\n                    );\n                } else if (giMonthType == CALENDAR_PERIOD_CREATURE) {\n                    u8 lowerFirst;\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giMonthTypeExtra]\n                    );\n                    u8 firstLetter = lowerName19[0];\n                    if (firstLetter >= 'A' && firstLetter <= 'Z')\n                        lowerFirst = firstLetter + ('a' - 'A');\n                    else if (firstLetter >= CP1251_CAPITAL_A\n                             && firstLetter <= CP1251_CAPITAL_YA)\n                        lowerFirst = firstLetter + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (firstLetter == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = firstLetter;\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_CREATURE_TEXT],\n                        gArmyNamesPlural[giMonthTypeExtra],\n                        lowerName19\n                    );\n                } else {\n                    sprintf(gText, cNewTurn[NEW_MONTH_PLAGUE_TEXT]);\n                }\n            } else {\n                musicTrack2 = NEW_WEEK_MUSIC_TRACK;\n                if (giWeekType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(gText, cNewTurn[NEW_WEEK_NORMAL_TEXT], gWeekNames[giWeekTypeExtra]);\n                } else {\n                    u8 lowerFirst;\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giWeekTypeExtra]\n                    );\n                    u8 firstLetter = lowerName19[0];\n                    if (firstLetter >= 'A' && firstLetter <= 'Z')\n                        lowerFirst = firstLetter + ('a' - 'A');\n                    else if (firstLetter >= CP1251_CAPITAL_A\n                             && firstLetter <= CP1251_CAPITAL_YA)\n                        lowerFirst = firstLetter + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (firstLetter == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = firstLetter;\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_WEEK_CREATURE_TEXT],\n                        gArmyNamesPlural[giWeekTypeExtra],\n                        lowerName19\n                    );\n                }\n            }\n            gpSoundManager->PlayAmbientMusic(musicTrack2);\n            gpMouseManager->SetPointer(0);\n            NormalDialog(gText, 1, -1, -1, -1, 0, -1, 0, -1, 0);\n            gpSoundManager->SwitchAmbientMusic(\n                giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n            );\n        }\n    }\n    gpSoundManager->SwitchAmbientMusic(\n        giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n    );\n    gpAdvManager->SetEnvironmentOrigin(\n        gpAdvManager->m_mapOriginX + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        gpAdvManager->m_mapOriginY + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        1\n    );\n}"
        },
        {
          "name": "unsigned_local_remove_natural",
          "replace": "void game::DoNewTurn(void) {\n    char lowerName19[NEW_TURN_LOWER_NAME_CAPACITY];\n    i32 musicTrack2;\n\n    CheckForTimeEvent();\n    if (!gbThisNetHumanPlayer[giCurPlayer]) {\n        CheckEndGame(END_GAME_FORCE_NONE, false);\n        return;\n    }\n    giBottomViewOverrideEndTime = KBTickCount() + NEW_TURN_BOTTOM_VIEW_DURATION;\n    giBottomViewOverride = BOTTOM_VIEW_NEW_TURN;\n    gpAdvManager->UpdBottomView(true, true, true);\n    gpAdvManager->SetInitialMapOrigin();\n    gpAdvManager->CompleteDraw(0);\n    gpAdvManager->UpdateScreen(0, 0);\n    CheckEndGame(END_GAME_FORCE_NONE, false);\n\n    if (gpCurPlayer->m_daysLeft >= 0) {\n        if (gpCurPlayer->m_daysLeft == 1) {\n            sprintf(gText, cNewTurn[1], cPlayerNames[giCurPlayer]);\n        } else {\n            sprintf(gText, cNewTurn[0], cPlayerNames[giCurPlayer], gpCurPlayer->m_daysLeft);\n        }\n        NormalDialog(\n            gText,\n            1,\n            -1,\n            -1,\n            NEW_TURN_DIALOG_TYPE,\n            gpGame->GetPlayerColor(giCurPlayer),\n            -1,\n            0,\n            -1,\n            0\n        );\n    }\n\n    if (gpCurPlayer->m_heroCount > 0) {\n        gpAdvManager->SetHeroContext(gpCurPlayer->NextHero(0), 0);\n    } else if (gpCurPlayer->m_townCount > 0) {\n        gpAdvManager->SetTownContext(gpCurPlayer->m_townIds[0]);\n    }\n    gpAdvManager->CheckDimNextHeroBut();\n\n    if (m_day == 1 && (m_month != 1 || m_week != 1 || m_day != 1)) {\n        if (gbThisNetHumanPlayer[giCurPlayer])\n            gSoundBackendsReady = 1;\n        if (giWeekType != CALENDAR_PERIOD_NONE) {\n            musicTrack2 = -1;\n            if (m_week == 1) {\n                musicTrack2 = NEW_MONTH_MUSIC_TRACK;\n                if (giMonthType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_NORMAL_TEXT],\n                        gMonthNames[giMonthTypeExtra]\n                    );\n                } else if (giMonthType == CALENDAR_PERIOD_CREATURE) {\n                    u8 lowerFirst;\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giMonthTypeExtra]\n                    );\n                    u8 firstLetter = lowerName19[0];\n                    if (firstLetter >= 'A' && firstLetter <= 'Z')\n                        lowerFirst = firstLetter + ('a' - 'A');\n                    else if (firstLetter >= CP1251_CAPITAL_A\n                             && firstLetter <= CP1251_CAPITAL_YA)\n                        lowerFirst = firstLetter + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (firstLetter == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = firstLetter;\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_CREATURE_TEXT],\n                        gArmyNamesPlural[giMonthTypeExtra],\n                        lowerName19\n                    );\n                } else {\n                    sprintf(gText, cNewTurn[NEW_MONTH_PLAGUE_TEXT]);\n                }\n            } else {\n                musicTrack2 = NEW_WEEK_MUSIC_TRACK;\n                if (giWeekType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(gText, cNewTurn[NEW_WEEK_NORMAL_TEXT], gWeekNames[giWeekTypeExtra]);\n                } else {\n                    u8 lowerFirst;\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giWeekTypeExtra]\n                    );\n                    u8 firstLetter = lowerName19[0];\n                    if (firstLetter >= 'A' && firstLetter <= 'Z')\n                        lowerFirst = firstLetter + ('a' - 'A');\n                    else if (firstLetter >= CP1251_CAPITAL_A\n                             && firstLetter <= CP1251_CAPITAL_YA)\n                        lowerFirst = firstLetter + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n                    else if (firstLetter == CP1251_CAPITAL_YO)\n                        lowerFirst = CP1251_SMALL_YO;\n                    else\n                        lowerFirst = firstLetter;\n                    lowerName19[0] = lowerFirst;\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_WEEK_CREATURE_TEXT],\n                        gArmyNamesPlural[giWeekTypeExtra],\n                        lowerName19\n                    );\n                }\n            }\n            gpSoundManager->PlayAmbientMusic(musicTrack2);\n            gpMouseManager->SetPointer(0);\n            NormalDialog(gText, 1, -1, -1, -1, 0, -1, 0, -1, 0);\n            gpSoundManager->SwitchAmbientMusic(\n                giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n            );\n        }\n    }\n    gpSoundManager->SwitchAmbientMusic(\n        giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n    );\n    gpAdvManager->SetEnvironmentOrigin(\n        gpAdvManager->m_mapOriginX + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        gpAdvManager->m_mapOriginY + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        1\n    );\n}"
        },
        {
          "name": "helper_char_retain_cast",
          "replace": "void game::DoNewTurn(void) {\n    char musicFile18[NEW_TURN_MUSIC_FILENAME_CAPACITY];\n    char lowerName19[NEW_TURN_LOWER_NAME_CAPACITY];\n    i32 musicTrack2;\n\n    CheckForTimeEvent();\n    if (!gbThisNetHumanPlayer[giCurPlayer]) {\n        CheckEndGame(END_GAME_FORCE_NONE, false);\n        return;\n    }\n    giBottomViewOverrideEndTime = KBTickCount() + NEW_TURN_BOTTOM_VIEW_DURATION;\n    giBottomViewOverride = BOTTOM_VIEW_NEW_TURN;\n    gpAdvManager->UpdBottomView(true, true, true);\n    gpAdvManager->SetInitialMapOrigin();\n    gpAdvManager->CompleteDraw(0);\n    gpAdvManager->UpdateScreen(0, 0);\n    CheckEndGame(END_GAME_FORCE_NONE, false);\n\n    if (gpCurPlayer->m_daysLeft >= 0) {\n        if (gpCurPlayer->m_daysLeft == 1) {\n            sprintf(gText, cNewTurn[1], cPlayerNames[giCurPlayer]);\n        } else {\n            sprintf(gText, cNewTurn[0], cPlayerNames[giCurPlayer], gpCurPlayer->m_daysLeft);\n        }\n        NormalDialog(\n            gText,\n            1,\n            -1,\n            -1,\n            NEW_TURN_DIALOG_TYPE,\n            gpGame->GetPlayerColor(static_cast<i8>(giCurPlayer)),\n            -1,\n            0,\n            -1,\n            0\n        );\n    }\n\n    if (gpCurPlayer->m_heroCount > 0) {\n        gpAdvManager->SetHeroContext(gpCurPlayer->NextHero(0), 0);\n    } else if (gpCurPlayer->m_townCount > 0) {\n        gpAdvManager->SetTownContext(gpCurPlayer->m_townIds[0]);\n    }\n    gpAdvManager->CheckDimNextHeroBut();\n\n    if (m_day == 1 && (m_month != 1 || m_week != 1 || m_day != 1)) {\n        if (gbThisNetHumanPlayer[giCurPlayer])\n            gSoundBackendsReady = 1;\n        if (giWeekType != CALENDAR_PERIOD_NONE) {\n            musicTrack2 = -1;\n            if (m_week == 1) {\n                musicTrack2 = NEW_MONTH_MUSIC_TRACK;\n                strcpy(musicFile18, \"newmonth.82m\");\n                if (giMonthType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_NORMAL_TEXT],\n                        gMonthNames[giMonthTypeExtra]\n                    );\n                } else if (giMonthType == CALENDAR_PERIOD_CREATURE) {\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giMonthTypeExtra]\n                    );\n                    lowerName19[0] = ToLowerCp1251(lowerName19[0]);\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_CREATURE_TEXT],\n                        gArmyNamesPlural[giMonthTypeExtra],\n                        lowerName19\n                    );\n                } else {\n                    sprintf(gText, cNewTurn[NEW_MONTH_PLAGUE_TEXT]);\n                }\n            } else {\n                musicTrack2 = NEW_WEEK_MUSIC_TRACK;\n                strcpy(musicFile18, \"newweek.82m\");\n                if (giWeekType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(gText, cNewTurn[NEW_WEEK_NORMAL_TEXT], gWeekNames[giWeekTypeExtra]);\n                } else {\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giWeekTypeExtra]\n                    );\n                    lowerName19[0] = ToLowerCp1251(lowerName19[0]);\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_WEEK_CREATURE_TEXT],\n                        gArmyNamesPlural[giWeekTypeExtra],\n                        lowerName19\n                    );\n                }\n            }\n            gpSoundManager->PlayAmbientMusic(musicTrack2);\n            gpMouseManager->SetPointer(0);\n            NormalDialog(gText, 1, -1, -1, -1, 0, -1, 0, -1, 0);\n            gpSoundManager->SwitchAmbientMusic(\n                giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n            );\n        }\n    }\n    gpSoundManager->SwitchAmbientMusic(\n        giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n    );\n    gpAdvManager->SetEnvironmentOrigin(\n        gpAdvManager->m_mapOriginX + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        gpAdvManager->m_mapOriginY + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        1\n    );\n}",
          "extra_edits": [
            {
              "insert_before": "} // namespace",
              "text": "    inline char ToLowerCp1251(u8 letter) {\n        char lowerFirst;\n        if (letter >= 'A' && letter <= 'Z')\n            lowerFirst = letter + ('a' - 'A');\n        else if (letter >= CP1251_CAPITAL_A && letter <= CP1251_CAPITAL_YA)\n            lowerFirst = letter + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n        else if (letter == CP1251_CAPITAL_YO)\n            lowerFirst = CP1251_SMALL_YO;\n        else\n            lowerFirst = letter;\n        return lowerFirst;\n    }\n\n"
            }
          ]
        },
        {
          "name": "helper_char_retain_natural",
          "replace": "void game::DoNewTurn(void) {\n    char musicFile18[NEW_TURN_MUSIC_FILENAME_CAPACITY];\n    char lowerName19[NEW_TURN_LOWER_NAME_CAPACITY];\n    i32 musicTrack2;\n\n    CheckForTimeEvent();\n    if (!gbThisNetHumanPlayer[giCurPlayer]) {\n        CheckEndGame(END_GAME_FORCE_NONE, false);\n        return;\n    }\n    giBottomViewOverrideEndTime = KBTickCount() + NEW_TURN_BOTTOM_VIEW_DURATION;\n    giBottomViewOverride = BOTTOM_VIEW_NEW_TURN;\n    gpAdvManager->UpdBottomView(true, true, true);\n    gpAdvManager->SetInitialMapOrigin();\n    gpAdvManager->CompleteDraw(0);\n    gpAdvManager->UpdateScreen(0, 0);\n    CheckEndGame(END_GAME_FORCE_NONE, false);\n\n    if (gpCurPlayer->m_daysLeft >= 0) {\n        if (gpCurPlayer->m_daysLeft == 1) {\n            sprintf(gText, cNewTurn[1], cPlayerNames[giCurPlayer]);\n        } else {\n            sprintf(gText, cNewTurn[0], cPlayerNames[giCurPlayer], gpCurPlayer->m_daysLeft);\n        }\n        NormalDialog(\n            gText,\n            1,\n            -1,\n            -1,\n            NEW_TURN_DIALOG_TYPE,\n            gpGame->GetPlayerColor(giCurPlayer),\n            -1,\n            0,\n            -1,\n            0\n        );\n    }\n\n    if (gpCurPlayer->m_heroCount > 0) {\n        gpAdvManager->SetHeroContext(gpCurPlayer->NextHero(0), 0);\n    } else if (gpCurPlayer->m_townCount > 0) {\n        gpAdvManager->SetTownContext(gpCurPlayer->m_townIds[0]);\n    }\n    gpAdvManager->CheckDimNextHeroBut();\n\n    if (m_day == 1 && (m_month != 1 || m_week != 1 || m_day != 1)) {\n        if (gbThisNetHumanPlayer[giCurPlayer])\n            gSoundBackendsReady = 1;\n        if (giWeekType != CALENDAR_PERIOD_NONE) {\n            musicTrack2 = -1;\n            if (m_week == 1) {\n                musicTrack2 = NEW_MONTH_MUSIC_TRACK;\n                strcpy(musicFile18, \"newmonth.82m\");\n                if (giMonthType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_NORMAL_TEXT],\n                        gMonthNames[giMonthTypeExtra]\n                    );\n                } else if (giMonthType == CALENDAR_PERIOD_CREATURE) {\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giMonthTypeExtra]\n                    );\n                    lowerName19[0] = ToLowerCp1251(lowerName19[0]);\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_CREATURE_TEXT],\n                        gArmyNamesPlural[giMonthTypeExtra],\n                        lowerName19\n                    );\n                } else {\n                    sprintf(gText, cNewTurn[NEW_MONTH_PLAGUE_TEXT]);\n                }\n            } else {\n                musicTrack2 = NEW_WEEK_MUSIC_TRACK;\n                strcpy(musicFile18, \"newweek.82m\");\n                if (giWeekType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(gText, cNewTurn[NEW_WEEK_NORMAL_TEXT], gWeekNames[giWeekTypeExtra]);\n                } else {\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giWeekTypeExtra]\n                    );\n                    lowerName19[0] = ToLowerCp1251(lowerName19[0]);\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_WEEK_CREATURE_TEXT],\n                        gArmyNamesPlural[giWeekTypeExtra],\n                        lowerName19\n                    );\n                }\n            }\n            gpSoundManager->PlayAmbientMusic(musicTrack2);\n            gpMouseManager->SetPointer(0);\n            NormalDialog(gText, 1, -1, -1, -1, 0, -1, 0, -1, 0);\n            gpSoundManager->SwitchAmbientMusic(\n                giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n            );\n        }\n    }\n    gpSoundManager->SwitchAmbientMusic(\n        giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n    );\n    gpAdvManager->SetEnvironmentOrigin(\n        gpAdvManager->m_mapOriginX + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        gpAdvManager->m_mapOriginY + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        1\n    );\n}",
          "extra_edits": [
            {
              "insert_before": "} // namespace",
              "text": "    inline char ToLowerCp1251(u8 letter) {\n        char lowerFirst;\n        if (letter >= 'A' && letter <= 'Z')\n            lowerFirst = letter + ('a' - 'A');\n        else if (letter >= CP1251_CAPITAL_A && letter <= CP1251_CAPITAL_YA)\n            lowerFirst = letter + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n        else if (letter == CP1251_CAPITAL_YO)\n            lowerFirst = CP1251_SMALL_YO;\n        else\n            lowerFirst = letter;\n        return lowerFirst;\n    }\n\n"
            }
          ]
        },
        {
          "name": "helper_char_remove_cast",
          "replace": "void game::DoNewTurn(void) {\n    char lowerName19[NEW_TURN_LOWER_NAME_CAPACITY];\n    i32 musicTrack2;\n\n    CheckForTimeEvent();\n    if (!gbThisNetHumanPlayer[giCurPlayer]) {\n        CheckEndGame(END_GAME_FORCE_NONE, false);\n        return;\n    }\n    giBottomViewOverrideEndTime = KBTickCount() + NEW_TURN_BOTTOM_VIEW_DURATION;\n    giBottomViewOverride = BOTTOM_VIEW_NEW_TURN;\n    gpAdvManager->UpdBottomView(true, true, true);\n    gpAdvManager->SetInitialMapOrigin();\n    gpAdvManager->CompleteDraw(0);\n    gpAdvManager->UpdateScreen(0, 0);\n    CheckEndGame(END_GAME_FORCE_NONE, false);\n\n    if (gpCurPlayer->m_daysLeft >= 0) {\n        if (gpCurPlayer->m_daysLeft == 1) {\n            sprintf(gText, cNewTurn[1], cPlayerNames[giCurPlayer]);\n        } else {\n            sprintf(gText, cNewTurn[0], cPlayerNames[giCurPlayer], gpCurPlayer->m_daysLeft);\n        }\n        NormalDialog(\n            gText,\n            1,\n            -1,\n            -1,\n            NEW_TURN_DIALOG_TYPE,\n            gpGame->GetPlayerColor(static_cast<i8>(giCurPlayer)),\n            -1,\n            0,\n            -1,\n            0\n        );\n    }\n\n    if (gpCurPlayer->m_heroCount > 0) {\n        gpAdvManager->SetHeroContext(gpCurPlayer->NextHero(0), 0);\n    } else if (gpCurPlayer->m_townCount > 0) {\n        gpAdvManager->SetTownContext(gpCurPlayer->m_townIds[0]);\n    }\n    gpAdvManager->CheckDimNextHeroBut();\n\n    if (m_day == 1 && (m_month != 1 || m_week != 1 || m_day != 1)) {\n        if (gbThisNetHumanPlayer[giCurPlayer])\n            gSoundBackendsReady = 1;\n        if (giWeekType != CALENDAR_PERIOD_NONE) {\n            musicTrack2 = -1;\n            if (m_week == 1) {\n                musicTrack2 = NEW_MONTH_MUSIC_TRACK;\n                if (giMonthType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_NORMAL_TEXT],\n                        gMonthNames[giMonthTypeExtra]\n                    );\n                } else if (giMonthType == CALENDAR_PERIOD_CREATURE) {\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giMonthTypeExtra]\n                    );\n                    lowerName19[0] = ToLowerCp1251(lowerName19[0]);\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_CREATURE_TEXT],\n                        gArmyNamesPlural[giMonthTypeExtra],\n                        lowerName19\n                    );\n                } else {\n                    sprintf(gText, cNewTurn[NEW_MONTH_PLAGUE_TEXT]);\n                }\n            } else {\n                musicTrack2 = NEW_WEEK_MUSIC_TRACK;\n                if (giWeekType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(gText, cNewTurn[NEW_WEEK_NORMAL_TEXT], gWeekNames[giWeekTypeExtra]);\n                } else {\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giWeekTypeExtra]\n                    );\n                    lowerName19[0] = ToLowerCp1251(lowerName19[0]);\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_WEEK_CREATURE_TEXT],\n                        gArmyNamesPlural[giWeekTypeExtra],\n                        lowerName19\n                    );\n                }\n            }\n            gpSoundManager->PlayAmbientMusic(musicTrack2);\n            gpMouseManager->SetPointer(0);\n            NormalDialog(gText, 1, -1, -1, -1, 0, -1, 0, -1, 0);\n            gpSoundManager->SwitchAmbientMusic(\n                giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n            );\n        }\n    }\n    gpSoundManager->SwitchAmbientMusic(\n        giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n    );\n    gpAdvManager->SetEnvironmentOrigin(\n        gpAdvManager->m_mapOriginX + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        gpAdvManager->m_mapOriginY + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        1\n    );\n}",
          "extra_edits": [
            {
              "insert_before": "} // namespace",
              "text": "    inline char ToLowerCp1251(u8 letter) {\n        char lowerFirst;\n        if (letter >= 'A' && letter <= 'Z')\n            lowerFirst = letter + ('a' - 'A');\n        else if (letter >= CP1251_CAPITAL_A && letter <= CP1251_CAPITAL_YA)\n            lowerFirst = letter + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n        else if (letter == CP1251_CAPITAL_YO)\n            lowerFirst = CP1251_SMALL_YO;\n        else\n            lowerFirst = letter;\n        return lowerFirst;\n    }\n\n"
            }
          ]
        },
        {
          "name": "helper_char_remove_natural",
          "replace": "void game::DoNewTurn(void) {\n    char lowerName19[NEW_TURN_LOWER_NAME_CAPACITY];\n    i32 musicTrack2;\n\n    CheckForTimeEvent();\n    if (!gbThisNetHumanPlayer[giCurPlayer]) {\n        CheckEndGame(END_GAME_FORCE_NONE, false);\n        return;\n    }\n    giBottomViewOverrideEndTime = KBTickCount() + NEW_TURN_BOTTOM_VIEW_DURATION;\n    giBottomViewOverride = BOTTOM_VIEW_NEW_TURN;\n    gpAdvManager->UpdBottomView(true, true, true);\n    gpAdvManager->SetInitialMapOrigin();\n    gpAdvManager->CompleteDraw(0);\n    gpAdvManager->UpdateScreen(0, 0);\n    CheckEndGame(END_GAME_FORCE_NONE, false);\n\n    if (gpCurPlayer->m_daysLeft >= 0) {\n        if (gpCurPlayer->m_daysLeft == 1) {\n            sprintf(gText, cNewTurn[1], cPlayerNames[giCurPlayer]);\n        } else {\n            sprintf(gText, cNewTurn[0], cPlayerNames[giCurPlayer], gpCurPlayer->m_daysLeft);\n        }\n        NormalDialog(\n            gText,\n            1,\n            -1,\n            -1,\n            NEW_TURN_DIALOG_TYPE,\n            gpGame->GetPlayerColor(giCurPlayer),\n            -1,\n            0,\n            -1,\n            0\n        );\n    }\n\n    if (gpCurPlayer->m_heroCount > 0) {\n        gpAdvManager->SetHeroContext(gpCurPlayer->NextHero(0), 0);\n    } else if (gpCurPlayer->m_townCount > 0) {\n        gpAdvManager->SetTownContext(gpCurPlayer->m_townIds[0]);\n    }\n    gpAdvManager->CheckDimNextHeroBut();\n\n    if (m_day == 1 && (m_month != 1 || m_week != 1 || m_day != 1)) {\n        if (gbThisNetHumanPlayer[giCurPlayer])\n            gSoundBackendsReady = 1;\n        if (giWeekType != CALENDAR_PERIOD_NONE) {\n            musicTrack2 = -1;\n            if (m_week == 1) {\n                musicTrack2 = NEW_MONTH_MUSIC_TRACK;\n                if (giMonthType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_NORMAL_TEXT],\n                        gMonthNames[giMonthTypeExtra]\n                    );\n                } else if (giMonthType == CALENDAR_PERIOD_CREATURE) {\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giMonthTypeExtra]\n                    );\n                    lowerName19[0] = ToLowerCp1251(lowerName19[0]);\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_CREATURE_TEXT],\n                        gArmyNamesPlural[giMonthTypeExtra],\n                        lowerName19\n                    );\n                } else {\n                    sprintf(gText, cNewTurn[NEW_MONTH_PLAGUE_TEXT]);\n                }\n            } else {\n                musicTrack2 = NEW_WEEK_MUSIC_TRACK;\n                if (giWeekType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(gText, cNewTurn[NEW_WEEK_NORMAL_TEXT], gWeekNames[giWeekTypeExtra]);\n                } else {\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giWeekTypeExtra]\n                    );\n                    lowerName19[0] = ToLowerCp1251(lowerName19[0]);\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_WEEK_CREATURE_TEXT],\n                        gArmyNamesPlural[giWeekTypeExtra],\n                        lowerName19\n                    );\n                }\n            }\n            gpSoundManager->PlayAmbientMusic(musicTrack2);\n            gpMouseManager->SetPointer(0);\n            NormalDialog(gText, 1, -1, -1, -1, 0, -1, 0, -1, 0);\n            gpSoundManager->SwitchAmbientMusic(\n                giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n            );\n        }\n    }\n    gpSoundManager->SwitchAmbientMusic(\n        giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n    );\n    gpAdvManager->SetEnvironmentOrigin(\n        gpAdvManager->m_mapOriginX + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        gpAdvManager->m_mapOriginY + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        1\n    );\n}",
          "extra_edits": [
            {
              "insert_before": "} // namespace",
              "text": "    inline char ToLowerCp1251(u8 letter) {\n        char lowerFirst;\n        if (letter >= 'A' && letter <= 'Z')\n            lowerFirst = letter + ('a' - 'A');\n        else if (letter >= CP1251_CAPITAL_A && letter <= CP1251_CAPITAL_YA)\n            lowerFirst = letter + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n        else if (letter == CP1251_CAPITAL_YO)\n            lowerFirst = CP1251_SMALL_YO;\n        else\n            lowerFirst = letter;\n        return lowerFirst;\n    }\n\n"
            }
          ]
        },
        {
          "name": "helper_byte_retain_cast",
          "replace": "void game::DoNewTurn(void) {\n    char musicFile18[NEW_TURN_MUSIC_FILENAME_CAPACITY];\n    char lowerName19[NEW_TURN_LOWER_NAME_CAPACITY];\n    i32 musicTrack2;\n\n    CheckForTimeEvent();\n    if (!gbThisNetHumanPlayer[giCurPlayer]) {\n        CheckEndGame(END_GAME_FORCE_NONE, false);\n        return;\n    }\n    giBottomViewOverrideEndTime = KBTickCount() + NEW_TURN_BOTTOM_VIEW_DURATION;\n    giBottomViewOverride = BOTTOM_VIEW_NEW_TURN;\n    gpAdvManager->UpdBottomView(true, true, true);\n    gpAdvManager->SetInitialMapOrigin();\n    gpAdvManager->CompleteDraw(0);\n    gpAdvManager->UpdateScreen(0, 0);\n    CheckEndGame(END_GAME_FORCE_NONE, false);\n\n    if (gpCurPlayer->m_daysLeft >= 0) {\n        if (gpCurPlayer->m_daysLeft == 1) {\n            sprintf(gText, cNewTurn[1], cPlayerNames[giCurPlayer]);\n        } else {\n            sprintf(gText, cNewTurn[0], cPlayerNames[giCurPlayer], gpCurPlayer->m_daysLeft);\n        }\n        NormalDialog(\n            gText,\n            1,\n            -1,\n            -1,\n            NEW_TURN_DIALOG_TYPE,\n            gpGame->GetPlayerColor(static_cast<i8>(giCurPlayer)),\n            -1,\n            0,\n            -1,\n            0\n        );\n    }\n\n    if (gpCurPlayer->m_heroCount > 0) {\n        gpAdvManager->SetHeroContext(gpCurPlayer->NextHero(0), 0);\n    } else if (gpCurPlayer->m_townCount > 0) {\n        gpAdvManager->SetTownContext(gpCurPlayer->m_townIds[0]);\n    }\n    gpAdvManager->CheckDimNextHeroBut();\n\n    if (m_day == 1 && (m_month != 1 || m_week != 1 || m_day != 1)) {\n        if (gbThisNetHumanPlayer[giCurPlayer])\n            gSoundBackendsReady = 1;\n        if (giWeekType != CALENDAR_PERIOD_NONE) {\n            musicTrack2 = -1;\n            if (m_week == 1) {\n                musicTrack2 = NEW_MONTH_MUSIC_TRACK;\n                strcpy(musicFile18, \"newmonth.82m\");\n                if (giMonthType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_NORMAL_TEXT],\n                        gMonthNames[giMonthTypeExtra]\n                    );\n                } else if (giMonthType == CALENDAR_PERIOD_CREATURE) {\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giMonthTypeExtra]\n                    );\n                    lowerName19[0] = ToLowerCp1251(lowerName19[0]);\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_CREATURE_TEXT],\n                        gArmyNamesPlural[giMonthTypeExtra],\n                        lowerName19\n                    );\n                } else {\n                    sprintf(gText, cNewTurn[NEW_MONTH_PLAGUE_TEXT]);\n                }\n            } else {\n                musicTrack2 = NEW_WEEK_MUSIC_TRACK;\n                strcpy(musicFile18, \"newweek.82m\");\n                if (giWeekType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(gText, cNewTurn[NEW_WEEK_NORMAL_TEXT], gWeekNames[giWeekTypeExtra]);\n                } else {\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giWeekTypeExtra]\n                    );\n                    lowerName19[0] = ToLowerCp1251(lowerName19[0]);\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_WEEK_CREATURE_TEXT],\n                        gArmyNamesPlural[giWeekTypeExtra],\n                        lowerName19\n                    );\n                }\n            }\n            gpSoundManager->PlayAmbientMusic(musicTrack2);\n            gpMouseManager->SetPointer(0);\n            NormalDialog(gText, 1, -1, -1, -1, 0, -1, 0, -1, 0);\n            gpSoundManager->SwitchAmbientMusic(\n                giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n            );\n        }\n    }\n    gpSoundManager->SwitchAmbientMusic(\n        giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n    );\n    gpAdvManager->SetEnvironmentOrigin(\n        gpAdvManager->m_mapOriginX + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        gpAdvManager->m_mapOriginY + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        1\n    );\n}",
          "extra_edits": [
            {
              "insert_before": "} // namespace",
              "text": "    inline u8 ToLowerCp1251(u8 letter) {\n        u8 lowerFirst;\n        if (letter >= 'A' && letter <= 'Z')\n            lowerFirst = letter + ('a' - 'A');\n        else if (letter >= CP1251_CAPITAL_A && letter <= CP1251_CAPITAL_YA)\n            lowerFirst = letter + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n        else if (letter == CP1251_CAPITAL_YO)\n            lowerFirst = CP1251_SMALL_YO;\n        else\n            lowerFirst = letter;\n        return lowerFirst;\n    }\n\n"
            }
          ]
        },
        {
          "name": "helper_byte_retain_natural",
          "replace": "void game::DoNewTurn(void) {\n    char musicFile18[NEW_TURN_MUSIC_FILENAME_CAPACITY];\n    char lowerName19[NEW_TURN_LOWER_NAME_CAPACITY];\n    i32 musicTrack2;\n\n    CheckForTimeEvent();\n    if (!gbThisNetHumanPlayer[giCurPlayer]) {\n        CheckEndGame(END_GAME_FORCE_NONE, false);\n        return;\n    }\n    giBottomViewOverrideEndTime = KBTickCount() + NEW_TURN_BOTTOM_VIEW_DURATION;\n    giBottomViewOverride = BOTTOM_VIEW_NEW_TURN;\n    gpAdvManager->UpdBottomView(true, true, true);\n    gpAdvManager->SetInitialMapOrigin();\n    gpAdvManager->CompleteDraw(0);\n    gpAdvManager->UpdateScreen(0, 0);\n    CheckEndGame(END_GAME_FORCE_NONE, false);\n\n    if (gpCurPlayer->m_daysLeft >= 0) {\n        if (gpCurPlayer->m_daysLeft == 1) {\n            sprintf(gText, cNewTurn[1], cPlayerNames[giCurPlayer]);\n        } else {\n            sprintf(gText, cNewTurn[0], cPlayerNames[giCurPlayer], gpCurPlayer->m_daysLeft);\n        }\n        NormalDialog(\n            gText,\n            1,\n            -1,\n            -1,\n            NEW_TURN_DIALOG_TYPE,\n            gpGame->GetPlayerColor(giCurPlayer),\n            -1,\n            0,\n            -1,\n            0\n        );\n    }\n\n    if (gpCurPlayer->m_heroCount > 0) {\n        gpAdvManager->SetHeroContext(gpCurPlayer->NextHero(0), 0);\n    } else if (gpCurPlayer->m_townCount > 0) {\n        gpAdvManager->SetTownContext(gpCurPlayer->m_townIds[0]);\n    }\n    gpAdvManager->CheckDimNextHeroBut();\n\n    if (m_day == 1 && (m_month != 1 || m_week != 1 || m_day != 1)) {\n        if (gbThisNetHumanPlayer[giCurPlayer])\n            gSoundBackendsReady = 1;\n        if (giWeekType != CALENDAR_PERIOD_NONE) {\n            musicTrack2 = -1;\n            if (m_week == 1) {\n                musicTrack2 = NEW_MONTH_MUSIC_TRACK;\n                strcpy(musicFile18, \"newmonth.82m\");\n                if (giMonthType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_NORMAL_TEXT],\n                        gMonthNames[giMonthTypeExtra]\n                    );\n                } else if (giMonthType == CALENDAR_PERIOD_CREATURE) {\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giMonthTypeExtra]\n                    );\n                    lowerName19[0] = ToLowerCp1251(lowerName19[0]);\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_CREATURE_TEXT],\n                        gArmyNamesPlural[giMonthTypeExtra],\n                        lowerName19\n                    );\n                } else {\n                    sprintf(gText, cNewTurn[NEW_MONTH_PLAGUE_TEXT]);\n                }\n            } else {\n                musicTrack2 = NEW_WEEK_MUSIC_TRACK;\n                strcpy(musicFile18, \"newweek.82m\");\n                if (giWeekType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(gText, cNewTurn[NEW_WEEK_NORMAL_TEXT], gWeekNames[giWeekTypeExtra]);\n                } else {\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giWeekTypeExtra]\n                    );\n                    lowerName19[0] = ToLowerCp1251(lowerName19[0]);\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_WEEK_CREATURE_TEXT],\n                        gArmyNamesPlural[giWeekTypeExtra],\n                        lowerName19\n                    );\n                }\n            }\n            gpSoundManager->PlayAmbientMusic(musicTrack2);\n            gpMouseManager->SetPointer(0);\n            NormalDialog(gText, 1, -1, -1, -1, 0, -1, 0, -1, 0);\n            gpSoundManager->SwitchAmbientMusic(\n                giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n            );\n        }\n    }\n    gpSoundManager->SwitchAmbientMusic(\n        giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n    );\n    gpAdvManager->SetEnvironmentOrigin(\n        gpAdvManager->m_mapOriginX + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        gpAdvManager->m_mapOriginY + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        1\n    );\n}",
          "extra_edits": [
            {
              "insert_before": "} // namespace",
              "text": "    inline u8 ToLowerCp1251(u8 letter) {\n        u8 lowerFirst;\n        if (letter >= 'A' && letter <= 'Z')\n            lowerFirst = letter + ('a' - 'A');\n        else if (letter >= CP1251_CAPITAL_A && letter <= CP1251_CAPITAL_YA)\n            lowerFirst = letter + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n        else if (letter == CP1251_CAPITAL_YO)\n            lowerFirst = CP1251_SMALL_YO;\n        else\n            lowerFirst = letter;\n        return lowerFirst;\n    }\n\n"
            }
          ]
        },
        {
          "name": "helper_byte_remove_cast",
          "replace": "void game::DoNewTurn(void) {\n    char lowerName19[NEW_TURN_LOWER_NAME_CAPACITY];\n    i32 musicTrack2;\n\n    CheckForTimeEvent();\n    if (!gbThisNetHumanPlayer[giCurPlayer]) {\n        CheckEndGame(END_GAME_FORCE_NONE, false);\n        return;\n    }\n    giBottomViewOverrideEndTime = KBTickCount() + NEW_TURN_BOTTOM_VIEW_DURATION;\n    giBottomViewOverride = BOTTOM_VIEW_NEW_TURN;\n    gpAdvManager->UpdBottomView(true, true, true);\n    gpAdvManager->SetInitialMapOrigin();\n    gpAdvManager->CompleteDraw(0);\n    gpAdvManager->UpdateScreen(0, 0);\n    CheckEndGame(END_GAME_FORCE_NONE, false);\n\n    if (gpCurPlayer->m_daysLeft >= 0) {\n        if (gpCurPlayer->m_daysLeft == 1) {\n            sprintf(gText, cNewTurn[1], cPlayerNames[giCurPlayer]);\n        } else {\n            sprintf(gText, cNewTurn[0], cPlayerNames[giCurPlayer], gpCurPlayer->m_daysLeft);\n        }\n        NormalDialog(\n            gText,\n            1,\n            -1,\n            -1,\n            NEW_TURN_DIALOG_TYPE,\n            gpGame->GetPlayerColor(static_cast<i8>(giCurPlayer)),\n            -1,\n            0,\n            -1,\n            0\n        );\n    }\n\n    if (gpCurPlayer->m_heroCount > 0) {\n        gpAdvManager->SetHeroContext(gpCurPlayer->NextHero(0), 0);\n    } else if (gpCurPlayer->m_townCount > 0) {\n        gpAdvManager->SetTownContext(gpCurPlayer->m_townIds[0]);\n    }\n    gpAdvManager->CheckDimNextHeroBut();\n\n    if (m_day == 1 && (m_month != 1 || m_week != 1 || m_day != 1)) {\n        if (gbThisNetHumanPlayer[giCurPlayer])\n            gSoundBackendsReady = 1;\n        if (giWeekType != CALENDAR_PERIOD_NONE) {\n            musicTrack2 = -1;\n            if (m_week == 1) {\n                musicTrack2 = NEW_MONTH_MUSIC_TRACK;\n                if (giMonthType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_NORMAL_TEXT],\n                        gMonthNames[giMonthTypeExtra]\n                    );\n                } else if (giMonthType == CALENDAR_PERIOD_CREATURE) {\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giMonthTypeExtra]\n                    );\n                    lowerName19[0] = ToLowerCp1251(lowerName19[0]);\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_CREATURE_TEXT],\n                        gArmyNamesPlural[giMonthTypeExtra],\n                        lowerName19\n                    );\n                } else {\n                    sprintf(gText, cNewTurn[NEW_MONTH_PLAGUE_TEXT]);\n                }\n            } else {\n                musicTrack2 = NEW_WEEK_MUSIC_TRACK;\n                if (giWeekType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(gText, cNewTurn[NEW_WEEK_NORMAL_TEXT], gWeekNames[giWeekTypeExtra]);\n                } else {\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giWeekTypeExtra]\n                    );\n                    lowerName19[0] = ToLowerCp1251(lowerName19[0]);\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_WEEK_CREATURE_TEXT],\n                        gArmyNamesPlural[giWeekTypeExtra],\n                        lowerName19\n                    );\n                }\n            }\n            gpSoundManager->PlayAmbientMusic(musicTrack2);\n            gpMouseManager->SetPointer(0);\n            NormalDialog(gText, 1, -1, -1, -1, 0, -1, 0, -1, 0);\n            gpSoundManager->SwitchAmbientMusic(\n                giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n            );\n        }\n    }\n    gpSoundManager->SwitchAmbientMusic(\n        giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n    );\n    gpAdvManager->SetEnvironmentOrigin(\n        gpAdvManager->m_mapOriginX + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        gpAdvManager->m_mapOriginY + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        1\n    );\n}",
          "extra_edits": [
            {
              "insert_before": "} // namespace",
              "text": "    inline u8 ToLowerCp1251(u8 letter) {\n        u8 lowerFirst;\n        if (letter >= 'A' && letter <= 'Z')\n            lowerFirst = letter + ('a' - 'A');\n        else if (letter >= CP1251_CAPITAL_A && letter <= CP1251_CAPITAL_YA)\n            lowerFirst = letter + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n        else if (letter == CP1251_CAPITAL_YO)\n            lowerFirst = CP1251_SMALL_YO;\n        else\n            lowerFirst = letter;\n        return lowerFirst;\n    }\n\n"
            }
          ]
        },
        {
          "name": "helper_byte_remove_natural",
          "replace": "void game::DoNewTurn(void) {\n    char lowerName19[NEW_TURN_LOWER_NAME_CAPACITY];\n    i32 musicTrack2;\n\n    CheckForTimeEvent();\n    if (!gbThisNetHumanPlayer[giCurPlayer]) {\n        CheckEndGame(END_GAME_FORCE_NONE, false);\n        return;\n    }\n    giBottomViewOverrideEndTime = KBTickCount() + NEW_TURN_BOTTOM_VIEW_DURATION;\n    giBottomViewOverride = BOTTOM_VIEW_NEW_TURN;\n    gpAdvManager->UpdBottomView(true, true, true);\n    gpAdvManager->SetInitialMapOrigin();\n    gpAdvManager->CompleteDraw(0);\n    gpAdvManager->UpdateScreen(0, 0);\n    CheckEndGame(END_GAME_FORCE_NONE, false);\n\n    if (gpCurPlayer->m_daysLeft >= 0) {\n        if (gpCurPlayer->m_daysLeft == 1) {\n            sprintf(gText, cNewTurn[1], cPlayerNames[giCurPlayer]);\n        } else {\n            sprintf(gText, cNewTurn[0], cPlayerNames[giCurPlayer], gpCurPlayer->m_daysLeft);\n        }\n        NormalDialog(\n            gText,\n            1,\n            -1,\n            -1,\n            NEW_TURN_DIALOG_TYPE,\n            gpGame->GetPlayerColor(giCurPlayer),\n            -1,\n            0,\n            -1,\n            0\n        );\n    }\n\n    if (gpCurPlayer->m_heroCount > 0) {\n        gpAdvManager->SetHeroContext(gpCurPlayer->NextHero(0), 0);\n    } else if (gpCurPlayer->m_townCount > 0) {\n        gpAdvManager->SetTownContext(gpCurPlayer->m_townIds[0]);\n    }\n    gpAdvManager->CheckDimNextHeroBut();\n\n    if (m_day == 1 && (m_month != 1 || m_week != 1 || m_day != 1)) {\n        if (gbThisNetHumanPlayer[giCurPlayer])\n            gSoundBackendsReady = 1;\n        if (giWeekType != CALENDAR_PERIOD_NONE) {\n            musicTrack2 = -1;\n            if (m_week == 1) {\n                musicTrack2 = NEW_MONTH_MUSIC_TRACK;\n                if (giMonthType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_NORMAL_TEXT],\n                        gMonthNames[giMonthTypeExtra]\n                    );\n                } else if (giMonthType == CALENDAR_PERIOD_CREATURE) {\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giMonthTypeExtra]\n                    );\n                    lowerName19[0] = ToLowerCp1251(lowerName19[0]);\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_MONTH_CREATURE_TEXT],\n                        gArmyNamesPlural[giMonthTypeExtra],\n                        lowerName19\n                    );\n                } else {\n                    sprintf(gText, cNewTurn[NEW_MONTH_PLAGUE_TEXT]);\n                }\n            } else {\n                musicTrack2 = NEW_WEEK_MUSIC_TRACK;\n                if (giWeekType == CALENDAR_PERIOD_NORMAL) {\n                    sprintf(gText, cNewTurn[NEW_WEEK_NORMAL_TEXT], gWeekNames[giWeekTypeExtra]);\n                } else {\n                    strcpy(\n                        lowerName19,\n                        gArmyNamesPlural[giWeekTypeExtra]\n                    );\n                    lowerName19[0] = ToLowerCp1251(lowerName19[0]);\n                    sprintf(\n                        gText,\n                        cNewTurn[NEW_WEEK_CREATURE_TEXT],\n                        gArmyNamesPlural[giWeekTypeExtra],\n                        lowerName19\n                    );\n                }\n            }\n            gpSoundManager->PlayAmbientMusic(musicTrack2);\n            gpMouseManager->SetPointer(0);\n            NormalDialog(gText, 1, -1, -1, -1, 0, -1, 0, -1, 0);\n            gpSoundManager->SwitchAmbientMusic(\n                giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n            );\n        }\n    }\n    gpSoundManager->SwitchAmbientMusic(\n        giTerrainToMusicTrack[IDX(gpAdvManager->m_currentTerrain)]\n    );\n    gpAdvManager->SetEnvironmentOrigin(\n        gpAdvManager->m_mapOriginX + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        gpAdvManager->m_mapOriginY + ENVIRONMENT_ORIGIN_TILE_OFFSET,\n        1\n    );\n}",
          "extra_edits": [
            {
              "insert_before": "} // namespace",
              "text": "    inline u8 ToLowerCp1251(u8 letter) {\n        u8 lowerFirst;\n        if (letter >= 'A' && letter <= 'Z')\n            lowerFirst = letter + ('a' - 'A');\n        else if (letter >= CP1251_CAPITAL_A && letter <= CP1251_CAPITAL_YA)\n            lowerFirst = letter + (CP1251_SMALL_A - CP1251_CAPITAL_A);\n        else if (letter == CP1251_CAPITAL_YO)\n            lowerFirst = CP1251_SMALL_YO;\n        else\n            lowerFirst = letter;\n        return lowerFirst;\n    }\n\n"
            }
          ]
        }
      ]
    }
  ]
}
*/
