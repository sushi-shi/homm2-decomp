// Experiment: direct sibling-family setup as one coherent structural rewrite.
// Artifact: build/tu-state-noise/iconf2bc-family-setup-120-20260726/
// Clean: 83.972290%. Best of 120 states: 86.319900%.
// Blocks stayed 76/76 (66 exact, 10 size-only).
// Disposition: rejected because it did not beat the retained MAX.

#if 0
u8* src = srcIcon->m_data;
IconEntry* entry = reinterpret_cast<IconEntry*>(src) + frame;
i32 w = entry->w;
i32 x0 = x - entry->x - w + 1;
gFCEntry = entry;
src += entry->srcOffset;
gFCX0 = x0;
i32 X = (gFCXEnd = w + x0 - 1);
i32 Y = y + entry->y;
gFCY = Y;
i32 pitch;
#endif
