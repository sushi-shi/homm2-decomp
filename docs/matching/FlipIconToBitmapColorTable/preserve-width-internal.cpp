// Experiment: conservative internal spellings after preserving icon width.
// Artifact: build/match-variants/iconf2bc-preserve-width-internal-clean-20260726/
// Families: statement order, relational/commutative order, declarations,
// pointer expressions, and small legitimate inline expressions.
// Best: 86.408060%. Disposition: rejected.

#if 0
i32 X = (gFCXEnd = w + x0 - 1);

// Representative internal arms.
dp = gFCRow + clipX;
count = (X - clipX) + 1;

i32 currentY = gFCY, left;
if (clipY <= currentY && gFCClipB >= currentY) {
}

selectedDst = gFCRow + right;
src = src + (X - right);
#endif
