// VC6 SP5 Castle source family s63-castle-setup-locals; parent f8b6c956.
// Source record only. All alternatives below are relative to the stated BEFORE,
// not cumulative diffs. No cross-family/TU-state census is claimed.
// build/source-variant-batch/s63-castle-setup-locals/results.json; RVA 0x24320
// Complete 32 arms; elapsed 17.22975s; input restored.
// Disposition: retain original; every tested alternative changes text.
// EH private symbol hashes may differ; native comparison canonicalizes actual section owners.
// trial / choices / bytes / text hash / normalized reloc hash / blocks / exact,size,shift,flow / graph exact
// 0 / {'source_family': 'keep', 'candidate': 'baseline'} / 3856 / c3f48f8d1e052b80 / dcfacc22e7679c2b / 138:138 / 138,0,0,0 / True
// 1 / {'source_family': 'cannotAffordIcon', 'candidate': 'baseline'} / 3847 / 776799d85a1c11e1 / 5bd50457b4ee1923 / 138:138 / 137,1,0,0 / True
// 2 / {'source_family': 'noBuildFrame', 'candidate': 'baseline'} / 3850 / 39a7475ca1f248f0 / 04338eb31e05b2ef / 138:138 / 137,1,0,0 / True
// 3 / {'source_family': 'noBuildFrame_cannotAffordIcon', 'candidate': 'baseline'} / 3841 / c525385825f3a374 / 562252aaa3214ab4 / 138:138 / 137,1,0,0 / True
// 4 / {'source_family': 'builtFrame', 'candidate': 'baseline'} / 3850 / 229224cecfce9376 / 04338eb31e05b2ef / 138:138 / 137,1,0,0 / True
// 5 / {'source_family': 'builtFrame_cannotAffordIcon', 'candidate': 'baseline'} / 3841 / b93d27dd473bd642 / 562252aaa3214ab4 / 138:138 / 137,1,0,0 / True
// 6 / {'source_family': 'builtFrame_noBuildFrame', 'candidate': 'baseline'} / 3844 / de2a70ea1483b90b / 2c6d25c376657bb8 / 138:138 / 137,1,0,0 / True
// 7 / {'source_family': 'builtFrame_noBuildFrame_cannotAffordIcon', 'candidate': 'baseline'} / 3835 / f9f5ac53d7fe5654 / 95a05861213ccf4a / 138:138 / 137,1,0,0 / True
// 8 / {'source_family': 'tileY', 'candidate': 'baseline'} / 3849 / 166e284af500653f / aeed363cd94deab3 / 138:138 / 137,1,0,0 / True
// 9 / {'source_family': 'tileY_cannotAffordIcon', 'candidate': 'baseline'} / 3840 / f69505e70431291c / 88c97fea31d1ec95 / 138:138 / 136,2,0,0 / True
// 10 / {'source_family': 'tileY_noBuildFrame', 'candidate': 'baseline'} / 3843 / d727339901f19289 / b68e3f6705f0c80a / 138:138 / 136,2,0,0 / True
// 11 / {'source_family': 'tileY_noBuildFrame_cannotAffordIcon', 'candidate': 'baseline'} / 3834 / caa6f355d36d160f / efb4ff82f01c0449 / 138:138 / 136,2,0,0 / True
// 12 / {'source_family': 'tileY_builtFrame', 'candidate': 'baseline'} / 3843 / 5d434e2df976eff7 / b68e3f6705f0c80a / 138:138 / 136,2,0,0 / True
// 13 / {'source_family': 'tileY_builtFrame_cannotAffordIcon', 'candidate': 'baseline'} / 3834 / cbf06df64b43fc35 / efb4ff82f01c0449 / 138:138 / 136,2,0,0 / True
// 14 / {'source_family': 'tileY_builtFrame_noBuildFrame', 'candidate': 'baseline'} / 3837 / 8bb4ee7665dc26e1 / a1d79f83c88cf248 / 138:138 / 136,2,0,0 / True
// 15 / {'source_family': 'tileY_builtFrame_noBuildFrame_cannotAffordIcon', 'candidate': 'baseline'} / 3828 / ca73a57ba950258f / 31323f46f1d09952 / 138:138 / 136,2,0,0 / True
// 16 / {'source_family': 'tileX', 'candidate': 'baseline'} / 3849 / cf44cfc6c6a09b23 / aeed363cd94deab3 / 138:138 / 137,1,0,0 / True
// 17 / {'source_family': 'tileX_cannotAffordIcon', 'candidate': 'baseline'} / 3840 / 49b07ff361fc6c23 / 88c97fea31d1ec95 / 138:138 / 136,2,0,0 / True
// 18 / {'source_family': 'tileX_noBuildFrame', 'candidate': 'baseline'} / 3843 / 008c6942761dbfe2 / b68e3f6705f0c80a / 138:138 / 136,2,0,0 / True
// 19 / {'source_family': 'tileX_noBuildFrame_cannotAffordIcon', 'candidate': 'baseline'} / 3834 / ed1214ed23040bdf / efb4ff82f01c0449 / 138:138 / 136,2,0,0 / True
// 20 / {'source_family': 'tileX_builtFrame', 'candidate': 'baseline'} / 3843 / ee6a6dd925416ce6 / b68e3f6705f0c80a / 138:138 / 136,2,0,0 / True
// 21 / {'source_family': 'tileX_builtFrame_cannotAffordIcon', 'candidate': 'baseline'} / 3834 / da51cea0aec901fe / efb4ff82f01c0449 / 138:138 / 136,2,0,0 / True
// 22 / {'source_family': 'tileX_builtFrame_noBuildFrame', 'candidate': 'baseline'} / 3837 / ce07f9004afdb66f / a1d79f83c88cf248 / 138:138 / 136,2,0,0 / True
// 23 / {'source_family': 'tileX_builtFrame_noBuildFrame_cannotAffordIcon', 'candidate': 'baseline'} / 3828 / ad5fd235440de98b / 31323f46f1d09952 / 138:138 / 136,2,0,0 / True
// 24 / {'source_family': 'tileX_tileY', 'candidate': 'baseline'} / 3842 / fc42811963554db1 / 8d6a68955cf72e8f / 138:138 / 137,1,0,0 / True
// 25 / {'source_family': 'tileX_tileY_cannotAffordIcon', 'candidate': 'baseline'} / 3833 / ed8e0b73099aa02b / c28e82fff93ac0c1 / 138:138 / 136,2,0,0 / True
// 26 / {'source_family': 'tileX_tileY_noBuildFrame', 'candidate': 'baseline'} / 3836 / 8bbf831bfd7ba060 / bb6c42fb293d346a / 138:138 / 136,2,0,0 / True
// 27 / {'source_family': 'tileX_tileY_noBuildFrame_cannotAffordIcon', 'candidate': 'baseline'} / 3827 / 8753ecc7079ac1ef / 88650ace1e474f6f / 138:138 / 136,2,0,0 / True
// 28 / {'source_family': 'tileX_tileY_builtFrame', 'candidate': 'baseline'} / 3836 / a352ce0000b19911 / bb6c42fb293d346a / 138:138 / 136,2,0,0 / True
// 29 / {'source_family': 'tileX_tileY_builtFrame_cannotAffordIcon', 'candidate': 'baseline'} / 3827 / 0d72e40cf4596b6b / 88650ace1e474f6f / 138:138 / 136,2,0,0 / True
// 30 / {'source_family': 'tileX_tileY_builtFrame_noBuildFrame', 'candidate': 'baseline'} / 3830 / 7bd44b92f23e641b / 85432712593c0cf3 / 138:138 / 136,2,0,0 / True
// 31 / {'source_family': 'tileX_tileY_builtFrame_noBuildFrame_cannotAffordIcon', 'candidate': 'baseline'} / 3821 / fe037ad71bec36d6 / 5dc10e7f623087db / 138:138 / 136,2,0,0 / True

// AXIS source_family
// BEFORE (complete exact span):
// | void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |     u32l captainQuarters;
// |     i32 H2_UNUSED(tileX);
// |     i32 H2_UNUSED(tileY);
// |     i32 terrainIconFrame;
// |     i32 column;
// |     i32 rowPos;
// |     widget* backgroundWidget;
// |     i32 raceBase;
// |     i16 H2_UNUSED(builtFrame);
// |     i32 slotNum;
// |     char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |     i32 backFrame;
// |     i16 H2_UNUSED(noBuildFrame);
// |     char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |     i32 stateFrame;
// |     tag_message msg;
// |     i16 H2_UNUSED(cannotAffordIcon);
// |
// |     casWin = window;
// |     builtFrame = IDX(FRAME_BUILT);
// |     noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// |     cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |     for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |         castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// |         if (castleSlotsBase[slotNum] >= BUILDING_SLOT_DWELLING_SECOND
// |             && castleSlotsBase[slotNum] <= BUILDING_SLOT_DWELLING_SIXTH
// |             && ((m_town->m_buildings & (1L << IDX(castleSlotsBase[slotNum])))
// |                 || (m_town->m_buildings
// |                     & (1L << (IDX(castleSlotsBase[slotNum]) + CASTLE_UPGRADE_OFFSET)))
// |                 || (castleSlotsBase[slotNum] == BUILDING_SLOT_DWELLING_SIXTH
// |                     && m_town->m_type == FACTION_WARLOCK
// |                     && (m_town->m_buildings & IDX(TOWN_BUILDING_ALTERNATE_UPGRADED_DWELLING_6))))
// |             && (gTownEligibleBuildMask[IDX(m_town->m_type)]
// |                 & (1L << (IDX(castleSlotsBase[slotNum]) + CASTLE_UPGRADE_OFFSET)))) {
// |             if (castleSlotsBase[slotNum] == BUILDING_SLOT_DWELLING_SIXTH
// |                 && m_town->m_type == FACTION_WARLOCK
// |                 && ((m_town->m_buildings & IDX(TOWN_BUILDING_UPGRADED_DWELLING_6))
// |                     || (m_town->m_buildings & IDX(TOWN_BUILDING_ALTERNATE_UPGRADED_DWELLING_6)))) {
// |                 castleSlotsUse[slotNum] = BUILDING_SLOT_DWELLING_LAST;
// |             } else {
// |                 castleSlotsUse[slotNum] = castleSlotsBase[slotNum] + CASTLE_UPGRADE_OFFSET;
// |             }
// |         }
// |     }
// |
// |     m_affordableBuildings = m_buildableBuildings = 0;
// |     for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |         if (CanBuy(m_town, castleSlotsUse[slotNum]))
// |             m_affordableBuildings |= 1L << IDX(castleSlotsUse[slotNum]);
// |         if (CanBuild(m_town, castleSlotsUse[slotNum]))
// |             m_buildableBuildings |= 1L << IDX(castleSlotsUse[slotNum]);
// |     }
// |
// |     msg.type = MESSAGE_WIDGET;
// |     msg.payload.widget.command = CASTLE_WIDGET_FRAME;
// |     for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |         msg.payload.widget.id = CONTROL_BUILDING_ICON_FIRST + slotNum;
// |         msg.payload.widget.data.value = IDX(castleSlotsUse[slotNum]);
// |         casWin->BroadcastMessage(msg);
// |     }
// |
// |     msg.payload.widget.command = CASTLE_WIDGET_ICON_FILE;
// |     sprintf(
// |         icnName,
// |         "cstl%s.icn",
// |         cHeroTypeShortName[IDX(m_town->m_type)]
// |     );
// |     msg.payload.widget.data.text = icnName;
// |     for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |         msg.payload.widget.id = CONTROL_BUILDING_ICON_FIRST + slotNum;
// |         casWin->BroadcastMessage(msg);
// |     }
// |
// |     msg.payload.widget.command = CASTLE_WIDGET_TEXT;
// |     for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |         msg.payload.widget.id = CONTROL_BUILDING_NAME_FIRST + slotNum;
// |         if (castleSlotsUse[slotNum] == CASTLE_MAGE_GUILD) {
// |             sprintf(
// |                 gText,
// |                 "%d \xfd\xf2\xe0\xe6 \xc3\xe8\xeb\xfc\xe4\xe8\xe8 \xec\xe0\xe3\xee\xe2"
// |                     /* "%d этаж Гильдии магов" */,
// |                 m_town->m_buildState + 1 < TOWN_MAGE_GUILD_MAX_LEVEL ? m_town->m_buildState + 1
// |                                                                      : TOWN_MAGE_GUILD_MAX_LEVEL
// |             );
// |             msg.payload.widget.data.text = gText;
// |         } else {
// |             msg.payload.widget.data.text = GetBuildingName(
// |                 m_town->m_type,
// |                 castleSlotsUse[slotNum]
// |             );
// |         }
// |         casWin->BroadcastMessage(msg);
// |     }
// |
// |     for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |         stateFrame = FRAME_NONE;
// |         if ((m_town->m_buildings & (1L << IDX(castleSlotsUse[slotNum])))
// |             && (castleSlotsUse[slotNum] != CASTLE_MAGE_GUILD
// |                 || m_town->m_buildState == TOWN_MAGE_GUILD_MAX_LEVEL)) {
// |             stateFrame = FRAME_BUILT;
// |         } else {
// |             if (!(m_buildableBuildings & (1L << IDX(castleSlotsUse[slotNum]))))
// |                 stateFrame = FRAME_CANNOT_BUILD;
// |             else if (!(m_affordableBuildings & (1L << IDX(castleSlotsUse[slotNum]))))
// |                 stateFrame = FRAME_CANNOT_AFFORD;
// |         }
// |
// |         if (stateFrame != FRAME_NONE) {
// |             msg.payload.widget.command = CASTLE_WIDGET_ENABLE;
// |             msg.payload.widget.id = CONTROL_BUILDING_BUTTON_FIRST + slotNum;
// |             msg.payload.widget.data.value = IDX(WIDGET_FLAG_DRAW);
// |             casWin->BroadcastMessage(msg);
// |             msg.payload.widget.command = CASTLE_WIDGET_FRAME;
// |             msg.payload.widget.data.value = stateFrame;
// |             casWin->BroadcastMessage(msg);
// |         } else {
// |             msg.payload.widget.command = CASTLE_WIDGET_DISABLE;
// |             msg.payload.widget.data.value = IDX(WIDGET_FLAG_DRAW);
// |             msg.payload.widget.id = CONTROL_BUILDING_BUTTON_FIRST + slotNum;
// |             casWin->BroadcastMessage(msg);
// |         }
// |
// |         if (stateFrame == FRAME_BUILT) {
// |             msg.payload.widget.command = CASTLE_WIDGET_DISABLE;
// |             msg.payload.widget.data.value = IDX(WIDGET_FLAG_DRAW);
// |             msg.payload.widget.id = CONTROL_BUILDING_OVERLAY_FIRST + slotNum;
// |             casWin->BroadcastMessage(msg);
// |         } else {
// |             msg.payload.widget.command = CASTLE_WIDGET_ENABLE;
// |             msg.payload.widget.data.value = IDX(WIDGET_FLAG_DRAW);
// |             msg.payload.widget.id = CONTROL_BUILDING_OVERLAY_FIRST + slotNum;
// |             casWin->BroadcastMessage(msg);
// |             msg.payload.widget.command = CASTLE_WIDGET_FRAME;
// |             msg.payload.widget.data.value =
// |                 stateFrame == FRAME_NONE ? IDX(FRAME_AVAILABLE) : FRAME_UNAVAILABLE;
// |             casWin->BroadcastMessage(msg);
// |         }
// |     }
// |
// |     captainQuarters = m_town->m_buildings & IDX(TOWN_BUILDING_CAPTAIN_QUARTERS);
// |     msg.payload.widget.command =
// |         captainQuarters != 0 ? CASTLE_WIDGET_DISABLE : CASTLE_WIDGET_ENABLE;
// |     msg.payload.widget.id = CONTROL_CAPTAIN_OVERLAY;
// |     msg.payload.widget.data.value = IDX(WIDGET_FLAG_ENABLED | WIDGET_FLAG_DRAW);
// |     casWin->BroadcastMessage(msg);
// |     msg.payload.widget.command = CASTLE_WIDGET_FRAME;
// |     msg.payload.widget.id = CONTROL_CAPTAIN_ICON;
// |     msg.payload.widget.data.value = captainQuarters != 0;
// |     casWin->BroadcastMessage(msg);
// |     sprintf(
// |         gText,
// |         "CSTLCAP%c.ICN",
// |         cHeroTypeInitial[IDX(m_town->m_type)]
// |     );
// |     msg.payload.widget.command = CASTLE_WIDGET_ICON_FILE;
// |     msg.payload.widget.id = CONTROL_CAPTAIN_ICON;
// |     msg.payload.widget.data.text = gText;
// |     casWin->BroadcastMessage(msg);
// |     msg.payload.widget.command =
// |         captainQuarters != 0 ? CASTLE_WIDGET_ENABLE : CASTLE_WIDGET_DISABLE;
// |     msg.payload.widget.id = CONTROL_CAPTAIN_FLAG;
// |     msg.payload.widget.data.value = IDX(WIDGET_FLAG_DRAW);
// |     casWin->BroadcastMessage(msg);
// |     if (captainQuarters != 0) {
// |         msg.payload.widget.command = CASTLE_WIDGET_FRAME;
// |         msg.payload.widget.id = CONTROL_CAPTAIN_FLAG;
// |         msg.payload.widget.data.value = gpCurPlayer->m_color;
// |         casWin->BroadcastMessage(msg);
// |     }
// |
// |     stateFrame = FRAME_NONE;
// |     if (captainQuarters != 0) {
// |         msg.payload.widget.command = CASTLE_WIDGET_TEXT;
// |         msg.payload.widget.data.text = gText;
// |         sprintf(
// |             gText,
// |             ""
// |         );
// |         for (slotNum = 0; slotNum < HERO_PRIMARY_STAT_COUNT; ++slotNum) {
// |             sprintf(
// |                 captainStatLine,
// |                 "%s\n",
// |                 gStatNames[slotNum]
// |             );
// |             strcat(gText, captainStatLine);
// |         }
// |         msg.payload.widget.id = CONTROL_CAPTAIN_STATS;
// |         casWin->BroadcastMessage(msg);
// |         sprintf(
// |             gText,
// |             ""
// |         );
// |         for (slotNum = 0; slotNum < HERO_PRIMARY_STAT_COUNT; ++slotNum) {
// |             sprintf(
// |                 captainStatLine,
// |                 "%d\n",
// |                 captainStats[IDX(m_town->m_type)][slotNum]
// |             );
// |             strcat(gText, captainStatLine);
// |         }
// |         msg.payload.widget.id = CONTROL_CAPTAIN_VALUES;
// |         casWin->BroadcastMessage(msg);
// |         msg.payload.widget.command = m_town->m_formation != TOWN_FORMATION_SPREAD
// |                                               ? CASTLE_WIDGET_DISABLE
// |                                               : CASTLE_WIDGET_ENABLE;
// |         msg.payload.widget.id = CONTROL_CAPTAIN_FORMATION_SPREAD_INACTIVE;
// |         msg.payload.widget.data.value = IDX(WIDGET_FLAG_DRAW);
// |         casWin->BroadcastMessage(msg);
// |         msg.payload.widget.command = m_town->m_formation == TOWN_FORMATION_SPREAD
// |                                               ? CASTLE_WIDGET_DISABLE
// |                                               : CASTLE_WIDGET_ENABLE;
// |         msg.payload.widget.id = CONTROL_CAPTAIN_FORMATION_GROUPED_INACTIVE;
// |         msg.payload.widget.data.value = IDX(WIDGET_FLAG_DRAW);
// |         casWin->BroadcastMessage(msg);
// |     } else {
// |         if (!CanBuild(m_town, CASTLE_CAPTAIN))
// |             stateFrame = FRAME_CANNOT_BUILD;
// |         else if (!CanBuy(m_town, CASTLE_CAPTAIN))
// |             stateFrame = FRAME_CANNOT_AFFORD;
// |         if (CanBuild(m_town, CASTLE_CAPTAIN))
// |             m_buildableBuildings |= IDX(TOWN_BUILDING_CAPTAIN_QUARTERS);
// |         if (CanBuy(m_town, CASTLE_CAPTAIN))
// |             m_affordableBuildings |= IDX(TOWN_BUILDING_CAPTAIN_QUARTERS);
// |     }
// |
// |     msg.payload.widget.command =
// |         stateFrame == FRAME_NONE ? CASTLE_WIDGET_DISABLE : CASTLE_WIDGET_ENABLE;
// |     msg.payload.widget.id = CONTROL_CAPTAIN_BUTTON;
// |     msg.payload.widget.data.value = IDX(WIDGET_FLAG_DRAW);
// |     casWin->BroadcastMessage(msg);
// |     if (stateFrame != FRAME_NONE) {
// |         msg.payload.widget.command = CASTLE_WIDGET_FRAME;
// |         msg.payload.widget.data.value = stateFrame;
// |         casWin->BroadcastMessage(msg);
// |     }
// |
// |     if (gpCurPlayer->m_resources[IDX(RES_GOLD)] < gHeroGoldCost)
// |         stateFrame = FRAME_CANNOT_AFFORD;
// |     else if (gpCurPlayer->m_heroCount == PLAYER_HERO_CAPACITY || m_town->m_occupyingHeroId != -1)
// |         stateFrame = FRAME_CANNOT_BUILD;
// |     else if (m_recruitResult != 0)
// |         stateFrame = FRAME_BUILT;
// |     else
// |         stateFrame = FRAME_NONE;
// |
// |     for (slotNum = 0; slotNum < PLAYER_AVAILABLE_HERO_COUNT; ++slotNum) {
// |         msg.payload.widget.data.value = IDX(WIDGET_FLAG_DRAW);
// |         msg.payload.widget.id = CONTROL_RECRUIT_FIRST + slotNum;
// |         if (stateFrame != FRAME_NONE) {
// |             msg.payload.widget.command = CASTLE_WIDGET_ENABLE;
// |             casWin->BroadcastMessage(msg);
// |             msg.payload.widget.command = CASTLE_WIDGET_FRAME;
// |             msg.payload.widget.data.value = stateFrame;
// |             casWin->BroadcastMessage(msg);
// |         } else {
// |             msg.payload.widget.command = CASTLE_WIDGET_DISABLE;
// |             casWin->BroadcastMessage(msg);
// |         }
// |         msg.payload.widget.command = CASTLE_WIDGET_ICON_FILE;
// |         sprintf(
// |             icnName,
// |             "port%04d.icn",
// |             IDX(gpGame->m_heroRecs[gpCurPlayer->AvailableHeroId(slotNum)].m_portrait)
// |         );
// |         msg.payload.widget.data.text = icnName;
// |         msg.payload.widget.id = CONTROL_HERO_FIRST + slotNum;
// |         casWin->BroadcastMessage(msg);
// |     }
// |
// |     tileX = BACKGROUND_LEFT;
// |     tileY = BACKGROUND_TOP;
// |     terrainIconFrame = (IDX(giGroundToTerrain
// |                                   [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// |                                        ->m_terrainImageIndex])
// |                           - 1)
// |                          * (TERRAIN_ICON_COLUMNS * TERRAIN_ICON_FRAMES);
// |     raceBase = IDX(m_town->m_type) * RACE_ICON_FRAMES;
// |     if (updateOnly == 0) {
// |         backFrame = 0;
// |         for (rowPos = BACKGROUND_TERRAIN_FIRST_ROW; rowPos <= BACKGROUND_TERRAIN_LAST_ROW; ++rowPos) {
// |             for (column = BACKGROUND_FIRST_COLUMN; column <= BACKGROUND_LAST_COLUMN; ++column) {
// |                 backgroundWidget = new iconWidget(
// |                     static_cast<i16>(
// |                         (column - BACKGROUND_FIRST_COLUMN) * BACKGROUND_TILE_SIZE + BACKGROUND_LEFT
// |                     ),
// |                     static_cast<i16>((rowPos - BACKGROUND_TOWN_FIRST_ROW) * BACKGROUND_TILE_SIZE),
// |                     BACKGROUND_TILE_SIZE,
// |                     BACKGROUND_TILE_SIZE,
// |                     "objntwba.icn",
// |                     static_cast<i16>(terrainIconFrame + backFrame),
// |                     ICON_DRAW_NORMAL,
// |                     TOWN_WIDGET_ID_NONE,
// |                     WIDGET_KIND_ICON_DIRECT,
// |                     BACKGROUND_FILL_COLOR
// |                 );
// |                 if (backgroundWidget == NULL)
// |                     MemError();
// |                 casWin->AddWidget(backgroundWidget, TOWN_WIDGET_INSERT_DEFAULT);
// |                 ++backFrame;
// |             }
// |         }
// |         backFrame = 0;
// |         for (rowPos = BACKGROUND_TOWN_FIRST_ROW; rowPos <= BACKGROUND_TOWN_LAST_ROW; ++rowPos) {
// |             for (column = BACKGROUND_FIRST_COLUMN; column <= BACKGROUND_LAST_COLUMN; ++column) {
// |                 if (rowPos == BACKGROUND_TOWN_FIRST_ROW && column != BACKGROUND_TOP_CENTER_COLUMN) {
// |                     continue;
// |                 }
// |                 backgroundWidget = new iconWidget(
// |                     static_cast<i16>(
// |                         (column - BACKGROUND_FIRST_COLUMN) * BACKGROUND_TILE_SIZE + BACKGROUND_LEFT
// |                     ),
// |                     static_cast<i16>((rowPos - BACKGROUND_TOWN_FIRST_ROW) * BACKGROUND_TILE_SIZE),
// |                     BACKGROUND_TILE_SIZE,
// |                     BACKGROUND_TILE_SIZE,
// |                     "objntown.icn",
// |                     static_cast<i16>(raceBase + backFrame),
// |                     ICON_DRAW_NORMAL,
// |                     TOWN_WIDGET_ID_NONE,
// |                     WIDGET_KIND_ICON_DIRECT,
// |                     BACKGROUND_FILL_COLOR
// |                 );
// |                 if (backgroundWidget == NULL)
// |                     MemError();
// |                 casWin->AddWidget(backgroundWidget, TOWN_WIDGET_INSERT_DEFAULT);
// |                 ++backFrame;
// |             }
// |         }
// |         if (xIsExpansionMap == 0 && m_town->m_type == FACTION_NECROMANCER) {
// |             backgroundWidget = new iconWidget(
// |                 EXPANSION_OVERLAY_X,
// |                 EXPANSION_OVERLAY_Y,
// |                 EXPANSION_OVERLAY_WIDTH,
// |                 EXPANSION_OVERLAY_HEIGHT,
// |                 "caslxtra.icn",
// |                 0,
// |                 ICON_DRAW_NORMAL,
// |                 TOWN_WIDGET_ID_NONE,
// |                 WIDGET_KIND_ICON_DIRECT,
// |                 BACKGROUND_FILL_COLOR
// |             );
// |             if (backgroundWidget == NULL)
// |                 MemError();
// |             casWin->AddWidget(backgroundWidget, TOWN_WIDGET_INSERT_DEFAULT);
// |         }
// |     }
// | }
// ALTERNATIVE cannotAffordIcon (unified source delta from BEFORE):
// | --- before
// | +++ cannotAffordIcon
// | @@ -15,12 +15,10 @@
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// | -    i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// |      builtFrame = IDX(FRAME_BUILT);
// |      noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// | -    cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// |          if (castleSlotsBase[slotNum] >= BUILDING_SLOT_DWELLING_SECOND
// ALTERNATIVE noBuildFrame (unified source delta from BEFORE):
// | --- before
// | +++ noBuildFrame
// | @@ -11,7 +11,6 @@
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | -    i16 H2_UNUSED(noBuildFrame);
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// | @@ -19,7 +18,6 @@
// |
// |      casWin = window;
// |      builtFrame = IDX(FRAME_BUILT);
// | -    noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// |      cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// ALTERNATIVE noBuildFrame_cannotAffordIcon (unified source delta from BEFORE):
// | --- before
// | +++ noBuildFrame_cannotAffordIcon
// | @@ -11,16 +11,12 @@
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | -    i16 H2_UNUSED(noBuildFrame);
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// | -    i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// |      builtFrame = IDX(FRAME_BUILT);
// | -    noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// | -    cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// |          if (castleSlotsBase[slotNum] >= BUILDING_SLOT_DWELLING_SECOND
// ALTERNATIVE builtFrame (unified source delta from BEFORE):
// | --- before
// | +++ builtFrame
// | @@ -7,7 +7,6 @@
// |      i32 rowPos;
// |      widget* backgroundWidget;
// |      i32 raceBase;
// | -    i16 H2_UNUSED(builtFrame);
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | @@ -18,7 +17,6 @@
// |      i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// | -    builtFrame = IDX(FRAME_BUILT);
// |      noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// |      cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// ALTERNATIVE builtFrame_cannotAffordIcon (unified source delta from BEFORE):
// | --- before
// | +++ builtFrame_cannotAffordIcon
// | @@ -7,7 +7,6 @@
// |      i32 rowPos;
// |      widget* backgroundWidget;
// |      i32 raceBase;
// | -    i16 H2_UNUSED(builtFrame);
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | @@ -15,12 +14,9 @@
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// | -    i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// | -    builtFrame = IDX(FRAME_BUILT);
// |      noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// | -    cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// |          if (castleSlotsBase[slotNum] >= BUILDING_SLOT_DWELLING_SECOND
// ALTERNATIVE builtFrame_noBuildFrame (unified source delta from BEFORE):
// | --- before
// | +++ builtFrame_noBuildFrame
// | @@ -7,19 +7,15 @@
// |      i32 rowPos;
// |      widget* backgroundWidget;
// |      i32 raceBase;
// | -    i16 H2_UNUSED(builtFrame);
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | -    i16 H2_UNUSED(noBuildFrame);
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// |      i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// | -    builtFrame = IDX(FRAME_BUILT);
// | -    noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// |      cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// ALTERNATIVE builtFrame_noBuildFrame_cannotAffordIcon (unified source delta from BEFORE):
// | --- before
// | +++ builtFrame_noBuildFrame_cannotAffordIcon
// | @@ -7,20 +7,14 @@
// |      i32 rowPos;
// |      widget* backgroundWidget;
// |      i32 raceBase;
// | -    i16 H2_UNUSED(builtFrame);
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | -    i16 H2_UNUSED(noBuildFrame);
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// | -    i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// | -    builtFrame = IDX(FRAME_BUILT);
// | -    noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// | -    cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// |          if (castleSlotsBase[slotNum] >= BUILDING_SLOT_DWELLING_SECOND
// ALTERNATIVE tileY (unified source delta from BEFORE):
// | --- before
// | +++ tileY
// | @@ -1,7 +1,6 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// |      i32 H2_UNUSED(tileX);
// | -    i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// |      i32 rowPos;
// | @@ -269,7 +268,6 @@
// |      }
// |
// |      tileX = BACKGROUND_LEFT;
// | -    tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// |                                         ->m_terrainImageIndex])
// ALTERNATIVE tileY_cannotAffordIcon (unified source delta from BEFORE):
// | --- before
// | +++ tileY_cannotAffordIcon
// | @@ -1,7 +1,6 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// |      i32 H2_UNUSED(tileX);
// | -    i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// |      i32 rowPos;
// | @@ -15,12 +14,10 @@
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// | -    i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// |      builtFrame = IDX(FRAME_BUILT);
// |      noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// | -    cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// |          if (castleSlotsBase[slotNum] >= BUILDING_SLOT_DWELLING_SECOND
// | @@ -269,7 +266,6 @@
// |      }
// |
// |      tileX = BACKGROUND_LEFT;
// | -    tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// |                                         ->m_terrainImageIndex])
// ALTERNATIVE tileY_noBuildFrame (unified source delta from BEFORE):
// | --- before
// | +++ tileY_noBuildFrame
// | @@ -1,7 +1,6 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// |      i32 H2_UNUSED(tileX);
// | -    i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// |      i32 rowPos;
// | @@ -11,7 +10,6 @@
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | -    i16 H2_UNUSED(noBuildFrame);
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// | @@ -19,7 +17,6 @@
// |
// |      casWin = window;
// |      builtFrame = IDX(FRAME_BUILT);
// | -    noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// |      cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// | @@ -269,7 +266,6 @@
// |      }
// |
// |      tileX = BACKGROUND_LEFT;
// | -    tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// |                                         ->m_terrainImageIndex])
// ALTERNATIVE tileY_noBuildFrame_cannotAffordIcon (unified source delta from BEFORE):
// | --- before
// | +++ tileY_noBuildFrame_cannotAffordIcon
// | @@ -1,7 +1,6 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// |      i32 H2_UNUSED(tileX);
// | -    i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// |      i32 rowPos;
// | @@ -11,16 +10,12 @@
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | -    i16 H2_UNUSED(noBuildFrame);
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// | -    i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// |      builtFrame = IDX(FRAME_BUILT);
// | -    noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// | -    cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// |          if (castleSlotsBase[slotNum] >= BUILDING_SLOT_DWELLING_SECOND
// | @@ -269,7 +264,6 @@
// |      }
// |
// |      tileX = BACKGROUND_LEFT;
// | -    tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// |                                         ->m_terrainImageIndex])
// ALTERNATIVE tileY_builtFrame (unified source delta from BEFORE):
// | --- before
// | +++ tileY_builtFrame
// | @@ -1,13 +1,11 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// |      i32 H2_UNUSED(tileX);
// | -    i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// |      i32 rowPos;
// |      widget* backgroundWidget;
// |      i32 raceBase;
// | -    i16 H2_UNUSED(builtFrame);
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | @@ -18,7 +16,6 @@
// |      i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// | -    builtFrame = IDX(FRAME_BUILT);
// |      noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// |      cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// | @@ -269,7 +266,6 @@
// |      }
// |
// |      tileX = BACKGROUND_LEFT;
// | -    tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// |                                         ->m_terrainImageIndex])
// ALTERNATIVE tileY_builtFrame_cannotAffordIcon (unified source delta from BEFORE):
// | --- before
// | +++ tileY_builtFrame_cannotAffordIcon
// | @@ -1,13 +1,11 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// |      i32 H2_UNUSED(tileX);
// | -    i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// |      i32 rowPos;
// |      widget* backgroundWidget;
// |      i32 raceBase;
// | -    i16 H2_UNUSED(builtFrame);
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | @@ -15,12 +13,9 @@
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// | -    i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// | -    builtFrame = IDX(FRAME_BUILT);
// |      noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// | -    cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// |          if (castleSlotsBase[slotNum] >= BUILDING_SLOT_DWELLING_SECOND
// | @@ -269,7 +264,6 @@
// |      }
// |
// |      tileX = BACKGROUND_LEFT;
// | -    tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// |                                         ->m_terrainImageIndex])
// ALTERNATIVE tileY_builtFrame_noBuildFrame (unified source delta from BEFORE):
// | --- before
// | +++ tileY_builtFrame_noBuildFrame
// | @@ -1,25 +1,20 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// |      i32 H2_UNUSED(tileX);
// | -    i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// |      i32 rowPos;
// |      widget* backgroundWidget;
// |      i32 raceBase;
// | -    i16 H2_UNUSED(builtFrame);
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | -    i16 H2_UNUSED(noBuildFrame);
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// |      i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// | -    builtFrame = IDX(FRAME_BUILT);
// | -    noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// |      cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// | @@ -269,7 +264,6 @@
// |      }
// |
// |      tileX = BACKGROUND_LEFT;
// | -    tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// |                                         ->m_terrainImageIndex])
// ALTERNATIVE tileY_builtFrame_noBuildFrame_cannotAffordIcon (unified source delta from BEFORE):
// | --- before
// | +++ tileY_builtFrame_noBuildFrame_cannotAffordIcon
// | @@ -1,26 +1,19 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// |      i32 H2_UNUSED(tileX);
// | -    i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// |      i32 rowPos;
// |      widget* backgroundWidget;
// |      i32 raceBase;
// | -    i16 H2_UNUSED(builtFrame);
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | -    i16 H2_UNUSED(noBuildFrame);
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// | -    i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// | -    builtFrame = IDX(FRAME_BUILT);
// | -    noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// | -    cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// |          if (castleSlotsBase[slotNum] >= BUILDING_SLOT_DWELLING_SECOND
// | @@ -269,7 +262,6 @@
// |      }
// |
// |      tileX = BACKGROUND_LEFT;
// | -    tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// |                                         ->m_terrainImageIndex])
// ALTERNATIVE tileX (unified source delta from BEFORE):
// | --- before
// | +++ tileX
// | @@ -1,6 +1,5 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// | -    i32 H2_UNUSED(tileX);
// |      i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// | @@ -268,7 +267,6 @@
// |          casWin->BroadcastMessage(msg);
// |      }
// |
// | -    tileX = BACKGROUND_LEFT;
// |      tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// ALTERNATIVE tileX_cannotAffordIcon (unified source delta from BEFORE):
// | --- before
// | +++ tileX_cannotAffordIcon
// | @@ -1,6 +1,5 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// | -    i32 H2_UNUSED(tileX);
// |      i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// | @@ -15,12 +14,10 @@
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// | -    i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// |      builtFrame = IDX(FRAME_BUILT);
// |      noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// | -    cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// |          if (castleSlotsBase[slotNum] >= BUILDING_SLOT_DWELLING_SECOND
// | @@ -268,7 +265,6 @@
// |          casWin->BroadcastMessage(msg);
// |      }
// |
// | -    tileX = BACKGROUND_LEFT;
// |      tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// ALTERNATIVE tileX_noBuildFrame (unified source delta from BEFORE):
// | --- before
// | +++ tileX_noBuildFrame
// | @@ -1,6 +1,5 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// | -    i32 H2_UNUSED(tileX);
// |      i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// | @@ -11,7 +10,6 @@
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | -    i16 H2_UNUSED(noBuildFrame);
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// | @@ -19,7 +17,6 @@
// |
// |      casWin = window;
// |      builtFrame = IDX(FRAME_BUILT);
// | -    noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// |      cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// | @@ -268,7 +265,6 @@
// |          casWin->BroadcastMessage(msg);
// |      }
// |
// | -    tileX = BACKGROUND_LEFT;
// |      tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// ALTERNATIVE tileX_noBuildFrame_cannotAffordIcon (unified source delta from BEFORE):
// | --- before
// | +++ tileX_noBuildFrame_cannotAffordIcon
// | @@ -1,6 +1,5 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// | -    i32 H2_UNUSED(tileX);
// |      i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// | @@ -11,16 +10,12 @@
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | -    i16 H2_UNUSED(noBuildFrame);
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// | -    i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// |      builtFrame = IDX(FRAME_BUILT);
// | -    noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// | -    cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// |          if (castleSlotsBase[slotNum] >= BUILDING_SLOT_DWELLING_SECOND
// | @@ -268,7 +263,6 @@
// |          casWin->BroadcastMessage(msg);
// |      }
// |
// | -    tileX = BACKGROUND_LEFT;
// |      tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// ALTERNATIVE tileX_builtFrame (unified source delta from BEFORE):
// | --- before
// | +++ tileX_builtFrame
// | @@ -1,13 +1,11 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// | -    i32 H2_UNUSED(tileX);
// |      i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// |      i32 rowPos;
// |      widget* backgroundWidget;
// |      i32 raceBase;
// | -    i16 H2_UNUSED(builtFrame);
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | @@ -18,7 +16,6 @@
// |      i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// | -    builtFrame = IDX(FRAME_BUILT);
// |      noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// |      cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// | @@ -268,7 +265,6 @@
// |          casWin->BroadcastMessage(msg);
// |      }
// |
// | -    tileX = BACKGROUND_LEFT;
// |      tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// ALTERNATIVE tileX_builtFrame_cannotAffordIcon (unified source delta from BEFORE):
// | --- before
// | +++ tileX_builtFrame_cannotAffordIcon
// | @@ -1,13 +1,11 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// | -    i32 H2_UNUSED(tileX);
// |      i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// |      i32 rowPos;
// |      widget* backgroundWidget;
// |      i32 raceBase;
// | -    i16 H2_UNUSED(builtFrame);
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | @@ -15,12 +13,9 @@
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// | -    i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// | -    builtFrame = IDX(FRAME_BUILT);
// |      noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// | -    cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// |          if (castleSlotsBase[slotNum] >= BUILDING_SLOT_DWELLING_SECOND
// | @@ -268,7 +263,6 @@
// |          casWin->BroadcastMessage(msg);
// |      }
// |
// | -    tileX = BACKGROUND_LEFT;
// |      tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// ALTERNATIVE tileX_builtFrame_noBuildFrame (unified source delta from BEFORE):
// | --- before
// | +++ tileX_builtFrame_noBuildFrame
// | @@ -1,25 +1,20 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// | -    i32 H2_UNUSED(tileX);
// |      i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// |      i32 rowPos;
// |      widget* backgroundWidget;
// |      i32 raceBase;
// | -    i16 H2_UNUSED(builtFrame);
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | -    i16 H2_UNUSED(noBuildFrame);
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// |      i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// | -    builtFrame = IDX(FRAME_BUILT);
// | -    noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// |      cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// | @@ -268,7 +263,6 @@
// |          casWin->BroadcastMessage(msg);
// |      }
// |
// | -    tileX = BACKGROUND_LEFT;
// |      tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// ALTERNATIVE tileX_builtFrame_noBuildFrame_cannotAffordIcon (unified source delta from BEFORE):
// | --- before
// | +++ tileX_builtFrame_noBuildFrame_cannotAffordIcon
// | @@ -1,26 +1,19 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// | -    i32 H2_UNUSED(tileX);
// |      i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// |      i32 rowPos;
// |      widget* backgroundWidget;
// |      i32 raceBase;
// | -    i16 H2_UNUSED(builtFrame);
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | -    i16 H2_UNUSED(noBuildFrame);
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// | -    i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// | -    builtFrame = IDX(FRAME_BUILT);
// | -    noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// | -    cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// |          if (castleSlotsBase[slotNum] >= BUILDING_SLOT_DWELLING_SECOND
// | @@ -268,7 +261,6 @@
// |          casWin->BroadcastMessage(msg);
// |      }
// |
// | -    tileX = BACKGROUND_LEFT;
// |      tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// ALTERNATIVE tileX_tileY (unified source delta from BEFORE):
// | --- before
// | +++ tileX_tileY
// | @@ -1,7 +1,5 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// | -    i32 H2_UNUSED(tileX);
// | -    i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// |      i32 rowPos;
// | @@ -268,8 +266,6 @@
// |          casWin->BroadcastMessage(msg);
// |      }
// |
// | -    tileX = BACKGROUND_LEFT;
// | -    tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// |                                         ->m_terrainImageIndex])
// ALTERNATIVE tileX_tileY_cannotAffordIcon (unified source delta from BEFORE):
// | --- before
// | +++ tileX_tileY_cannotAffordIcon
// | @@ -1,7 +1,5 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// | -    i32 H2_UNUSED(tileX);
// | -    i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// |      i32 rowPos;
// | @@ -15,12 +13,10 @@
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// | -    i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// |      builtFrame = IDX(FRAME_BUILT);
// |      noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// | -    cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// |          if (castleSlotsBase[slotNum] >= BUILDING_SLOT_DWELLING_SECOND
// | @@ -268,8 +264,6 @@
// |          casWin->BroadcastMessage(msg);
// |      }
// |
// | -    tileX = BACKGROUND_LEFT;
// | -    tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// |                                         ->m_terrainImageIndex])
// ALTERNATIVE tileX_tileY_noBuildFrame (unified source delta from BEFORE):
// | --- before
// | +++ tileX_tileY_noBuildFrame
// | @@ -1,7 +1,5 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// | -    i32 H2_UNUSED(tileX);
// | -    i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// |      i32 rowPos;
// | @@ -11,7 +9,6 @@
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | -    i16 H2_UNUSED(noBuildFrame);
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// | @@ -19,7 +16,6 @@
// |
// |      casWin = window;
// |      builtFrame = IDX(FRAME_BUILT);
// | -    noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// |      cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// | @@ -268,8 +264,6 @@
// |          casWin->BroadcastMessage(msg);
// |      }
// |
// | -    tileX = BACKGROUND_LEFT;
// | -    tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// |                                         ->m_terrainImageIndex])
// ALTERNATIVE tileX_tileY_noBuildFrame_cannotAffordIcon (unified source delta from BEFORE):
// | --- before
// | +++ tileX_tileY_noBuildFrame_cannotAffordIcon
// | @@ -1,7 +1,5 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// | -    i32 H2_UNUSED(tileX);
// | -    i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// |      i32 rowPos;
// | @@ -11,16 +9,12 @@
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | -    i16 H2_UNUSED(noBuildFrame);
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// | -    i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// |      builtFrame = IDX(FRAME_BUILT);
// | -    noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// | -    cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// |          if (castleSlotsBase[slotNum] >= BUILDING_SLOT_DWELLING_SECOND
// | @@ -268,8 +262,6 @@
// |          casWin->BroadcastMessage(msg);
// |      }
// |
// | -    tileX = BACKGROUND_LEFT;
// | -    tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// |                                         ->m_terrainImageIndex])
// ALTERNATIVE tileX_tileY_builtFrame (unified source delta from BEFORE):
// | --- before
// | +++ tileX_tileY_builtFrame
// | @@ -1,13 +1,10 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// | -    i32 H2_UNUSED(tileX);
// | -    i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// |      i32 rowPos;
// |      widget* backgroundWidget;
// |      i32 raceBase;
// | -    i16 H2_UNUSED(builtFrame);
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | @@ -18,7 +15,6 @@
// |      i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// | -    builtFrame = IDX(FRAME_BUILT);
// |      noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// |      cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// | @@ -268,8 +264,6 @@
// |          casWin->BroadcastMessage(msg);
// |      }
// |
// | -    tileX = BACKGROUND_LEFT;
// | -    tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// |                                         ->m_terrainImageIndex])
// ALTERNATIVE tileX_tileY_builtFrame_cannotAffordIcon (unified source delta from BEFORE):
// | --- before
// | +++ tileX_tileY_builtFrame_cannotAffordIcon
// | @@ -1,13 +1,10 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// | -    i32 H2_UNUSED(tileX);
// | -    i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// |      i32 rowPos;
// |      widget* backgroundWidget;
// |      i32 raceBase;
// | -    i16 H2_UNUSED(builtFrame);
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | @@ -15,12 +12,9 @@
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// | -    i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// | -    builtFrame = IDX(FRAME_BUILT);
// |      noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// | -    cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// |          if (castleSlotsBase[slotNum] >= BUILDING_SLOT_DWELLING_SECOND
// | @@ -268,8 +262,6 @@
// |          casWin->BroadcastMessage(msg);
// |      }
// |
// | -    tileX = BACKGROUND_LEFT;
// | -    tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// |                                         ->m_terrainImageIndex])
// ALTERNATIVE tileX_tileY_builtFrame_noBuildFrame (unified source delta from BEFORE):
// | --- before
// | +++ tileX_tileY_builtFrame_noBuildFrame
// | @@ -1,25 +1,19 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// | -    i32 H2_UNUSED(tileX);
// | -    i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// |      i32 rowPos;
// |      widget* backgroundWidget;
// |      i32 raceBase;
// | -    i16 H2_UNUSED(builtFrame);
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | -    i16 H2_UNUSED(noBuildFrame);
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// |      i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// | -    builtFrame = IDX(FRAME_BUILT);
// | -    noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// |      cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// | @@ -268,8 +262,6 @@
// |          casWin->BroadcastMessage(msg);
// |      }
// |
// | -    tileX = BACKGROUND_LEFT;
// | -    tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// |                                         ->m_terrainImageIndex])
// ALTERNATIVE tileX_tileY_builtFrame_noBuildFrame_cannotAffordIcon (unified source delta from BEFORE):
// | --- before
// | +++ tileX_tileY_builtFrame_noBuildFrame_cannotAffordIcon
// | @@ -1,26 +1,18 @@
// |  void townManager::SetupCastle(heroWindow* window, i32 updateOnly) {
// |      u32l captainQuarters;
// | -    i32 H2_UNUSED(tileX);
// | -    i32 H2_UNUSED(tileY);
// |      i32 terrainIconFrame;
// |      i32 column;
// |      i32 rowPos;
// |      widget* backgroundWidget;
// |      i32 raceBase;
// | -    i16 H2_UNUSED(builtFrame);
// |      i32 slotNum;
// |      char icnName[TOWN_OBJECT_FILENAME_SIZE];
// |      i32 backFrame;
// | -    i16 H2_UNUSED(noBuildFrame);
// |      char captainStatLine[CAPTAIN_STAT_LINE_CAPACITY];
// |      i32 stateFrame;
// |      tag_message msg;
// | -    i16 H2_UNUSED(cannotAffordIcon);
// |
// |      casWin = window;
// | -    builtFrame = IDX(FRAME_BUILT);
// | -    noBuildFrame = IDX(FRAME_CANNOT_BUILD);
// | -    cannotAffordIcon = IDX(FRAME_CANNOT_AFFORD);
// |      for (slotNum = 0; slotNum < CASTLE_SLOT_COUNT; ++slotNum) {
// |          castleSlotsUse[slotNum] = castleSlotsBase[slotNum];
// |          if (castleSlotsBase[slotNum] >= BUILDING_SLOT_DWELLING_SECOND
// | @@ -268,8 +260,6 @@
// |          casWin->BroadcastMessage(msg);
// |      }
// |
// | -    tileX = BACKGROUND_LEFT;
// | -    tileY = BACKGROUND_TOP;
// |      terrainIconFrame = (IDX(giGroundToTerrain
// |                                    [gpGame->m_worldMap.GetCell(m_town->m_x, m_town->m_y)
// |                                         ->m_terrainImageIndex])
