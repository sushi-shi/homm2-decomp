# Analysis dossiers, 2026-07-29 — executable plans for the next wave

Four parallel read-only analyses over the open queue. Items already executed
this session: FlyTo frame solution (banked 99.75), CheckForCreatureUpgrades
solution (banked 99.55), NextPlayer frame facts (EXACT), MirrorImage decl swap
(disproved — buckets 11 vs 13, needs a rename instead). Everything below is
verified analysis awaiting execution.

## Ready to execute, high confidence

- **EffectSpellCure** (SPELLAI 0x887c1, ~90%, expected EXACT): three axes —
  (A) `targetIndex != index` operand order at SPELLAI.cpp:1076;
  (B) the mirror-image arm is `else { influence loop }`, not `continue;`
  (retail's forward jmp to the for-body tail proves it);
  (C) `case ARMY_SPELL_INFLUENCE_ANTI_MAGIC: break;` moves BETWEEN the positive
  and negative case groups (case bodies emit in source order; the jump table
  follows). Run as a 2x2x2 matrix.
- **RightClick** (COMMAND 0x2ce19, ~80%, expected exact): first divergence is
  B23, not B6. The blocked-hex `return 0;` is a then-branch WITH an else
  wrapping everything after; the side guard is written positively
  (`side != NONE`) with `return 0;` in the else (its je targets a private
  xor/jmp stub placed AFTER the switch); the default case ends with an
  explicit `break;`. Variant R1 in the agent transcript has the full text.
- **oldmain** (KB 0x96e98): zero instruction diffs; three missing stubs
  (14 bytes) at rows 9/121/219+221 — forward-goto stubs per
  docs/patterns/od-goto-stubs-and-inline-continuations.md §1-§2. Read the
  trailing-jmp counts and add the gotos.
- **LoadIcons** (CMBTMGR 0x91dda): single row, single stub — retail passes
  through one more stub: likely `default: break;` (§3) or if-goto without else
  at the heroColor site. Lowest-risk closure in the band.
- **UpdateRadar** (ADVMGR 0x5e0da): ONE real site — the MAP_DIMENSION_MEDIUM
  arm evaluates the screen-pixels pointer FIRST where SMALL/LARGE (byte-equal
  both sides) use offset-first. Respell only the MEDIUM arm.

## Medium

- **DoAI** (PHILAI 0x39631): six witnessed sites, all goto-vs-structured facts;
  thunk-table accounting closes at −30 vs the measured −29. Axes A-F with
  exact replacement text in the agent transcript (retail: `goto aiCleanup`,
  `aiRetarget:` labels replacing both do/while loops, if/else-if pathLength
  chain, `goto aiTownCheck` pair that SKIPS DeactivateCurrHero).
- **PollRemote** (REMOTE 0xa41ec): axes P1-P4 (nextIncoming label at TOP of
  the while body wrapping the whole body in
  `if (receiveResult && sender != giThisNetPos)`; heartbeat exits merge into
  one `&&`-guarded assignment + `goto pollDone`; in-loop returns become gotos;
  the alloc block is the third for-loop's body). Afterwards a declaration-order
  pass: retail walks receiveResult first, queueIndex fourth.
- **NormalDialog** (KB 0xa0d9f): rows 100-108 are MSVC's branchless
  two-constant conditional (`(arr[i] == -1) ? 0x40 : 0x4c` via inc/cmp/adc/and)
  where we emit a real branch — respell as `?:` with constant arms; plus one
  extra/misplaced 4-byte local ahead of the -0x94 group.
- **GoodAdjacent** (PHILAI 0x3849d): THREE wrong enumerators (two
  `MAP_OBJECT_MONSTER` should be `MAP_OBJECT_STONE_LITHS` (0x24), one
  `MAP_OBJECT_ARTIFACT` should be `MAP_OBJECT_WHIRLPOOL` (0x27)) — real value
  bugs; then rename `directionIndex` to a bucket-0 name (`dirIdx`) — the only
  frame defect; then the two `continue;`s want forward exits.
- **GetTurnAIVars** (PHILAI 0x3a329): land the non-frame fixes first
  (giCurTurn 0x7 not 0x8; a 0x5-vs-0x6; swapped field pair +0x1d/+0x19 and
  +0x5/+0x4; fields +0x31-vs-0x35, +0x18-vs-0x1b; enum 0x2a vs 0xab), then
  rename `unusedFightValue7` -> `fightValueSpare` (b15) moved to decl index 3;
  cycle B needs `threatHeroPtr6` HOISTED to function scope + a b15 name.
- **CatAttack** (CMBTMGR 0x9311f): misclassified as at-state — the two `=`
  stores beside it use retail's order, so the `++m_wallStates[wallIndex17 +
  IDX(COMBAT_WALL_SLOT_SECTION_FIRST)]` at CMBTMGR.cpp:1604 is a spelling
  fact; try a hoisted local index (§7's proven flipper) or a `u8* slot`.
- **DrawCell** (ADVMGR 0x5bb7c): the stone-arm else must be EMPTY with the
  rest of the drawing back at function scope (our dead jmp targets the wrong
  join); the visibility-map read wants per-term scaling
  (`add ecx,ecx` + `lea [ecx+eax*2]` + scale-1 deref). The banked 99.0674
  cited in its doc is NOT in the ledger (epoch accident) — re-run islands
  after the fixes.
- **Earthquake** (SPELLS 0x29ae0): frames already equal (0x160) — NOT a frame
  gap. Retail merges our {structure, frame#2} into `i` and {impact, structure}
  into `j`, recomputes `frame - impactDelay[impact]` inline (no cloudFrame
  local), splits `impactCount += 2` into two `++`, and has dead slots at
  -0x68/-0xa4. Restructure first, then the layout solve.

## Class facts confirmed this session

- The hover-compare (`m_lastHoverId ==`) idiom canonicalizes against retail in
  VIEW/SPELLS/Castle at every probe state — the accessor-on-one-side lever
  (§6 exception) is the only untried move; FightValueOfStack (single row,
  `jle` wants LEFT-side accessor) is the clean testbed.
- `!(a <= b)` compiles identically to `a > b` (ARMY.cpp:815's shape comes from
  its TU state, not the spelling).
- tu_state_noise runs are repo-wide exclusive (trial files race any concurrent
  hashing pass, including match_variants and homm2 build).
