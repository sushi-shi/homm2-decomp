// advManager::MoveOrigin (SOURCE/CURSOR, RVA 0xff07) — 2026-07-29 matrix
//
// Matrix: build/moveorigin-axes.json -> build/source-variant-batch/moveorigin
// (6 arms x {clean + 8 forest states}, complete).
//
// cell_binding splice_234 (WINNER, integrated): retail expands the fullMap
// GetCell inline only at the FIRST cursor-cell binding; the other three sites
// splice cells + width * y + x textually (no continuation stub). 97.51 ->
// 99.36 at the clean state; official replay banked 99.37.
//
// or_join axis: explicit_ne / boolean_terms / demorgan all leave our extra
// `jmp $+0` after the directionY test (the || join two-jump). OPEN: that stub
// plus the m_cursorMapX/m_mapOriginX add order are the remaining rows —
// at-state class or an untried join spelling (try the CheckBuyStuff-style
// restructure if a legitimate one exists).
