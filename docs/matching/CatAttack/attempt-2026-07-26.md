# CatAttack residual attempt — 2026-07-26

Target:

- Unit: `SOURCE/CMBTMGR`
- Symbol: `?CatAttack@combatManager@@QAEXH@Z`
- RVA: `0x9311f`
- Size: 4,110 bytes
- Effective-source hash: `4f07ce5b1159`
- TU source SHA-256: `27507b3d8f05f43ef307638bb1dc1f6c8f146adf0e5c53f503b4831b1435e172`

## Structural audit

The clean candidate and retail target have the same 1,027 relocation-masked
instructions, the same 173 basic blocks, the same 244-byte frame and saved
register set, and 150 relocation sites on each side. The residual raw byte
differences localize to two operand-order sites:

- `(startDirection + frame18) % COMBAT_CATAPULT_DIRECTION_COUNT`
- `projectileX11 += xStep0`

The reviewed axis matrix tested both orders of the first expression and three
equivalent spellings of the second. All six clean-state forms compiled to the
same target state, so no source spelling was retained.

## Complete unchanged-source state census

Command:

```sh
python3 scripts/tu_state_noise.py \
  --source src/SOURCE/CMBTMGR.cpp \
  --rva 0x9311f \
  --trials 120 \
  --record-max \
  --retain-best \
  --output build/tu-state-noise/CatAttack-clean-120-20260726
```

All 120 trials completed. The baseline scored `99.995440%`; trial 118 reached
`99.997720%`. No trial was exact, and the generated MAX update was correctly
reported as `not_higher` because `99.9977` was already retained.

The local retained evidence is under
`build/tu-state-noise/CatAttack-clean-120-20260726/`.

## Expression axes × TU states

The combined matrix requested the full Cartesian product of six reviewed
expression forms and 121 states (clean plus 120 forest probes), for 726 total
variants. Its 900-second wall limit stopped it after 702 variants. Its best
reported variant was trial 540, state trial 90, at `99.980610%`; no exact
closure appeared, and the expression choices remained codegen-equivalent
within every observed state.

This combined matrix is **diagnostic-only** because it was truncated. It does
not count as complete state coverage and must not be cited to exclude the
uncompiled 24 combinations.

The local diagnostic artifacts are under
`build/source-variants/CatAttack-final-states-matrix/`.

## Disposition

No reconstructed source change was justified or retained. The durable result
is the unchanged-source `99.9977` MAX plus the structural evidence above.
Future work should start from a materially different structural hypothesis or
complete only the missing Cartesian combinations; repeating small equivalent
spellings in the same states is not useful.
