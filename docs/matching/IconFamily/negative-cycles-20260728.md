# Icon-family measured negatives, 2026-07-28 late cycles

Complete bounded matrices, each crossed with clean plus 40 top-of-TU forest
states; sources retained unchanged after every run.

- **Iconm2b left/x publication chain** (`s_x = s_left = ...` and the reversed
  chain vs the current two statements): the chain never beats the recorded
  85.0663 island; retail's back-to-back same-register stores arise from the
  two-statement form at the right state.
  `build/iconm2b-left-chain-{axes,manifest}-20260728.json`,
  `build/source-variant-batch/iconm2b-left-chain/`.
- **Iconmf2b right-edge arithmetic** (`right = width + x0 + 1; x0++` lea+inc
  hybrids with early/late s_y vs the parenthesized post-increment form): the
  current shape retains 81.2515 (trial 37 pushes the structural frontier to
  B1); both hybrids score lower.
  `build/iconmf2b-right-edge-{axes,manifest}-20260728.json`,
  `build/source-variant-batch/iconmf2b-right-edge/`.

Both axes may become observable again under a future structural parent; do
not re-run them in the current orbits.
