# Icon-family measured negatives, 2026-07-29 steered cycles

Complete bounded matrices against clean plus 40 top-of-TU forest states;
sources retained unchanged after every run.

- **Iconf2b entryY read site** (after-src / after-width-sub / dropped-local
  pointer read): the current after-src spelling keeps both the 88.60 trial-39
  island and best topology; the pointer read lifts only the clean score
  (87.18). `build/iconf2b-entryy-position-{axes,manifest}-20260729.json`.
- **icon2bs scale residency** (top pixelScale local copy vs direct param):
  byte-identical in every state — MSVC copy-propagates the local away. The
  retail frame keeps scale in ebx from entry (0x18 frame, step in ebp,
  tmpPixels spilled); no plain source spelling expresses that allocation.
  `build/icon2bs-scale-local-{axes,manifest}-20260729.json`. icon2bs stays
  parked at 85.83 (saturated through 1024 additional states).

Wide fast censuses with the parallel engine: Iconf2b 88.60 -> 89.10 across
256 + 1024 states (seeds 777777, 13131313); icon2bs unchanged at 1024.
