// VC6 SP5 source family s70-setup-default-name; parent292b9b73.
// Source record only: alternatives are relative to BEFORE, not cumulative.
// No cross-family Cartesian product or TU-state census is claimed.
// build/source-variant-batch/s70-setup-default-name/results.json; RVA 0x92817
// Complete 2 arms; elapsed 1.606721s; source restored.
// Disposition: retain original; every measured nonoriginal changes function text.
// Full98 native objects/allocated sections/ordered relocations preserved after restoration.
// Nine private EH/FS site-model gaps remain explicit, not waived by native equality.
// trial / choices / bytes / text hash / normalized reloc hash / blocks / exact,size,shift,flow,missing / graph exact
// 0 / {'family': 'keep', 'candidate': 'baseline'} / 557 / 354da3a620dda960 / 926b9546adc7d24a / 29:29 / 28,1,0,0,0 / True
// 1 / {'family': 'one_char', 'candidate': 'baseline'} / 557 / 32c7085babe23aa7 / 926b9546adc7d24a / 29:29 / 28,1,0,0,0 / True

// AXIS family
// BEFORE (complete exact span):
// | char name[DEFAULT_PLAYER_NAME_CAPACITY];
// ALTERNATIVE one_char (unified delta from BEFORE):
// | --- before
// | +++ one_char
// | @@ -1 +1 @@
// | -char name[DEFAULT_PLAYER_NAME_CAPACITY];
// | +char name[1];
