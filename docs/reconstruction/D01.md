# D01: withdrawn — rejected reconstructed control

The address-correction finding is invalid and all its source/guard changes
have been reverted. The purported retail input was actually a reconstructed
link copied from the local runnable-game directory.

- Rejected control SHA-256: `3575e85a2d4840d8a6bf8ecc2ec2273f6f4e5800d2c2a6c0ffb9ad883eb0893e`.
- Supported retail SHA-256: `bc7e9c9320aa3e5c1ffca6d2bfa530ecedb5a3bca1b91c959501c15ad72c329a`.

The wrong control produced plausible instruction matches but different global
placements and some call targets. Those observations cannot establish retail
owners. The resulting 140 source-address changes, shared compiler-guard change,
and eight annotation withdrawals were not justified. None was committed or
published. The misleading placement table has been removed.

The genuine executable was verified independently in preserved extracted Buka
media and the existing exact-link SHA assertion. `homm2 redelink` (including
`homm2 init`) and `homm2 build` now reject a wrong hash before generating inputs
or compiling. Five focused tests cover acceptance, rejection, missing input,
and rejection before either workflow has side effects.

Native source-before/source-after comparisons do not use the executable and
remain valid within their stated scope. C01/O01/O02/O03/S14 retail claims must
use the genuine control, and their reports record the revalidation separately.
The genuine image confirms the original pathfinding scratch field addresses;
O01 likewise keeps the original Miles ready/handles/count/volume addresses.
This is an ongoing audit, not a full-game correctness or completion claim.
