# C27: pass the level-up sample literal directly

Applied in `hero::CheckLevel`: `LoadPlaySample("nwherolv.82m")` replaces
the unnecessary `const_cast<char*>`. The existing API already accepts
`H2_CONST char*` and forwards that name to the const-correct sample resource
lookup. No declaration, ABI, string contents, source line count or runtime
call changes.

Both reviewed source arms compiled in 1.81 seconds in the complete two-arm
matrix (`build/c27-{axes,manifest}.json`, `build/c27-batch/results.json`).
Both have 1699 bytes, 56 relocations and 67/67 matching blocks/442 instructions.
No TU-state probes were needed for this byte-identical clean-state cleanup.

The matrix's strict flag rejects **both** arms for relocation identity; the
direct-literal arm also reports four sibling raw-relocation changes. Those
diagnostics are not ignored or represented as successful exact flags. Removing
the cast changes compiler-private string counters. Stronger verification after
integration resolves them by actual targets/string bytes:

- Native before/after: all 44 emitted functions and every allocated section
  are identical under the established private-name normalization, including
  complete ordered relocations/addends (`build/c27-native.log`).
- Independent genuine-retail CheckLevel check: all 1699 bytes, complete DIR32
  sites and every ordered target pass (`build/c27-retail.log`).
- At `+0x468`, `b9 …` loads the same 13-byte literal at `0x004f7cc0`; at
  `+0x46d`, `e8 …` calls the same `LoadPlaySample` at `0x0046d534`; at
  `+0x472`, `89 45 d8` saves the result to the same stack slot. No conversion
  instruction is introduced or removed.

The source arms and outcome are preserved in
`docs/matching/hero-CheckLevel/sample-literal-const.cpp`.
`homm2 build`, focused 56/56 relocation review and all 877 selftests pass.
Other CheckLevel findings remain individually open; this does not close its
skill-choice joins, unused locals or input/text contracts.
