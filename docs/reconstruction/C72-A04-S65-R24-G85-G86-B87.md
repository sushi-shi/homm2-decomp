# Overview: redundant conversions, real heap rows and scroll boundaries

All 1578 parent source lines (including private enums, macros and globals),
seven functions and the complete 42-line owner header were read. Each function
has all ten checklist verdicts in the ledger. No manual argument-slot varargs
or recovery of an owner by subtracting a member address occurs.

## C72 and S65: retained changes

Remove 117 written casts: 30 literal-name const_casts and 87 scalar casts whose
conversions are already supplied by the destination, parameter, default argument
promotion or usual arithmetic conversions. Keep the nine allocator-to-pointer
conversions, six strong-domain-to-image-frame conversions and the conditional
captain/portrait filename const_cast. Reduce the local mine-count array from
eight elements to the seven actually initialized and accessed.

The first broad cast arms were VC6-neutral but included seven removals that
were not valid across the source dialects. The conditional filename expression
is const char* in C++98, unlike the legacy conversion of an ordinary literal;
the six enum-to-frame conversions are necessary under C++20 strong enums.
Those seven original casts were restored. The final source passes the
[dialect checker](check-overview-syntax.py): no errors in this source in either
mode, with exactly the same 16 external diagnostic identities as parent
ee5ee1cf. A successful native-byte experiment does not override type correctness.

All 26 bounded source matrices completed: 116 arms in 86.632211 seconds, with
input restored and every arm compiled. The first 24 families use ee5ee1cf;
the final curve/knob numeric families use the integrated, boundary-corrected
source. No Cartesian product across families or compiler-state census is claimed.
All [source families](../matching/Overview/) preserve exact before spans,
alternative deltas, hashes, topology and dispositions, including the three
partially retained broad cast arms. The combined final 98-object comparison
passes all allocated sections and ordered relocations (1826 emitted function
instances), not just the modified functions.

## A04, S65, R24, G85/G86: measured residuals

The dynamic widget owners are actual flat heap pointer arrays. Each allocation
is 11200 bytes, while only four rows of 70 pointers (1120 bytes) are initialized
and used. This over-allocation is in retail, not an invented stack aggregate.
The address macros operate within that flat owner; the header row typedefs
are unused declarations, not instantiated alternate owners. Keep allocation
size and address stages. Macro flattening changes SetupDynamicStuff from7750
to7549 bytes; cleanup flattening to7727; initialization flattening changes
Overview from1202 to1197. These three tested forms are rejected, not evidence
that a new row-pointer type has been tried or disproven.

Removing the unread SetupDynamicStuff scalar changes7750 to7744 bytes. Splitting
dwelling/skill/artifact roles changes7750 to7771; separating the displayed count
changes it to7756. Splitting Overview's record/resource/lighthouse counters or
ProcessIconSelect's non-strict skill/artifact values changes bytes despite
unchanged1202/1187 sizes. These remain explicit residuals. The two handler tab
gotos share real dispatch tails; duplicating either or both tails changes1202
to1229/1229/1253. Preserve the original joins. No generated probes are retained.

## B87: finite bounds and remaining runtime contracts

Town rows create at most24 icon and18 text widgets; hero rows58 and17, all
strictly below70. Real fixed owners include five army slots, four stats,
eight skills, fourteen artifacts, twelve dwelling slots and two overview types.
Dwelling building bits19..30 stay below32. Army consumers use the normal
type/count fields; they do not resolve the separate U06 alternate-view debt.

Positive signed16 troop counts fit the six-byte decimal allocation including
NUL. Unguarded signed16 garrison values need seven bytes over their full domain;
negative values below-9999 are not covered by the six-byte allocation. Full
signed8 stats need five bytes, versus the four allocated, although nonnegative
stats fit. Valid secondary-skill levels0..9 fit two bytes; malformed signed8
results do not. The three HERO getter bodies and complete retail bytes/sites/
targets were checked. The fixed checker enumerates these domains rather than
assuming all small integers are nonnegative. Name allocations require terminated
input and successful allocation. The seven global resource formats fit the
768-byte buffer for every signed32 value. Widget text ownership and resources
remain lifecycle contracts.

There are144 mine records but char counters: sufficiently many same-type mines
can exceed signed-char127. The test resourceType<=GOLD does not exclude negative
malformed types before indexing. Valid resource types0..6, lighthouse100 and
alchemy-lab101 are still input contracts; signedness is not silently changed.

With more than four valid roster items, drag-knob divisors are positive, mouse
y is clamped and top is bounded to0..count-4. Integer track scrolling additionally
needs a bounded roster so32100/(count-3) is nonzero. In contrast, NUMPAD1 and
NUMPAD3 assign count-4 without a lower clamp: for count0..3 the resulting top
is negative. SetupDynamicStuff rejects only an index at or beyond count, not
a negative index, before roster access. Delivery of those key events is the
relevant condition; these arithmetic and guard facts are pinned to retail bytes.

ProcessIconSelect derives row0..3 from widget IDs200..999 but accepts index==count
because its check is `>`, not `>=`, before GetHero/GetTown. Normally created
widgets avoid empty rows; stale/invalid widget reachability is not established
merely by this guard. Likewise, cached last-type/top are only reset in this TU,
not updated: ordinary type0/1 short-circuits the initial NONE comparison, so an
ineffective cache is not evidence of a negative cache lookup. Actual subwidget
ranges, selected owner domains and window/input lifecycle remain required.

## Verification and precise retail limits

The final full build reports1727/1727 and291987/291987 data bytes. Every one of
the seven functions has matching complete bytes. Sizes/ordered relocations:
SetupDynamicStuff7750/303, SetupNewOverviewType836/53, SetupResources119/7,
Overview1202/84, DoKnob686/45, OverviewHandler1202/105, ProcessIconSelect1187/58.
All ordinary targets pass. The four existing EH model gaps remain explicit:
handler+6 and except-list+c,+14 plus respectively+1e10,+33a,+4aa,+499 in
SetupDynamicStuff, SetupNewOverviewType, Overview and ProcessIconSelect.
These functions are not declared fully retail-relocation closed.

[Fixed evidence checks](check-overview-evidence.py) independently prove the five
numerical operands, their exact full-width bytes, zero addends and DIR32 sites:
Setup's double303, float18 and double0.4, and DoKnob's two float1 operands.
The same checker pins allocation, row-address instructions and both problematic
guards. Final block views are234/234,26/26,5/5,42/42,27/27,61/61 and59/55.
The last four blocks are helpers outside ProcessIconSelect's1187-byte boundary;
embedded jump data also explains size-only diagnostic rows, not missing code.

Logs: `build/overview-*`, `build/build-overview-final.log`,
`build/overview-final-all-native.log`, `build/overview-syntax-final.log` and
the complete26 matrices. No core tool or retail manifest changed.
