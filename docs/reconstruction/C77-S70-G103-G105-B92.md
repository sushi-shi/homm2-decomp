# C77 / S70 / G103–G105 / B92 — setup dialogs

All1,184 source lines,23 functions, four private domains, the single boolean
data owner and the complete26-line declaration header were read. The earlier
SetupComPort B37 review is reconciled here; the other22 definitions now have
all ten pattern verdicts. No code or header change survives the experiments.

## Complete measured alternatives

All nine products completed:24 arms in18.080395 seconds, with source restored.
Each source arm, text/relocation hash and block census is recorded under
`docs/matching/SETUP/`. No generated compiler-state input is retained and no
cross-family/state census is claimed.

| Finding | Product and result |
| --- | --- |
| C77 SetupGame | All eight subsets of three signed16-bit switch conversions:1433 bytes becomes1432/1431/1430. Every removal changes text. The actual truncations remain. |
| C77 PickLoadGame | Two arms; removing signed16-bit conversion changes651→650. Retain. |
| G103 modem cases | Two arms; grouped case labels with direct-connect choice assignment changes551→543. Retain shared goto. |
| G104 remote tail | Two arms; move common remote setup after choice switch under the initialized network/modem domain test:1433→1451. Retain six shared-tail gotos. |
| G105 cleanup | Two arms; replace all done gotos with explicit flag cleanup and early return:1433→1514. Retain shared cleanup. |
| S70 library initializer | Two arms; removing overwritten hLib=NULL changes641→631. Retain. |
| S70 empty player name | Two arms;24-byte local→one byte keeps557 size but changes text. Retain reservation. |
| S70 file mask | Two arms;12-byte local→six bytes keeps651 size but changes text. Retain reservation. |
| S70 empty event dispatch | Two arms; deleting the no-effect nested switch changes231→192. Retain the retail branch structure. |

The four explicit casts in this TU are real word truncations, not casts at
already-narrow destinations. SDK-owned OSVERSIONINFO/HINSTANCE types are real
API types. The version record is zeroed at its actual SDK size and passed to
GetVersionEx; unused output fields do not make it a fabricated stack aggregate.
Successive window pointers and the two player-slot loops keep related roles,
not unrelated variable reuse. There is no manual argument-slot access,
enclosing-owner subtraction, artificial address arithmetic or new union.

## Every function

All help handlers retain the real tagged-message view and delegate to
BaseSetupHandler. Their right-button/select tests do not first require a widget
message type; that retail behavior is not changed. Each help lookup remains
bounded because a switch assigns only a listed constant index or -1 and the
lookup requires index>=0. The13 actual help catalogs contain56 terminated
strings, with maximum lengths59/205/362/etc. independently checked; the largest
is362 bytes including NUL. This is default-catalog evidence, not a guarantee
for changed resource/configuration text or downstream window lifetimes.

| Function (RVA;bytes) | Findings and contracts |
| --- | --- |
| SetupCampaignGame (924e0;36) | Plays actual intro4 and selection35, ignores both playback return values and returns1. Requires media/executive lifecycle; no local storage or pointer arithmetic. |
| SetupBaud (92504;311) | Real window creation/dialog/deletion; four baud choices update the selected two-entry configuration slot. gbDirectConnect must be0/1, not merely nonzero. Cancel returns0; an unknown choice returns1 with the old baud unchanged. |
| SetupComPort (9263b;476) | B37 reconciled: real40-byte local, initialized input ATZ, GetDataEntry capacity40 returns at most39 chars, then copy to config100 fits. COM choices and baud cancellation preserve their order; config slot needs direct-connect0/1. Later interpretation of modem initialization as a format string remains B37, not an overrun invented here. |
| SetupHotSeatGame (92817;557) | S70 keeps24-byte empty initial-name reservation. Five choices establish2..6 humans; fixed six-name clear is bounded. Name entry capacity20 returns at most19 chars into actual21-byte slots. Initial text is empty, not arbitrary caller text. Variable-count entry loop requires human count<=6; unrecognized choice leaves prior count. |
| SetupNetworkGame (92a44;288) | Actual widget payload disables host control when no-CD flag is set. Choice1/2 selects network host/guest, cancel returns0, unknown choice leaves mode unchanged. This is UI disabling, not independent input authorization/validation; window/resource lifecycle remains. |
| SetupNetworkGame2 (92b64;641) | Real OSVERSIONINFO is fully zeroed, size field set and API return checked before NT-specific disable. LoadLibraryA probes DPLAYX.DLL; successful reference is not freed in this function, deliberately retained. S70 overwritten handle initializer remains. Four protocol choices plus host/guest dialog; failed API/library calls do not validate every possible choice. |
| SetupModemGame (92de5;816) | Real config0/1 connection slots choose configured/unconfigured window. Host phone entry has capacity35 into36-byte numbuf, empty initial text and at most34 returned chars. Direct connect skips phone entry; modem/COM cancellation and configuration flag transitions retained. Resource and initialized config domains required. |
| SetupMultiPlayerGame (93115;551) | G103 shared modem/direct-connect join retained. Hot-seat/network/modem dispatch uses actual enum modes. Boolean continuation and config request are real related loop state; configuration can repeat until no request remains. Cancellation returns without inventing retries or cleanup effects. |
| SetupGame (9333c;1433) | C77 three real word truncations and G104/G105 shared remote/flag-cleanup paths retained. Initializes global setup mode, supports explicit restart/load mappings, then normal or campaign/multiplayer dialogs. Common cleanup clears gbInSetupDialog on normal/cancel/menu paths. Media, remote and campaign-choice validity/side effects remain contracts; successful-looking return does not independently prove connection success. Window pointer reuse is the same dialog-owner role. |
| PickLoadGame (938d5;651) | C77 word conversion and S70 twelve-byte mask retained. Campaign masks and player counts1..6 need6 bytes including NUL; arbitrary signed human count can need16. The downstream requester still copies that six-byte pattern into its five-byte extension field (B88). Wait-for-remote bypass and file requester/load/delete order preserved. Filename termination, paths/resources and serialized load validation remain contracts. |
| SetupCampaignGameHandler (93b60;19) | Pure dispatcher to BaseSetupHandler with unchanged event reference/result. No cast, local buffer, goto or fake aggregate; inherits event/window lifecycle. |
| SetupComPortHandler (93b73;254) | Choice1..4/cancel maps to0..4 in one of two five-entry modem/direct-connect COM help catalogs. Negative sentinel skips lookup; raw event-view behavior and base dispatch retained. |
| SetupBaudHandler (93c71;254) | Choice1..4/cancel maps to0..4 in one of two five-entry baud help catalogs. Conditional owner selection is real and bounded, not an overlaid array. |
| SetupHotSeatGameHandler (93d6f;238) | Choice1..5/cancel maps to0..5 in actual six-entry help catalog; default -1 skips lookup. Embedded switch table is data, not a fake stack table. |
| SetupModemGameHandler (93e5d;239) | Choice1..3/cancel maps to0..3 in one of two four-entry modem/direct-connect help catalogs. Flag controls table owner, not an unchecked numeric index. |
| SetupMultiPlayerGameHandler (93f4c;207) | Choice1..4/cancel maps to0..4 in five-entry help catalog. No local storage debt beyond actual scalar index and event reference. |
| SetupNetworkGameHandler (9401b;161) | Choice1/2/cancel maps to0..2 in three-entry host/guest help catalog; bounded sentinel dispatch retained. |
| SetupNetworkGame2Handler (940bc;192) | Choice1..3/cancel maps to0..3 in four-entry protocol help catalog. The fourth selectable protocol deliberately has no help case; not silently added. |
| SetupGameHandler (9417c;231) | Choice1..3/cancel maps to0..3 in four-entry main help catalog. S70 no-effect nested non-right-button widget dispatch retained after measured deletion changes code. It still delegates to the base handler. |
| ExpNewCampaignHandler (94263;161) | Choice1/2/cancel maps to0..2 in actual three-entry expansion campaign catalog. Same real event/reference domain and base dispatch, not a reason to deduplicate source. |
| ExpLoadCampaignHandler (94304;161) | Choice1/2/cancel maps to0..2 in the same campaign catalog. Distinct retail function kept; no copied stack aggregate or owner cast. |
| ExpStdGameHandler (943a5;161) | Choice1/2/cancel maps to0..2 in actual standard/expansion map catalog. The New Game and load callers use this same ABI and retain their separate word-choice conversions. |
| BaseSetupHandler (94446;181) | Polls sound; widget DESELECT accepts IDs1..1000 or cancel. A pending menu command overrides the type/handled test and forces cancel result. Mutates actual widget ID/command to close-dialog values and forwards; otherwise updates shingle animation. Does not validate every widget ID against each caller's smaller choice set; no fake local state. |

The shared entry implementation was reread completely: GetDataEntry first
copies the supplied initial string (bounded here by ATZ/empty), and its result
handler zeroes the destination capacity and copies capacity-1. Consequently
the40-byte modem local is not a41-byte overflow. Initial-text and entry
lifecycle issues for unrelated callers are not closed by this proof.

## Verification

Restored SETUP and all98 native objects preserve1826 emitted functions and
every allocated section/ordered relocation. Full build passes1727/1727 function
gates and291987/291987 data bytes. All23 complete retail spans have exact
bytes;14 also pass every site and ordered destination without exceptions.

The other nine retain their existing private EH/FS model gaps at+6,+c,+14 and
the final FS restore below. Ordinary destinations pass. These are not full
retail-closure claims.

| Function | Handler VA | Final FS operand offset |
| --- | --- | --- |
| SetupBaud | 004e8b0b | 12f |
| SetupComPort | 004e8b20 | 1d4 |
| SetupHotSeatGame | 004e8b35 | 211 |
| SetupNetworkGame | 004e8b4a | 118 |
| SetupNetworkGame2 | 004e8b62 | 279 |
| SetupModemGame | 004e8b98 | 328 |
| SetupMultiPlayerGame | 004e8bad | 21f |
| SetupGame | 004e8bd8 | 54c |
| PickLoadGame | 004e8bf8 | 283 |

Final SetupGame skeleton has69/69 exact blocks. Some permutation states
partition its trailing embedded switch data differently (67/69); similarly
hot-seat's trailing data can be size-only in a matrix. Native byte and ordered
target checks cover that data, not a guessed instruction interpretation.
BaseSetupHandler navigation includes four trailing helper blocks beyond its
181-byte claim; all14 in-range blocks agree.

The supporting GetDataEntry1124-byte span is exact with its unchanged private
EH/FS gaps; DataEntryWindowHandler506 bytes, sites and ordered targets all pass.
`check-setup-evidence.py` pins actual entry-length instructions, fixed help
catalogs and conditional formatter/destination bounds. No code changes or
modern API replacements are smuggled into this review.
