# Cross-version spelling ledger

The 2.1 branch's larger purpose is cross-referencing against the 2.0
reconstruction to recover the one true source tree. Whenever this branch's
byte-matching forces a spelling that DIFFERS from the PoL 2.0 spelling, the
divergence is an **unresolved claim**, not a conclusion: matching Buka's VC6
bytes proves the spelling is *sufficient for 2.1*, never that it is what the
devs wrote. Each entry below must eventually resolve to one of:

- **invariant** - the 2.1 spelling also reproduces 2.0's bytes under MSVC
  4.2 (test in the 2.0 tree). The 2.0 reconstruction then adopts it; PoL's
  spelling was just another member of 4.2's equivalence class.
- **mixed** - neither current spelling satisfies both compilers; search for
  a third that does before concluding anything.
- **dev-change** - no single spelling satisfies both, after a genuine
  search: the devs edited the line between 2.0 and 2.1. Document in
  docs/version-changes.md with the evidence.

Resolution protocol: compile the candidate spelling in the 2.0 tree
(homm2-decomp-master, MSVC 4.2 harness) and byte-compare against the 2.0
target the same way `reloc_donation.masked_equal` does here. Until that run
happens, entries stay OPEN. Timing: the verification runs are deferred until
the 2.1 reconstruction is complete - this branch only records divergences
while chasing exactness; the cross-analysis is a later dedicated phase.

## Open entries (2026-08-02)

| Site | PoL 2.0 spelling | 2.1 byte-pinned spelling | State |
|---|---|---|---|
| combatManager::CastSpell SPELL_MAGIC_ARROW case | (no guard) | `if (1) { ... }` around the whole case body - retail emits `mov eax,1; test; je <break>` with no back edge | OPEN - a build switch with no surviving name |
| combatManager::ChainLightning DoBolt angle args | `CHAIN_LIGHTNING_MIN_ANGLE`, `CHAIN_LIGHTNING_MAX_ANGLE` | `firstBolt ? CHAIN_LIGHTNING_FIRST_*_ANGLE : CHAIN_LIGHTNING_*_ANGLE` with both arms equal (10/10 and 80/80); the two FIRST_ constants are new names for a split the shipping values collapsed | OPEN |
| combatManager::Armageddon + Earthquake shake pointers | `(offY > 0 ? offY : 0) * WIDTH + (offX > 0 ? offX : 0)` | X term FIRST, Y term multiplied: `(offX >= 0 ? offX : 0) + (offY >= 0 ? offY : 0) * WIDTH` (Armageddon, `and`-form select) and `(off < 0 ? 0 : off)` / `(off > 0 ? 0 : -off)` arm order (Earthquake, branchy select) | OPEN |
| combatManager::ResetBoltAngle angle scaling | `(float)((BIAS - ratio) / DIV) * angle` | `angle * ((BIAS - ratio) / DIV)` - the accumulator operand moves to the LEFT and the inner `static_cast<float>` disappears (the divide stays double); also `/ (static_cast<float>(bolt->totalDistance))` for the parenthesised-divisor `fild` form | OPEN |
| combatManager::DoBolt child position | `sin(angle) * dist + bolts[i].pixelX` | `bolts[i].pixelX + dist * sin(angle)` - retail spills BOTH integer operands to double temps before the call, which only happens with the pixel term on the left | OPEN |
| combatManager::DoBolt loop exits | `break` out of the drawPass loop; `goto skipBranch` at the branch cooldown | `goto` past the whole while (retail's break target is the `delete[]`); `continue` for the cooldown | OPEN |
| gfCombatSpeedMod delay products (11 sites) | `gfCombatSpeedMod[speed] * DELAY` | `DELAY * gfCombatSpeedMod[speed]` - the int literal is folded to a float constant and pushed FIRST (`flds K; fmuls gfCombatSpeedMod[i]`); the reverse spelling gives `flds gfCombatSpeedMod[i]; fmuls K`. Not uniform in the image: 6 pre-existing sites already read const-first | OPEN as an FP-order class |
| advManager::StartCursor cycle select | `if (walkSpeed > SLOWEST) m_cursorCycle = 1; else m_cursorCycle = SLOW_CURSOR_CYCLE_START;` | ternary `walkSpeed > SLOWEST ? 1 : SLOW_CURSOR_CYCLE_START` - VC6 folds adjacent-constant `?:` to `setle cl; inc ecx`; the if/else emits two stores + a jmp | OPEN |
| advManager::StopCursor / MoveOrigin / MoveHero origin sums (7 sites) | `m_cursorMapX + m_mapOriginX` (cursor/previous term first) | `m_mapOriginX + m_cursorMapX` (origin term first) at every site, incl. `*eventX/*eventY` and both `GetCell` argument pairs | OPEN - whole-TU class |
| advManager::StopCursor previous-cursor reset | `m_previousCursorMapY = -1; m_previousCursorMapX = m_previousCursorMapY;` | two literals (`chained-assignment-vs-repeated-constant`) | OPEN |
| advManager::MoveOrigin cell writes | four `mapCell*` locals + `cells + width*y + x` + `DebugCheck()` | no locals at all: `m_mapData->GetCell(x, y)->m_flags &= ~F;` in one statement (retail's frame is 0x34 with exactly 4 named slots, 4 inline-`this` temps and 4 lvalue temps) | OPEN |
| advManager::DrawCursor flag frame | `m_updateMaxY % 8 + ((m_cursorFrame & MASK) + FLAG_BASE)` | `(m_cursorFrame & MASK) + m_updateMaxY % 8 + FLAG_BASE` (the `lea 0x38(%eax,%edx)` folds the base last) | OPEN |
| advManager::MoveHero step deltas | `pixelsPerStep * directionX` | `directionX * pixelsPerStep` (both axes) | OPEN |
| advManager::MoveHero switch/if tail | fall-through into `movementDone:` | two extra explicit `goto movementDone;` (one closing the cursor-cell `switch`'s block, one after the `if`) - pinned by the 12-stub census, see goto-label-jump-stub | OPEN |
| advManager::MoveHero adjacent-monster block | nested `if (embarked) {} else { if (!eventCell) goto checkAdjacent; if (== BOAT) {} else { checkAdjacent: ... } }` | two forward guards: `if (embarked) goto adjacentDone;` / `if (eventCell && (trigger & MASK) == BOAT) goto adjacentDone;` with the label after the whole `if (!forceMove)` block | OPEN |
| advManager::MoveHero stoppingEvent label | inside the `MAP_OBJECT_CASTLE` case (default duplicates the block) | inside the `default:` case (CASTLE duplicates the block) - the two `goto stoppingEvent;` stubs target 0x13e3 | OPEN |
| advManager::ProcessMapChange compares + recruit cell | `change.player != giCurPlayer`, `change.x != hero->m_x`; two `mapCell*` locals from `cells + width*y + x` | `giCurPlayer != change.player`, `mapHero->m_x != change.x`; `gpGame->m_worldMap.GetCell(x, y)->field = ...` written out twice | OPEN |
| searchArray::Init allocation size | `MAP_WIDTH * sizeof(searchCell) * MAP_HEIGHT` | `MAP_WIDTH * MAP_HEIGHT * sizeof(searchCell)` | OPEN |
| CalcTerrainCost structure | `baseCost`/`roadCost` locals + three `goto`s | no locals at all (retail's frame is 8 bytes = the two spilled fastcall params); `if (mobility >= t[0] \|\| (useRoad && mobility >= road[0])) { ... }` nested in the `[1]` guard, then a straight fall-through tail | OPEN |
| searchArray::PushPoint entry guards + search bounds | one `if (x >= 0 && x <= W-1 && ... && m_queueCount < CAP) { body }` ; `gSearchLow = 0` first ; `gSearchLow >= gSearchHigh` ; `giCurTempMobility < cost` ; visited test as one `\|\|` chain | three early-return guards (`x < 0 \|\| x >= MAP_WIDTH \|\| ...`, then `m_queueCount >= CAP`); `gSearchHigh = m_queueCount` first; `gSearchHigh <= gSearchLow`; `cost > giCurTempMobility`; visited test split into two `return`s | OPEN |
| searchArray::TestPossibleDirections shape | `do {...} while (dir < 8)` with a shared `invalidDirection:` block; `m_objTypeBits & SEARCH_OBJECT_TYPE_MASK`; `mapExtra[y*W + x]`; `MAP_WIDTH <= nextX` | `for (dir = 0; dir < 8; dir++)` with THIRTEEN longhand `gSearchTerrain = TERRAIN_INVALID; goto storeDirection;` sites; `m_objectTileset != TILESET_DUMMY` (bitfield accessor); `*(mapExtra + x + y*MAP_WIDTH) & giCurPlayerBit`; `nextX >= MAP_WIDTH` | OPEN |
| searchArray::FindCombatPath shape | `moatHex`/`offset`/`currentHex`/`moatCost` locals, inline QuickDistance arithmetic, inline Clear body, `m_storage.nodes[bestHex]`, `if (Valid...) { body }` | none of those locals (retail re-reads `moatCell[i]` and re-evaluates the facing ternary); real `QuickDistance(...)` and `Clear()` calls; `GetNode(bestHex, 0)`; `if (!ValidHex(a) \|\| !ValidHex(b) \|\| unit == NULL) goto restoreMoatFailure;` guard with the body un-nested; `while (m_queueCount > 0)`; separate `result` local | OPEN |
| searchArray::SeedCombatPosition frame | 3 locals | 3 live locals + SEVEN dead 4-byte slots (retail frame 0x2c, only -0x8/-0x14/-0x24 referenced); one counter `index` serves all four loops | OPEN - dead-local class |
| searchArray::PushCombatPoint shape | `if (ValidHex(hex)) { i32 low = 0; i32 high = m_queueCount; ... }` nested | four early `return`s; `high` assigned before `low`; `(high + low) / 2`; `m_queueCount > m_maxQueueCount` | OPEN |
| army::GetMoveMask return mask | `blockedMaskValue \| SPECIAL_DIRECTION_MASK` (0xc0) | `\| (1 << WIDE_WEST) \| (1 << WIDE_EAST)` - retail emits `or al,0x40; or al,0x80` | OPEN |
| army::GetAttackMask / GetAdjacentCellIndex / ValidAttack selects | if/else pairs, and `static_cast<u32>(m_facing) < static_cast<u32>(ARMY_FACING_RIGHT)` | `?:` everywhere; `m_facing == ARMY_FACING_LEFT` in ValidAttack (no leading `sub`), `m_facing == ARMY_FACING_RIGHT` in GetAdjacentCellIndex (leading `sub $1`) - see neg-sbb-ternary-zero-compare | OPEN |
| army::ValidAttack target compares | `m_targetSide == occupantSide`; `OppositeCombatSide(m_currentSide) == occupantSide` | `occupantSide == m_targetSide`; `occupantSide == OppositeCombatSide(...)` | OPEN |
| army::CheckLuck creature-name temp | two ternaries inline in the sprintf argument lists | a named `char* creatureName` local, assigned per branch - PROVEN by frame: retail's frame is 0x10 with ONE 4-byte slot, ours reserved two ternary temps at 0x14. Four arms measured: single hoist 511 (-30, frame exact), duplicated if/else 557 (+16, frame exact, KEPT), duplicated ternary 569 (+28), original inline 557 (+16, frame wrong). Retail 541 sits between the hoisted and duplicated forms | OPEN - permuter matrix queued |
| bitmap::GrabBitmap + DrawToBuffer linkage | declared `__declspec(dllexport) inline` | plain out-of-line methods - retail CALLS both (GrabScreen emits `call ?GrabBitmap@...`, and both have their own claimed spans at 0xc6150 / 0xc6090) | OPEN - PoL may have inlined them; check 2.0 bytes |
| HandleRemoteDeadPlayerExit guard | `giThisGamePos == pos` | `pos == giThisGamePos` (a1-moffs local-first) | OPEN |
| MapExtraPosAndAdjacentsSet reads | `mapExtra[MAP_WIDTH * y + x]`, `mapExtra[checkY * MAP_WIDTH + checkX]` | flat `*(mapExtra + x + MAP_WIDTH * y)` (both); the `if (MAP_HEIGHT <= checkY) {} else` empty-if is REAL (removing it loses 2 bytes) but must read `checkY >= MAP_HEIGHT` | OPEN - 2 jump-distance bytes left: retail places the loop-back jmp BEFORE the body, ours after |
| BlitBitmapToScreenVesa convention | `__fastcall` | `__cdecl` (509 -> 501 vs retail 486; residual is body work) | OPEN |
| a1-moffs compare order (2 sites) | UpdateAppSpecificMenus `hmnuAdv == hMenu`; SetMapSize `h == MAP_HEIGHT && w == MAP_WIDTH` | `hMenu == hmnuAdv` (local-first, -1 byte); `MAP_HEIGHT == h && MAP_WIDTH == w` (global-first, +1 byte) - the class runs BOTH directions, read it off the bytes | OPEN |
| fullMap cell-read sweep (24 sites) | `Row(y)[x]` / `Row(y) + x` across GAME, EVENTS, ADVMGR | `GetCell(x, y)` (the nested-inline shape) | OPEN - same class as the GetCell body change |
| combatManager::SetupAdjacencyArray locals | `sourceHex`; rowIndex/direction declared inside the outer loop | `fromHex`; both hoisted to fn scope (retail's toHex sits at -0x10, only reachable with 4 fn-scope slots). 41 -> 13 diffs, 3-cycle -> 2-cycle. RESIDUAL: rowIndex/direction pair - the bucket model predicts direction@-4 and MATCHES the target, but VC6 compiles the reverse; 4 probes (dir/y/decl-swap) never flipped it. First hard model-vs-VC6 disagreement on an ENUM-typed local | OPEN - permuter identifier_rename queued |
| advManager::SaveAdventureBorder local | `screenPixel` | `screen` (bucket 0 ties savedPixels; later decl wins the 3-cycle) | OPEN |
| advManager::DrawAdventureBorder slots | - | PARKED: the sibling's rename does NOT transfer (Draw declares screen first and wants savedPixels above it); decl-order swap measured 232 diffs, `saved`/`dst`+`src` measured worse - permuter queued | OPEN |
| slot-solve batch 4 (3 fns) | PoL names | GetBitmap/GetMIDIWrap `id`->`fileId`; GiveArmy `tmp`->`swap` | OPEN as slot-name class |
| DDSetFullScreenStatus / SetupHotSeatGame slots | - | PARKED: retail's 5-slot order needs x/y declared ASYMMETRICALLY (868 solver hits, all requiring e.g. `x` + `top`/`y1`) - unnatural for a paired coordinate function; SetupHotSeatGame's `window` slot resists both bucket-1 and bucket-2 names | OPEN - permuter/identifier_rename matrix queued |
| slot-solve batch 3 (7 fns) | PoL names | HandleVictory mapIndex->map; dpSendMessage result->status; DetermineHeroToMove idx/val->best/value; GetTurnAttentionValue factor->scale; Expunge cur/next (ShutDownSystem echo); ValidPath reserved->extra (dead slot); ValidSpellTarget unusedSpellWord5->unused (dead slot) | OPEN as slot-name class |
| slot-solve batch 2 | PoL names | GetOutOfItMask armyIndex2/bit1/mask5/currentArmy10 -> idx/bitMask/result/currentArmy (GetStrength family echo); IsHomogeneous nRaces/last -> numRaces/prev | OPEN as slot-name class |
| slot-solve batch 1 (8 fns) | PoL names | GetMoveShowIt directionX/Y->dx/dy; GetSSLevel bonus/ssLevel; DemobilizeCurrHero hp/cell; StopOnTrigger trigger->special (bucket model mispredicts 'metadata' on VC6); RandomizeBarrier idx->index; CanBuy idx->r; NumOfGivenArtifact jj hoisted to fn top; InitializePasswords jj->j + table-read mirror | OPEN as slot-name class |
| combatManager::EffectSpellCure six sites | `index != targetIndex`; fightValue-cast leading FP chain; `fightValue * m_quantity`; `>= MAX ? MAX : X` duration ternaries; `IDX(m_currentSide) == sideWork` (x2) | `targetIndex != index`; `cast(curePoints) * MODIFIER * fightValue`; `m_quantity * fightValue`; `< MAX ? X : MAX`; `sideWork == IDX(m_currentSide)` | OPEN - slot residual parked |
| combatManager::ProcessCombatMsg switch layout | case order as in source | retail lays inner-switch case bodies in a DIFFERENT order (jump-table targets differ; first body is the m_?f2c3 member call, ours is the 0/1 local store) + one member/local mirror | OPEN - PARKED for body-diff pass: needs jump-table target mapping |
| combatManager::Resurrect five sites | `m_initialQuantity < m_quantity`; `count == index+1`; `A<B?A:B` min-ternary; two FP/index orders | `m_quantity > m_initialQuantity`; `index+1 == count`; `REVERSE<frames?REVERSE:frames`; FIZZLE_DELAY const-first | OPEN - slot residual now CLOSED, fn EXACT |
| combatManager::VaporizeCreature stripe writes | `gyModify[row*W + firstY + top]`; `gyModify[lastY + (row*-W - bottom)]` | `*(row*W + gyModify + top + firstY)`; `*(gyModify - row*W - bottom + lastY)` (computed-term-first flat sums; simple loads canonicalize, muls do not; the LAST additive term becomes the index register, which pins top before firstY in the first store) | OPEN - slot residual now CLOSED, fn EXACT |
| combatManager::TurnToStone limit increment | `++m_limitCreatureCount[side][index]` | `m_limitCreatureCount[side][index]++` - POST-increment; the 4-arm matrix (pre / post / `+= 1` / self-assign) separates cleanly, post is the only arm that reads the second subscript before the row base. Same fix closed BloodLustEffect, MirrorImage (x2) and CastSpell | OPEN - respelling proven for 2.1, test under 4.2 |
| fullMap::GetCell inline body | `cells + y * width + x` | `&Column(x)[y * width]` (nested-inline: y*width via this-temp first, x*12, cells - the retail shape at ~119 call sites; fuzzy +0.3% TU-wide, 0 drops) | OPEN - test under 4.2 |
| game::HasLateOverlay cell fetch | `WORLDMAP->Row(row) + col` | `WORLDMAP->GetCell(col, row)` | OPEN |
| ComputeUALoc probe reads | `gpGame->m_worldMap.Row(y)[x].f` (x4) | `gpGame->m_worldMap.GetCell(x, y)->f` | OPEN - slot residual parked |
| combatManager::SummonElemental hex index | `summonHexes_l[side*3 + (rand+off)%3]` | flat `*(summonHexes + side*3 + (rand+off)%3)` | OPEN - slot residual now CLOSED, fn EXACT |
| game::GetLuck block order | clamps, Battle Garb, then Rainbow | Rainbow BEFORE clamps (VALUE - see version-changes) | dev-change candidate |
| strip::DrawIcons X exprs + type compare | `m_x + slot * STEP + FIRST_X` (5 sites); `m_army->types[slot] != cached[slot]` | `m_x + FIRST_X + slot * STEP` (NEW CLASS: const after member steers member-first eval); `cached[slot] != army types` | OPEN |
| listBoxWidget::listBoxWidget(void) store order | `items, scrollbar, selectedIndex, itemCount, lastSelected` | `itemCount, items, selectedIndex, lastSelected, scrollbar` | OPEN |
| button::button(void) store order | `normalFrame, pressedFrame, iconId, selectMode, hotkey, icon` | `iconId, icon, normalFrame, pressedFrame, selectMode, hotkey` | OPEN |
| combatManager::ShowMassSpell frame clamps + frame loop | `effectFrames < member` (x2); `returnFrames7 < member`; `effectFrames > frame` | member-first `> effectFrames` / `> returnFrames7`; counter-first `frame9 < effectFrames4` | OPEN - slot residual now CLOSED, fn EXACT |
| combatManager::Fireball FP factor | `mod[speed] * SPELL_AREA_ANIMATION_DELAY` | `DELAY * mod[speed]` | OPEN - slot residual now CLOSED, fn EXACT |
| combatManager::DoVictory compares | `winningSide == Opposite(...)`; `fadeCount > fadeIndex26`; `emptySlots > iMaxTransferArtifacts`; `!= CREATURE_GHOST` | `Opposite(...) == winningSide`; `fadeIndex26 < fadeCount`; `iMaxTransferArtifacts < emptySlots`; `!= CREATURE_SKELETON` (VALUE - see version-changes) | OPEN - slot residual parked |
| mouseManager::Open store order | `cursorL/T, mouseX, sizeIdx x2, cursorBottom, mouseY`; `m_active` before `m_priority` | R/B then L/T then X/Y pairs, size indexes last; `m_priority` before `m_active` | OPEN |
| executive::ShutDownSystem manager walk | `next = head; while ((cur = next) != NULL) { next = cur->m_next; ... }` | `baseManager* next; baseManager* cur = head; while (cur != NULL) { next = ...; ...; cur = next; }` (decl order flips the 14/14 tie) | OPEN |
| game::SetVisibility mapExtra writes + distance terms + locals | `mapExtra[MAP_WIDTH * row + col] \|=`; x-term first; `col/row/visibility`, distance in-loop | `*(mapExtra + col + MAP_WIDTH * row) \|=` (RMW emits base-first load, index-first store); y-term first; `i/j/vis`, distance hoisted to fn scope | OPEN |
| game::MakeAllWaterVisible map access | `WORLDMAP->Row(y)[x]`; `mapExtra[y * MAP_WIDTH + x]` | in-loop `fullMap* map = WORLDMAP;` + `map->Column(x)[y * map->width]`; `*(mapExtra + x + MAP_WIDTH * y)` | OPEN - third Column-class site (GetNewCellExtra pair, now this) |
| philAI::RVConversion resource sum order | ORE, GEMS, MERCURY, GOLD, WOOD, SULFUR, CRYSTAL | GOLD, WOOD, ORE, CRYSTAL, SULFUR, MERCURY, GEMS | OPEN |
| advManager::ScreenScroll locals + 4 compares | `originX/originY`; bound-first clamps; `m_mapOriginX != originX \|\| ...` | `xOrigin/yOrigin` (yOrigin 2 < xOrigin 11 flips the pair); local-first clamps and != compares | OPEN |
| combatManager::UpdateMouseGrid 5 compares | `m_mouseGridHex == hexIndex`; extent updates expr-first | `hexIndex == m_mouseGridHex`; `giMinExtent* > expr`, `giMaxExtent* < expr` (global-left) | OPEN - slot residual parked |
| game::DoKnob knob clamp compare | `floatChain < cast(mouse.y)` | `cast(mouse.y) > floatChain` (fild-first) | OPEN - slot residual parked |
| philAI::GetBestCreature random compare | `weakestArmyValue < table.randomValue` | `table.randomValue > weakestArmyValue` | OPEN - slot residual parked |
| soundManager::PollSound locals | `volume`, one `delta` reused | `musicFadeStep` (write-only copy of `m_musicFadeSteps`, retail slot -0x4), `volume`, `delta`, `switchDelta` - retail's frame is 0x1c and holds a fourth 4-byte slot our three locals cannot fill; the od_slots bucket model picked the triple and VC6 agreed on the first try (first confirmed VC6 slot-hash prediction on this branch) | OPEN as slot-name class |
| soundManager::PollSound body shape | three guard clauses then a flat body | `if (m_musicFadeSteps == 0) return; if (musicVolume == MUTED) return; if (m_musicFadeSteps > 0) { <whole body> }` - the third test is a single `jle` to the epilogue, not the two-jump inverted guard | OPEN |
| soundManager::Open / SetMusicQuality branch order | `if (musicSource == MIDI) {...} else {...}` | the `!= MIDI` arm is emitted FIRST in both functions, so the source tests `!=`; SetMusicQuality also tests `gConfig.musicSource` after the store, not the parameter, and assigns `previousTrack` inside each backend arm rather than once above them | OPEN |
| soundManager::MemorySample / AdjustMusicVolumes guards | one `\|\|` chain | separate `if (...) return;` statements, one per term | OPEN |
| soundManager::StartupMilesBackend nAvgBytesPerSec | `(bits / 8) * sampleRate * channels` | `sampleRate * (bits / 8) * channels` - the imul destination register names the LEFT operand | OPEN |
| MIDIIsPlaying guard polarity | `if (A && B && C && D) return status == 4; return false;` | `if (!A \|\| !B \|\| !C \|\| !D) return false; return status == 4;` - retail places the single `xor al,al` block BEFORE the true arm | OPEN |
| playerData::NextHero local + hero compare | `current`; `m_heroIds[i] == m_currentHero` | `curHero`; `m_currentHero == m_heroIds[i]` | OPEN |
| textWidget::textWidget(void) store order | `color, alignment, font, text, kind` | `font, text, color, alignment, kind` | OPEN |
| dropListWidget::dropListWidget(void) store order | `items, savedBackground, itemCount, selectedIndex` | `itemCount, items, selectedIndex, savedBackground` | OPEN |
| combatManager::DoBlast FP factor + segment loop | `gfCombatSpeedMod[speed] * BLAST_FRAME_DELAY`; `segmentCount > segment` | `BLAST_FRAME_DELAY * gfCombatSpeedMod[speed]`; counter-first `segment_h < segmentCount9` | OPEN - same class as combatManager::Main; slot residual now CLOSED, fn EXACT |
| heroWindow::MoveWindow clamp sums | `m_winWidth + newX`; `m_winHeight + newY` | `newX + m_winWidth`; `newY + m_winHeight` | OPEN - slot 6-perm residual parked (no symmetric rename satisfies newX<newY<=oldH<=oldX<oldY<oldW) |
| army::LoadResources dead locals + loopCount | eight `unusedLoadWord*` decls; `loopCount = 1` | no dead locals (frame 0x08); `loopCount = 0` | OPEN - loopCount is VALUE-LEVEL (see version-changes) |
| swapManager::SplitMons type compare | `selectedArmy->types[selSlot] == targetArmy->types[tgtSlot]` | target-first mirror | OPEN |
| combatManager::ShotIsThroughWall trace compares | `iWallToHexCell[i] == traceHex11`; `iTowerToHexCell[i] == traceHex11` | `traceHex11 == table[i]` (both) | OPEN - slot 14-perm residual parked |
| CreateJoinFile diff loop | `while (diffSize1 > position1)` | `while (position1 < diffSize1)` | OPEN - slot residual parked |
| recruitUnit::Update dead buffer | `char text[NAME_SIZE];` declared (PoL) | ABSENT - retail frame is 0x20, exactly NAME_SIZE smaller; sprintf targets gText | OPEN - check whether 2.0 bytes carry the 0x34 frame |
| army::AttackTo standing if/else + two compares | `if (pI == 1 \|\| steps >= speed) fS=1; else fS=0;`; `m_hex == m_moveTargetHex`; `pathLength - 1 != pI` | De Morgan: `if (pI != 1 && steps < speed) fS=0; else fS=1;`; `m_moveTargetHex == m_hex`; `pI != pathLength - 1` | OPEN - residual: retail slots finishStanding BELOW the this-spill; no rename/scope/register spelling reproduces it (3 probes measured); parked as slot-wall with GetBestDirection |
| game::ExperienceValueOfStack factors | `hitPoints * m_quantities[i]` | `m_quantities[i] * hitPoints` | OPEN |
| town::Deallocate locals + owner compare | `playerRecord/position`; `m_id == ->m_currentTown` | `pp/pos`; `pp->m_currentTown == m_id` | OPEN |
| armyGroup::DamageGroup locals + kill compare | `percentChance/numKilled`; `m_creatureCounts[i] == killed` | `killChance/killed`; `killed == m_creatureCounts[i]` | OPEN |
| combatManager::EffectSpellResurrect locals | `quantityResult[COMBAT_SIDE_COUNT]` array + `targetStack/resurrectPowerWork/armyIndexWork` | scalar `count` + dead `float workChance` (slot -16 unreferenced, bucket 15) + `target/resurrectPower/armyIndex`; two operand mirrors | OPEN - VALUE-SHAPE: 2.1 frame cannot host the [0]-at-fc array; check what 2.0 bytes hold |
| widget::widget(void) store order | `m_id` first | `m_id` after `m_prev` | OPEN |
| widget::widget(x,y,w,h,id,kind) store order | `m_id` after `m_zOrder` | `m_id` before `m_flags` | OPEN |
| army::LeftX else-branch addends | `->w + ->x` | `->x + ->w` | OPEN |
| combatManager::SetupGridForArmy locals + hex compare | `savedTargetSide/targetIndexSave/hexIndex`; `m_hex == hexIndex` | `oldSide/oldIndex/j`; `j == armyPtr->m_hex` | OPEN |
| hero::DoSSLevelDialog local + icon-frame expr | `skillLevelText`; `skills[...] + skill * STRIDE - BASE` | `skillText`; `skill * STRIDE + skills[...] - BASE` | OPEN |
| combatManager::EffectSpellCreateCreature locals + imul | `spellPowerValue/creatureType`; `fightValue * IDX(type)` | `spellPower/monType`; `IDX(monType) * fightValue` | OPEN |
| combatManager::Main timers + FP factor + army decl | `KBTickCount() > glTimers[...]` (x2); `mod[speed] * DELAY`; `army* currentArmy` in bare block | `glTimers[...] < KBTickCount()`; `DELAY * mod[speed]`; `army* currentArmy;` hoisted to fn top (goto forbids initialized decl; hoist also moves it into the outer frame group - inner-block locals slot AFTER outer ones) | OPEN |
| advManager::SetTownContext locals + two compares | `currentTownValue/contextValue7`; `m_currentTerrain != cast(ctx)`; `page + 3 - 1 < ctx` | `tp/townNo`; `cast(townNo) != m_currentTerrain`; `townNo > page + 3 - 1` | OPEN |
| dpnet_init locals + guest loop | `enumerateFunction/result`; `giNumHumanPlayers > guestIndex` | `dpEnumerate/rc`; `guestIndex < giNumHumanPlayers` | OPEN |
| army::GetBestDirection compares | source-first (`sCC < tC` etc.); `leftFlag == isMovingRight` | target-first (`tC > sCC`, `tC != sCC`, `tRV > sR`, `tRV != sR`); `isMovingRight == leftFlag` | OPEN - slot 8-perm residual parked: no natural rename satisfies b(down)<=b(tCol)<=b(sCol)<b(left)<=b(up)<=b(tRow)<=b(sRow)<b(right) |
| border::border(void) store order | `bitmap, icon, fillColor` | `fillColor, bitmap, icon` | OPEN |
| iconWidget::iconWidget(void) store order | `frame, fillColor, icon, orientation, iconId` | `icon, frame, fillColor, orientation, iconId` | OPEN |
| resourceManager::PointToFile locals | `isFound/entryIndex/aggregateIndex/ignoredPosition` | `found/entry/i/position` | OPEN - 2.0 names read PoL-invented |
| resourceManager::GetFileSize locals | `isFound/entryIndex/matchedAggregate/fileIndex` | `found/entry/matched/i` | OPEN |
| ComputeAdvNetControl locals + while compares | `selectedPlayer/player/currentPlayer(dead)`; `giCurPlayer != player` | `selected/player/myPlayer(dead)`; `player != giCurPlayer` (x2) | OPEN |
| resourceManager::GetBackdropAtLoc locals + loop bound | `backdropIcon/dataWidth/imageHeight/row`; `row < imageHeight + destinationY` | `width`/`curRow` renames; `curRow < destinationY + imageHeight` | OPEN |
| hero::GetLevel local + while compare | `increment`; `experience < experienceValue` | `growth`; `experienceValue > experience` | OPEN |
| advManager::IsCrystalBallInEffect locals + distance terms | `heroIndex/range`; `(m_y-y)^2 + (m_x-x)^2` | `i/distance`; `(m_x-x)^2 + (m_y-y)^2` | OPEN |
| GetMonType threshold compares | `table[...] >= score`; `table[...] <= score` | `score <= table[...]`; `score >= table[...]` | OPEN |
| philAI::NetValueOfArtifact addend order | `a4 * cost[a3] + a2 * cost[GOLD]` | `a2 * cost[GOLD] + a4 * cost[a3]` | OPEN |
| hero::HeroScreenUpdate | `index`; `index == giHeroScreenSrcIndex` | `i`; `giHeroScreenSrcIndex == i` | OPEN |
| advManager::CheckScreenScroll locals + origin compares | `mouseX6/mouseY1/oldOriginX9/oldOriginY3`; `m_mapOriginX == old...` | `mouseX/mouseY/oldMapX/oldMapY`; `oldMapX == m_mapOriginX` (both axes) | OPEN - 2.0 names are PoL auto-suffixed |
| textEntryWidget::textEntryWidget(void) store order | `cursor, icon, kind, maxLength, iconFrame, displayOffset` | `cursor, maxLength, icon, iconFrame, displayOffset, kind` | OPEN |
| DimensionDoorHandler mouse fields + flag | `payload.mouse.x/.y` (+4/+8); `handled` | `payload.mouse.screenX/.screenY` (+0x10/+0x14); `result` | OPEN - VALUE-SEMANTIC: different union fields read; check what 2.0's bytes read before classifying |
| game::IsMobile locals + return compare | `cp`; `CalcTerrainCost(...) <= m_remainingMobility` | `cell`; `m_remainingMobility >= CalcTerrainCost(...)` | OPEN |
| combatManager::GetStrength locals + and-mask | `armyIndex4/bit36/strength7/currentArmy8`; `(bit36 & mask)` | `idx/bitMask/totalStrength/currentArmy`; `(mask & bitMask)` | OPEN - 2.0 names are PoL auto-suffixed; dev names unknown on 4.2 side too |
| advManager::CheckSetEvilInterface | `m_evilInterface != gbUseEvilInterface`; `interfaceMessage` | `gbUseEvilInterface != m_evilInterface`; `message` | OPEN |
| resource::resource(void) store order | `m_id = 0;` before `m_refCount = ...` | `m_refCount` before `m_id` (header order) | OPEN |
| wsnet_init locals + strlen guard | `socketMode`, `player`; `strlen(gcTCPAddress) != 0` | `blockMode`, `plr`; `strlen(gcTCPAddress) > 0` | OPEN |
| philAI::CanBuyBHC locals + two compares | `jb/idx`; `garrison[what] < num`; `cost[idx] * num > m_resources[idx]` | `j/index`; `num > garrison[what]`; `m_resources[index] < cost[index] * num` | OPEN |
| fullMap::GetNewCellExtra{Overlay,Object} reads | `Row(y)[x]` | `Column(x)[y * width]` (+ new `Column` accessor) | OPEN - test Column form under 4.2; only ~25 arms tried, uniqueness unproven |
| fullMap::Cell body / cell assigns | `cells + width * y + x` | `cells + x + y * width` | OPEN |
| fullMap::Read memcpy args | `cells + width * y + x` etc. | `cells + x + y * width` | OPEN |
| fullMap::GetNewCellExtraIndex locals | `nb, i, j` | `n, i, j` | OPEN - 4.2 hash differs from VC6; both may hold per-compiler with dev rename in either direction |
| fullMap::Read locals | `tmp` | `tmp1` | OPEN (same class) |
| fullMap::ChangeTilesetIndex head | if/else | zero-arm ternary `t = index != MAPCELL_SPRITE_NONE ? tileset : TILESET_NONE;` | OPEN |
| fullMap::ChangeTilesetIndex unused local | `dummy` | `a` (bucket-1 pin) | OPEN |
| mouseManager::Close head | `if (m_active == 1) { ... }` | `if (m_active != 1) return;` unwrap | OPEN - 4.2 lowering of wrap-vs-guard untested |
| inputManager::Close head | wrap | guard-clause unwrap | OPEN |
| heroWindowManager::Close head | wrap | guard-clause unwrap | OPEN |
| AiPrint head | wrap | guard-clause unwrap | OPEN |
| soundManager::AdjustSoundVolumes | n/a (Buka-era body) | `&&` split into guard + if | 2.1-only body, no 2.0 counterpart |
| UpdateSystemOptionsMenu guard | (compare order unrecorded in PoL) | `hmnuApp != hmnuAdv` (reloc-pinned) | OPEN - check 2.0 bytes' operand order |
| ws/dpWaitForExtraGuests first compare | `iWSLastMsg... != giNum...` | `giNumHumanPlayers != i...LastMsg...` | OPEN (same check possible in 2.0) |
| ws/dpnet_rcv first compare | `Tail == Head` | `Head == Tail` | OPEN |
| REQUEST GetMapHeader CRT calls | n/a | `open/read/close` (oldnames) | matches 2.0 house style; uncontroversial |
| Bzip spotBlock locals | upstream `pos, delta, newdelta` | `index, delta, updatedDelta2` | OPEN - upstream names cannot give VC6's slots; either Buka renamed (dev-change) or the 4.2/VC6 hash split resolves it; test upstream names in 2.0 tree |
| PATH FindPath / ValidRange, HERO GetNumSpells / GetExperience, KB CanBuild, TOWN GiveSpells, CURSOR pair, REMOTE calc_crc_long, Newgame GetLossConditionText, wingraph DDInitializePalette locals | PoL names | bucket-pinned renames (see f5d5b4c3) | OPEN as a class - slot names are per-compiler hash pins; resolving needs the 4.2-vs-VC6 hash comparison per name |

| TeleportTo telescope bonus | `>= 1` u32 cast (PoL-era spelling) | `HasArtifact(...) != 0` | OPEN |
| TeleportTo terrain compare | `m_currentTerrain != terrain` | `terrain != m_currentTerrain` | OPEN |
| TeleportTo locals | savedShow11/terrain5/oldCell2/unused47 | savedShow/terrain/cellOld2/unused (bucket pins) | OPEN as slot-name class |
| InsertSound bounds guard spelling | `MAP_WIDTH <= x \|\| MAP_HEIGHT <= mapY` | param-first, axes swapped (see Resolved) | - |
| WGAppPaint scroll adds | `giScrollX + x` (a1-form) | `destX7 + giScrollX` etc. (local first) | OPEN |
| WGAppPaint locals | padding8/destY1/sourceX7/sourceY7/destWidth0 | decl swap + destW/fromY/xSource/destY0 (bucket pins) | OPEN as slot-name class |
| InitCampaignMap priority compare | `bestLocal < best` | `best > bestLocal` (mirror) | OPEN |
| InitCampaignMap faction switch | BARBARIAN, WARLOCK, NECROMANCER | WARLOCK, BARBARIAN, NECROMANCER (je-distance pinned) | OPEN |
| InitCampaignMap carryover product | ternary * counts | counts * ternary (permuter trial 135) | OPEN |
| ElementalStorm delay product + storm frame index | `speedMod * DELAY`, `col*STEP + frame + row` | `DELAY * speedMod`, `frame + col*STEP + row` (matrix winners) | OPEN |
| m_armies indexing (SPELLS + AI, 19 sites) | flattened `m_armies[0] + side*21 + idx` | 2D `m_armies[side] + idx` (single-imul fold, byte-pinned) | OPEN |
| ElementalStorm locals | *_suffixed epoch names | hit/whichSide/c/baseDam/dmg2/stack/iter/member (bucket pins) | OPEN as slot-name class |
| Random head compare | `low == high` | `high == low` | OPEN |
| netwin done-callbacks compare | `&gNbSessNcb[i] == ncb` | `ncb == &gNbSessNcb[i]` | OPEN |
| townObject::Draw frame sum | `m_animationFrame + baseFrame + 1` | `baseFrame + m_animationFrame + 1` | OPEN |
| Relational/commutative mirror class (AddWidget, DeleteItem, SetSpellInfluence, DrawWindow, GetNthSpell, recruitUnit::Open, wsWaitForHost, game::Scan, Random, netwin callbacks, townObject::Draw) | PoL-order spellings | mirrored operand order, byte-pinned per fn | OPEN as a class |
| GetCategoryStats rare-resource sum | mercury+sulfur+crystal+gems | gems+crystal+sulfur+mercury (reversed, byte-pinned) | OPEN - PoL sits at 99.05 with the forward order; likely the same residue there |
| advManager::Open / UpdBottomViewResMsg / UpdBottomViewKingdom / DoHeroKnob / DoTownKnob / SaveGame / DimensionDoor / TrimLoopingSounds / ViewPuzzle / ControlPanel / SystemOptions / DoVisions locals (12 fns) | PoL-fitted identifiers (resourceIndex, oldSampleVolumeState, textY5, countText14, previousPageSlot, ...) | 2.1-fitted bucket names (i/savedVolume/savedPlayer/savedShowIt, lineCnt/countString, nCastles/rowY/countText/numVillage, scale/prevPage/count/offset/x/y/pageIndex, ok/nPlayers/pattern/suffix/req/status, window/newX/newY, keep/i/loaded, xOff/yOff/end/pixPtr/pWin/puzzleX/puzzleIcn/puzzleY/visibleCount/i/j/order, panel, heroMobile/n, bestY/dist/count/hitX/type/fRatio/tryX/tryY/spot/nearDist/msg/isForced/joinFee/joinNum) - all byte-pinned by the retail frame | OPEN as slot-name class (2.0 is the fork; see docs/cross-version-slot-layout.md) |
| DoHeroKnob / DoTownKnob message | `tag_message message = gpInputManager->GetEvent();` | declaration split from the assignment - retail copies the sret temp with `rep movsl` and reserves 0x1c more frame | OPEN |
| ControlPanel message scope | declared inside `if (gbRemoteOn)` | declared at function scope - retail slots it before `heroWasMobilized`, i.e. in the outer scope group | OPEN |
| AdvPanel current-hero test | `gpCurPlayer->CurrentHero() == INVALID_HERO` | `gpCurPlayer->m_currentHero == INVALID_HERO` - the inline `i8` accessor spills a temp retail does not have | OPEN |
| DimensionDoor target coords | `m_lastHoverCell + m_mapOriginX`, `m_hoverCellY + m_mapOriginY` | `m_mapOriginX + m_lastHoverCell`, `m_mapOriginY + m_hoverCellY` (load order byte-pinned) | OPEN (relational/commutative mirror class) |
| DoVisions nearest-monster test | `nearestDistance > (dist = ...)` | `(dist = ...) < nearestDistance` | OPEN (relational mirror class) |
| DoVisions strength ratio | `(float)a / (float)(fightValue * count)` | `(double)a / (double)(count * fightValue)` - the divide's precision selects `fild;fild;fdivp` over `fidiv` | OPEN |
| SOURCE/ADVMGR string pooling | `base` profile (`/Gf`) | `base_nogf` - retail keeps 9 separate `"advmice.mse"` cells inside the unit block | OPEN (unit-flag class, code-byte neutral) |

| combatManager::FindResurrectArmyIndex corpse guard | one `&&` chain `sides != NONE && (frames != LEFT \|\| next free) && (frames != RIGHT \|\| prev free)` with an inner `if (sides != side) continue;` | De Morgan dual as a nested shape: `if (sides != NONE) { if ((frames == LEFT && next occupied) \|\| (frames == RIGHT && prev occupied)) continue; if (sides == side) { ... } }`. PINNED by two distinct `jmp <increment>` blocks in the retail loop body (see docs/patterns/two-backedge-jmps-in-one-loop.md) | OPEN |
| combatManager::AddBolt drawVertically | `if (abs(dx) > abs(dy)) v = 1; else v = 0;` | `bolt->drawVertically = abs(endX - startX) > abs(endY - startY);` (retail emits `xor ecx,ecx; cmp esi,eax; setg cl; mov [edx+0x34],ecx` - a boolean store, not a branch) | OPEN |
| combatManager::DoLuck bolt colour | `LuckBoltColor` helper spelling the select as `REVERSE + (((targetX >= startX) - 1) & (FORWARD - REVERSE))` | a plain ternary written INVERTED at the call site: `startX_b > targetX ? BOLT_COLOR_RAINBOW_FORWARD : BOLT_COLOR_RAINBOW_REVERSE`. `dec ecx` vs `sub ecx,1` proves a real `?:`; VC6 emits `set!cond` so the source polarity is the mirror of the asm (docs/patterns/od-constant-ternary-lowering.md). The `#if H2_STRICT_ENUMS` helper/macro pair is deleted | OPEN |
| combatManager::RippleCreature four expression sites | `(amplitudeIndex * step + base) * skipDistance`; `-giMaxExtentY + phaseTerm + row` / `phaseTerm - row + giMinExtentY`; `mod[speed] * SPELL_VANISH_END_DELAY`; `wave[waveIndex] * amplitude` | int-operand first `skipDistance5 * (amplitudeIndex5 * amplitudeStep7 + amplitudeBase6)` (retail emits two `fild`s + `fmulp`, not `fimul`); extent-global first, then +-row, then the phase term; `SPELL_VANISH_END_DELAY * mod[speed]` (same const-first class as VaporizeCreature); and `(wave[waveIndex]) * amplitude5` - the redundant parentheses are the ONLY one of ten measured spellings that stops VC6 commuting the pair (docs/patterns/paren-suppresses-fp-commute.md) | OPEN - the parenthesisation is the weakest claim here; retest under 4.2 |
| SPELLS slot-name class (12 fns) | PoL-fitted `_x`/`_N` epoch names | bucket-pinned re-fit for the 2.1 mainline layout in FindResurrectArmyIndex, Fireball, MeteorShower, VaporizeCreature, RippleCreature, ShowMassSpell, CastMassSpell, SummonElemental, DoLuck, DoBlast, Resurrect (AddBolt needed none). Several drop their PoL suffix entirely (`target_j`->`target`, `damage_f`->`damage`, `affectedHexes_f`->`hexes`, `wave8`->`wave`, `start0`->`start`, `height0`->`height`, `armyIndex0`->`armyIndex`, `randomOffset_a`->`randomOffset`); the rest take a fresh digit/letter pin. Declarations are hoisted to function top where an initialiser sat in the middle (codegen-neutral, needed for the intra-bucket decl-order tie-break) | OPEN as slot-name class |
| PlayerMonsterInteract / ComputerMonsterInteract army-strength ratio | `(float)FightValueOfStack(...) / (float)(fightValue * count)` | `(double)FightValueOfStack(...) / (double)(count * fightValue)` | OPEN - both retail images emit `fild;fild;fdivrp`; VC6 folds the float-typed form to `fidiv`, VC4.2 does not (see docs/patterns/fp-divide-double-not-fidiv.md). Likely compiler-version artifact, not a dev edit |
| CombatMonsterEvent per-stack count | `(idx < count % stacks) + count / stacks` | `count / stacks + (count % stacks > idx)` (byte-pinned: quotient first, then `setg`) | OPEN as a commutative/relational mirror; PoL's order is the mirror image |
| CombatMonsterEvent mine-guardian facing | if/else assigning 0/1 | `m_mineGuardianFacingLeft = eventHero->m_x < combatX;` (retail `xor;cmp;setl`) | OPEN - the boolean-assignment form is byte-forced here; unknown whether 4.2 lowers it the same |
| CombatMonsterEvent / SRand + DoCombat seed | `combatY + combatX` | `combatX + combatY` | OPEN as a commutative mirror |
| ComputerMonsterInteract gold check | `players[..].gold < joiningCost` | `joiningCost > players[..].gold` | OPEN as a relational mirror |
| ComputerMonsterInteract experience / necromancy products | `hitPoints * count`, `count * GetSSLevel() * FRACTION` | `count * hitPoints`, `count * FRACTION * GetSSLevel()` | OPEN as a commutative mirror class |
| PlayerMonsterInteract join-count compare | `monsterCount == joining` | `numJoining == creatureCount` | OPEN as a relational mirror |
| EVENTS local-name set (BarrierEvent, StrEqNoCase, ExpansionRecruitEvent, JailEvent, EventWindow, RecruitEvent, CombatMonsterEvent, PlayerMonsterInteract, ComputerMonsterInteract, DoNetCombat, DoCombat, ReceiveHeroTownData) | PoL 2.0 epoch names | rebucketed names + declaration reordering (od_slots pins) | OPEN as slot-name class - 2.0/2.1 slot deltas here are the fork's own local sets, not renames |
| combatManager::SetCombatDirections wide-direction map | `else if (d == WIDE_WEST) m = WIDE_EAST; else m = WIDE_WEST;` | `m = d == WIDE_WEST ? WIDE_EAST : WIDE_WEST;` - the two constants differ by 1, so retail's `sete/add 6` has no `dec`/`and` (see docs/patterns/od-constant-ternary-lowering.md) | OPEN |
| combatManager::SetCombatDirections / GetCommand local scope | `outputDirection`/`mappedDirection`/`previous`/`next` (and `enemySide`/`targetIndex`/`ourArmy`) declared in the nested blocks that use them | all hoisted to function scope - retail's frame interleaves them with the outer locals, which only one scope table can produce (see docs/patterns/od-frame-function-scope-locals.md) | OPEN |
| combatManager::SetCombatDirections hex compare | `directionHexes[d] == currentArmy->m_hex` | `currentArmy->m_hex == directionHexes[d]` | OPEN |
| combatManager::CheckSetMouseDirection empty quadrant arm | `if (relativeY < 0) { } else sector += QUARTER;` | `if (relativeY < 0) sector += DIRECTION_SECTOR_ORIGIN; else sector += QUARTER;` - retail emits the load/store pair of a folded `+= 0` | OPEN |
| combatManager::CheckSetMouseDirection ratio | `(float)relativeX / relativeY` | `(float)relativeX / ((float)relativeY)` - a parenthesised divisor CAST is what stops VC6's `fidiv` fold here, not the double/float rule (see docs/patterns/fidiv-vs-fild-fdivp.md) | OPEN |
| combatManager::ProcessCombatMsg switch case order | help-id cases first, `CONTROL_MAIN_BUTTON` last; DESELECT cases ATTACK/WAIT/DISABLE/OPTIONS | `CONTROL_MAIN_BUTTON` FIRST; DESELECT order DISABLE/WAIT/ATTACK/OPTIONS - MSVC lays case bodies out in source order | OPEN |
| combatManager::ProcessCombatMsg right-button arm | `if (RIGHT) {...} else { switch ... }` then one `break` | `if (RIGHT) { ...; break; }` then the switch - retail's then-arm jumps straight to the outer switch end, not to the if/else join | OPEN |
| combatManager::ProcessCombatMsg / GetCommand relational operand order | member-first (`m_selectedHex != selectedHex`, `m_previousCommand != m_currentCommand`, `m_currentArmySide != enemySide`, ...) | local-first (`selectedHex != m_selectedHex`, `m_currentCommand != m_previousCommand`, `enemySide != m_currentArmySide`, ...) | OPEN |
| combatManager::CheckWin retreat winner | `if (m_sideRetreated[0]) m_combatResult = DEFENDER; else = ATTACKER;` | `m_combatResult = m_sideRetreated[0] != 0 ? DEFENDER : ATTACKER;` (adjacent constants, `setne` + no add) | OPEN |
| combatManager::GetCommand move/fly command | `(i8)(HAS(...) ? IDX(FLY) : IDX(MOVE))` | arms cast individually: `HAS(...) ? (i8)IDX(FLY) : (i8)IDX(MOVE)` - retail's `setne/inc/movsbl` has no `neg/sbb/neg` normalisation | OPEN |
| WinCombatHandler close command | `message.payload.widget.command = BaseWidgetCommand(message.payload.widget.id)` (both sites) | `BaseWidgetCommand(WIN_LOSE_CLOSE_COMMAND)` - retail stores the literal, it does not re-read the field it just wrote | OPEN |
| WinCombatHandler frame timer | `KBTickCount() > glTimers[0]` | `glTimers[0] < KBTickCount()` | OPEN |
| combatManager::DoLoseWindow player test | `m_playerId[SIDE] == giCurPlayer` (both arms) | `giCurPlayer == m_playerId[SIDE]` | OPEN |
| combatManager::ShowDeadArmies casualty tables | one merged `casualtyQuantity[42]` reached with `*(&a[0][0] + side*20 + n)` pointer arithmetic | three arrays - `casualtyQuantity[2]`, `casualtyCount[2][20]`, `casualtyType[2][20]` - with plain 2D subscripts; retail's `imul 0x50` + unscaled SIB is a row `lea`, not a flat index | OPEN |
| combatManager::ShowDeadArmies casualty test / row y / loop bound | `m_quantity < m_initialQuantity`; `if (side == ATTACKER) y = A; else y = B;`; `displayedCount > armyIndex` | `m_initialQuantity > m_quantity`; `y = side ? B : A` (int-truthiness `neg/sbb` select); `armyIndex < displayedCount` | OPEN |
| combatManager::ShowDeadArmies icon geometry | `armyIndex * spacing + startX + K`; `(y + TOP - icon->y - icon->h) + 32`; `displayedCount * spacing` | `startX + spacing * armyIndex + K` (SIB base names the left `+` operand, see docs/patterns/lea-base-names-add-order.md); `y + TOP - icon->y + (32 - icon->h)`; `spacing * displayedCount` | OPEN |
| combatManager::ProcessNextAction / CycleCombatScreen cleared-field copies | `m_smallViewSide[ATTACKER] = m_smallViewSide[DEFENDER];`, `m_heroDeathPending[s] = m_heroAlternateDeathPending[s];` (after clearing the RHS) | direct constant stores of `COMBAT_SIDE_NONE` / `0` | OPEN |
| combatManager::CycleCombatScreen cycling counter | `nextHeroAnimation[COMBAT_SIDE_COUNT]` (a 3-element array whose last slot is a counter) | a separate `i32 cyclingCount` plus a 2-element `nextHeroAnimation[COMBAT_SIDE_COUNT]` - retail's frame has them 8 bytes apart with `currentArmy` in between | OPEN |
| combatManager::CycleCombatScreen side loops | one `CombatSide side` for every loop | the generic counter (`index`, retail slot -0x14) runs the three side loops AND the inner army loop; `side` (-0x44) is used only as the outer of the two nested loops | OPEN |
| combatManager::CycleCombatScreen guards | `if (m_heroOverlayIcons[i] == NULL) { } else {...}` (both hero loops) | `if (...) continue;` - retail's guard jumps BACKWARD to the increment (see docs/patterns/empty-then-branch-is-continue.md) | OPEN |
| combatManager::CycleCombatScreen stand jitter | `lastAnimationTime + (standStillDelay * 0.25 - Random(0, delay) * 0.5)` | `lastAnimationTime + (Random(0, delay) * 0.5 - standStillDelay * 0.25)` - opposite sign of the jitter; `de e9` after `fild rand; fmul 0.5; fild delay; fmul 0.25` computes `rand*0.5 - delay*0.25` | OPEN - semantic, may be a 2.0 delta |
| combatManager::CycleCombatScreen stand roll / cycle timer | `accumulatedChance > roll`; `gfCombatSpeedMod[...] * FACTOR` | `roll < accumulatedChance` (`test ah,1`); `FACTOR * gfCombatSpeedMod[...]` | OPEN |
| COMMAND local-name set (14 functions) | PoL 2.0 epoch names | whole-frame renames solved from od_slots buckets, plus seven unreferenced `unusedCasualtyWord*` slots in ShowDeadArmies and one `unusedRotateWord`/`unusedLoseWord` each in ResetCyclingCreatures / DoLoseWindow | OPEN as slot-name class |

| BASE icon decoder working state (11 functions) | locals plus inline helpers (`ReadIconRleByte`, `Entries()`, `IconRowVisible`, `MonoInitialRow`, ...) | NO locals at all: retail's frame is `sub esp,8` (the two `__fastcall` spills) and every value is a file static reached through DIR32; the RLE cursor read is two statements `s_run = *s_src; s_src++;` and the entry pointer is `reinterpret_cast<IconEntry*>(srcIcon->m_data) + frame` (see docs/patterns/no-local-frame-file-statics.md). Covers IconToBitmap, FlipIconToBitmap, IconToBitmapColorTable, FlipIconToBitmapColorTable, IconToBitmapYModify, FlipIconToBitmapYModify, DimIconToBitmap, FlipDimIconToBitmap, MonoIconToBitmap, FlipMonoIconToBitmap | OPEN - if PoL 2.0 banked these with locals, the frames genuinely differ and this is a dev-change; otherwise the 2.0 reconstruction adopts the static form |
| bmap2 FillBitmapAreaClip body | the clip arithmetic followed by an inlined copy of FillBitmapArea's memset loop | clip-then-TAIL-CALL: the whole body ends in `call ?FillBitmapArea@@YIXPAVbitmap@@HHHHH@Z`, and the four rejection tests are an `\|\|` chain with `return;` | OPEN |
| bmap2 destination-pointer term order | `bmp->m_pixels + bmp->m_width * y + x` in all four functions | two different spellings in one file: `bmp->m_pixels + x + y * bmp->m_width` (FillBitmapArea, BlitBitmap) vs `bmp->m_pixels + y * bmp->m_width + x` (DimBitmapArea) - byte-pinned by which operand is evaluated first (docs/patterns/od-add-operand-evaluation-order.md) | OPEN |
| bmap2 row loops | `gFillRow = 0; if (h > 0) do { ... } while (gFillRow < h);` | plain `for (gFillRow = 0; gFillRow < h; gFillRow++)` (jmp-to-test header, no zero guard) | OPEN |
| icon decoder clipped-span if/else polarity (14 sites) | `if (s_x + s_run > s_clipR) <clipped>; else <full>;` | `if (s_x + s_run <= s_clipR) <full>; else <clipped>;` - the emitted `jcc` negates the source condition and the fall-through is the then-arm (docs/patterns/if-else-arm-from-jcc-polarity.md) | OPEN |
| IconToBitmapColorTable dim-level index | `(flags & MASK) * STRIDE` with a signed shift | `(static_cast<u32>(s_run & ICON_RLE_DIM_LEVEL_MASK) >> 2) * DIM_PALETTE_COLOR_COUNT` - retail splits the scale into `shrl $2` + `shll $8`, and the `shrl` needs the unsigned conversion (docs/patterns/shr-not-sar-pins-unsigned-cast.md) | OPEN |
| FlipIconToBitmapYModify dim-length self-store | (absent) | a redundant `s_dimLen = s_dimLen;` in the `s_x <= s_clipR && s_x - s_dimLen + 1 >= clipX` arm - retail emits `mov eax,[s_dimLen]; mov [s_dimLen],eax` at 0x43f | OPEN |
| IconToBitmapScale locals | `step/srcBase/tmp/rowOff/pitch/dstRow/srcRow/rows/dstPix/srcPix/cols` plus a `pitch` cache | ten function-scope locals whose od_slots buckets reproduce retail's `-0x10..-0x34` order: `inc/source/lineStep/srcOrg/dstOrg/temp/x/y/srcBase/destPix`, no `pitch` cache (retail re-reads `dest->m_width`), and the memset offset shares the outer row counter | OPEN as slot-name class |
| IconToBitmapScale parameter names | `x`, `y` | `destX`, `destY` - only so the pixel-loop counters can be `x`/`y`, which is what the frame layout requires; codegen-neutral | OPEN |

## Resolved

| Site | Resolution | Evidence |
|---|---|---|
| ValidHex bound (125 vs <117) | **dev-change** | PoL ledger banks ValidHex 100.0000 with `<= 125`; this image compares `< 117` |
| TransferArtifacts bound (WAND vs GOOSE) | **dev-change** | PoL ledger banks TransferArtifacts 100.0000 with `<= WAND(3)`; this image compares `<= 7` |
| InsertSound loopCount 0 vs 1 | **dev-change** | both values byte-pinned (PoL 2.0 = 0, Buka = 1); ledgered |
| InsertSound bounds guard | **dev-change (or 2.0 recheck)** | retail 2.1 swaps the axes (x vs HEIGHT); verify what 2.0's bytes compare before classifying further |
| In-code UI strings (33 literals) + SExecutiveText | **dev-change (Buka)** | retail CP1251 content differs from 2.0's English; ledgered in version-changes.md. SPELLS adds four: Holy Word/Holy Shout and Death-spell damage reports (CastMassSpell), plural and singular resurrection reports (Resurrect) - hashes 419ad7aa/60056d07/aa5bf315/7705ea95 now present in our object |
| EDITOR/mapcell KB.h include (STL pair) | **dev-change (2.1)** | the two new functions exist only in the 2.1 image |
| game::SetupPuzzlePieces obelisk ratio | `(float)GetNumObelisks(player) / m_obeliskCount` | `GetNumObelisks(player) / (double)m_obeliskCount` - a `float` cast on the divisor still folds into `fidiv`; only `double` forces `fild`+`fdivp` (see docs/patterns/fidiv-vs-fild-fdivp.md) | OPEN |
| game::SetupPuzzlePieces / SaveGame / LoadGame / LoadMap / PerMonth / ConvertObject / RandomizeTown / RandomizeMine / CreateDiffFile / CreateJoinFile / CompressTest / NewMap slots | PoL names | whole-frame renames solved from od_slots buckets (e.g. SaveGame `oldTag/chunkTag/lastTag/genName/savePath/outFile/iFile/humans/plBuf/workBuf/nHuman`, LoadGame `wide/rows/fd/expTag/pathBuf/plBuf/chunkTag/isHuman/ndx/junkBuf/workData`, NewMap 20-slot solve) | OPEN as slot-name class |
| game::SaveGame chunk marker | `int markerBuffer[3]; write(fd, markerBuffer, sizeof(markerBuffer[0]))` | scalar `i32 chunkTag; write(outFile, &chunkTag, sizeof(chunkTag))` - retail's marker slot is 4 bytes at -0x8, directly above the legacy -1 tag at -0x4 | OPEN |
| game::LoadGame turn number | `(m_week-1)*7 + (m_month-1)*28 + m_day` | `m_day + (m_week-1)*7 + (m_month-1)*28` | OPEN |
| game::LoadGame scratch sizes | `height9[11]`, `currentPlayer6[8]`, `marker0[8]` | scalar `rows`, `plBuf[4]`, `chunkTag[4]` plus a separate 40-byte `junkBuf` and two dead ints - the retail frame has the holes elsewhere | OPEN |
| game::ConvertObject loop headers | `right >= x`, `bottom >= y`, `MAP_HEIGHT <= y` | `x <= right`, `y <= bottom`, `y >= MAP_HEIGHT` | OPEN |
| game::RandomizeMine footprint writes | `columnOffset + x`; `if (A) if (B) continue;` | `x + iCol`; `if (A && B) { } else { stores }` (empty then-arm - see docs/patterns/if-else-two-jmp-backedge.md) | OPEN |
| CreateDiffFile byte compares | `oldData[position + copyLength]`, `position + compareOffset + copyLength` | flat `*(prevData + position + length)` and term order `position + length + matchLen` (pointer-first flat sums) | OPEN |
| CreateDiffFile guard + loop exit | `remotePlayer != iLastDiffSendTo`; `break;` out of the copy loop; a redundant `continue;` in the short-match arm | `iLastDiffSendTo != remotePlayer`; `goto Finish;` (label on the join - see docs/patterns/goto-label-jump-stub.md); no `continue` | OPEN |
| CreateDiffFile file handles | one `joinFile1` reused for the read and both writes | separate `readFile` (join read) and `destFile` (both writes) - retail has three distinct handle slots | OPEN |
| game::PerMonth troop count | `firstCount = GetRandomNumTroops(...); secondCount = ...; metadata = (a+b)|0` | one expression, two compiler temps, no `|0` | OPEN |
| game::NewMap starting-hero vision | `giVisRange[(i8)m_heroRecs[...].m_cursorType]` | `giVisRange[IDX(m_heroRecs[...].m_secondarySkills[IDX(HERO_SKILL_SCOUTING)])]` - retail reads hero+0x60 from m_cursorType; the scouting-skill form is the house idiom (CURSOR/ADVMGR) | OPEN - semantic, may be a 2.0 delta |
| game::NewMap campaign award pair | `CAMPAIGN_AWARD_SORCERESS_GUILD \|\| CAMPAIGN_AWARD_DWARFBANE` | `... \|\| CAMPAIGN_AWARD_NECROMANCER_GUILD` (this+0x84, not +0x85) and the class pick is a ternary | OPEN - semantic, may be a 2.0 delta |
| game::NewMap interface flag | `m_evilInterface = A \|\| B \|\| C;` and `= (m_campaignType == ARCHIBALD);` | if/else stores of literal 1 / 0 (both sites) | OPEN |
| game::NewMap handicap factor | inner-block `double resourceScale;` set by if/else | inline ternary in the multiply (the temp is allocated last, below `this`) | OPEN |
| game::NewMap variable reuse | separate `campaignHero15` / `ultimateDistance5` / `ultimateTries4` / `heroClass5` per phase | retail reuses one town index for the hero-placement loop, the hero x/y pair for the loss/victory town coordinates, and a separate class slot in the final interface loop | OPEN |
| philAI::DoAI loop shape | nested `do { do { ... } while (C1); ... } while (C2);` | one `aiRetarget:` label with two `if (C1) goto aiRetarget;` / `if (C2) goto aiRetarget;` - retail's back edges are `jcc <exit>; jmp <top>`, the bare-goto shape, not a do-while's `jcc <top>` | OPEN |
| philAI::DoAI movement block | `goto aiMovementExit/aiMovementDone/aiEventCheck/aiMovementContinue` with four trailer labels | nested `if (destX != -1 && destY != -1) { if (pathLength == 0) {...} else if (pathLength > 0) {...} else {...} ... } else { heroDone = 1; }` - retail's two `je <far>` pairs and the two skip-else `jmp`s are an if/else-if chain, not gotos | OPEN |
| philAI::DoAI hero-loop exits | `break` out of the inner `while` (3 sites) | `goto aiCleanup;` for the gbGameOver test and `goto aiNextHero;` (label after `DeactivateCurrHero()`) for the owner-changed and currentHero==-1 exits - retail's stubs skip `m_remainingMobility = 0; DeactivateCurrHero();` | OPEN |
| philAI::DoAI hero-slot store | `currentHero = GetHeroSlot(id); currentHero->m_lastTownInteractionTurn = ...;` | `GetHeroSlot(id)->m_lastTownInteractionTurn = ...;` - retail folds the accessor into one `movw $0xff9d, 0x27cb(%edx,%ecx)` (no local, no `lea`) | OPEN |
| philAI::DoAI / GetTurnAIVars trigger constant | `(trigger & MAP_TRIGGER_TYPE_MASK) != (MAP_TRIGGER_ACTION_FLAG \| MAP_OBJECT_BOAT)` (compiles to `cmp 0xab`, unreachable) | `!= MAP_OBJECT_HERO_INTERACTION` (`cmp 0x2a`) - PoL 2.0's retail also compares 0x2a, so the 2.0 reconstruction is simply wrong here, not a version delta | OPEN (2.0 fix) |
| philAI::GetTurnAIVars constants | `giCurTurn <= 8`, `m_owner <= 6`, `ownedTownCount >= 4`/`>= 9`, `m_buildings & KB_DWELLING_UPGRADE_SIXTH_FLAG` (0x40000000) | `<= 7`, `<= 5`, `>= 5`/`>= 10`, `& TOWN_BUILDING_CASTLE` (0x40) - PoL 2.0's retail carries the same constants, so these are 2.0 reconstruction errors | OPEN (2.0 fix) |
| philAI::GetTurnAIVars turn number | `(m_month-1)*28 + (m_week-1)*7 + m_day` | `m_day + (m_week-1)*7 + (m_month-1)*28` (same class as game::LoadGame) | OPEN |
| philAI gai* map writes | `gaiEnemyHeroReachable[MAP_WIDTH * y + x]`, `gaiTurnValueOfMine[MAP_WIDTH * y + x]` | `*(gai... + x + y * MAP_WIDTH)` - the pointer form accumulates `base + x` and leaves `y*W` for the SIB index; the subscript form computes the whole index first (see docs/patterns/flat-map-index-pointer-form.md) | OPEN |
| philAI::GetTurnAIVars / RVOfPosition search-row read | `gpSearchArray->GetRow(MAP_WIDTH, y)[x]` | `gpSearchArray->GetRow(x, 1)[MAP_WIDTH * y]` - the inlined accessor forces subscript-first evaluation, so the row term has to be the SUBSCRIPT and the column the accessor argument (see docs/patterns/inline-call-operand-index-first.md) | OPEN |
| philAI::GetBestHero randomized score | `(Random(1,10) + BASE) * benefitCost / DIVISOR` | `benefitCost * (Random(1,10) + BASE) / DIVISOR` - the float operand is converted to a double temp and spilled across the call, which only happens when it is evaluated first | OPEN |
| philAI::HeroInteractionAtHero share compare | `if (armyShare <= desiredShare) delta = desired - army; else delta = army - desired;` plus a named `shareDelta` | `transferFraction = (desiredShare < armyShare) ? (army - desired) : (desired - army);` - retail uses the `<` mask (`test ah,1`) and keeps the result in a compiler temp below `this` | OPEN |
| philAI::RVOfPosition debug object name | `char* objectName; if (...) objectName = ...; else objectName = " "; sprintf(...)` | the conditional inline in the `sprintf` argument (a `?:` temp, allocated below `this`) | OPEN |
| philAI::RVOfPosition value chain | `targetLiveChance * eventValue`, `adjacentEventChance * eventValue`, `(strategicDelta + totalValue + eventValue)`, `(strategicDelta + eventValue)` | `eventValue * targetLiveChance`, `eventValue * adjacentEventChance`, `(totalValue + eventValue + strategicDelta)`, `(eventValue + strategicDelta)` | OPEN |
| philAI slot names (14 functions) | PoL names | whole-frame renames solved from od_slots buckets; `philAI::DoAI` also needs an unreferenced `i32 unusedVars1[7]` at the frame bottom and `philAI::RVOfPosition` an unreferenced `i32 unusedValue3` between the last live local and `debugObjectType` | OPEN as slot-name class |
| Misc FadeIn/FadeOut last-step re-entry | `for (;;) { if (level >= COUNT) { if (done) break; level = LAST; } ... }` | a real `for (i = 0; i < COUNT; i += increment) { fadeStep: ... }` plus a trailing `if (done == 0) { i = LAST; goto fadeStep; }` - the for-layout (init, `jmp cond`, increment block, cond block, body, `jmp increment`) is byte-decisive and the re-entry is a goto INTO the loop body | OPEN |
| Misc FadeIn/FadeOut/FadeTo/CreatePCXFile/GetDataEntry locals | PoL names, declared at first use | all locals hoisted to one function-top declaration group (a single bucket table), names solved from od_slots: FadeOut `done,i,j,delayTime,pal`; FadeIn adds `threshold`; FadeTo `temp,current,to,idx,change,diff,move,delay,iLevel,nextTime,k`; CreatePCXFile `fd,iLen,bMark,color,sourceIndex,x,y,endPos,rowPtr,encodedRow,pcxHdr,palOut,runLength`; GetDataEntry `savedCursorType,wId,nRows,windowName,entryY,nHeight,textLines,cBuf,pText,msg,nFrame` | OPEN as slot-name class |
| Misc FadeTo abs + dead delay | ternary `difference < 0 ? -difference : difference`; no delay local | two real `abs()` calls (CRT `_abs` relocs) and a dead `i32 delay = 50;` that is doubled when `increment` clamps to 1 but never used - the loop's `KBTickCount() + 50` uses the literal | OPEN |
| Misc FileSize null check | one `if (fp == NULL) FileError(name);` | the check is emitted TWICE (`cmp [fp],0; jne L; cmp [fp],0; jne L; call FileError; L:`) - a nested `if (f == NULL) if (f == NULL) FileError(filename);` (or `&&` of the same test) | OPEN - looks like a macro expansion |
| Misc GetDataEntry row arithmetic | `rows = LineLength*16; if (cancel) rows += 39; rows = (rows+15)/45; ...; entryY = rows*45 - (cancel?30:0) + 95` | `nHeight = textLines*16; if (cancel) nHeight += 39; nHeight += 40; nRows = (nHeight-25)/45; ...; entryY = (nRows+1)*45 + 50 - (cancel?30:0)` - same value, different constant split, byte-pinned by `add 0x28` / `sub 0x19` / `imul 0x2d` / `add 0x32` | OPEN |
| Misc GetDataEntry + DataEntryWindowHandler dead `i16` | absent | both open with `wId = ENTRY_TEXT_WIDGET;` into a 2-byte frame slot that is never read (`mov word ptr [ebp-N], 0xa`); the slot is required for the rest of the frame to line up | OPEN |
| Misc BlitBitmapToScreen mouse test | `if (IsVis() && gBlitRight >= m_savedLeft && m_cursorRight >= destX && ...) { save; blit; ...; restore; return; } blit;` | the De Morgan `||` form with a real `else`: `if (IsVis() == 0 \|\| gBlitRight < m_savedLeft \|\| destX > m_cursorRight \|\| ...) { blit; } else { save; blit; ...; restore; }` | OPEN |
| Misc BlitBitmapToScreen extent sums | `width + destinationX - 1`, `height + destinationY - 1` | `destinationX + width - 1`, `destinationY + height - 1` (the `lea` base is the FIRST source term, loaded second) | OPEN |
| Misc FindToken/FindLastToken/CreatePCXFile byte reads | `text[i]`, `pixels[runEnd]`, `encodedRow[n]` | pointer form `*(text + i)`, `*(rowPtr + endPos)`, `*(encodedRow + iLen)` throughout (same class as the PHILAI flat-map sweep) | OPEN |
| Misc registry return capture | `if (RegCreateKeyA(...) != 0) return;` | `lRet = RegCreateKeyA(...); if (lRet == 0) { <whole body> }` - retail spills the result to `[ebp-4]`/`[ebp-8]` and jumps the failure straight to the epilogue, so the body is nested in the success arm | OPEN |
| Misc ReadPrefsFromFile fclose result | `fclose(f);` | `result = fclose(file);` into a named slot at `[ebp-4]` | OPEN |
| TOWNMGR relational side (9 sites) | `KBTickCount() > glTimers[0]`; `m_town->m_type != m_lastTownType`; `m_pendingStrip == m_swapStrip`; `swap[...] != pending[...]`; `strongestCreatureValue < db[...].fightValue`; `gpGame->m_playerCount - m_deadPlayerCount == firstAtRank`; `lastAtRank < position`; `spellCounts[level+1] <= slot`; `gSpellLimits[l] + lib <= slot` | the mirrored forms - the memory reference is retail's LEFT relational operand throughout (see docs/patterns/integer-relational-operand-side.md) | OPEN as relational-side class |
| townManager::Main quickView init | `i32 quickView = HAS(msg.modifiers, RIGHT_BUTTON) != 0;` | uninitialised `i32 quickView;` plus a real `if/else` statement (`mov 1 / jmp / mov 0`); `!= 0` and `? 1 : 0` both lower to `neg/sbb/neg` | OPEN |
| townManager::Main case exits (3 sites) | `if (quickView) { A } else { B }` | `if (quickView) { A break; } B` - and `showBuildingInformation:` sits INSIDE the `if (quickView) {` arm, past the guard | OPEN |
| townManager::SetupTown crest frame | `static_cast<i8>(cond ? WITHOUT_HERO : WITH_HERO)` | `cond ? static_cast<i8>(WITHOUT_HERO) : static_cast<i8>(WITH_HERO)` - casting the ARMS (not the result) is what makes the select 8-bit | OPEN |
| townManager::SetupMage spell state | `if (spellCounts[l+1] <= slot) s = 1; else s = 0;` + `if (s == 999) cmd = CLEAR; else cmd = SET;` | `s = static_cast<i16>(slot >= spellCounts[l+1]);` (the `movswl %ax` is the i16 round-trip) + `cmd = s == 999 ? CLEAR : SET;` | OPEN |
| townManager::DoCommand dismiss flag | named `i32 dismissAllowed` assigned by if/else | the boolean expression passed inline as the ViewArmy argument (temp lands BELOW `this`) | OPEN |
| TOWNMGR inline-accessor sites (4) | `m_town->OccupyingHero()`, `gpCurPlayer->Color()`, `gpCurPlayer->TownId(i)` x2, `gpCurPlayer->AvailableHeroId(i)` | direct member reads `m_occupyingHeroId`, `m_color`, `m_townIds[i]`, `m_availableHeroIds[i]` (narrow-inline-accessor-spill) | OPEN |
| TOWNMGR chained NULL/-1 stores | `m_pendingStrip = NULL; m_swapStrip = m_pendingStrip; m_selectedStrip = m_swapStrip;` (and the army-slot triple) | six independent constant stores | OPEN |
| townManager::DoCommand merge count | `counts[p] = counts[p] + counts[s];` | `counts[p] += counts[s];` (16-bit `movw`/`addw` in place) | OPEN |
| Tavern/SplitArmy dialog-select command | `message.payload.widget.command = BaseWidgetCommand(message.payload.widget.id);` | `= WIDGET_COMMAND_DIALOG_SELECT;` (a second constant store, not a copy of the id) | OPEN |
| townManager::BuyBuild row geometry | `row * ROW_HEIGHT + lineCount * LINE_HEIGHT + windowY + FIRST_Y`; `if (row == 0) n = top; else n = bottom;`; `x += spacing + entryWidth` | `lineCount * LINE_HEIGHT + windowY + row * ROW_HEIGHT + FIRST_Y`; `n = row == 0 ? top : bottom;`; `x += entryWidth + spacing` | OPEN |
| townManager::BuyBuild cost init | `resourceTypes[i] = static_cast<i8>(costs[i]);` | `resourceTypes[i] = -1;` (a direct constant, no reload of costs) | OPEN |
| SOURCE/KB `CheckEndGame` loss-by-time and campaign day index | `lossConditionValue < (week-1)*7 + (month-1)*28 + day` and `currentDayIndex = (week-1)*7 + (month-1)*28 + day` | `day + (week-1)*7 + (month-1)*28 > lossConditionValue` and `currentDayIndex = (month-1)*28 + (week-1)*7 + day` — the two day-index sums use DIFFERENT term orders in the same function, each byte-pinned by the accumulate order | OPEN |
| SOURCE/KB `CheckEndGame` player comparisons | `giThisGamePos == player`, `giCurPlayer == player` (x2) | `player == giThisGamePos`, `player == giCurPlayer` — retail loads the local into the register and compares against the global | OPEN |
| SOURCE/KB `CheckEndGame` campaign score | `m_campaignScenarioDays[..] + m_campaignScenarioBonus[..]` | `m_campaignScenarioBonus[..] + m_campaignScenarioDays[..]` | OPEN |
| SOURCE/KB `CheckEndGame` carryover hero search exit | `if (m_players[0].m_heroCount == campaignHeroIndex)` | `if (campaignHeroIndex == m_players[0].m_heroCount)` | OPEN |
| SOURCE/KB `RecruitHeroHandler`/`WaitHandler` dialog close | `msg.payload.widget.command = BaseWidgetCommand(msg.payload.widget.id);` | `msg.payload.widget.command = WIDGET_COMMAND_DIALOG_SELECT;` — retail stores the literal 10 twice instead of reloading the id (`EventWindowHandler` in the same TU still shows the reload form and is unclaimed by this lane) | OPEN |
| SOURCE/KB `AddScoreToHighScore` standard-mode rank test | `entries[entry].score <= score && type == STANDARD` | `score >= entries[entry].score && type == STANDARD` — the other two disjuncts keep `score <= entries[entry].score` | OPEN |
| SOURCE/KB `SmackFade` luminance sum and best-distance test | `(src[i*3+BLUE] + src[i*3+RED] + src[i*3+GREEN]) / 3` and `if (d > p)` | `(src[i*3+RED] + src[i*3+GREEN] + src[i*3+BLUE]) / 3` and `if (p < d)` | OPEN |
| SOURCE/KB `QuickViewWait` loop flag | `if (a \|\| b \|\| c) done = 1; else done = 0;` | `done = a \|\| b \|\| c;` (dword 0/1 temp, false arm first) | OPEN |
| SOURCE/KB `ReceiveRemotePlayerExit` broadcast filter | `exitInfo.netPosition == recipient`, `exitInfo.netPosition != recipient`, `giNumHumanPlayers > recipient` | `recipient == exitInfo.netPosition`, `recipient != exitInfo.netPosition`, `recipient < giNumHumanPlayers` | OPEN |
| SOURCE/KB `ReceiveHostReportsPlayerExit` net-position shift guard | `exitInfo.netPosition < giThisNetPos` | `giThisNetPos > exitInfo.netPosition` | OPEN |
| SOURCE/KB `SetupDynamicWindow` content extents / tile coordinates | `columnsSize * TILE + *contentLeft - 1`, `edge * TILE + leftOffset - EDGE_OFFSET`, `columnIndex * TILE + leftOffset`, `columnsSize > edge` | the named operand first everywhere: `*contentLeft + columnsSize * TILE - 1`, `leftOffset + edge * TILE - EDGE_OFFSET`, `leftOffset + columnIndex * TILE`, `edge < columnsSize` | OPEN |
| SOURCE/KB `PopNetBox` chat input character | `inputText[len] = (char)event.payload.keyboard.keyCode;` | `inputText[len] = (char)(event.payload.keyboard.keyCode & 0xff);` — the range guard two lines above reads `keyByte` and is byte-neutral between the two spellings, so only the store discriminates | OPEN |
| SOURCE/KB slot names (13 functions) | PoL names | whole-frame renames solved from od_slots buckets: `RecruitHeroHandler`, `ShowMoraleInfo`, `ShowLuckInfo`, `QuickViewWait`, `AddScoreToHighScore`, `SmackFade`, `CheckEndGame`, `PopNetBox`, `SetupDynamicWindow`, `TestDynamicWindow`, `ReceiveHostReportsPlayerExit`, `ReceiveRemotePlayerExit`, `SetWinText` | OPEN as slot-name class |
| SOURCE/ADVMGR `ProcessSearch` mobility guard | `hero->m_mobility != hero->m_remainingMobility` | `hero->m_remainingMobility != hero->m_mobility` | OPEN |
| SOURCE/ADVMGR `CompleteDraw` all-black origin reset | `m_mapOriginY = 0; m_mapOriginX = m_mapOriginY;` | two independent stores `m_mapOriginY = 0; m_mapOriginX = 0;` | OPEN |
| SOURCE/ADVMGR `UpdateHeroLocator` / `UpdateTownLocators` locator colour | `if (sel) msg...value = SELECTED; else msg...value = NORMAL;` | `msg...value = (sel) ? SELECTED : NORMAL;` (branch ternary, temp past `this`) | OPEN |
| SOURCE/ADVMGR `UpdateHeroLocator` / `UpdateTownLocators` selection compares | `gpCurPlayer->m_heroIds[...] == curHero`, `gpCurPlayer->m_currentHero == whichHero`, `gpCurPlayer->m_currentTown == whichTown` | the local first: `curHero == gpCurPlayer->m_heroIds[...]`, `whichHero == gpCurPlayer->m_currentHero`, `whichTown == gpCurPlayer->m_currentTown` | OPEN |
| SOURCE/ADVMGR `UpdBottomViewEnemyTurn` hourglass phase guard | `iLastHourGlassPhase > iCurHourGlassPhase` | `iCurHourGlassPhase < iLastHourGlassPhase` | OPEN |
| SOURCE/ADVMGR `HeroQuickView` / `TownQuickView` widget placement | `<index expr> * <stride> + <base>` and `... - entry->h + BASELINE` | `<base> + <stride> * <index expr>` and `... + (BASELINE - entry->h)` | OPEN |
| SOURCE/ADVMGR `TownGate` town distance | `abs(town.m_y - hero->m_y) + abs(town.m_x - hero->m_x)` | X term first: `abs(town.m_x - hero->m_x) + abs(town.m_y - hero->m_y)` | OPEN |
| SOURCE/ADVMGR `ShowRoute` route cell index / arrow frame | `m_visibilityMap[mapY * MAP_WIDTH + mapX]`, `gbArrow[...] + frame * STRIDE + OFFSET` | `m_visibilityMap[mapX + mapY * MAP_WIDTH]`, `frame * STRIDE + gbArrow[...] + OFFSET` | OPEN |
| SOURCE/ADVMGR `ComboDraw` visibility index | `m_visibilityMap[tileY * MAP_WIDTH + tileX]` | pointer form `*(m_visibilityMap + tileX + tileY * MAP_WIDTH)` | OPEN |
| SOURCE/ADVMGR `FindAdjacentMonster` row / exclusion compares | `originY > s_adjacentMonsterY`, `excludedY != s_adjacentMonsterY` | the static first: `s_adjacentMonsterY < originY`, `s_adjacentMonsterY != excludedY` | OPEN |
| SOURCE/ADVMGR `UpdateSystemOptions` frame-base sums | `(gConfig.x != 0) + ADVMGR_SYSTEM_OPTIONS_*_FRAME_BASE` | `static_cast<i32>(gConfig.x != 0) + ADVMGR_SYSTEM_OPTIONS_*_FRAME_BASE` (enum addend masks the bool) | OPEN |
| SOURCE/ADVMGR `APanelHandler` / `TownPortalHandler` dialog close | `message.payload.widget.command = static_cast<BaseWidgetCommand>(message.payload.widget.id);` | the literal command `WIDGET_COMMAND_DIALOG_SELECT` | OPEN |
| SOURCE/ADVMGR `CastSpell` mine-guardian local | `CreatureType guardianTypes1[MINE_GUARDIAN_VALUE_COUNT]` with `[MINE_GUARDIAN_TYPE_INDEX]` | scalar `CreatureType mineGuard;` plus one unreferenced `i32 guardianCount;` (retail's frame has 5 four-byte locals, one dead) | OPEN |
| SOURCE/ADVMGR slot names (13 functions) | PoL names | whole-frame renames solved from od_slots buckets: `ProcessSearch`, `UpdateHeroLocator`, `UpdateTownLocators`, `UpdBottomViewEnemyTurn`, `HeroQuickView`, `TownQuickView`, `CastSpell`, `TownGate`, `ShowRoute`, `ComboDraw`, `UpdateSystemOptions` | OPEN as slot-name class |
| SOURCE/GAME `SetupOrigData` calendar / patrol / destination / ultimate-artifact groups | chained `b = a;` after `a = K;` | each member spelled with its own literal (`m_week = INITIAL_CALENDAR_VALUE;` etc.) | OPEN |
| SOURCE/GAME `ClaimTown` new-owner turn count | `m_turnsOwned = (m_owner == -1) ? TOWN_NEW_OWNER_TURN_COUNT : 0;` | branchy `if/else` pair of stores | OPEN |
| SOURCE/GAME `ViewSpells` tab widget id | `if (spellType == SPELL_TYPE_COMBAT) id = COMBAT_TAB; else id = ADVENTURE_TAB;` | `static_cast<i16>(VIEW_SPELL_COMBAT_TAB_ID + static_cast<i32>(spellType != SPELL_TYPE_COMBAT))` | OPEN |
| SOURCE/GAME `NextPlayer` bottom-view override | `giBottomViewOverride = gbThisNetHumanPlayer[giCurPlayer] ? BOTTOM_VIEW_NEW_TURN : BOTTOM_VIEW_NONE;` (also `WaitForPlayer`) | branchy `if/else` pair of stores | OPEN |
| SOURCE/GAME `ComputeDailyGold` town income | `if (buildings & CASTLE) gold += VILLAGE; else gold += TOWN;` | `gold += (buildings & CASTLE) ? DAILY_GOLD_VILLAGE_INCOME : DAILY_GOLD_TOWN_INCOME;` | OPEN |
| SOURCE/GAME `ComputeDailyGold` / `CalcBaseScore` identity scalings | empty `if` bodies for DIFFICULTY_NORMAL / MAP_DIMENSION_MEDIUM | real `* 1.0` multiplies (`GAME_DIFFICULTY_NORMAL_GOLD_FACTOR`, `GAME_SCORE_MEDIUM_DAY_SCALE`) | OPEN |
| SOURCE/GAME `PerDay` / `CheckForTimeEvent` turn number | `(m_week - 1) * 7 + (m_month - 1) * 28 + m_day` | `m_day` first: `m_day + (m_week - 1) * 7 + (m_month - 1) * 28` | OPEN |
| SOURCE/GAME `PerDay` AI resource bonus | `m_players[player].m_resources[m_day - 1]++;` | `... += 1;` (changes the read's evaluation order) | OPEN |
| SOURCE/GAME `PerDay` spell-point / mage-guild guards | `hero->m_spellPoints < restored`, `townHero->m_spellPoints < maxSpellPoints` | the local first: `restored > hero->m_spellPoints`, `maxSpellPoints > townHero->m_spellPoints` | OPEN |
| SOURCE/GAME `ProcessMapExtra` column loop | `MAP_WIDTH > col` | `col < MAP_WIDTH` | OPEN |
| SOURCE/GAME `SetupTowns` dwelling count | `if (--dwellingCount != 0) buildings |= DWELLING_2; dwellingCount--;` | the second decrement inside the guard | OPEN |
| SOURCE/GAME `SetupTowns` custom-building mask | `(gTownEligibleBuildMask[type] & extra->buildings) | (buildings & (CASTLE|TENT))` | operands swapped on both `&` and `|` | OPEN |
| SOURCE/GAME `SetupTowns` spell rolls | `raceChance[type] < Random(0,10)`, `spellValue < Random(1,1500)`, `(POWER ? 4 : 1) * aiValue` | `Random(...) >` first, and `aiValue * (POWER ? 4 : 1)` | OPEN |
| SOURCE/GAME `SetupTowns` filled mage-guild slot | `if (...) { usedSpells[...] = 1; } else { ... }` | `continue;` in the then-arm, else-body unindented | OPEN |
| SOURCE/GAME `SetupTowns` / `CheckForTimeEvent` / `ProcessOnMapHeroes` slot names | PoL names | whole-frame renames solved from od_slots buckets (SetupTowns also merges the building/slot counter and the outer spell/slot counter into one local each, and adds one dead local) | OPEN as slot-name class |
| SOURCE/GAME `ProcessOnMapHeroes` jail flag | `if ((trigger & MASK) == MAP_OBJECT_JAIL) isJail = 1; else isJail = 0;` | `isJail = (trigger & MASK) == MAP_OBJECT_JAIL;` | OPEN |
| SOURCE/GAME `ProcessOnMapHeroes` cell fetch | `&WORLDMAP->Row(y)[x]` | `WORLDMAP->GetCell(x, y)` (Column form) | OPEN |
| SOURCE/GAME `TransmitSaveGame` ack marker | `acknowledged[batch * BATCH + packet] = 1;` | pointer form `*(acknowledged + packet + batch * BATCH) = 1;` | OPEN |
| SOURCE/GAME `TransmitSaveGame` / `ReceiveSaveGame` old music track | `oldTrack = static_cast<i8>(gpSoundManager->m_musicTrack);` | no cast (dword load) | OPEN |
| SOURCE/GAME `ReceiveSaveGame` packet timeout / decoded-buffer compare | `KBTickCount() > lastPacketTime + TIMEOUT`, `incomingData != decodedData` | `lastPacketTime + TIMEOUT < KBTickCount()`, `decodedData != incomingData` | OPEN |
| SOURCE/GAME `WriteDiffHeaderInfo` header flags | `flags = (cmd << SHIFT) | flags;`, `(len >> 8) & 0x1f`, `static_cast<u8>(len)` | `flags |= cmd << SHIFT;`, `(len & 0x1f00) >> 8`, `len & 0xff` | OPEN |
| SOURCE/GAME `CalcBaseScore` first-tier deduction | `score -= -(-days);` | `score -= days;` | OPEN |
| SOURCE/GAME `CheckForTimeEvent` event guards | `event->firstDay < dayNumber`, `resources[i] < -amount` | `dayNumber > event->firstDay`, `-amount > resources[i]` | OPEN |
| SOURCE/PHILAI x87 operand order (12 functions, ~20 sites) | the computed term first (`(mod + 1.0f) * stackValue`, `townValue * winChance + result`, `(3.0f - w*2.0f) * result`, `(diff) * qty * MULT`, `(ternary) * avgStrength`, `FutureDeflator(costs) * value`, `(weights) * value`, ...) | the accumulated local first everywhere: `stackValue * (1.0f + mod)`, `result + townValue * winChance`, `result * (3.0f - w*2.0f)`, `qty * MULT * (diff)`, `avgStrength * (ternary)`, `value * FutureDeflator(costs)`, `value * (weights)` - the leading `fild`/`fld` names the left operand (see docs/patterns/x87-push-order-names-operand-order.md) | OPEN |
| SOURCE/PHILAI relational operand side (14 sites) | `goldAmount <= resources[GOLD]`, `spellCount > scoreIndex`, `armyValue*2 < spellValue`, `armyValue < spellValue`, `giMax-2 <= heroCount`, `t->m_x == victoryConditionValue`, `giBestShipyardDist > dist`, `townPtr->m_id != giBestShipyardId`, `gsSpellInfo[i].level <= eagleEye+1`, `EARLY_TURN_BASE - ... > giCurTurn` | the other side in every case: `resources[GOLD] >= goldAmount`, `scoreIndex < spellCount`, `spellValue > armyValue*2`, `spellValue > armyValue`, `heroCount >= giMax-2`, `victoryConditionValue == t->m_x`, `dist < giBestShipyardDist`, `giBestShipyardId != townPtr->m_id`, `eagleEye+1 >= gsSpellInfo[i].level`, `giCurTurn < EARLY_TURN_BASE - ...` | OPEN |
| SOURCE/PHILAI `DoAnywhereDDoorTownGate` control flow + searchArray reads | nested `if (abs(dx) <= 7) { ... }` with an inner `continue`; `if (!(A \|\| B)) {} else { body }`; `gpSearchArray->GetRow(y, MAP_WIDTH)[x]` | flat `if (abs(dx) > 7 \|\| abs(dy) > 7) continue;` and `if (!(A \|\| B)) continue;`; `gpSearchArray->GetRow(x, 1)[MAP_WIDTH * y]` (the row-accessor argument spelling that reproduces retail's index/base split) | OPEN |
| SOURCE/PHILAI `QuickCombat` retreat guard | `(attackerWon == 0 \|\| defenderHero != NULL)` | the redundant `(attackerWon == 0 \|\| (attackerWon != 0 && defenderHero != NULL))` - retail emits three tests, the middle one dead | OPEN |
| SOURCE/PHILAI `ValueOfBuyingHero` spellcaster flag | `magicHero = (a \|\| b \|\| c \|\| d);` (bool temp past `this`) | `if (a \|\| b \|\| c \|\| d) spellCaster = 1; else spellCaster = 0;` - retail stores 1 then 0 straight into the named slot | OPEN |
| SOURCE/PHILAI `HeroInteractionAtTown` army selection + spell multiplier | `if (townWins) firstArmy = &town->m_army; else firstArmy = &hero->m_army;` (x2); a named `spellMultiplier` local | `firstArmy = (townWins ? &town->m_army : &hero->m_army);` (x2, temps past `this`); the multiplier inlined as `gsSpellInfo[...].aiValue * (HAS(...) ? Stats(KNOWLEDGE) : 1)` - retail has no named slot for it | OPEN |
| SOURCE/PHILAI `ValueOfTown` turn-cost term | `gafAITurnCostResource[GOLD] * 1250.0f * 1.5 + sum` | `sum + 250.0f * gafAITurnCostResource[GOLD] * 5.0f * 1.5` - retail keeps 250 and 5 as separate float constants | OPEN |
| SOURCE/PHILAI double-typed divisions | `(float)desired / townStrength / 3.0f`; `(float)townStrength / (townStrength + heroStrength)` | `(float)desired / ((float)townStrength) / 3.0f` (parenthesised divisor cast) and `(double)townFV / (townFV + battlePower)` - both stop the `fidiv` fold, see docs/patterns/fidiv-vs-fild-fdivp.md | OPEN |
| SOURCE/PHILAI narrow inline accessors (4 sites) | `gpCurPlayer->TownId(i)`, `HeroId(i)`, `AvailableHeroId(i)`, `CurrentHero()` | direct member reads `m_townIds[i]`, `m_heroIds[i]`, `m_availableHeroIds[i]`; `CurrentHero()` needs an `i32` return type instead (see docs/patterns/inline-accessor-return-width.md) | OPEN |
| SOURCE/PHILAI `EvaluateGenericSite` metadata decode | `H2_ENUM_DECODE_MASKED(...)` (a `do { } while (0)` macro) | two plain statements - retail has no back edge (see docs/patterns/do-while-zero-macro-tail.md) | OPEN |
| SOURCE/PHILAI `EvaluateHeroEvent` mapExtra read | `mapExtra[x + MAP_WIDTH * y]` | flat `*(mapExtra + x + MAP_WIDTH * y)` | OPEN |
| SOURCE/PHILAI slot names (16 functions) | PoL names | whole-frame renames solved from od_slots buckets: `CheckForCreatureUpgrades`, `CheckBuyStuff`, `DoAnywhereDDoorTownGate`, `SetupRelativeHeroStrengths`, `GetBestBHC`, `ValueOfBuyingHero`, `ValueOfTown`, `FightValueOfStack` (26 locals), `QuickCombat` (22), `HeroInteractionAtTown` (21), `BuildHero`, `BuildCreature`, `TownEvent`, `ComputeUpgradeValue`, `EvaluateGenericSite`, `EvaluateHeroEvent` | OPEN as slot-name class |
| SOURCE/searchArray `searchArray::GetCell` inline body | `return (m_storage.cells + y * MAP_WIDTH)[x];` | `return (m_storage.cells + x)[MAP_WIDTH * y];` — retail scales both terms separately and puts `MAP_WIDTH*y*9` in the SIB index; used only by ADVMGR (`ProcessHover` x3, `ProcessSelect` x1) | OPEN |
| SOURCE/ADVMGR `DoAdvCommand` hover reset / route end tests | `m_lastHoverCell = m_hoverCellY;`, `m_destinationY == m_y`, `m_hoverCellY + m_mapOriginY` | `m_lastHoverCell = CURSOR_INVALID_POSITION;`, `m_y == m_destinationY`, `m_mapOriginY + m_hoverCellY` | OPEN |
| SOURCE/ADVMGR `ProcessHover` visibility / unreachable tests | `giCurPlayerBit & mapExtra[y * MAP_WIDTH + x]`, `!(A && B && (SetPointer(...), 1))`, two trailing `return`s | `*(mapExtra + x + MAP_WIDTH * y) & giCurPlayerBit` (per-site product order: `MAP_WIDTH * y` in the entry guard, `y * MAP_WIDTH` in the COAST/default arms), a plain `if (A && B && C) { SetPointer } else { ... }`, and one shared trailing `return` after the outer if/else | OPEN |
| SOURCE/ADVMGR `DrawCell` stone border / cloud / visibility / monster frame | `(mapY & 3) + BASE`, `(mapY + mapX) & 3`, `m_visibilityMap[mapY*W + mapX]`, `(MONSTER_REVERSED_FRAME - (facingLeft == 0)) + index*9`, `(modulo) + objectIndex + 1`, `m_lastQuickViewX == mapX` | `((mapY + SHIFT) & 3) + BASE`, `(mapX + mapY) & 3`, `*(m_visibilityMap + mapX + mapY*W)`, `index*9 + (facingLeft != 0 ? BASE+1 : BASE)`, `objectIndex + (modulo) + 1` (nine sites), `mapX == m_lastQuickViewX` | OPEN |
| SOURCE/ADVMGR `DrawCell` overlay bracket | `if (mask&OVERLAY && idx!=NONE && drawnAsOverlay && puzzleOk) { A } ; extras loop outside` | `if (mask&OVERLAY && idx!=NONE) { if (drawnAsOverlay && puzzleOk) { A } extras loop }` | OPEN |
| SOURCE/ADVMGR `UpdBottomView` player-colour compare | `gpGame->m_players[giCurPlayer].m_color != gpCurPlayer->m_color` | `gpCurPlayer->m_color != gpGame->m_players[giCurPlayer].m_color` | OPEN |
| SOURCE/ADVMGR `UpdBottomViewHero` army label | `((count <= MAX) - 1 & PAD) + strlen(text) * W`, `blockWidth - 1 + iconX - (countWidth - 1)`, `i32 creatureBoundsLocal[2]` | `strlen(text) * W + (count > MAX ? PAD : 0)` (constant `?:`, `dec` not `sub`), `iconX + blockWidth - 1 - (countWidth - 1)`, two separate dead `i32` locals | OPEN |
| SOURCE/ADVMGR `SetHeroContext` cursor reset / type / guards | `m_cursorMapX = m_cursorMapY;`, `m_previousCursorMapX = m_previousCursorMapY;`, `if (embarked) m_cursorType = BOAT; else m_cursorType = hero->m_cursorType;`, `page + N - 1 < slot`, `m_currentTerrain != (TerrainType)v` | repeated literals, a `?:` for `m_cursorType` (temp past `this`), `slot > page + N - 1`, `(TerrainType)v != m_currentTerrain` | OPEN |
| SOURCE/ADVMGR `CheckDimNextHeroBut` button frame | `if (!net || !HasMobileHero()) frame = ENABLE; else frame = DISABLE;` | `frame = net && gpCurPlayer->HasMobileHero() ? DISABLE : ENABLE;` | OPEN |
| SOURCE/ADVMGR `SummonBoat` adjacency / distance | `normalDirTable[d].x + m_mapOriginX + OFFSET`, `abs(boat.y - hero->m_y) + abs(boat.x - hero->m_x)` | `m_mapOriginX + OFFSET + normalDirTable[d].x` (constant folded into the `lea`), X term first in the distance | OPEN |
| SOURCE/ADVMGR `CPanelHandler` confirmation block | `char confirmation[200]` and `i32 helpIndex` in nested blocks; a `switch` that only fills the buffer, then `if (id==A||id==B||id==C) { handled = 1; ... }` | both locals at function scope (`question`, `helpIndex`), and the three cases fall into one shared label inside the switch: `strcpy(...); goto confirm_reset;` twice plus fall-through from the third | OPEN |
| SOURCE/ADVMGR `SystemOptionsHandler` digital-sound guard / video toggle | `!(IsAudiereBackend(gp) || IsMilesBackend(gp))`, `if (gConfig.slowVideo) gConfig.slowVideo = 0; else gConfig.slowVideo = 1;`, dead `char textData[120]` | `static_cast<bool>(IsAudiereBackend(gp) || IsMilesBackend(gp)) == false` (retail materialises the `||` into a third bool temp; the bare `!` short-circuits straight to the skip), `gConfig.slowVideo = gConfig.slowVideo == 0;`, no buffer | OPEN |
| SOURCE/ADVMGR slot names (7 functions) | PoL names | whole-frame renames solved from od_slots buckets: `DoAdvCommand`, `ProcessHover`, `UpdBottomViewHero`, `SetHeroContext`, `SetEnvironmentOrigin`, `SummonBoat`, `DrawCell` (the last by dropping two named locals into `?:` temps) | OPEN |
| SOURCE/EVENTS `RecruitSiteEvent` / `RecruitSiteAIEvent` / `GenericSiteEvent` / `GenericSiteAIEvent` masked decode | `H2_ENUM_DECODE_MASKED(...)` repo macro | the two statements spelled out (`x = raw; x &= static_cast<Enum>(MASK);`) — the macro's `do{}while(0)` emits a live `xor/test/jne` at /Od, see docs/patterns/do-while-zero-macro-loop-test.md | OPEN (macro artifact, not a dev spelling) |
| SOURCE/EVENTS `EraseObj` / `GenericSiteEvent` cell fetch | `gpGame->m_worldMap.Row(y) + x` | `gpGame->m_worldMap.GetCell(x, y)` (Column form) | OPEN |
| SOURCE/EVENTS `EraseObj` wide-object cell coords | `cellX_b[ERASE_COORDINATE_COUNT]` / `cellY_e[...]` arrays with if/else assignments, `currentCell = i == 0 ? cell : cells[i-1]` | scalars `cellX_l` / `cellY_o` plus an 8-byte dead local, `?:` for the wide-object coords, plain `if/else` for `currentCell` | OPEN |
| SOURCE/EVENTS `EraseObj` wide-object x | `frame_k - 9 + x` | `x + frame_k - 9` (frame loaded first into the lea index) | OPEN |
| SOURCE/EVENTS `SendHeroTownData` owner/gold fields | four `if (hero) { f = ...; } else { f = ...; }` | four `?:` (retail reserves four ternary temps; frame 0x28 vs 0x18) | OPEN |
| SOURCE/EVENTS `GiveTakeArtifactStat` frame | `i32 statChanges[COUNT+1]` with the loop counter folded into `statChanges[COUNT]` | `i32 stats[4]; i32 i; i32 maxSpellPoints;`, and `(take == TAKE ? -1 : 1) * stats[i]` (ternary is the accumulator) | OPEN |
| SOURCE/EVENTS `DoAIEvent` / `DoEvent` chest experience | `(md * 4 - 4) * 125` | `(md - 1) * 500` (`CHEST_EXPERIENCE_MULTIPLIER` retuned to 500) | OPEN |
| SOURCE/EVENTS `DoAIEvent` / `DoEvent` teleport search | `(type == STONE_LITHS ? A : B) < abs(y2 - y) + abs(x2 - x)`, `count > 0` | `abs(x2 - x) + abs(y2 - y) > (type == STONE_LITHS ? A : B)`, `count >= 1` | OPEN |
| SOURCE/EVENTS `DoAIEvent` / `DoEvent` teleport trigger compare | `cell->m_triggerType == (eventType \| MAP_TRIGGER_ACTION_FLAG)` | `== static_cast<u8>(eventType \| MAP_TRIGGER_ACTION_FLAG)` (retail masks the RHS with 0xff) | OPEN |
| SOURCE/EVENTS `DoAIEvent` whirlpool | `DoWhirlpool(hero); goto teleportEvent;` with a label on the STONE_LITHS case | fallthrough (no goto, no label) | OPEN |
| SOURCE/EVENTS `DoAIEvent` resource/artifact index | `cell->m_objectIndex >> 1` | `cell->m_objectIndex / 2` (signed-division `cltd/sub/sar`) | OPEN |
| SOURCE/EVENTS `DoAIEvent` dwelling metadata | `cell->m_objectData = (u16)((md - count) << 3 \| (objectData & 7))` | `cell->m_objectMetadata -= count;` (bitfield compound assignment, 16-bit throughout) | OPEN |
| SOURCE/EVENTS `DoEvent` visit-flag sites (fort, standing stones, witch doctor, mercenary camp, gazebo, xanadu) and witch hut | `if (!(visits & bit)) { reward } else { already-visited message }` | early-out guard: `if (visits & bit) { message; break; } reward` | OPEN |
| SOURCE/EVENTS `DoEvent` hill fort / freeman's foundry | `if (first == NONE) { none-msg } else { ... }` and nested `third == NONE` / `second == NONE` | `if (first != NONE) { ... } else { none-msg }` and `if (third != NONE) ... else if (second != NONE) ... else ...` | OPEN |
| SOURCE/EVENTS `DoEvent` treasure chest | `if (!(md & ARTIFACT_FLAG)) { gold } else { artifact }` with the `chestGold:` label on the case | `if (md & ARTIFACT_FLAG) { artifact } else { chestGold: gold }` | OPEN |
| SOURCE/EVENTS `TownEvent` occupying hero | `town->m_occupyingHeroId = gpCurPlayer->CurrentHero();` (i8 accessor) | `gpCurPlayer->m_currentHero` today; retail needs an `int`-returning `CurrentHero()` — see docs/patterns/int-accessor-for-narrow-store.md | OPEN - blocked on a shared-header change |
| SOURCE/KB `ShowMoraleInfo` / `ShowLuckInfo` modifier test | `static_cast<i32>(strlen(gText)) == modifierStart` | `modifierStart == static_cast<i32>(strlen(gText))` - retail emits `cmp [ebp-0x8],eax` (`39`), the local as the LEFT relational operand; one row each, both functions now EXACT | OPEN |
| SOURCE/ADVMGR `SystemOptionsHandler` forward-message fields | `message.payload.widget.command = static_cast<BaseWidgetCommand>(message.payload.widget.id);` | each field takes its own literal: `... = static_cast<BaseWidgetCommand>(IDX(SYSTEM_OPTION_FIRST));` (retail stores `0xa` twice and never reloads `.id`) | OPEN |
| SOURCE/ADVMGR `ComboDraw` defer/limit comparisons | `originX + drawX == giDeferObjDrawX`, `originY + drawY == giDeferObjDrawY (+ 1)`, `giLimitUpdMaxX < drawX`, `giLimitUpdMinY > drawY`, `giLimitUpdMaxY < drawY`, `giLimitUpdMaxX < giLimitUpdMinX \|\| giLimitUpdMaxY < giLimitUpdMinY` | global-first in the defer tests (`giDeferObjDrawX == originX + drawX`, `giDeferObjDrawY + 1 == originY + drawY`), loop-variable-first in the limit tests (`drawX > giLimitUpdMaxX`, `drawY < giLimitUpdMinY`, `drawY > giLimitUpdMaxY`), min-first in the collapse test | OPEN |
| SOURCE/ADVMGR `ComboDraw` cull-pass subscript | `bComboDraw[0][drawY + drawX * COMBO_GRID_CELLS]` | `bComboDraw[drawX][drawY]` - the flat form makes `drawY` the SIB base (`[ecx+eax]`); the two-subscript form keeps the scaled row there (`[eax+ecx]`), matching the other three reads in the same block | OPEN |
| SOURCE/TOWNMGR `townObject::townObject` slots | `fileName`, `buildingId_h` | `name`, `id_h` - retail's order is w,h,x,tempY,y,name,id_h; only bucket 13 for the buffer and a bucket above it for the id holder produce it (od_slots predicted the frame exactly on the first try, second confirmed VC6 slot-hash prediction) | OPEN as slot-name class |
| `playerData` narrow inline accessors (7) | `i8 CurrentTown/Color/HeroCount/TownCount/HeroId/TownId/AvailableHeroId` | `i32` return on all seven; the four with call sites are byte-pinned (an `i8` return spills a `/Ob1` return temp retail does not have), the other three carry no call site and are unproven family consistency | OPEN - the three call-site-free members need 2.0 evidence |
| SOURCE/ADVMGR `Main` town hot-key compare | `TownId(i) == m_currentTown` | `CurrentTown() == m_townIds[cheatDigitLocal]` - the inlined accessor must sit on the LEFT so the subscript is evaluated first (see patterns/inline-call-operand-index-first) | OPEN |
| SOURCE/Castle `SetupCastle` | `m_buildableBuildings = 0; m_affordableBuildings = m_buildableBuildings;`, `(t - 1) * TERRAIN_ICON_COLUMNS * TERRAIN_ICON_FRAMES`, PoL local names | `m_affordableBuildings = m_buildableBuildings = 0;`, `(t - 1) * (TERRAIN_ICON_COLUMNS * TERRAIN_ICON_FRAMES)` (VC6 does not reassociate `(e*5)*2`, retail folds to `imul 0xa`), 17-local rename solved from od_slots buckets | OPEN |
| SOURCE/Newgame `GetMap` frame | `char savedName[44]` | `char savedName[16]` (the gMapName width - retail's base address proves it) plus a declared-but-unreferenced 28-byte local above it; type/name (`tag_message dlgMessage`) is INFERRED, only size 28 and od_slots bucket 15 are byte-proven | OPEN |
| SOURCE/REMOTE `SendRemoteData` frame | `u8 remotePacket[REMOTE_MESSAGE_SIZE + sizeof(i32)]` | `u8 remotePacket[REMOTE_MESSAGE_SIZE]` plus a separate declared-but-unreferenced `i32` between `out` and `sendStatus`; name (`retry`) is INFERRED, only the bucket-3 slot is byte-proven | OPEN |
| slot-solve batch 5 (17 functions) | PoL names | whole-frame renames solved from od_slots buckets: ARMY `PowEffect`, DRAWING `UpdateGrid`/`UpdateMouseGrid`, X_CAMPGN `InitMap`, SWAPMGR `Open`/`SplitMons`/`SwapArtifacts`, Overview `DoKnob`, REQUEST `Open`, SPELLAI `DoSpellAI`, CURSOR `DrawCursorShadow`/`ValidMoveWithEvent`, wingraph `DDCreateSurface`/`DDSetFullScreenStatus`/`SetGraphicsType`/`WGUpdatePalette`, SETUP `PickLoadGame`, EVENTS `TownEvent`/`PasswordEvent`/`FizzleCenter`, HERO `UpdateArmies`/`DoHeroSplit`, AI `WalkTowardArmy`/`AttemptAdjacentAttack`, Netbios `InitNetHost`/`InitNetGuest`, Modem `Connect` | OPEN as slot-name class |
| scope hoists for the /Od group order (5) | inner-block declarations | `swapManager::Open` and `SwapArtifacts` hoist `skillWidget`/`slotSkill` to the block that owns `message`; `fileRequester::Open` hoists `dot` to function scope; `Netbios::InitNetGuest` hoists `namePending` (renamed `unregistered`) out of the case block; `SendRemoteData` reorders `size/out/sendStatus` | OPEN |
| relational/additive operand sides (4 sites) | `m_creatureTypes[sourceSlot] == ...[destinationSlot]`; `(ternary) + shadowPic`; `maximumDamageFrames > frameTotal ? ...`; `scrollRange + topValue`, `topBefore != topNow` | destination-first, `shadowPic + (ternary)`, `frameTotal > maximumDamageFrames ? frameTotal : ...`, `topValue + scrollRange`, `topNow != topBefore` | OPEN as operand-side class |
| SOURCE/GAME `GenerateStandardFileName` frame | `extension` / `sourceLength` / `c` (char) | `ext` / `length` / `chr` (u8) - the fold reads with `and reg,0xff`, and `chr = chr - 0x20` (not `-=`) to keep the int-width compound | OPEN |
| SOURCE/GAME `GiveTroopsToNeutralTown` switch selector | `m_castleRecs[id].m_type + tierKey` | `tierBase + m_castleRecs[id].m_type` (tier key is the accumulator) | OPEN |
| SOURCE/GAME slot names (10 functions) | PoL names | whole-frame renames solved from od_slots buckets, then reordered so the `= constant` stores match retail: `RandomizeEvents`, `ClaimMine`, `UpdateSpellWidgets`, `ViewArmy`, `ViewArmyHandler`, `PerWeek`, `GetRandomArtifactId`, `SetRandomHeroArmies`, `ProcessRandomObjects`, `SetupNewRumour`, `CountShrines`, `SetupAdjacentMons` | OPEN |
| SOURCE/GAME `RandomizeEvents` shrine/pyramid spell pick | `GetRandomEventSpell(level)` inline helper | the `Random(...)+1` / `while (gsSpellInfo[...].level != level)` loop written out in each of the four cases, writing straight into `cell->m_objectMetadata` (retail has no helper param slot) | OPEN |
| SOURCE/GAME `RandomizeEvents` monster guard predicate | `CanGenerateMonsterGuard(monsterType)` inline helper + cached `monsterType` | five `cell2->m_objectIndex != CREATURE_*` re-reads in one `&&` chain (retail spills no parameter) | OPEN |
| SOURCE/GAME `RandomizeEvents` resource/artifact index | `cell->m_objectIndex >> 1` and a `resourceType` local | `cell->m_objectIndex / 2` (signed `cdq/sub/sar`) written straight into the bitfield, and the inner `switch` on `cell2->m_objectMetadata` | OPEN |
| SOURCE/GAME `RandomizeEvents` town-object range test | `IsTownObjectTileset(a) && IsTownObjectTileset(b)` inline helper | four direct `>= TILESET_OBJNTOWN` / `<= TILESET_OBJNTWRD` comparisons (the helper spills a parameter slot) | OPEN |
| SOURCE/GAME `RandomizeEvents` boat coords | `townRec->m_boatY = -1; townRec->m_boatX = townRec->m_boatY;` | two independent `-1` stores | OPEN |
| SOURCE/GAME `RandomizeEvents` skeleton / wagon-camp cases | if/else-if with a separate `SKELETON_EMPTY` arm; `if/else` for the wagon camp | `Random(..) <= ROLL_MAX ? GetRandomArtifactId(..)+2 : SKELETON_EMPTY` ternary, and a `break`-terminated guard for the wagon camp (its then-arm jumps to the switch end, not over the else) | OPEN |
| SOURCE/GAME `PerWeek` available-hero index | `(inner - outer + outer*(sizeof(playerData)+1))[gpGame->m_players[0].m_availableHeroIds]` (6 sites) | `gpGame->m_players[outer].m_availableHeroIds[inner]` | OPEN |
| SOURCE/GAME `PerWeek` / `ProcessRandomObjects` / `InitRandomArtifacts` loop headers | `MAP_HEIGHT > y` / `MAP_WIDTH > x` | `y < MAP_HEIGHT` / `x < MAP_WIDTH` (retail loads the counter into the accumulator) | OPEN |
| SOURCE/GAME `PerWeek` cell fetch | `WORLDMAP->Row(y)[x]` | `WORLDMAP->GetCell(x, y)` | OPEN |
| SOURCE/GAME `ViewArmy` facing modifier | `facing == ARMY_FACING_RIGHT ? -1 : 1` | `if/else` (the ternary lowers branchlessly through `neg/sbb/and/add`) | OPEN |
| SOURCE/GAME `ViewArmy` monster origin | `(w/2) * viewArmyFacingWIPXMod`, `walkX[0] * mod` | `viewArmyFacingWIPXMod * (w/2)` and `... + viewArmyFacingWIPXMod * walkX[0]` (mod is the accumulator) | OPEN |
| SOURCE/GAME `ViewArmy` spell row X | `X_BIAS - count + spellCenterX - (count*spacing)/2` | `spellCenterX + (X_BIAS - count) - ...` (centre is the accumulator; this is what forces retail's `push esi`) | OPEN |
| SOURCE/GAME `ViewArmyHandler` close message | `command = BaseWidgetCommand(msg.payload.widget.id)` | `BaseWidgetCommand(VIEW_ARMY_CLOSE_ID)` (retail stores the literal twice, it does not re-read `id`) | OPEN |
| SOURCE/GAME `ViewArmyHandler` upgrade cost | `(costUp - costBase) * numTroops * 2` | `numTroops * 2 * (costUp - costBase)` | OPEN |
| SOURCE/GAME `SetRandomHeroArmies` first-stack roll | `CHANCE + bonus > Random(0,100)` | `Random(0,100) < CHANCE + bonus` | OPEN |
| SOURCE/GAME `SetupNewRumour` frame | dead `char rumourBuffer[..]` scratch | no scratch buffer (retail's frame is 0x3c, ours was 0xa0), and `categoryStats[0] != categoryStats[1]` | OPEN |
| SOURCE/GAME `SetupNewRumour` ultimate-artifact direction tests | `IDX(m_mapHeader.width) * 0.33 <= m_ultimateArtifactX` | `m_ultimateArtifactX >= IDX(m_mapHeader.width) * 0.33` (x87 pushes the left operand first) | OPEN |
| SOURCE/GAME `CountShrines` / `SetupAdjacentMons` scoping | inner `{ }` block around the loop locals | all locals at function scope (retail puts the loop counters on the shallowest slots) | OPEN |
| SOURCE/GAME `InitRandomArtifacts` artifact index | `cell->m_objectIndex >> 1` | `cell->m_objectIndex / 2` | OPEN |
| SOURCE/GAME `GetRandomArtifactId` loop head | `while (1)` | `for (;;)` - retail emits no head test here | OPEN |
| SOURCE/GAME `GetRandomArtifactId` level mask | `HAS(levelMask, gArtifactLevel[artifact])` | `HAS(gArtifactLevel[artifact], levelMask)` | OPEN |
| SOURCE/X_CAMPGN `MessageHandler` timeout test + close-command fields | `giDialogTimeout < KBTickCount()`; `BaseWidgetCommand(message.payload.widget.id)` (x2) | `KBTickCount() > giDialogTimeout` (call result as the LEFT relational operand); `BaseWidgetCommand(CAMPAIGN_CLOSE_COMMAND)` - retail stores the literal `0xa` twice and never reloads `.id` (same class as the ADVMGR `SystemOptionsHandler` row) | OPEN |
| SOURCE/X_CAMPGN `InitMap` / `UpdateInfo` / `ShowInfo` slots | `campaignChoice`, `choiceHero`, `heroPosition`, `award`; `map`, `armyName8`, `unusedCampaignData`; `map`, `campaignIcon` | `bonus`, `pHero`, `heroSlot`, `whichAward`; `i`, `armyName` (declared 2nd, before `hasVisibleAward`), no dead array; `mapIndex`, `campIcon`. All three frames now reproduce retail slot-for-slot; `unusedCampaignData` was a fabricated 0x14-byte pad | OPEN as slot-name class |
| SOURCE/X_CAMPGN `UpdateInfo` bonus-choice address | `xCampaignChoices[0][viewMap] + campaignId * MAX_MAP * BONUS + i` | `&xCampaignChoices[campaignId][viewMap][i]` - retail's imul order is `campaignId * 0x78` then `viewMap * 0xf`, the plain 3-D subscript | OPEN |
| SOURCE/X_CAMPGN `UpdateInfo` selected-track test | `IDX(m_viewMap) == i` | `i == IDX(m_viewMap)` (loop counter as the LEFT operand) | OPEN |
| BASE/INPUTMGR `GetEvent` / `PeekEvent` guard polarity | `if (active == 1 && read != write) { dequeue } else { empty }` | `if (active != 1 \|\| read == write) { empty } else { dequeue }` - retail's second test is `jne <then>`, the `\|\|` shape, with the empty-event arm laid out first | OPEN |
| BASE/INPUTMGR empty-event initialiser | `type = NONE; unknown0x08 = 0; unknown0x04 = 0; unknown0x0c = 0;` | `type = NONE; widget.id = 0; widget.command = BaseWidgetCommand(widget.id); widget.parameter = IDX(widget.command);` - retail reloads each freshly written field into the next store; a chained `a = b = c = 0` constant-folds under VC6 and does NOT reproduce it | OPEN |
| BASE/INPUTMGR ring-wrap compare (3 sites) | `m_writeIndex == m_readIndex` | `m_readIndex == m_writeIndex` in KeyboardMessageHandler, MouseMessageHandler and ForceMouseMove (read index as the LEFT operand); MouseMessageHandler also drops its `readIndex` copy local | OPEN |
| BASE/INPUTMGR `MakeScanCodeTable` encode helper | `static inline u32 EncodeScanCode(u32)` | a macro - retail folds every constant call site to an immediate (`mov word ptr [..], 0x1d00`), which an inline function never does at /Od while still emitting `shl` for the loop's variable call | OPEN |
| BASE/INPUTMGR `MouseMessageHandler` switch subject | `switch (message - WM_MOUSEMOVE)` with `case WM_x - WM_MOUSEMOVE` | `switch (message)` with plain `case WM_x` - retail stores the subject and THEN subtracts 0x200 (two stores), and the case bodies are laid out MOUSEMOVE, LBUTTONDBLCLK, LBUTTONDOWN, RBUTTONDOWN, RBUTTONDBLCLK, LBUTTONUP, RBUTTONUP | OPEN |
| BASE/INPUTMGR `KeyboardMessageHandler` modifier case order | CONTROL, LEFT_SHIFT, RIGHT_SHIFT, ALT | CONTROL, ALT, LEFT_SHIFT, RIGHT_SHIFT (both inner switches; jump-table target order) | OPEN |
| SOURCE/KB `NormalDialog` EXPERIENCE sizing arm | `if (value == NO_VALUE) h = EXP_H; else h = EXP_H + LABEL_H;` | `h = value == NO_VALUE ? EXP_H : EXP_H + LABEL_H;` - retail's arm is the branchless `sub $-1/neg/sbb/and $0xc/add $0x40` select, which the if/else never produces | OPEN |
| SOURCE/KB `NormalDialog` MONSTER/PRIMARY_SKILL sizing cases | one shared `case MONSTER: case PRIMARY_SKILL:` arm | two separate cases with the same body (MONSTER first) - retail's jump table has 15 arms with distinct entries for 18 and 25 | OPEN |
| SOURCE/KB `NormalDialog` max-icon accumulate | `if (maxIconHeight < sizingIconHeight)` | `if (sizingIconHeight > maxIconHeight)` | OPEN |
| SOURCE/KB `NormalDialog` icon-area test | `if (maxIconHeight)` | `if (maxIconHeight > 0)` | OPEN |
| SOURCE/KB `NormalDialog` window clamp sums | `windowX + windowWidth`, `windowY + windowHeight` | `windowWidth + windowX`, `windowHeight + windowY` | OPEN |
| SOURCE/KB `NormalDialog` resource-type branch | `type >= RESOURCE_FIRST && type <= RESOURCE_LAST` | `type <= RESOURCE_LAST` only (the `!= NO_RESOURCE` break above already excludes -1) | OPEN |
| SOURCE/KB `NormalDialog` resource-amount chain | `if (v < 1) { if (v == 0) ... else if ... else ... } else sprintf("%d")` | `if (v > 0) sprintf("%d"); else if (v == 0) ...; else if (v < -DAILY) ...; else ...` | OPEN |
| SOURCE/KB `NormalDialog` expmrl branch test | `type >= EXPMRL_FIRST && type <= EXPMRL_LAST` | five `==` tests in the order EXPERIENCE, MORALE_BONUS, MORALE_PENALTY, LUCK_BONUS, LUCK_PENALTY | OPEN |
| SOURCE/KB `NormalDialog` icon-size switch order | RESOURCES, GOLD, ARTIFACT, SPELL, CREST, LUCK+/-, MORALE+/-, EXPERIENCE, HERO, SECONDARY, MONSTER+PRIMARY | PRIMARY_SKILL, ARTIFACT, LUCK+, LUCK-, MORALE+, MORALE-, EXPERIENCE, CREST, HERO, GOLD, RESOURCES, SPELL, SECONDARY, MONSTER (jump-table arm addresses) | OPEN |
| SOURCE/KB `NormalDialog` label-present test | `if (strlen(text))` | `if (strlen(text) > 0)` - retail's `test eax,eax` is followed by `jbe`, the unsigned form | OPEN |
| SOURCE/KB `NormalDialog` first-slot centre | `if (...) centre = A; else centre = B;` | `centre = cond ? A : B;` - retail routes both arms through one compiler temp | OPEN |
| SOURCE/KB `NormalDialog` spell x-nudge | `+ (type == SPELL) * SPELL_X_OFFSET` | `+ (type == SPELL ? SPELL_X_OFFSET : 0)` - retail is `setne/dec/and $2`, not `sete/and $0xff/lea (,,2)` | OPEN |
| SOURCE/KB `NormalDialog` label Y sums (5 sites) | `sizingIconHeight + resourceY - K` | `resourceY + sizingIconHeight - K` | OPEN |
| SOURCE/KB `NormalDialog` secondary-skill label height | `RESOURCE_LABEL_HEIGHT` | the same `type == SPELL ? SPELL_LABEL_HEIGHT : RESOURCE_LABEL_HEIGHT` ternary used by the main label | OPEN |
| SOURCE/KB `NormalDialog` or-text x | `NormalDialogCenterOffset(windowWidth) - K` | `windowWidth / CENTER_PART_COUNT - K` - the inline call in that argument list reorders the sibling `textWidgetId++` narrowing temp | OPEN |
| SOURCE/KB `NormalDialog` or-text buffer size | `OR_TEXT_LENGTH = 3` | `4` (the three-byte string plus its NUL) | OPEN |
| BASE/soundmgr `ConvertVolume` scale product | `(FADE_TOTAL_STEPS - configVolume) * volume` | `volume * (FADE_TOTAL_STEPS - configVolume)` - retail loads `volume` into a register for the `imul`, which only the volume-first spelling produces | OPEN |
| BASE/soundmgr `SwitchAmbientMusic` track compares | `m_musicFadeTargetTrack != track`, `m_musicTrack != track` | `track != m_musicFadeTargetTrack`, `track != m_musicTrack` (parameter as the LEFT operand; the earlier `m_musicTrack == track` guard keeps the member on the left) | OPEN |
| BASE/soundmgr `SwitchAmbientMusic` fade-restart test | `m_musicFadeSteps < FADE_TOTAL_STEPS` | `m_musicFadeSteps <= FADE_HOLD_STEPS` - retail compares against 10 with `jg`, not against 11 with `jge` | OPEN |
| BASE/soundmgr `StartupMilesBackend` sample-format local | `bitsPerSample` | `bits` (frame slot order needs bucket(bits)=5 < bucket(sampleRate)=6) | OPEN |
| BASE/soundmgr `Open` key-state local | `keyState` | `asyncState` (frame slot order needs musicTrack ahead of it) | OPEN |
| SOURCE/KB `NormalDialog` local names + declaration order | 35 locals with `_<letter>` disambiguating suffixes, declared in an ad-hoc order | 12 of them recover their plain form; the rest keep a (re-solved) suffix, and the whole block is declared in the reverse of the retail frame order. Retail's frame is `panelHeight -0x10 ... imageHeight -0xc8`; the `/Od` name-hash buckets must be non-decreasing down that order, which `homm2/core/od_slots.py` reproduces exactly on VC6 (35/35 predicted offsets before the change). The devs' real names are unknown; any name set with the same bucket sequence matches | OPEN |
| fileRequester::InitializeFiles filter guards | four separate `if (...) goto CountNextFile;` statements | one `if (A \|\| B \|\| (C && D)) goto CountNextFile;` OR-chain plus a separate `if (!ShowThisMapGame(...)) goto ...;`, with `header.maxHumanPlayers < giNumHumanPlayers` and `header.width != giMapSizes[...]` (member on the LEFT at both sites) | OPEN |
| fileRequester::InitializeFiles insert-position search | `break` out of the ordering loop | `goto InsertName;` to a label on the following `strcpy` - retail routes it through a parked 5-byte stub (`goto-label-jump-stub`) | OPEN |
| fileRequester::DoKnob event fetch | `tag_message dragMessage = gpInputManager->GetEvent();` | declared then assigned (`knobMessage = gpInputManager->GetEvent();`), which leaves the `rep movsl` sret copy and a SECOND 28-byte compiler temp in the frame | OPEN |
| fileRequester::DoKnob upper clamp | `mouseOffset + travel + minY < (float)msg.y` | `(float)knobMessage.payload.mouse.y > knobOffset + fGutterTravelLength + fGutterMinY` - the message term is pushed FIRST and the mask becomes `testb $0x1` / `je` | OPEN |
| fileRequester::Update knob position | `m_topIndex * gutterStep + fGutterMinY` | `fGutterMinY + m_topIndex * gutterStepCount1` (the constant is `fld`-ed first, then `faddp`) | OPEN |
| fileRequester::Update list loops | `iMaxListSize > i`, `m_topIndex + i == m_selectedIndex`, `IDX(giMapSizeFilter) == i` | `i < iMaxListSize`, `m_selectedIndex == m_topIndex + i`, `i == IDX(giMapSizeFilter)` | OPEN |
| fileRequester::Main list hit test | if/else-if chain over the five widget-id ranges, then `if/else if` on the selection | five independent `if (...) { ...; goto SelectListItem; }` guards (five parked stubs) and `if (...) { ...; break; } if (iResult + m_topIndex >= m_fileCount) break;` | OPEN |
| fileRequester::Main OK / filter cases | `if (cond) { NormalDialog(...); } else { ... }` | `if (cond) { NormalDialog(...); break; }` then the else body at case level (`switch-case-early-break`) | OPEN |
| combatManager::CombatMessage(char*) expiry guard | `KBTickCount() < m_combatMessageExpiration` | `m_combatMessageExpiration > KBTickCount()` (member on the LEFT) | OPEN |
| combatManager::DrawFrame moat tests | `moatCell[row] == giWalkingTo \|\| ...`, `(from/13 > to/13 ? from/13 : to/13) != row`, `giWalkingFrom <= giWalkingTo` | globals on the LEFT in the four-term `\|\|` (`giWalkingTo == moatCell[IDX(row)]`), `IDX(row) != (to/13 > from/13 ? to/13 : from/13)`, and `if (giWalkingTo < giWalkingFrom) { ... } else { ... }` instead of the `goto checkWalkingTo` form | OPEN |
| combatManager::DrawFrame castle-hex index | `hexIndex6` computed once before the `if (m_inCastleCombat ...)` and reused in the DrawOccupant guard | assigned as the FIRST statement inside the guard, and the guard re-computes `IDX(row) * COMBAT_GRID_ROW_LENGTH + column1` twice longhand | OPEN |
| combatManager::DrawSmallView position selects | `side == ATTACKER ? LEFT_X : RIGHT_X` ternaries (4 sites) | plain `if/else` stores; the ternary form emits `neg/sbb/and/add`, retail emits `cmp/jne/mov/jmp/mov` | OPEN |
| combatManager::DrawSmallView extent maths | `m_smallViewWidth[i] + m_smallViewLastX[i] - 1` | `m_smallViewLastX[i] + m_smallViewWidth[i] - 1` (both axes; `lea edx,[eax+ecx-1]` with lastX as the SIB base) | OPEN |
| combatManager::ResetLimitCreature mirror flag | `if (HAS(...)) c = -1; else c = 0;` | `c = HAS(...) != 0 ? -1 : 0;` (`neg/sbb` with no mask) | OPEN |
| combatManager::ClearCombatMessages / CombatMessage expiry clears | `m_previousCombatMessageExpiration = 0; m_combatMessageExpiration = m_previousCombatMessageExpiration;` | two separate literal zeroes (`chained-assignment-vs-repeated-literal`) | OPEN |
| fileRequester::SetOK command/flag selects | four `if/else` pairs of stores | four `?:` (`enabled ? CLEAR : SET`, `m_active == 1 ? DIMMED : GRAYED`, `enabled ? SET : CLEAR`) - VC6 folds them to `setne/add`, `sub/neg/sbb/and/add` and `neg/sbb/add` | OPEN |
| BASE/TEXTWDGT `SetText` guard | `if (kind != TEXT && kind != TEXT_ENTRY) { m_text = text; return; }` | `if (kind == TEXT \|\| kind == TEXT_ENTRY) { ... } else { m_text = text; }` and `newLen > strlen(m_text)` (the `u16` local as the LEFT relational operand) | OPEN |
| BASE/TEXTWDGT `Main` / BASE/droplist `Main` case order | button-down, button-up, widget | `case MESSAGE_WIDGET` FIRST, then the button cases (body layout order) | OPEN |
| BASE/TEXTWDGT `Main` id test | `if (m_id != msg.payload.widget.id) goto normalEvent;` | `if (msg.payload.widget.id == m_id) { ...; return CONSUME; } break;` - retail's `cmp mem,reg` puts `.id` on the left, and the case falls out through the switch's own break | OPEN |
| BASE/TEXTWDGT `Main` relative coords | `i32 relativeX` | `i16 x` (retail stores the difference with `movw`) and an `&&` bounds chain, not an `\|\|` early return | OPEN |
| BASE/TEXTWDGT `Draw` colour | `FontDrawMode color = DIMMED; if (!HAS(...)) color = m_color;` | `HAS(m_flags, WIDGET_FLAG_DIMMED) ? FONT_DRAW_DIMMED : m_color` inline in the call - the temp slot sits DEEPER than the `this` spill, so it is a `?:`, not a named local | OPEN |
| BASE/TEXTWDGT `Draw` / BASE/droplist `Draw` screen coords | `m_x + m_owner->m_posX` | `m_owner->m_posX + m_x` (owner field is the accumulator) | OPEN |
| BASE/TEXTWDGT `Read` tail | `m_id = ReadWord(); ReadWord(); m_kind = WIDGET_KIND_TEXT;` | `m_id = ReadWord(); m_kind = ReadWord(); m_kind = WIDGET_KIND_TEXT;` - the discarded word IS stored to `m_kind` first; also `m_alignment = ReadWord() & 0xff` | OPEN |
| BASE/TEXTWDGT ctor member order | color, font, text, alignment, kind | font, text, color, alignment, kind | OPEN |
| BASE/EXEC `AddManager` priority default | `priority = tail == NULL ? 0 : tail->m_priority + 1;` | an `if`/`else` pair of stores into the parameter slot (a `?:` would spend a temp slot) | OPEN |
| BASE/EXEC `AddManager` search loop | `if (cur != NULL) do { if (cur->m_priority <= priority) break; ... } while (cur);` | `while (cur != NULL && cur->m_priority > priority) cur = cur->m_prev;` - retail's `jle` goes straight to the loop exit, with no `break` stub | OPEN |
| BASE/EXEC `AddManager` tail-insert order | `mgr->m_next = NULL; mgr->m_prev = m_managerListTail;` | `mgr->m_prev = m_managerListTail; mgr->m_next = NULL;` | OPEN |
| BASE/EXEC `CallManager` remove argument | `RemoveManager(saved);` | `RemoveManager(m_activeManager);` - retail re-reads the member after saving it | OPEN |
| BASE/EXEC `PrintManagerList` / `DoDialog` list walks | `for (m = head; m; m = m->m_next)` | `m = head; while (m != NULL) { ...; m = m->m_next; }` (retail's test is at the top with the advance in the body) | OPEN |
| BASE/EXEC `MainLoop` shape | `if (head != NULL) { ... do { ... } while (!done); }`, locals `keepDispatching`/`result`/`manager` | `if (m_managerListHead == NULL) return;` then `while (!done)`, inner `while (m_activeManager != NULL && dispatch && !done)`, no `result`/`manager` locals (both are compiler temps / direct member reads) | OPEN |
| BASE/EXEC `MainLoop` executive command order | TERMINATE_LOOP, REMOVE_MANAGER, RETURN_RESULT | TERMINATE_LOOP, RETURN_RESULT, REMOVE_MANAGER (body layout order) | OPEN |
| BASE/Misc `MAKEFILEID` | hashes `text` in place | `strcpy(buf, text)` into a local `char buf[0x160]` first, then uppercases and hashes the COPY; `fileId = (fileId << 5) + (fileId >> 25)` in one statement | OPEN |
| BASE/Misc `FadeToColorTable` | pointer-walk `do/while` over the palette, `i32 rows/columns` counters | three `tempPal[i*3+c] = pal[colorTable[i]*3+c]` statements in one `for (i = 0; i < 256; ++i)`, and plain `for (y..) for (x..)` screen loops | OPEN |
| BASE/droplist `Main` id test | `if (m_id == message.payload.widget.id) { ... }` | `if (message.payload.widget.id != m_id) break;` (early-exit guard; retail's `je` skips a 5-byte `jmp` to the switch end) | OPEN |
| BASE/droplist `Main` append/replace locals | `char* text` declared inside each case block | `char* text; char** newItems;` at function scope (retail gives the two cases ONE `text` slot) | OPEN |
| BASE/droplist `DrawDropStuff` middle-row item | `i32 item = m_topIndex + i; m_items[item]` | `*(m_items + m_topIndex + i)` - retail leas `m_items + m_topIndex*4` and then adds `i*4` | OPEN |
| BASE/droplist `DrawDropStuff` row loop | `while (i < m_visibleItemCount - 1 && m_topIndex + i < m_itemCount)` | `for (i = 1; i < m_visibleItemCount - 1; i++) { if (m_topIndex + i >= m_itemCount) break; ... }` | OPEN |
| BASE/droplist `ProcessSelectDialog` loop head and exits | `for (;;)` with `break` | `while (1)` with two `goto closeList;` (the loop exits are 2-byte `jmp` stubs parked after the epilogue-bound code, in reverse source order) | OPEN |
| BASE/droplist `ProcessSelectDialog` visible-count | `if (scrollRange > 0) ... else { if (n <= 3) n = 3; ... }` | one nested `?:`: `m_scrollRange > 0 ? m_maxVisibleItems : (m_itemCount > 3 ? m_itemCount : 3)` | OPEN |
| BASE/droplist `ProcessSelectDialog` scroll metrics | `i16 scrollWidth[2]`, `scrollTopHeight[2]`, `scrollBottomWidth[2]`, `scrollBottomHeight` scratch | no scratch at all: the members are assigned from `entry->w`/`entry->h` and then read back as members (retail's frame is 0x68 with a single `IconEntry* entry`) | OPEN |
| SOURCE/HERO `CalcMobility` / `Deallocate` / `CheckLevel` / `HeroHandler` / `SetupHeroView` slots | `mobilityResult`, `landMobility`, `seaBaseMobilityCurrent`, ...; `player`, `index`, `mapCellRecord`; `line`, `indexValue`, `currentLevelIndex`; `quickView0`, `armySlot7`, `dialogMessage`; `message`, `index`, `cannotDismiss` | `movePoints`, `mobilityTable`, `seaBaseMobility`, `lighthousePoints`, `astrolabeMobility`, `nomadBootsBonus`, `creatureIndex`; `playerPtr`, `i`, `oldOwner`, `heroNum`, `curTown`, `availSlot`, `map`, plus no `mapCellRecord`; `text`, `slot`, `nLevel`, `samp`, `choices`, `weight`, `rnd`, `skill`, `tryCount`; `tmp`, `armySlot`, `bExit`, `newMsg` + two dead ints `dummy`/`j`; `msg`, `i`, `bNoDismiss`, `heroLuck`, `moraleValue`, `tempBonus`, `statusMsg`. All five frames now reproduce retail slot-for-slot | OPEN as slot-name class |
| SOURCE/HERO `Deallocate` map write | `mapCellRecord = gpGame->m_worldMap.cells + gpGame->m_worldMap.width * m_y + m_x; mapCellRecord->m_flags &= ~F;` | `fullMap* map = &gpGame->m_worldMap; map->GetCell(m_x, m_y)->m_flags &= ~F;` - retail keeps a `fullMap*` local (3 refs) and a compiler lvalue temp BELOW the `this` spill; same class as the ADVMGR `MoveOrigin` row | OPEN |
| SOURCE/HERO `Deallocate` compares + destination reset | `m_owner != giCurPlayer`, `m_owner == giCurPlayer`, `giCurPlayer == heroOwner`; `m_destinationY = NONE; m_destinationX = m_destinationY;` | `giCurPlayer != m_owner`, `giCurPlayer == m_owner`, `oldOwner == giCurPlayer`; two literals | OPEN |
| SOURCE/HERO `ApplyBattleWinTemps` mermaid arm | `m_luck = m_luck - 1;` | `m_luck--;` (retail's `movb`/`subb` narrow arithmetic, not `movsbl`/`subl`) | OPEN |
| SOURCE/HERO `CheckLevel` shape | `if (newLevel == m_level) { } else { body }`; `if (slot==0 && ...) { choices[slot]=WISDOM; } else { search }`; `m_primaryStats[slot] = m_primaryStats[slot] + statBonuses[slot];` | early `return`; `continue` after the WISDOM store; `m_primaryStats[slot] += static_cast<i8>(statBonuses[slot]);` | OPEN |
| SOURCE/HERO `CheckLevel` dialog argument order | `IDX(m_secondarySkills[...]) + IDX(choices[n]) * STRIDE` (x3); two-skill sprintf as (level, skill, level, skill) | `IDX(choices[n]) * STRIDE + IDX(m_secondarySkills[...])`; two-skill sprintf as (skill, level, skill, level) while the ONE-skill sprintf keeps (level, skill) | OPEN |
| SOURCE/HERO secondary-skill row dispatch (`UpdateHeroScreenStatusBar`, `HeroHandler`) | `>= ROWn_FIRST && <= ROWn_LAST` chain ending in `goto default_hero_text;` | `>= ROWn_FIRST && < ROW(n+1)_FIRST` guards, the third one wrapping the shared body with the `goto` label INSIDE its then-arm and no default label at all | OPEN |
| SOURCE/HERO `HeroHandler` selects | `i32 canDismiss` if/else before `ViewArmy`; `(static_cast<u32>(id - UI_PREVIOUS_HERO) >= 1 ? 1 : -1) + heroCount + pos`; `message.payload.widget.command = BaseWidgetCommand(message.payload.widget.id)` | inline `?:` in the `ViewArmy` argument (temp lands BELOW the `this` spill); `pos + heroCount + (id == UI_PREVIOUS_HERO ? -1 : 1)`; `BaseWidgetCommand(UI_DIALOG_CLOSE_COMMAND)` (literal, same class as the X_CAMPGN row) | OPEN |
| SOURCE/HERO `HeroHandler` army-slot split/exchange | `if (!HAS(SHIFT) \|\| (types[slot] != NONE && types[src] != types[slot])) { exchange } else { DoHeroSplit }` | `if (HAS(SHIFT) && (types[slot] == NONE \|\| types[slot] == types[src])) { DoHeroSplit } else if (types[slot] == types[src]) { combine } else { swap }`; also `giHeroScreenSrcIndex == id - UI_ARMY_SELECTOR_FIRST` | OPEN |
| SOURCE/HERO `SetupHeroView` selects + guard | three `if/else` command stores; `if (magnitude <= UI_MIN_MODIFIER_ICONS)` | three `?:` (`i >= magnitude ? DISABLE : ENABLE`, `HAS(GROUPED) ? DISABLE : ENABLE`, `HAS(GROUPED) ? ENABLE : DISABLE`); `if (magnitude <= 0)` | OPEN |
| SOURCE/AI `AttemptAdjacentAttack` out-param | `i32 attackHexes[ARMY_ATTACK_HEX_COUNT]` + `attackHexes[0]` | a scalar `i32 destHex` passed as `&destHex` (matching the PATH/ARMY call sites) plus a dead bucket-7 `i32 hexes` at -0xc; `army::ValidAttack` writes its out-param only at offset 0 in the whole image | OPEN |
| SOURCE/AI `GetClosestArmy` / `GetModLichDamage` hit-point product | `target->m_monster.hitPoints * target->m_quantity` | `target->m_quantity * target->m_monster.hitPoints` (see widened-operand-multiply-accumulator) | OPEN |
| SOURCE/AI `GetClosestArmy` / `GetModLichDamage` relationals | `closestValue > value`; `remainingHitPoints < modifiedDamage`; `(bit & mask)` | `val < bestValue`; `modifiedDamage > remainingHitPoints`; `(mask & bitFlag)` | OPEN |
| SOURCE/AI `DoLichShot` marked-array writes | `damaged[IDX(side) * SLOTS + index]` (3 sites) | `*(marked + IDX(side) * SLOTS + index)` - retail leas `marked + side*20` first and adds the index in the SIB | OPEN |
| SOURCE/AI `AICheckRetreat` | `strengths[opp] = static_cast<i32>(strengths[opp] * MOD);`; `(float)strengths[cur] / (strengths[0] + strengths[1])`; `if (chance > (threshold = ...))`; `m_artifacts[i] != ARTIFACT_NONE`; `m_heroes[cur]->m_id == victoryConditionValue` | `force[opp] *= MOD;`; `/ static_cast<double>(...)`; two statements with `if (retreatRatio < chance)`; `>= ARTIFACT_VALID_BEGIN`; `victoryConditionValue == m_heroes[cur]->m_id` | OPEN |
| SOURCE/AI `DoCompAI` | right-nested `a \| (b \| (c \| (d \| e)))`; `(archerLevel * SCALE + BASE) * (numArchers * STRENGTH)`; `if (currentShooterStrength > total/5)`; goldenBow if/else; `break` out of the trailing adjacency loop; no dead guard | flat `a \| b \| c \| d \| e`; `numArchers * STRENGTH * (archerLevel * SCALE + BASE)`; `if (total/5 < myShootPower)`; `archeryBonus = cond ? 0 : 1;`; `return;`; `if (COMBAT_AI_ATTACK_NONE) goto finish;` at the head of the WALK arm | OPEN |
| SOURCE/kbwin `SetMenus` locals | `count`/`commandId`/`scanPosition`/`commandPosition`/`disableFlag`/`index` | `count`/`id`/`match`/`pos`/`disabled`/`index` (slot order -0x4 `match`, -0x8 `count`, -0xc `disabled`, -0x10 `index`, -0x14 `pos`, -0x18 `id`) | OPEN |
| SOURCE/kbwin `AppCommand` dialog-proc local | `appDialogProc` | `lpfnDlgProc` (slot -0x4, i.e. shallower than `command`) | OPEN |
| SOURCE/kbwin `ResizeWindow` origin selection | `if (x == -1) windowX = rect.left; else windowX = x;` | `windowX = (x == -1 ? windowRect.left : x);` - retail materialises a `?:` temp | OPEN |
| SOURCE/kbwin `AppWndProc` style tests | `(lTemp & (WS_MINIMIZE|WS_MAXIMIZE)) == 0` | two separate terms, MAXIMIZE first: `(lTemp & WS_MAXIMIZE) == 0 && (lTemp & WS_MINIMIZE) == 0` | OPEN |
| SOURCE/kbwin `AppWndProc` WM_SIZE clamps | `LOWORD(d) > MIN ? LOWORD(d) : MIN` | `LOWORD(d) < MIN ? MIN : LOWORD(d)` (arms swapped, `jge` not `jle`) | OPEN |
| SOURCE/ADVMGR `UpdateRadar` locals | descriptive `maxXLocal`/`minXSlot`/`mapRow`/... plus four `unusedRadar*` fillers | `line`/`ymod`/`xrem`/`color`/`pixPtr`/`w`/`xhi`/`minx`/`i`/`oldColor`/`frame`/`townx`/`yhi`/`miny`/`cx`/`j`/`bNoFrame`/`towny`/`fScale`/`delta`/`offX`/`cell`/`owner`/`setId` (all at function scope; `w`,`oldColor`,`cx`,`delta` are the four dead slots) | OPEN |
| SOURCE/ADVMGR `UpdateRadar` owner colour | inner-block `i32 ownerColorIndex;` + `if/else` | `gOwnerColors[owner < 0 ? RADAR_NEUTRAL_OWNER : gpGame->m_players[owner].m_color]` (a `?:`, whose temp lands below `this`) | OPEN |
| SOURCE/ADVMGR `Main` locals | `processResult`/`exitRequestedFlag`/`eventCellsResult[3]`/`cheatDigitLocal`/... with inner-block `helpIndexState`/`cheatHero`/`movementChanged`/`movementEnded` | one function-scope group: `quit`/`result`/`c`/`mx`/`my`/`town`/`evtCell`/`direction`/`hero2`/`helpId`/`curHero`/`bMoved`/`bEnded`/`curMusic` (`mx`,`my` are the two dead slots; the event cell is a single `mapCell*`, not an array) | OPEN |
| SOURCE/ADVMGR `ProcessSelect` locals | `mouseX`/`currentCell`/`objectTypeState`/`pageState`/`radarScale`/`currentMessageLocal`/`radarMessage` | `iPage`/`mouseX`/`objectIdIndex`/`mouseY`/`objectTypeState`/`visible`/`theCell`/`radMsg`/`fScale`/`msg` (the two `tag_message`s swap: the drag copy is shallower than the polling message) | OPEN |
| SOURCE/ADVMGR `QuickInfo` locals | `quickInfoShowFlag`/`currentCell`/`heroLocal`/`quickInfoScreenX`/`dialogTopPosition`/`windowLocal`/`siteFrameLocal[2]`/`savedTextLocal[200]` | `icn`/`visitedMaskValue`/`currentCell`/`iFrame`/`pWin`/`posX`/`posY`/`pHero`/`savedTextLocal[800]`/`guardStr[200]`/`message`/`expansionSite`/`j`/`quickInfoShowFlag`/`ch`/`blocked`/`uppercaseResult` (`j` is a dead slot; the site frame is a scalar) | OPEN |
| SOURCE/ADVMGR flat mapExtra reads | `(mapExtra + y*MAP_WIDTH)[x]` with `giCurPlayerBit &` on the left | `*(mapExtra + x + MAP_WIDTH * y) & giCurPlayerBit` in `UpdateRadar`, `GetCloudLookup`, `ProcessSelect` and `QuickInfo` alike | OPEN |
| SOURCE/ADVMGR `GetCell` out-of-range arm | `return m_mapData->Cells();` | `return m_mapData->GetCell(0, 0);` (the inlined `0*width` product survives) | OPEN |
| SOURCE/ADVMGR `UpdateRadar` castle-neighbour probe | `m_mapData->Row(j)[i - 1].m_triggerType` | `m_mapData->GetCell(i - 1, j)->m_triggerType` (y product computed first) | OPEN |
| SOURCE/Castle `CastleHandler` locals + control flow | 8 locals (`buildSlot`, `loopIndex`, `result`, `textControl`, `cannotRecruitHero`, ...); `if (quickFlag) {A} else {B}`; `if (A \|\| B \|\| C) goto buy_building;`; empty-body `for` with a compound condition; `command = BaseWidgetCommand(id)` | 7 locals renamed to the retail bucket order (`whichBuilding`, `objIndex`, `ret`, `statusWidgetId`); `if (quickFlag) { ...; break; }` early-break; `if (!quickFlag) { if (X \|\| !Y) break; }` guard; `for (...) { if (id == slot) break; }`; `command = WIDGET_COMMAND_DIALOG_SELECT` literal; the recruit flag moves into a file-static `inline i32 CannotRecruitHero(void)` (see `inline-callee-local-holds-branchy-value`) | OPEN |
| SOURCE/COMMAND `ValidHexToStandOn` row test | `MAP_WIDTH - 1 == hexIndex % COMBAT_GRID_ROW_LENGTH` | `hexIndex % COMBAT_GRID_ROW_LENGTH == MAP_WIDTH - 1` (the modulo is the LEFT operand and is evaluated first, so `MAP_WIDTH-1` needs no callee-saved register) | OPEN |
| SOURCE/COMMAND `DoSurrender` cost product + selects + locals | `gMonsterDatabase[...].cost * m_armies[...].m_quantity`; two `if/else` pairs writing `message.payload.widget`; `armyIndex_n`/`dialogType`/`dialogResult`/`textWidth_t` | `m_armies[...].m_quantity * gMonsterDatabase[...].cost`; both selects are `?:` (`neg/sbb` and a branchy temp below `this`); locals renamed `armyNum`/`dlgType`/`dlgResult`/`lineWidth` for the bucket order | OPEN |
| SOURCE/HEXCELL `DrawOccupant` relational sides | `m_occupantSide == gpCombatManager->m_currentArmySide`; `m_armies[...].m_facing != m_occupantFrame` | both flipped: the *other* operand is the source's left one (`cmp mem,reg` vs `cmp reg,mem`) | OPEN |
| SOURCE/SEARCH `BuildPath` / `SeedPosition` shape | `register` copies of the parameters, `searchArray* const search = this;`, index locals, `do/while` loops, `mapExtra[y*W + x]`, `while (m_queueSize != 0)` | no locals at all except one `i32 targetTerrain` (retail's frame is 8 bytes = `this` + it); the parameters are written in place; `for (...)` loops with `continue` guards; `*(mapExtra + x + y*W)`; `while (m_queueCount > 0)` (unsigned member of the union); node addresses through `GetColumn(x)[MAP_WIDTH*y]` | OPEN |
| SOURCE/SMACKMGR `PlaySmacker` locals | `i8 savedPalette[]` declared before `i32 oldUpdateFlags` | declaration order swapped (both bucket 6; the later declaration takes the shallower slot) | OPEN |
| SOURCE/VIEW, SOURCE/GAME, SOURCE/x_arena widget-id locals | `msgConst<N>` / `<role>_<N>` suffixes solved for the PoL 2.0 frame | every suffix re-solved for the Buka bucket order (see `slot-permutation-suffix-solve`); `ViewGeneral` also needs its declaration block written in the retail frame order to break four bucket ties | OPEN |
| SOURCE/GAME `ViewArmy` + SOURCE/TOWNMGR `SetupThievesGuild` CP1251 fold | open-coded 4-arm `if/else` writing the destination directly | a per-TU file-static `inline char ToUpperCp1251(u8)`; the fold's result lives in the inline's local, one slot above the `this` spill | OPEN |
| SOURCE/GAME `ViewArmy` morale + spell-count | `morale = theGroup ? theGroup->GetMorale(...) : 0;`; `count < LIMIT ? count : LIMIT` | `if (theGroup) morale = ...; else morale = 0;`; `count > LIMIT ? LIMIT : count` (the arms swap, so the `jle` targets the count arm) | OPEN |
| SOURCE/VIEW `ViewGeneral` captain colour + side test | `if (m_playerId[...] == -1) value = FRAME; else value = colour;`; `m_currentSide != giCurGeneral` (3 sites) | `?:` (temp below `this`); `giCurGeneral != m_currentSide` at all three sites | OPEN |
| SOURCE/x_arena `ArenaWindowHandler` close pair | `command = BaseWidgetCommand(message.payload.widget.id)` (2 sites); `type_13` assigned before `extra_18` | `command = WIDGET_COMMAND_DIALOG_SELECT` literal; the two `-1` initialisations swap order | OPEN |
| SOURCE/Wsnetwin `wsProcessMessages` / `wsSendMessage` | `while (1)`; `giNetPosToDCOPos[giThisNetPos] == remote.sin_addr.s_addr`; `giNumHumanPlayers > netPlayer`; `giThisNetPos == netPlayer`; `receiveSize`, `peerAddress` | `for (;;)`; all three relationals flipped to put the non-global term on the left; `bufLen`, `remote` (bucket order: `remote` must share bucket 6 with `attemptCount`) | OPEN |
| SOURCE/RECRUIT `SetupRecruitWin` locals | `recruitName[20]`, `label[40]`, `message` | `char ch; char monsterName[40]; char label[40]; tag_message message;` (retail slots monsterName above message; `ch` is a named `char`, not a `?:` temp) | OPEN |
| SOURCE/RECRUIT `recruitUnit` ctors / `QuickViewRecruit` locals | `costs`, `resource`, `goldPrice`, `paymentType`, `unitType`, `resourcePrice`, `recruitWindow`, `amount`, `resourceIndex` | `unitCosts`, `resourceIndex`; and `monsterType, resourceType, recruitWindow, unitCosts, resourceCost, goldCost, resourceIndex, avail` in that declaration order | OPEN |
| SOURCE/RECRUIT `recruitUnit::Main` quick-view flag | `i32 quickView = (HAS(...)) != 0;` | `i32 quickView;` then `if (HAS(...)) quickView = 1; else quickView = 0;` (retail stores each arm directly, no `neg/sbb/neg`), and `close` -> `done` for its slot | OPEN |
| SOURCE/Overview `OverviewHandler` locals | `closeDialog5, quickView15, scrollItemCount0, scrollDivisor3, scrollY7, newTop8` | `topIndex, scrollSpan, scrollDivisor, done, quickView, y` (declared in that order) | OPEN |
| SOURCE/Overview widget-row access | `OVERVIEW_TEXT_WIDGET_ROWS[row][item]` (cast to a row typedef) | `*(textWidgetDynamic + (row) * OVERVIEW_DYNAMIC_WIDGETS_PER_ROW + (item))` - retail `lea` the row base and defers `item` into the store's SIB | OPEN |
| SOURCE/Overview `game::Overview` locals | `mineCounts4, lighthouseCount4, mine4, message8, row0` | `mineCounts, lighthouseCount, mine, message, y` | OPEN |
| SOURCE/Overview `SetupDynamicStuff` locals | `row, item, textItemCount, iconCount, rowWidgetId6, valueText0` + `unused/knobScale/scrollDivisor` + block locals | `rowIndex, i, texts, icons, rowWidgetId, valueText`; `steps/pixelsPerItem`; `building, displayedTroops, heroData, capt`; `shown, curHero, column, skillIndex, detailRow` | OPEN |
| SOURCE/Overview artifact-row Y term order | `rowIndex * OVERVIEW_ROW_HEIGHT + detailRow * DETAIL_ROW_STRIDE` | `detailRow * DETAIL_ROW_STRIDE + rowIndex * OVERVIEW_ROW_HEIGHT` (retail's `lea` base holds the detailRow product) | OPEN |
| SOURCE/KB `InterpretCommandLine` / `EventWindowHandler` / `ShowCongrats` locals | `len, dst, src`; `type, extra`; `savedPalette[0x304], score_e, rating` | `size, dstIndex, srcIndex`; `resType, resExtra`; `palette[MISC_PALETTE_BYTE_COUNT], unused, realScore, ratingText` | OPEN |
| SOURCE/KB `oldmain` locals | `quit_g, firstMainScreen_c, command_a, transmissionResult_i, mainScreenLoaded_b, unusedMenuState_d, unusedPlayerState_c, matchedGamePlayers_e, netPlayer_i, netBuffer_b` | `quit, firstMainScreen_h, command_c, transmissionResult_d, mainScreenLoaded_h, unusedMenuState, unusedPlayerState_f, matchedGamePlayers_c, netPlayer_k, netBuffer_f`; the three `giNumHumanPlayers` loops use `player_h`, and the campaign result is a block-scope `campaignResult` | OPEN |
| SOURCE/Newgame `GetSideDesc` / `ProcessNGKeyPress` / `UpdateNewGameWindow` locals | `colorName3, sideSize, localPlayerOnSide, unusedSideWord1, localPlayerIndex, player5, listedPlayerCount5, otherPlayerCount2`; `workText`; `messageTemp, playerIndex3` | `colorStr, sideCount, i, onSide, otherPlayerCount, localPlayer, listedPlayerCount` (that declaration order, no dead local); `buf`; `message, playerIndex` | OPEN |
| SOURCE/Newgame `InitNewGame` tail | `if (giMenuCommand == -1) { ... }` fall-through | `goto selected_player;` at the end of the first arm, label on `m_selectedSetupPlayer = ...` | OPEN |
| SOURCE/Newgame player-column X term order | `playerSpacingTemp * playerCounter + firstPlayerXLocal` | `firstPlayerXLocal + playerSpacingTemp * playerCounter` (17 sites) | OPEN |
| SOURCE/ARMY `GoBerserk` shape | `do { if (targetFound) goto walkToward; dir = Random(...); } while (mask & BIT(dir));` then a `goto walkToward;`, an if/else at `walkToward`, and a flat `{ }` block for the nearest-target section | `while (!targetFound) { dir = Random(...); if (!(mask & BIT(dir))) { ...; goto berserkFinish; } }` inside `if (attackMask != ALL) { ... } else { ... }` (no `goto walkToward` at all - the then-arm's end jump IS the label); `if (HAS(DEAD) \|\| quantity <= 0) continue;` in the scan loop; `goto berserkFinish;` inside the shooter arm; `if (giNextActionGridIndex != -1) goto berserkFinish;`; `if (!HAS(FLYING)) { if (Walk(...)) goto berserkFinish; if (Walk(...)) goto berserkFinish; }` - pinned by the 5-stub census | OPEN |
| SOURCE/ARMY `DoAttack` guards | one `&&` chain for the retaliation guard; `if (target->m_quantity > 0) { if (!breath \|\| breath->m_quantity > 0) {...} }`; `if (HYDRA) {...} else {...}` | `if (target->m_quantity <= 0 \|\| (breath && breath->m_quantity <= 0)) break;`; `if (target && target->m_quantity > 0) { if (<9-term `\|\|` chain>) goto secondAttack; ... }` with `secondAttack:` on the second-attack `if`; `if (HYDRA) { DoHydraAttack(...); goto attackDone; }` + `attackDone:` label (retail's 3 tail stubs) | OPEN |
| SOURCE/ARMY `MoveAttack` shape | `if (occupantSide == NONE \|\| (== current && == index)) goto move;` ... `goto finish; move: if (FLYING) {...} else {...} finish:` | one positive guard `if (occupantSide != NONE && (!= current \|\| != index)) { ...attack... goto finish; } else if (HAS(FLYING)) {...} else { WalkTo(...); }` - no `move:`/`finish:` labels survive | OPEN |
| SOURCE/SPELLAI `EffectSpellDamage` | inner-switch arms ordered LIGHTNING/CHAIN/empty/HOLY/FIREBALL; `(step<7 \|\| FIREBLAST) && step<19` with an else; `= (i32)(acc + ...)`; `hitPoints * killed`; `fightValue * (def-new) * (qty-killed)` | arms ordered HOLY/COLD_RING/FIREBALL/LIGHTNING/CHAIN/empty; `if ((step>=7 && spell!=FIREBLAST) \|\| step>=19) { doneWork=1; break; }`; `+=` accumulators; `killed * hitPoints`; `(def-new) * (qty-killed) * fightValue`; `if (!doneWork) { if (hex<0 \|\| hex>=0x74) continue; ... }`; `creaturesKilledResult++`; the chain-lightning argument written out as `&m_armies[side][index]`, not the `targetCreature` local | OPEN |
| SOURCE/SPELLAI `DetermineEffectOfSpell` | 40 `if (X) { } else { body }` empty-then guards; `>= MAX ? MAX : v` duration ternaries; `Raw(...) * mods[...] - effect`; if/else on the occupant test; `SPELL_AI_LAST_HEX` as the AREA bound; no `SPELL_COLD_RAY` in the ENEMY mode group | 40 `if (X) break;` early guards; `v < MAX ? v : MAX`; `effect - Raw(...) * mods[...]`; `if (index >= 0 && side >= 0) {...} else { targetCreature = NULL; }`; a private `SPELL_AI_AREA_LAST_HEX = 0x2b`; `case SPELL_COLD_RAY:` added to `SPELL_AI_ENEMY` (its absence left an unreachable 9th dispatch arm - PoL 2.0's retail has 0x2b too) | OPEN |
| SOURCE/SPELLAI `RawEffectSpellInfluence` | one HASTE/SLOW arm with `if (influence == SLOW) {...} else {...}`; block-scope locals; `(float)a / (float)b` divisors; `(damageMin + damageMax)` | `case SLOW: ...; goto hasteSlowCommon; case HASTE: ...; hasteSlowCommon:`; every local at function scope; PARENTHESISED divisor casts `/ (static_cast<float>(x))` (see fidiv-vs-fild-fdivp); `(damageMax + (damageMin))` with the second operand parenthesised | OPEN - 99.62%, frame-slot residual |
| SOURCE/SWAPMGR `Main` quickView | `quickView = HAS(modifiers, RIGHT_BUTTON) != 0;` | `if (HAS(...)) quickView = 1; else quickView = 0;` - the `!= 0` and the `?:` forms both give branchless `neg/sbb/neg` | OPEN |
| SOURCE/SWAPMGR `SwapMons` / `Main` army compares | `selectedArmy->types[selSlot] == targetArmy->types[tgtSlot]`; `target->counts[t] = selected->counts[s] + target->counts[t]` | target side FIRST in both compares; `target->m_creatureCounts[t] += selected->m_creatureCounts[s];` (narrow `mov dx`/`add dx`, no `movsx`) | OPEN |
| SOURCE/PHILAI `ResetHeroRVs` / `ValueOfEventAtPosition` RV access | a local `HERO_RV_AT(values, byteOffset)` byte-offset macro | flat i16 pointer form `*(gaiHeroEventStratRVOfPos + x + y * MAP_WIDTH)`; loop tests `node < MAP_WIDTH` (counter on the LEFT); `abs(x - ...) + abs(y - ...)` | OPEN |
| SOURCE/PHILAI `CheckReload` row access | `gpSearchArray->GetRow(MAP_WIDTH, p)[node]` | `gpSearchArray->GetRow(node, 1)[MAP_WIDTH * p]` (the same spelling the other three PHILAI sites already use); `jb > kn * 2`; `(ratio - 1.0f) * (mobility + K)`; `NUMERATOR / (BASE + friendly + idx)` | OPEN |
| SOURCE/PHILAI `ValueOfBuyingBuilding` | three separate `(K + w) * adjustedValue` statements; a named `mageGuildLevelLocal`; separate `indexBuilding`/`buildingLevel`+`buildingLevel` counters | `adjustedValue * (w + K)` per statement (see float-self-multiply-chain-keeps-st0); the mage-guild level as an inline `?:` argument (retail's temp sits BELOW the `this` spill); one counter for the two 5-slot loops, a second only for the dwelling-level loop; `m_creatureTypes[i] == gDwellingType[...]`; `gMonsterDatabase[cur].iconIndex * 1.2 > gMonsterDatabase[...].iconIndex` | OPEN |
| SOURCE/PHILAI `ValueOfEventAtPosition` FP factors | `gpCurAIHero->m_aiFightValue * AI_*_VALUE_FACTOR`; folded `AI_XANADU_VALUE_FACTOR 8000.0f` and `AI_DAEMON_FIGHT_VALUE_FACTOR 300.0` | constant FIRST at all 16 sites; the factors are PRODUCTS in retail - `AI_TRAINING_SITE_VALUE_FACTOR * 8.0f` and `share * 1000.0` (two `fmul`s, not one folded constant); `Stats(...) <= MAX ? table[Stats(...)] : table[MAX]` polarity | OPEN - 95.83%, residual is one extra `push ebx` + 3 frame slots |
| SOURCE/wingraph `DDSD` frame | `HRESULT restoreResult; i32 unused; char errorMessage0[200];` (buckets 15/4/1 -> array at -0x4) | `HRESULT hres; i32 unused; char errorMessage[200];` (buckets 9/4/14 -> the array is the DEEPEST slot at -0xd0) | OPEN |
| SOURCE/wingraph `DDAppPaint` loop + locals | `for (;;)` blit-retry loop; `sourceWidth + sourceLeft - 1`; `sourceTop4`/`sourceWidth5`/`sourceHeight3`/`sourceLeft3`/`point0`/`paint3` | `while (1)` (retail materialises `mov ecx,1; test; je`); `sourceLeft6 + sourceWidth - 1`; suffixes re-solved (`sourceWidth`, `sourceHeight5`, `sourceTop8`, `sourceLeft6`, `paint7`, `point1`) for the retail slot order | OPEN |
| SOURCE/wingraph `WGInitializePalette` second loop | `screenImage.colors[e].rgbRed = LogicalPalette.entries[e].peRed;` (read-back of the value just zeroed) | `screenImage.colors[e].rgbRed = 0;` - retail stores the literal into both arrays, three sites; `entry` renamed `entry0` for the slot order | OPEN |
| SOURCE/Campaign `ShowCampaignInfo`, `CampaignInfoUpdate`, `CampaignHandler` spellings | `IDX(m_campaignType) + IDX(m_campaignStartingSide) * IDX(CAMPAIGN_SIDE_COUNT)`; `if (iCurViewMap == map)`; three `if/else` pairs adding a track-frame step; `giDialogTimeout < KBTickCount()`; `command = BaseWidgetCommand(message.payload.widget.id)`; locals `map`, `savedInterface`, ... | product term first; `if (map == iCurViewMap)`; all three steps are `?:` (`neg/sbb/and/add`); `KBTickCount() > giDialogTimeout`; `command = BaseWidgetCommand(CAMPAIGN_CLOSE_COMMAND)` literal (2 sites); `mapIndex` + declaration order re-solved | OPEN |
| SOURCE/Campaign `InitCampaignMap` frame | 12 function-scope locals + a per-case `i32 savedNewGameSetup` in the EXPERIENCE arm | 13 function-scope locals: the EXPERIENCE arm's temp is hoisted to the top (it takes the 12th slot, ahead of `bonusHeroIndexPosition`), the SCENARIO_SEVEN block keeps its own `savedNewGame`; six identifiers get suffixes for the bucket ties | OPEN |
| SOURCE/SETUP `BaseSetupHandler` + `SetupHotSeatGame` | `command = BaseWidgetCommand(message.payload.widget.id)`; `defaultName`/`i`/`win` | `command = WIDGET_COMMAND_DIALOG_SELECT` literal; `i`, `dialogWindow`, `name` in that declaration order | OPEN |
| SOURCE/FLY `CanFit` rear-hex guard | `if (ValidHex(candidateHex)) cell = &m_hexCells[candidateHex];` | `if (ValidHex(candidateHex) && candidateHex % ARMY_HEX_COLUMNS != 0 && candidateHex % ARMY_HEX_COLUMNS != ARMY_HEX_COLUMNS - 1)` - the same triple test as the early return, repeated positively (two `idiv $13`) | OPEN |
| SOURCE/FLY `ValidFlight` / `FlyTo` spellings + frame | four `if/else` pairs stepping the target hex and the direction mask; `attackHex7[ARMY_ATTACK_HEX_COUNT]`; `static_cast<u32>(m_facing) < ARMY_FACING_RIGHT ? -1 : 1) + hex`; `(float)a / b`; `((speed>>1) + dist) / speed`; `flightFrameCount + frameBegin`; `flightSteps - 1 == segmentIndex`; `!(flightSteps <= segmentIndex)`; `(prevMinX <= giMinExtentX)` family | all four are `?:`; `attackHex` is a scalar passed by address (retail's frame has 13 scalars, three of them dead); `hex + (m_facing == ARMY_FACING_LEFT ? -1 : 1)`; `(float)a / ((float)b)` - the parenthesised divisor cast; `(dist + (speed>>1)) / speed`; `frameBegin + flightFrameCount`; `segmentIndex == flightSteps - 1`; `segmentIndex < flightSteps`; `giMinExtentX < prevMinX` (four sites); all 27 FlyTo locals re-solved for the retail slot order | OPEN |
| SOURCE/HISCORE `Open` / `Main` / `Update` spellings | `if (a \|\| b) m_showCampaignScores = 1; else ... = 0;`; `monAnimDrawFrame[...] + m_monsterTypes[e]*9 + 1`; four `if/else` selects; `rank == giHighScoreRank`; `entry`/`scoreEntry`/`inputFile`/`missingFile`/`filename`/`messageValue` | `m_showCampaignScores = a \|\| b;`; `m_monsterTypes[e]*9 + monAnimDrawFrame[...] + 1` (both sites); all four selects are `?:`, two of them wrapped in `static_cast<i16>` (see `short-cast-around-constant-select`); `giHighScoreRank == rank`; locals renamed `highScore`/`rank`/`inputFile`/`noScoreFile`/`scorePath`/`hsMessage` | OPEN |
| SOURCE/Viewwrld `ViewWorld` / `ViewWorldDialogHandler` | `MAP_WIDTH <= iVWMapOriginX + iVWViewableCells` (4 sites); `command = BaseWidgetCommand(message.payload.widget.id)`; `paletteData`/`viewIconNames`/`dialogMessage`/`viewWorldWindow`, `radarX0`/`radarY1`/`radarScale6`/`savedMessage`/`currentMessage7` | `iVWMapOriginX + iVWViewableCells >= MAP_WIDTH`; `BaseWidgetCommand(EVENT_WINDOW_CLOSE_COMMAND)` literal; renamed/reordered `window`/`palette`/`iconNames`/`legendMessage` and `radarScale`/`oldMessage`/`eventMessage`/`mapX`/`mapY` | OPEN |
| SOURCE/Viewwrld `VWCompleteDraw` spellings | `giCurPlayerBit & mapExtra[MAP_WIDTH*y + x]` (7 sites); `cell->m_objectData & 1`; `(x - originX)*scale + xOffset`; `arrayX[lookup] + pixelX`; five named `*Highlight` locals written by `if/else`; `if (idx == 0 \|\| Extra(idx)->objectIndex == NONE) e = NULL; else e = Extra(idx);` (6 sites); `if (!currentHeroHere)`; `if (heroX != x \|\| heroY != y) here = 0; else here = 1;`; `m_objectIndex >> 1`; `if (owner < 0) frame = 6; else frame = colour;`; `p = pixels + y*W; end = p + WORLD_RIGHT; for (p += WORLD_LEFT; ...)` | `*(mapExtra + x + MAP_WIDTH*y) & giCurPlayerBit`; the `m_objectLayerBit0` bitfield (see `bitfield-read-vs-masked-storage`); `xOffset + (x - originX)*scale`; `pixelX + arrayX[lookup]`; every highlight is an inline `?:` in the call argument (no named local - retail's frame is 0x14 bytes smaller); all six extra-list guards inverted to the `&&` form with the `Extra()` arm first; `if (currentHeroHere)` with the arms swapped; the `&&` form for `currentHeroHere`; `m_objectIndex / 2` (signed, `cdq/sub/sar`); `frame = owner < 0 ? 6 : colour;`; `p = pixels + y*W + WORLD_LEFT; end = p + WORLD_DRAW_SIZE; for (; ...)`; all 20 locals renamed for the slot order | OPEN |
| SOURCE/Newgame `InitNewGameWindow` / `ShowScenInfo` locals | `availableWidthResult, textControlLocal, firstPlayerXLocal, multiplayerYOffsetValue, playerCounter, playerSpacingTemp, iconControlLocal, playerGapValue, label, raceTextWidth, singlePlayerYOffsetValue` (+ `mapSizeIndex, playerLockedLocal, scenarioMessageTemp, scenarioWindowValue`) | one monotone-bucket vocabulary shared by both bodies: `columnGap, nameWidget, firstColumnX, playerCounter, availWidth, iconControl, playerStep, multiplayerYOffset, raceNameWidth, yExtra, name` plus `mapSize, locked, msg, window`, declared `columnGap, availWidth, firstColumnX, playerStep, playerCounter, mapSize, locked, name, yExtra, raceNameWidth, msg, multiplayerYOffset, nameWidget, iconControl, window` | OPEN - pure /Od slot-hash pins, see od-slot-solve |
| SOURCE/Newgame race-icon / race-name / race-cycle Y term order | `multiplayerYOffsetValue + singlePlayerYOffsetValue + K` (6 sites) | the single-player offset is the LEFT addend: `yExtra + multiplayerYOffset + K` (the `lea K(%ecx,%eax)` base names it) | OPEN |
| SOURCE/Newgame `NewGame` remote-receive shape | `do { PollSound(); ...; if (buf && buf->type == RELIABLE) { switch ... } } while (!playerDataReceived || !mapHeaderLoaded);` then the window setup, then `if (cancel) result = 0; else result = 1;` | `for (;;) { ...; if (buf != NULL && buf->type == RELIABLE) { switch ...; if (playerInfoOk && mapHeaderOk) { <window setup>; if (cancel) { result = 0; goto cleanup; } result = 1; goto cleanup; } } }` - both guards' joins ARE the loop back edge, and a `cleanup:` label carries the two exits | OPEN |
| SOURCE/Newgame `NewGame` map-retry shape | `for (;;) { ...; if (!ok || n < min || n > max) gpGame->GetMap(); else break; }` | `pick_map: ...; if (!mapHeaderRead || n < min || n > max) { gpGame->GetMap(); goto pick_map; }` - one back edge, no `$else`/`$end` stubs | OPEN |
| SOURCE/Newgame `NewGame` locals | `netPlayerPacket, mapInfoPacket, windowMessage, mapHeaderLoaded, playerDataReceived, remoteBuffer, choiceWindow, extension, textBufferIndex, mapHeaderResult, result, wrongExpansionType, transmitResult` | `mapExt, mapHeaderOk, playerInfoOk, mapHeaderRead, mapInfo` for five of them (bucket pins); declaration order `netPlayerPacket, mapInfo, windowMessage, mapHeaderOk, playerInfoOk, remoteBuffer, choiceWindow, result, wrongExpansionType, mapExt, mapHeaderRead, textBufferIndex, transmitResult` | OPEN |
| SOURCE/Newgame `NewGameHandler` locals | `transmitResultTemp, redrawWindow, synchronizeSetupResult, remoteMapHeaderValue` | `sendResult, redraw, needSync, mapHeader`, with `redraw`/`needSync` declared AFTER `windowMessage` and `sender` after `mapHeader` | OPEN |
| SOURCE/Newgame `NewGameHandler` RELEASE-switch case order | `GAME_MAP_OPTIONS_CONTROL, GAME_DIALOG_OK, GAME_DIALOG_CANCEL, default` | `GAME_DIALOG_OK, GAME_DIALOG_CANCEL, GAME_MAP_OPTIONS_CONTROL, default` - the compare chain is value-sorted in both, only the body run moves | OPEN |
| BASE/WIDGET, BASE/BORDER, BASE/ICONWDGT, BASE/BUTTON `Main` overrides | `i16 mx = (i16)msg.payload.mouse.x; heroWindow* window = m_owner; mx -= (i16)window->m_posX;` then `m_x <= mx && ... && mx < m_width + m_x`; `switch (type) { default: goto normalEvent; case ...: goto hoverEvent; }` with labelled blocks; a `type`/`eventType` snapshot of `msg.type`; `IsInsideWidget(this, x, y)` helper in WIDGET | one statement `i16 x = msg.payload.mouse.x - m_owner->m_posX;` (no cast, no `window` local); `x >= m_x && y >= m_y && x < m_x + m_width && y < m_y + m_height` (widget side first, `m_x + m_width`); a plain `switch (msg.type)` whose arms `break`, with the tail `return widget::Main(msg)` after it; no `type` local (retail re-reads `msg.type`); the four comparisons written out in `widget::Main` | OPEN |
| BASE/WIDGET `widget::Main` case order / arg order | `case MESSAGE_MOUSE_MOVE` before `case MESSAGE_WIDGET`; `UpdateScreenRegion(m_x + m_owner->m_posX, m_y + m_owner->m_posY, ...)`; `m_x = static_cast<i16>(value)` for SET_X/SET_Y/SET_WIDTH | `case MESSAGE_WIDGET` first (retail's WIDGET body sits at the head of the switch, the mouse body last); `UpdateScreenRegion(m_owner->m_posX + m_x, m_owner->m_posY + m_y, ...)` - the window coordinate is the accumulator at all four sites; `m_x = message.payload.widget.data.value` with no cast (retail folds to `mov dx,[ecx+0x18]`) | OPEN |
| BASE/WIDGET, BASE/ICONWDGT narrow value extraction | `static_cast<H2EnumStorage<WidgetFlag,i16>>(value)` / `static_cast<i16>(value)` | `value & 0xffff` (see `narrowing-cast-folds-source-mask-does-not`): every cast spelling folds into a 16-bit memory access, only a source-level mask emits retail's `and r32,0xffff`. Same at byte width for `iconWidget::Read`'s `m_orientation` (`ReadWord() & 0xff`) | OPEN |
| BASE/ICONWDGT `iconWidget::Main` inner switch order | cases `SET_FRAME`, `SET_FILL_COLOR`, `SET_ICON`, `REPLACE_ICON`; `if (m_id != msg.payload.widget.id) goto normalEvent;` per case | cases `REPLACE_ICON`, `SET_ICON`, `SET_FRAME`, `SET_FILL_COLOR` (retail's body layout; the jump/index tables are identical either way); `if (msg.payload.widget.id == m_id) { ...; return CONSUME; } break;` - the message field is the LEFT operand (`cmp mem,reg`), and the non-matching path is the case's `break` | OPEN |
| BASE/BUTTON `button::Main` timer compare | `KBTickCount() > glTimers[GLOBAL_BUTTON_REPEAT_TIMER_SLOT]` | `glTimers[GLOBAL_BUTTON_REPEAT_TIMER_SLOT] < KBTickCount()` - the call is emitted first because it needs a register, and the array cell is the `cmp` destination | OPEN |
| BASE/BUTTON `SET_WIDGET_MESSAGE` macro | `command; type; id` | `type; command; id` - the order retail emits in both `button::Deselect` and the `WIDGET_COMMAND_ALTERNATE_SELECT` site; `iconWidget`/`border` write the three fields out longhand with the command set before `type` | OPEN |
| baseManager::Activate | `void Activate(void) { m_active = true; }` (2.0-exact) | `void Activate(bool on = true) { m_active = on; }` - the `bool` parameter is what stops VC6 folding the immediate into the store; 34 int-typed spellings all fold | OPEN - 2.0 is byte-exact with the int form, so a genuine dev edit is the leading reading |
| combatManager::RawEffectSpellInfluence army value | `target->m_monster.fightValue * target->m_quantity` | `target->m_quantity * target->m_monster.fightValue` (accumulator operand is the LEFT one; 2.0 emits the reverse) | OPEN |
| combatManager::RawEffectSpellInfluence locals | per-case block scopes, separate `dragonMod`/`shooterMod` | all twenty at function scope, ONE shared `factor` float for DRAGON_SLAYER+SHIELD, plus three dead 4-byte locals - and 2.0's retail frame shows the SAME 17-live/3-dead single-scope census, so the PoL block scoping is wrong in both versions | OPEN - fix applies to 2.0 too |
| combatManager::RawEffectSpellInfluence local names | `workChance`/`armyValue`/`newSpeed`/`otherArmy`/`averageDamage`/`damageEffect`/`column`/`distance`/`oldTurns`/`newTurns`/`attackMask`/`adjacent`/`dragonCount`/`shooterCount`/`i` | `castChance`/`worth`/`newSpd`/`other`/`avgDmg`/`damageDelta`/`columnIndex`/`distance`/`beforeTurns`/`afterTurns`/`attackMask`/`adjacent`/`dragonCounter`/`shooters`/`cnt`, plus dead `unused30_j`/`unused38_h`/`unused48_e` - the monotone bucket chain over retail's 20-slot order admits no naming closer to PoL's | OPEN as slot-name class |
| combatManager::RawEffectSpellInfluence switch-arm order | ANTI_MAGIC, STONESKIN, STEELSKIN | STONESKIN, STEELSKIN, ANTI_MAGIC (pinned by the dispatch table) | OPEN |
| philAI::ValueOfEventAtPosition SKELETON arm | `value = meta == 1 ? 0 : gArtifactBaseRV[meta - 2];` | `if (meta == 1) value = 0; else value = gArtifactBaseRV[meta - 2];` - the `?:` join temp costs a slot and a `push ebx` | OPEN |
| philAI::ValueOfEventAtPosition player-bit test | `!(giCurPlayerBit & mapExtra[MAP_WIDTH * y + x])` | `!(*(mapExtra + x + MAP_WIDTH * y) & giCurPlayerBit)` - the map read is the LEFT operand and the pointer form defers the row product to the SIB | OPEN (same class as the MapExtraPosAndAdjacentsSet row) |
| _nb_sess locals | `oldSess`, `detachFlag` | `oldsess` (bucket 2), `detach` (bucket 12) - retail's slot order is peer, rc, args, oldsess, controlBlock, destinationSession, detach | OPEN as slot-name class |
| advManager::DoEvent visited-flag cases (BUOY, FAERIE_RING, IDOL, FOUNTAIN, WATERING_HOLE, OASIS, TEMPLE) | `if (!HAS(flags, BIT)) { reward } else { alreadyVisited }` | `if (HAS(flags, BIT)) { alreadyVisited } else { reward }` - the emitted `jcc` is the negation of the source condition and the fall-through block is the then-arm | OPEN - whole-family class |
| advManager::DoEvent one-shot guards (XANADU, FORT, STANDING_STONES, WITCH_DOCTOR_HUT, MERCENARY_CAMP, GAZEBO, WITCH_HUT) | `if (visited) { dlg(); break; } reward(); break;` | `if (visited) { dlg(); } else { reward(); } break;` (WITCH_HUT chains two guards into `else if`) - see case-guard-break-vs-else-arm | OPEN |
| advManager::DoEvent SPHINX + PYRAMID arms | `if (A) { X } else { Y }` | `if (A) { X; break; } else { Y }` - an explicit `break` inside an arm that already has an `else` | OPEN |
| advManager::DoEvent mine family (MINE, ALCHEMIST_LAB, SAWMILL, LIGHTHOUSE) | `if (owner != giCurPlayer) { body }` with `claimMine:` in the MINE case | `if (owner == giCurPlayer) break;` early-out, `claimMine:` at the end of the SAWMILL case | OPEN |
| advManager::DoEvent TROLL_BRIDGE / CITY_OF_DEAD / DRAGON_CITY guarded arms | two `if (...) break;` guards then a flat body | nested `if (dialogResult == YES) { if (Combat(...) == ATTACKER) { ... } } else { refusal } break;` (DRAGON_CITY forces nesting - statements sit between the two jcc); `recruit*:` labels move inside the last arm's `== YES` guard | OPEN |
| advManager::DoEvent MAP_OBJECT_ARTIFACT inner switch | case order PICKUP, GOLD, WISDOM, LEADERSHIP, RESOURCE_3, RESOURCE_5; `giveArtifact:` in the guarded arm | WISDOM, LEADERSHIP, PICKUP, GOLD, RESOURCE_3, RESOURCE_5 (jump-table order); `giveArtifact:` in `case PICKUP`; the guarded arm is the then-arm of `if (metadata & GUARDED_FLAG)` and the Rogue arm ends `goto artifactFight;` | OPEN |
| advManager::DoEvent WAGON / LEAN_TO / SEA_CHEST / TREASURE_CHEST value locals | `resourceType`/`resourceAmount6`/`eventValue1` locals hold `(meta & MASK) - 1` etc. | the bitfield expression repeated at each use (retail has no local); SEA_CHEST becomes `if (FLAG && NumArtifacts() < CAP) {...} else if (meta != EMPTY) {...} else {...}`; WAGON becomes `if (meta == 0) { wagonEmpty: ...; break; }` with `goto wagonEmpty;` from the full-artifact case | OPEN |
| advManager::DoEvent shrine family | `eventValue1 = cell->m_objectMetadata - 1;` per case, then `gsSpellInfo[eventValue1]` | no local: `gsSpellInfo[cell->m_objectMetadata - 1]` at every use, and the wisdom test with the LEVEL on the left: `IDX(level) <= wisdom + SHRINE_WISDOM_BONUS` | OPEN |
| advManager::DoEvent artesian spring / magic well | `if (spellPoints < X) { refill } else { already }` | `if (spellPoints >= X) { already } else { refill }`; the well's outer flag test is likewise inverted | OPEN |
| advManager::DoEvent FREEMANS_FOUNDRY + HILL_FORT upgrade reset | `thirdUpgrade = secondUpgrade1 = firstUpgrade_e = CREATURE_NONE;` (and three separate statements in FREEMANS) | `firstUpgrade_e = secondUpgrade1 = thirdUpgrade = CREATURE_NONE;` at BOTH sites - the innermost assignment is `thirdUpgrade` | OPEN |
| advManager::DoEvent daemon-cave artifact guard | `if (NumArtifacts() == CAP \|\| GetRandomArtifactId(...) == -1) goto daemonExperienceGold;` | two separate `if (...) goto daemonExperienceGold;` statements | OPEN |
| advManager::DoEvent tail | `playedSample3 = eventSample_f;` before UpdateRadar | statement absent; `playedSample3` stays a declared-but-dead slot | OPEN |
| advManager::DoAIEvent locals | `eventResults[AI_EVENT_RESULT_COUNT]`, one `spellPower_j`, a named `spellValueFactor_i`, and `resourceWork_p`/`rewardWork_e` arrays | three separate scalars (`ComputerMonsterInteract(cell, hero, &eraseObject)`), THREE distinct spell-power locals (mine / artesian / well), the pyramid factor folded into a nested `?:` (a compiler temp), and no `resourceWork_p`/`rewardWork_e` at all - all pinned by the 0x128 frame | OPEN |
| advManager::DoAIEvent teleport + stone-liths counters | `if (exitCount > 0)`, `--exitCount < 1` | `if (exitCount >= 1)`, `--exitCount <= 0` (same in DoEvent's `--teleportCount <= 0`) | OPEN |
| advManager::DoAIEvent abandoned-mine value | `gaiTurnValueOfMine[x + y * MAP_WIDTH]` | `*(gaiTurnValueOfMine + x + y * MAP_WIDTH)` - identical to the already exact `philAI::EvaluateMineEvent`, so the flat-deref form is the tree's proven spelling for this formula, not an invention | OPEN - resolve together with PHILAI |
| advManager::DoAIEvent WAGON artifact index | `artifact_g = ArtifactType(meta & WAGON_ARTIFACT_MASK)` | the loop counter `index_h` is reused as the artifact temp (`reused-loop-counter-shrinks-frame`) | OPEN |
| BASE/WINDOW `heroWindow` ctors + `RemoveWidget` null pairs | `m_prevWindow = NULL; m_nextWindow = m_prevWindow;` (load+store) | `m_nextWindow = m_prevWindow = NULL;` - VC6 constant-propagates the chained assignment to two immediate stores, retail's shape; same for `m_posX = m_posY = 0` and `m_widgetListTail = m_widgetListHead = NULL` (4 functions, 7 sites) | OPEN - may be a 4.2-vs-VC6 lowering difference rather than a source edit |
| BASE/WINDOW `heroWindow::RemoveWidget` list-head/tail tests | `m_widgetListTail == w`, `m_widgetListHead == w` | `w == m_widgetListTail`, `w == m_widgetListHead` - the parameter is the register operand | OPEN |
| BASE/WINDOW `heroWindow(i32,i32,char*)` locals | `jb, idx, pte, ptw, type, pbtn, pdl, pbd, pwdg, picn, plist, pdim` (PoL slot order `idx, jb, pte, type, ptw, pbtn, plist, picn, pwdg, pbd, pdl, pdim`) | one monotone-bucket vocabulary for the 2.1 order: `jb, idx, pListBox, pDimmer, type, pBorder, pDrop, pButton, pIcon, pTextEnt, pText, pWidget`, declared in reverse slot order | OPEN - pure /Od slot-hash pins, see od-slot-solve |
| BASE/WINMGR `heroWindowManager::Main` loop exit | `result = w->BroadcastMessage(msg); if (result >= CONSUME && result <= FORWARD) break;` | `switch (ret = w->BroadcastMessage(msg)) { case CONTINUE: break; case CONSUME: case FORWARD: return ret; }` - the two-case range check plus a second `return`, mirroring the already-exact `heroWindow::BroadcastMessage`; locals `result`/`w` -> `ret`/`w` for the slot order | OPEN |
| BASE/WINMGR `heroWindowManager::AddWindow` shape | separate `i32 z` local, `z = 0; if (cur != NULL) z = cur->m_zOrder + 1;`, `do { if (cur->m_zOrder <= z) break; ... } while (cur != NULL);`, `if (m_windowListHead == NULL) m_windowListTail = w;` | assigns the `zOrder` PARAMETER in place (retail's frame has one named slot), `if (cur == NULL) zOrder = 0; else zOrder = cur->m_zOrder + 1;`, `while (cur != NULL && cur->m_zOrder > zOrder) cur = cur->m_prevWindow;`, and the tail test reads `m_windowListTail` | OPEN - the head/tail test is a semantic difference, see version-changes |
| BASE/WINMGR `heroWindowManager::RedrawScreen` | `for (heroWindow* w = m_windowListHead; w != NULL; w = w->m_nextWindow)` | `heroWindow* w = m_windowListHead; while (w != NULL) { ...; w = w->m_nextWindow; }` - the `for` costs the leading `jmp` over the increment | OPEN |
| BASE/WINMGR `heroWindowManager::FadeScreen` | `PollSound();` inside each switch arm incl. a `default:` | one `PollSound();` after the switch, no `default:`; and `m_updateFlags = saved \| FadeSavedUpdate();` (saved on the LEFT) | OPEN |
| BASE/WINMGR `CycleColors` guards + `gbEveryOtherCycle` join | `if (gpWindowManager == NULL \|\| gpBufferPalette == NULL \|\| gpWindowManager->m_active != 1) return;`; `if (gbEveryOtherCycle != 0) { ... }` then a top-level `if (giCycleType == DEFAULT) ... goto updatePalette;` chain | three separate `if (...) return;` guards (three `jmp <epilogue>`); ONE stub in the tail block, i.e. exactly one live `goto`: `if (gbEveryOtherCycle == 0) goto cycleType;` whose label sits INSIDE the `else` of `if (giCycleType == WORLD_VIEW) {...} else { <rotations> cycleType: if/else-if/else-if }`; the three `goto updatePalette` become the `else if` chain's structural arm ends | OPEN |
| BASE/WINMGR `CycleColors` reflection selects (3 sites) | `i32 frame = iCombatCycleFrame; if (frame >= T) frame = N - frame; else frame = iCombatCycleFrame;` | `i32 cycleFrame = iCombatCycleFrame < T ? iCombatCycleFrame : N - iCombatCycleFrame;` - the `?:` materialises the temp retail spills below the parameter spill | OPEN |
| BASE/WINMGR `CycleColors` world-view memcpy source | `i8* src = gpBufferPalette->m_data + (...)*3;` then `memcpy(dst, src, 3)` | no `src` local; the expression is inline in the call (retail's frame has no slot for it), and the block's locals are `cycleFrame, colorIndices, colorIndex` for the slot order | OPEN |
| BASE/WINMGR `FizzleForward` shape + locals | body nested in `if (bShowIt != 0) { ... if (width > 0 && height > 0) { ... } }`; running `screenOffset`/`workOffset` accumulators; `*workPixel++ \| (*savedPixel++ << 8)` folded into a `u16 lookup` local; `delay + tick`; `(end[i] - start[i]) * (frame + 1)`; `if (m_fizzleSource != NULL) delete ...` | guard clauses (`if (bShowIt == 0) return;`, `if (width <= 0 \|\| height <= 0) return;`); all eleven locals declared at function top in one scope (`saveFlags, ccycleBuf, sourceX, sourceY, frame, i, tickStart, savePixel, paletteBuf, screenPixel, workPixel` in slot order); pointers recomputed from `(sourceY - y)`; separate `savePixel++/workPixel++/screenPixel++` statements; `tickStart + delay`; `(frame + 1) * (end[i] - start[i])`; bare `delete m_fizzleSource;` | OPEN |
| BASE/WINMGR `DoDialog` teardown | unconditional `RemoveWindow`/`Flush` after a `do/while` | `while (done == 0) { ... }` then `if (done != 0) { if (window != NULL) RemoveWindow(window); gpInputManager->Flush(); }`; `m_dialogResult` is set BEFORE `done = 0` | OPEN |
| BASE/listbox `listBoxWidget::Main` case order + guards | `case MESSAGE_MOUSE_MOVE/LEFT_BUTTON_UP` first, then the button-down pair, then `MESSAGE_WIDGET`; inner commands `SET_SELECTION, GET_SELECTION, REPLACE_ITEM, APPEND_ITEM, DELETE_ITEM, CLEAR_ITEMS`; `if (m_id == msg.payload.widget.id) { ... }` | `case MESSAGE_WIDGET` first, then LEFT/RIGHT_BUTTON_DOWN, then MOUSE_MOVE/LEFT_BUTTON_UP; inner commands `CLEAR_ITEMS, DELETE_ITEM, REPLACE_ITEM, APPEND_ITEM, SET_SELECTION, GET_SELECTION` (body run, the jump table is value-sorted either way); `if (msg.payload.widget.id != m_id) break;` (`cmp mem,reg` + `je`/`jmp` guard); `if (!HAS(m_flags, WIDGET_FLAG_DRAW)) break;` | OPEN |
| BASE/listbox `listBoxWidget::Main` locals + hit test | `i16 mx/my/left` declared in the button case, `left = m_x` hoisted; `left > mx \|\| m_y > my \|\| ...` returning CONTINUE; `char* text` / `char** newItems` per case block | function-scope `i16 x; i16 y; char* text; char** newItems;` (that declaration order puts them on retail's `text,x,y,newItems` slots); `if (x >= m_x && y >= m_y && x < m_x + m_width && y < m_y + m_height) { ... } return CONTINUE;`; no `left` local | OPEN |
| BASE/listbox `listBoxWidget::ProcessMouseMessage` shape | `goto done;`/`goto redraw;` joins, per-arm `row`/`clickedIndex`/`newTop`, `i16 firstRowHeight` snapshot, `m_listX <= mouseX`, thumb travel `(mouseY - m_scrollThumbHeight/2 - m_scrollTrackY - 4)` | function-scope `i32 offset; i32 x; i32 itemIndex; i32 y;` (one shared index local), a plain `switch` whose arms `break` with `return MESSAGE_DISPATCH_CONSUME` after it, `x >= m_listX`, and `(y - m_scrollTrackY - SCROLL_DRAG_Y_ADJUSTMENT - m_scrollThumbHeight/2)` - the same term order `dropListWidget::ProcessSelectDialog` already uses | OPEN |
| BASE/listbox `listBoxWidget` byte flags | `char m_scrollUpPressed/m_scrollDownPressed/m_scrollThumbDragging/m_itemSelectionTracking` | `u8` (retail reads them `xor r32,r32; mov r8`, never `movsbl`), matching `dropListWidget`'s already-exact declaration | OPEN |
| BASE/listbox `DrawLBStuff` spellings | counter `i`, `x = m_listX + m_owner->m_posX`, `1 == m_maxVisibleItems - i`, `m_items[m_topIndex + i]`, `m_selectedIndex == m_topIndex`, `(i-1) * m_rowHeight + m_scrollTrackY + m_owner->m_posY`, thumb `?:` as the LEFT addend | counter `row` (bucket 2 - `i` cannot precede `x` in the frame), `x = m_owner->m_posX + m_listX`, `row == m_maxVisibleItems - 1`, `*(m_items + m_topIndex + row)`, `m_topIndex == m_selectedIndex`, `m_owner->m_posY + m_scrollTrackY + m_rowHeight * (row - 1)`, and the `?:` as the THIRD addend of `m_scrollThumbY` | OPEN |
| BASE/listbox `listBoxWidget::Read` frame | ten i16 staging locals (`frameHeight[2]`, `bottomY`, `topY`, `rightX`, `firstRowHeight`, `listX`, `rowHeight`, `listY`, `lastRowHeight`) + `iconEntry`/`iconName` | exactly two locals - `IconEntry* entry; i8 name[16];` - with every value written straight to its member, `entry = reinterpret_cast<IconEntry*>(m_icon->m_data) + <frameMember>` (eight sites, `imul $0xd`), `m_scrollUpX = m_x + m_width - 1 - (m_scrollUpWidth - 1)` and `m_scrollDownY = m_y + m_height - 1 - m_scrollDownHeight + 1` | OPEN |
| BASE/Textntry `textEntryWidget` ctor statement order | `m_displayOffset` before `m_icon`; `m_rectX/m_kind/m_rectY/m_rectW/m_maxLength/...` interleaved; `H2_ALLOC(static_cast<u16>(maxLength) + 5)` | `m_cursorPosition, m_maxLength, m_icon, m_iconFrame, m_displayOffset, m_kind, m_rectX, m_rectY, m_rectW, m_rectH, m_maxLines, m_preserveTextOnFocus, m_color, m_maxLength` (the first `m_maxLength` store is dead), then `H2_ALLOC(m_maxLength + 5)` reading the member back | OPEN |
| BASE/Textntry `textEntryWidget::Read` | an `i32 preserveText` staging local for `m_maxLines`/`m_preserveTextOnFocus`; the third trailing `ReadWord()` discarded | `m_maxLines = 1; if (type == MULTILINE) m_preserveTextOnFocus = 1; else m_preserveTextOnFocus = 0;`; `m_kind = ReadWord(); m_kind = WIDGET_KIND_TEXT_ENTRY;` (see `overwritten-store-vs-discarded-call`) | OPEN |
| BASE/Textntry `textEntryWidget::Main` | `windowX`/`windowY` snapshots + `mouseX`/`mouseY`; `do { ... } while (done == 0)`; `switch` cases ESC/LEFT/RIGHT/DELETE and numpad 7,8,9,4,5,6,1,2,3,0; `typed = static_cast<char>(keyCode)`; `(keyCode >> 8) & 0xff`; `if (m_maxLines >= lineLength) {} else { ... }`; 600-byte buffers | `i16 done; i16 x; i16 y; tag_message event;` at function scope and four 1000-byte buffers in the hit-test block (`original`, `edit`, `copy`, `swap` - retail's slot run is copy, original, edit, swap); `while (done == 0)`; cases ESC/DELETE/LEFT/RIGHT and numpad 0..9 ascending; `typed = keyCode & 0xff`; `(keyCode & 0xff00) >> 8` stored in a local `key`; `if (lineLength > m_maxLines) { ... }` | OPEN |
| BASE/Textntry `Draw` / `SetupDisplayString` | `if (LineWidth > m_innerW) { do { display[length-1]=0; length--; } while (...); }`; a named `FontDrawMode color` local for the dim select; `do { shifted = 0; ... } while (shifted)`; `if (cursor != 0)`; `if (cursor < strlen(source))`; 600/300-byte buffers | `while (m_font->LineWidth(display) > m_innerW) display[--len] = 0;`; the dim select is an inline `?:` argument (its temp sits BELOW the `this` spill); `changed = 1; while (changed) { changed = 0; ... }` (test at the top, twice); `if (cursor > 0)`; `if (strlen(source) > cursor)`; both buffers 1000 bytes, locals `display`/`len` and `changed`/`display` | OPEN |
| philAI::ValueOfEventAtPosition float products (15 sites) | `gafAITurnCostResource[...] * AMOUNT`, `AI_..._FACTOR * m_aiFightValue` everywhere | per-site: `AMOUNT * gafAITurnCostResource[...]` (TREASURE_CHEST, CAMPFIRE gold, MAGIC_GARDEN, FLOTSAM x2, SEA_CHEST x3, TREE_KNOWLEDGE costs) and `m_aiFightValue * AI_..._FACTOR` (GAZEBO, TREE_OF_KNOWLEDGE 1/2/3, FORT, WITCH_DOCTOR_HUT, STANDING_STONES, MERCENARY_CAMP, XANADU) — while OASIS/BUOY/TEMPLE/FAERIE_RING/IDOL/FOUNTAIN/WATERING_HOLE keep factor-first. Only visible by resolving each DIR32 target | OPEN as an FP-order class |
| philAI::ValueOfEventAtPosition CAMPFIRE | six-resource average first, gold term added last; sum runs GEMS, MERCURY, ORE, SULFUR, WOOD, CRYSTAL | gold term FIRST; sum runs WOOD, ORE, CRYSTAL, SULFUR, MERCURY, GEMS | OPEN |
| philAI::ValueOfEventAtPosition DAEMON_CAVE | one flat left-associated sum with the first three terms parenthesised | `A + (B + artifactValue) + (C + goldTerm) + penalty` — one parenthesised sum per daemon-cave outcome | OPEN |
| philAI::ValueOfEventAtPosition ABANDONED_MINE | `(float)gaiTurnValueOfMine[MAP_WIDTH*y + x] * gMineCharacteristics[GOLD] * gafAITurnCostResource[GOLD]` | `gMineCharacteristics[GOLD] * gafAITurnCostResource[GOLD] * *(gaiTurnValueOfMine + x + y * MAP_WIDTH)` — no float cast, pointer index form, row product on the right | OPEN |
| philAI::ValueOfEventAtPosition travel-gate scan | `MAP_HEIGHT > exitY`; `cell->m_triggerType == otherCell->m_triggerType` | `gateY < MAP_HEIGHT`; scanned cell first in both equality guards | OPEN |
| philAI::ValueOfEventAtPosition HILL_FORT / FREEMANS_FOUNDRY | DWARF, ORC, OGRE / SWORDSMAN, PIKEMAN, IRON_GOLEM | ORC, OGRE, DWARF / IRON_GOLEM, PIKEMAN, SWORDSMAN (pinned by the pushed creature ids) | OPEN |
| philAI::ValueOfEventAtPosition locals (30) | `cell`/`value`/`creature`/`purchaseCost`/`purchaseValue`/`creatureFlag`/`battleValue`/`index`/`exitX`/`exitY`/`exitValue`/`otherCell`/`bestExit*`/`routeLiveChance[4]`/`currentValue`/`resources` | `theCell`/`eventRV`/`buyCreature`/`numToBuy`/`armySlot`/`freeFlag`/`prize`/`i`/`gateX`/`gateY`/`exitRV`/`exitCell`/`chosenExit*`/`bestRV`/`exitLiveChance` (scalar, not an array)/`positionValue`/`costList`, plus nine dead `*State` fillers; `bucket(cell)=14 > bucket(value)=1` makes the obvious pair unreachable | OPEN as slot-name class |
| army::SpecialAttack locals (50) | the obvious English role names | UNRESOLVED: retail's chain forces >=35 of 50 respellings (longest non-decreasing subsequence of the natural buckets is 15/50); no pen<=2 vocabulary is feasible. Left at 99.78% rather than land `iMyX`/`pow`/`clipLeft` | OPEN - refused, see docs/matching/army-SpecialAttack |
| BASE/Bzip `qsortFull` SWAP macro locals | `Int32 zl0`, `Int32 zr8`, `Int32 zt` | `Int32 zl`, `Int32 zr`, `Int32 zt` - the UPSTREAM bzip 0.21 names (bzip.c:1776), which bucket to retail's per-SWAP slot order `zr, zt, zl` in all six expansions | OPEN - strongest invariant candidate in the ledger: the codec is unedited third-party text, so the 2.1 spelling RECOVERS the original and PoL's suffixes are a VC4.2 equivalence-class member. Re-test `zl/zr/zt` under 4.2 before believing the PoL row |
| BASE/Bzip `EncodeData`/`DecodeData` locals | `retVal`, `fp9`, `flen3` (decl order `fname, fd, retVal, fp9, flen3`) | `result`, `fp`, `flen` - retail puts the int result at `-0x4`, ABOVE the 452-byte `fname` at `-0x1c8`, which needs bucket(result) <= bucket(fname) = 0 with a later declaration | OPEN - these two wrappers are NWC's own, not upstream bzip |
| BASE/SAMPLE `sample::sample` suffix buffer | `filename` | `fileName` (bucket 0 vs 3, so it slots ahead of `size`: retail `fileName@-0x2c`, `size@-0x30`) | OPEN - the surrounding ctor is a genuine 2.1 dev change, see version-changes |
| BASE/BITMAP `bitmap::DrawToBufferCareful` / `GrabBitmapCareful` shape | `if (fits) { DrawToBuffer(x, y); } else { ...clip...; if (width >= 0 && height >= 0) Blit(...); }`, plus a `bitmap* screen` local caching `gpWindowManager->m_screen` in `DrawToBufferCareful` | early return (`if (fits) { DrawToBuffer(x, y); return; }`) and `if (clipWidth < 0 \|\| clipHeight < 0) return;` before the blit - retail's `jl <stub>; jge <blit>; <stub>: jmp <epilogue>` needs the `\|\|` form; no `screen` local, `gpWindowManager->m_screen` is reloaded at each of its four uses | OPEN |
| BASE/BITMAP `Careful` blitter local names | `width, destinationY, height, destinationX` / `sourceX, sourceY, height, width` | `destX, destY, clipWidth, clipHeight` and `sourceX, sourceY, clipWidth, clipHeight`, both declared in that order - the two functions have DIFFERENT retail slot orders ([X,Y,W,H] vs [X,W,Y,H]), so one set of names cannot serve both | OPEN - slot-hash forced, no semantic content |
| BASE/FONT `font::DrawBoundedString` align dispatch | `if (align == LEFT) ... else if (align == CENTER) ... else if (align == RIGHT) ...` | `switch (align) { case ...: ...; break; }` - retail spills the selector to a temp below `this` | OPEN |
| BASE/FONT `font::DrawBoundedString` line loop | `for (; size > pos && ...; yOffset += m_height) { ...; lineWidth = 0; }` | `while (pos < len && ...) { ...; yPosition += m_height; lw = 0; }` - the `for` costs the leading `jmp` over the step block and swaps the last two statements; the first relational also loads `pos` first | OPEN |
| BASE/FONT `font::DrawBoundedString` / `LineLength` local block | `size, space, xPosition, yOffset, pos, wordBreak, lineWidth, savedWidth, line` (+ `drawMode`) | `len, blank, [lastPos,] xPosition, yPosition, pos, spaceWidth, wordWidth, lw, prevPos, line` (+ `drawMode`) - retail initialises THREE unused zeroed ints, not two, and `DrawBoundedString` additionally declares one never-referenced uninitialised local (frame slot `-0x14`, zero references) | OPEN - the spare locals are real retail frame slots; only their names are free |
| BASE/FONT `font::ExtractLine` overflow test + hyphen index | `if (maxWidth < width)`; `line[pos + 1 - *position]`, `line[pos + 2 - *position]` | `if (width > maxWidth)` (retail loads `width` first); `line[curPos - *position + 1]`, `line[curPos - *position + 2]` - the trailing literal folds into the SIB displacement | OPEN |
| BASE/FONT `font::ExtractLine` hyphen test | `text[pos] == '-'` written inline at both sites | `IsHyphen(text[curPos])`, a `static inline bool IsHyphen(u8 c) { return c == '-'; }` next to `IsVowel` - retail materialises the comparison as a `bool` (`sete` + `and 0xff` + `test`) over a zero-extended byte | OPEN |
| BASE/FONT `font::ExtractLine` local names | `width, pos, wordStart, wordWidth, lineEnd, lineEndWidth` | `width, curPos, wStart, savedWidth, lastEnd, lastWidth` in that declaration order | OPEN - slot-hash forced, no semantic content |
| BASE/MOUSEMGR `mouseManager::SetPointer(char*, i32, MouseCursorType)` | the auto-resolve chain overwrites the `cursorType` PARAMETER, no `else` arm; `m_cursorType != cursorType && (m_cursorType = cursorType, gbColorMice != 0)` | a function-scope local `type` (slot -0x4, `this` at -0x1c) written by all four arms including an explicit `else type = cursorType;`, and the compare is `type != m_cursorType` with the member on the RIGHT (`cmp reg,[this+0x42]`) | OPEN |
| BASE/MOUSEMGR `mouseManager::SetPointer(i32)` guards / index / tail | one `\|\|` chain `m_active != true \|\| m_cursorFrame == frame \|\| gbInSetPointer != 0`; `iMouseOffset[type] + frame`; tail `RestorePosition(); gbInSetPointer = false; gbPutzingWithMouseCtr--;` | three separate `if (...) return;` guards with `frame == m_cursorFrame` (parameter LEFT); `frame + iMouseOffset[IDX(m_cursorType)]`; tail order `gbPutzingWithMouseCtr--; gbInSetPointer = false;` | OPEN |
| BASE/MOUSEMGR `mouseManager::SetPointer(i32)` colour-mouse arm | `if (gbColorMice != 0) { NewUpdate(1); } else { ...; SetCursor(...); }` then the shared tail | `if (gbColorMice != 0) { NewUpdate(1); goto done; }` + unnested body + `done:` on the tail — retail carries the goto stub pair (`eb 02 jmp <epilogue>; eb d8 jmp <tail>`) before the epilogue | OPEN |
| BASE/MOUSEMGR `mouseManager::SetPointer(i32)` AND-mask loop | `u8* colorBits = static_cast<u8*>(cColorBits[i]);` inside the inner loop; `colorBits[row * 32 + column]`, `static_cast<u8*>(cAndBits[i])[row * 4 + (column >> 3)]`; loop locals `row`/`column` | no staging local at all (retail's frame has exactly `filename[16]`, then the two counters); pointer form `*(static_cast<u8*>(cColorBits[i]) + x + y * 32)` and `*(static_cast<u8*>(cAndBits[i]) + y * 4 + (x >> 3))` (see `flat-map-index-pointer-form`); counters named `x`/`y` so the INNER one takes the -0x14 slot | OPEN as slot-name + index-form class |
| BASE/MOUSEMGR `mouseManager::NewUpdate` control flow | `if (gbColorMice == 0) goto updateDone;` and `if (force == 0 && ... ) goto updateDone;`; the "reset bounds" block sits AFTER the outer `if`, reached by falling out of it and by `goto updateBoundsReady` from the else arm | nested `if (gbColorMice != 0) { if (force != 0 \|\| m_cursorLeft != ... \|\| m_cursorTop != ...) { ... } }` (both retail transfers are single near `jcc`s, see `direct-jcc-vs-goto-stub`); the reset block is the TAIL of the then-arm and is entered by `goto resetBounds;` from the first bounds guard, with a second `goto finishUpdate;` before `m_drawnCursorSizeIndex = m_cursorSizeIndex;` — exactly two stubs before the epilogue | OPEN |
| BASE/MOUSEMGR `mouseManager::NewUpdate` relational sides | globals first: `gOldMouseRight < m_savedLeft`, `gOldMouseLeft > m_cursorRight`, `gOldMouseLeft > m_savedLeft`, `gOldMouseRight < m_savedLeft + iMouseSize[...] - 1` | member/computed value first at every site: `m_savedLeft > gOldMouseRight`, `m_cursorRight < gOldMouseLeft`, `m_savedLeft < gOldMouseLeft`, `m_savedLeft + iMouseSize[...] - 1 > gOldMouseRight` (`cmp reg,[global]` = `3b`, see `integer-relational-operand-side`) | OPEN as whole-function class |
| BASE/MOUSEMGR `mouseManager::NewUpdate` saved-origin clamp | `m_savedLeft = 0; if (m_cursorLeft >= 0) m_savedLeft = m_cursorLeft;` (and the same for top) | `if (m_cursorLeft < 0) m_savedLeft = 0; else m_savedLeft = m_cursorLeft;` | OPEN |
| BASE/ICON `icon::CombatClipDrawToBuffer` / `icon::FillToBuffer` entry access | `i32 entryOffset = frame * sizeof(IconEntry);` staging local + `reinterpret_cast<IconEntry*>(m_data + entryOffset)->x`; `FillToBuffer` uses the inline accessor `Entries()[frame].x` | no locals at all (retail's frame is `push ecx`): `reinterpret_cast<IconEntry*>(m_data)[frame].x` respelled at every site, `frame * 13` recomputed per use. The `icon::Entries()` accessor is deleted — under `/Ob1` it materialises its return into a per-call-site frame temp retail does not have (see `inline-accessor-return-temp`) | OPEN |
| BASE/ICON limit sums | field first: `Entries()[frame].x + x`, `Entries()[frame].w + limits->left - 1` | argument/accumulator first: `x + <entry>.x`, `limits->left + <entry>.w - 1`, `y + <entry>.y`, `limits->top + <entry>.h - 1` (the SIB base of the `lea -1(...)` names the LEFT operand, see `lea-base-names-add-order`) | OPEN |
| BASE/miscwin `BlitBitmapToScreenVesa` row copy | `i32 row = 0; if (height > row) { i32 destinationOffset = destinationY * 640; do { memcpy(pixels + destinationX + destinationOffset, src + sourceX + (sourceY + row) * m_width, width); destinationOffset += 640; ++row; } while (row < height); }`, guard `gpWindowManager->m_screen != sourceBitmap` | `for (row = 0; row < height; row++) memcpy(pixels + (destinationY + row) * 640 + destinationX, src + (row + sourceY) * m_width + sourceX, width);` with no running offset, guard `sourceBitmap != gpWindowManager->m_screen` | OPEN - the frame size (`sub esp,0x14` vs `0x18`) and the `for` skeleton make this a strong dev-change candidate, see version-changes |
| BASE/RESMGR `resourceManager::ReadBlock` failure arm | `i32 errorCode = errno;` before the `sprintf` and `i32 debugTrap = 0; debugTrap++; debugTrap++;` after `LogStr` | neither local: `sub esp,8` holds only the `this` spill and `bytesRead`, and there is one `call __errno`, not two | OPEN - dead debug scaffolding, likely a real 2.0 -> 2.1 edit |
| BASE/Midi `MIDISetVolume` fade arms | `((VOLUME_LOW_RANGE - m_fadeSteps) * MAX_VOLUME) / VOLUME_LOW_RANGE` (a soundManager member reading `m_fadeSteps`) | `(volume * (VOLUME_LOW_RANGE - fadeSteps)) / VOLUME_LOW_RANGE` - the running local on the LEFT, which stops it folding into `imull -0x4(%ebp),%eax` | OPEN - the whole function also moved from a soundManager member to a free function taking `i32& fadeSteps` |
